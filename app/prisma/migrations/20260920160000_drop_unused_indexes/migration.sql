-- Два индекса из Р-185 планировщик не берёт ни при каком объёме, и проверка
-- на двухстах тысячах строк это показала (решение Р-186).
--
-- Project[startedOn]: выборка «начатые работы» возвращает почти всю таблицу,
-- и последовательное чтение дешевле обращения по индексу.
DROP INDEX IF EXISTS "Project_startedOn_idx";

-- AuditEvent[action]: перечень видов действий больше не собирается обходом
-- журнала — он берётся из словаря названий, и индексу нечего обслуживать.
DROP INDEX IF EXISTS "AuditEvent_action_idx";
