/**
 * Картинка превью ссылки — одна на весь сайт.
 *
 * Заголовок и описание у каждой страницы свои, а обложка одна: она
 * отвечает на вопрос «от кого пришла ссылка», а не «о чём эта страница».
 * Собрана из фирменных элементов теми же гарнитурами, что и страницы.
 *
 * Формат PNG, а не WebP: часть сборщиков превью в мессенджерах WebP не
 * разбирает и показывает ссылку без картинки.
 *
 * Описатель вынесен сюда, потому что каждая страница задаёт свой блок
 * `openGraph`, а Next заменяет им родительский целиком, а не дополняет.
 * Без общей константы имя файла пришлось бы держать в семи местах.
 *
 * Решение Р-68 в docs/DECISIONS.md.
 */
export const OG_COVER = {
  url: '/og-cover.png',
  width: 1200,
  height: 630,
  alt: 'ProDisser — научное сопровождение аспирантов, студентов и компаний',
} as const;
