/**
 * Последняя миграция, которую ждёт этот код.
 *
 * Проверка здоровья сверяет с ней журнал миграций базы. Без сверки сайт
 * месяц отвечал «здоров» на базе без единой таблицы кабинета: образ
 * миграций не пересобирался, и девять миграций на боевую базу не легли, а
 * `select 1` этого не видел (решение Р-218). Имя держится в коде, а не
 * читается из каталога миграций: в рабочий образ каталог не попадает.
 *
 * Проверка `schema-version.test.ts` не даёт забыть поднять имя вместе с
 * новой миграцией.
 */
export const LATEST_MIGRATION = '20260924090000_lead_letters';
