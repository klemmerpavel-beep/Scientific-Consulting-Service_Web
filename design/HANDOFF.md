# Handoff: ProDisser — сайт научного консалтинга

## Overview
Восемь поверхностей научно-консультационного сервиса **ProDisser** (коммерческое
обозначение ООО «РУСДРОН»): пять публичных страниц, внутренний справочник дизайн-системы
и три правовых документа.

| Файл | Что это | Публичный адрес |
|---|---|---|
| `StartPage.dc.html` | посадочная страница для всех — общий вход | `/` → `index.html` |
| `MainPage.dc.html` | аспирантам, соискателям и молодым учёным | `main.html` |
| `StudentsPage.dc.html` | студентам, магистрантам и специалистам | `students.html` |
| `BusinessPage.dc.html` | бизнесу и стартап-командам (НИОКР, ИС, гранты) | `business.html` |
| `DesignSystem.dc.html` | справочник дизайн-системы, **в публикацию не идёт** | — |
| `OfferPage.dc.html` | публичная оферта, 16 разделов | `offer.html` |
| `PrivacyPage.dc.html` | политика обработки персональных данных, 15 разделов | `privacy.html` |
| `ConsentPage.dc.html` | согласие на обработку персональных данных, 10 разделов | `consent.html` |

В папке `baseline/` — снимки утверждённых версий для сверки и откатов, актуальны
на 7 сентября 2026 (переход на новую айдентику, решения Р-51…Р-69).

> **Посадочная страница принята 6 сентября 2026** (решение Р-63). Принята
> сборка коммита `6f60ec8`; принятое состояние закреплено замороженной веткой
> `accepted/landing-2026-09-06` — она указывает на коммит с записью о приёмке,
> который отличается от принятой сборки только документацией. В эту ветку не
> коммитят и её не двигают. Копии макетов в `baseline/` побайтно равны рабочим
> на этот момент. Принятые параметры — таблица в Р-63
> (`docs/DECISIONS.md`). Правка, меняющая любое из этих чисел, — изменение
> принятого результата: её согласовывают отдельно и записывают в журнал
> решений. Приёмка касается только `StartPage.dc.html`; правовые страницы,
> три профильные страницы и задания в `docs/briefs/` под неё не подпадают.

**Навигация.** С посадочной три карточки блока «С чего начнём вашу работу» ведут на
`main.html`, `students.html`, `business.html`. Обратно — логотип в шапке. Правовые
документы связаны ссылками в подвалах всех страниц и у чекбоксов согласия в формах.

Ключевое позиционирование, от которого зависит весь копирайт: **компания не пишет работы
за клиента**. Продукт — экспертиза и сопровождение. Любая формулировка, читаемая как
«напишем за вас», — дефект. Прямое «мы не пишем» тоже не используется: тон остаётся
лояльным, роль описывается через действия сервиса.

## About the Design Files
Файлы — **дизайн-референсы на HTML**, прототипы задуманного вида и поведения, а не
продакшн-код для копирования. Задача — **воспроизвести макеты в среде целевой кодовой
базы** (React, Vue, Next.js — что принято в проекте), опираясь на её паттерны. Если среды
нет, выбрать фреймворк и реализовать в нём.

Формат: Design Component (`.dc.html`) — шаблон с плейсхолдерами `{{ }}`, класс логики
`Component extends DCLogic` и **инлайновые стили**. Инлайн — требование среды
прототипирования, не рекомендация к продакшену: переносите значения в CSS-модули,
styled-components или Tailwind-конфиг.

**Одно исключение, которое обязано сохраниться.** `grid-template-columns` и `display`
вынесены из инлайна в правила классов (`pd-`, `sp-`, `bp-`) внутри `<style>` в
`<helmet>` — инлайн перебивает медиазапрос по специфичности, и на узких экранах
раскладка не перестраивается. Эта ошибка уже была допущена и исправлена; при переносе
в CSS-подход она исчезает сама, но проверьте результат на 390px.

## Fidelity
**High-fidelity.** Цвета, типографика, отступы, состояния и тексты — финальные.
Воспроизводить попиксельно. Палитра переведена на цвета РуВики; все значения ниже.

## Screens / Views

### 1. StartPage — посадочная
Контейнер 1220px, паддинг 30px (20px ниже 480), фон `#FFFFFF`.

**Фоновый слой** — `z-index:0`, контент на `z-index:1`. Два мотива, больше вводить нельзя:
- Орбиты вокруг звезды: SVG 640×580, эллипсы r=120/196 (обводка `#D9E1EC`), r=262
  (`#EDF2F8`), четырёхлучевая звезда `#D8E4F3`, точки-планеты `#D8E4F3` r=6 и
  акцентная `#14417A` r=4. Две сцены: правая сверху (1280px, top −290) и левая ниже
  (900px, top 820).
- Точечная сетка `radial-gradient(#D8DEE6 1px, transparent 1.2px)`, шаг 34px, opacity .4,
  маска сверху и снизу. Маска — единственное место, где градиент допустим: он
  задаёт прозрачность, а не окраску.

**Хедер.** Вордмарк `PRO` акцентом `#14417A` + `DISSER` графитом `#14161C`,
Literata 600, 23px, letter-spacing .005em. Подсветка при наведении
`rgba(216,228,243,.62)`, радиус 10px, цель нажатия 44px. Справа — три ссылки
направлений и pill-кнопка «Оставить заявку» (`#14161C`, hover `#262A33`); ниже
480px кнопка убирается — та же заявка стоит формой в первом экране.

**Hero.** Сетка `1fr / 1fr`, gap 40px, колонки тянутся на общую высоту
(`align-items: stretch`). Надзаголовка нет: он повторял заголовок и отодвигал
форму. Текст слева сокращён до продающего минимума — решения Р-58 и Р-59.
В левой колонке остались заголовок, одна строка подводки, заголовок формы и
сама форма. Ширина формы 392px (Р-61): при 344px строка «Даю согласие на
обработку персональных данных» переносилась на вторую строку. Совпадение
оснований плашек и строки согласия правая колонка держит резервом
`padding-bottom:67px` — ровно тем, что занимает кнопка под строкой согласия
слева. **Меняете отступ или высоту кнопки — пересчитайте резерв**, иначе
основания разъедутся. Замер: 721 и 721. Правовая оговорка о предмете договора и условия консультации
со шкалой сроков сняты с первого экрана: оба факта стоят ниже на той же
странице (панель «Оставьте заявку») и в оферте.
- H1 Literata 500, `clamp(28px,2.8vw,40px)`, line-height 1.24, `#14161C`, max-width 19ch:
  «ProDisser — научное сопровождение аспирантов, студентов и компаний», где
  «ProDisser» в плашке-подсветке `#D8E4F3`, радиус 6px, `box-decoration-break: clone`.
  Подсветка на инлайновом отрезке выше строки на высоту глифового бокса Literata
  (~1.48em), поэтому под неё берётся интерлиньяж 1.4 и вертикальный паддинг 0 —
  иначе плашка режет строку сверху.
- Форма: pill-переключатель «E-mail / Телефон» (активная `#14161C` + белый текст), поле
  56px, радиус 10px, рамка `#E3E7EC`, фокус `#14417A`; кнопка «Оставить заявку».
  Две галочки согласия — **не предзаполнены** (по 152-ФЗ предзаполненная галочка
  согласием не считается), рядом ссылки на `privacy.html` и `offer.html`.
- Правая колонка: два круглых слота под фото — 190px и 130px, второй опущен.
  Снимки полнокадровые, кадрируются `object-fit: cover` с точкой привязки из
  `app/src/components/photos.ts`. Обесцвечивание снято (Р-11). Подписи — только
  по роли, без имён и должностей.
- Правая колонка распределена: портреты прижаты к середине свободной высоты
  (`margin-top: auto`), плашки — к низу. Нижний отступ колонки равен высоте
  кнопки отправки, поэтому основание плашек встаёт вровень с последней строкой
  галочек согласия, а не «где получилось».
- Три плашки первого экрана, каждая со ссылкой на носитель факта внутри сайта:
  «52 позиции» → каталог `BusinessPage`, «6 этапов» → трек этапов `MainPage`,
  «с 2016» → блок команды. Первая плашка — акцентная (`#0D2B52`, текст `#FFFFFF`),
  вторая — тонированная (`#EDF2F8`), третья — белая с рамкой. Разница задаётся
  плотностью и рамкой, не градиентом. Радиус 14px.

### 2. StartPage — «С чего начнём вашу работу»
Три карточки-ссылки, сетка `.pd-routes`, gap 16px. Белый фон, рамка `#E3E7EC`,
радиус 14px, внутренняя кромка снизу `inset 0 -3px 0` акцентом и его светлыми
ступенями, тень `0 1px 2px rgba(20,22,28,.05)`.
Фотополоса 355px: снимок закрывает слот целиком (`cover`), цветная подложка
`.pd-routebg` видна только пока изображение грузится. При наведении на **всю
карточку** подложка уходит в прозрачность — переход 220ms.
Заголовки-результаты: «Довести диссертацию до защиты», «Сдать диплом и опубликовать
статью», «Защитить разработку и получить грант».

### 3. StartPage — карусели
Карточка каруселей: паддинг 18/20/20, внутренний шаг 10px, номер 15px, нижняя
строка отбита 12px. До Р-58 было 24/24/24, 12px и 17px — карточки занимали
на двадцать пикселей больше и растягивали полосу без пользы.

Первая строка карточки — пиктограмма 22px и номер (или надзаголовок) в одной
строке, шаг 10px. Пиктограмма из набора `pd-*.svg`, цвет берётся от номера,
поэтому в приглушённых карточках она приглушена вместе с ним. Знак стоит в
существующей строке, а не над ней: высота карточки от него не растёт.

Две карусели по 6 карточек: «Что входит в сопровождение» (4 в кадре) и «Гарантии
и документы» (3 в кадре). `scroll-snap-type: x mandatory`, `scroll-behavior: smooth`
на треке. **Прокрутка задаётся присваиванием `scrollLeft`** — не `behavior:'smooth'`
в JS, иначе не гасится по `prefers-reduced-motion`. Число страниц меряется в
`componentDidMount` и на resize через `requestAnimationFrame`. Полоса прогресса:
заполнение = видимая доля + прогресс. Трекам заданы `tabindex="0"` и `aria-label`.

### 4. StartPage — CTA и подвал
Две панели, `.pd-cta`, gap 24px, `align-items: stretch` — основания совпадают
по построению. Левая тёмная `#0D2B52` со «Стандартом качества», правая
светлая `#ECF1F8` с формой. Градиентов нет.

Решением Р-60 обе панели уплотнены: вертикальный внутренний отступ 16px, шаг
заголовок→абзац 8px, форма отбита сверху 6px, внутренний шаг формы 8px. Решением
Р-61 у кнопки снят собственный верхний отступ. Высота 549 → 372 пикселя. Состав полей, порядок, валидация и тексты галочек согласия
при этом не менялись.

Подвал (Р-58, переработан Р-60 и Р-62) — три строки `.pd-foot`: вордмарк,
телефон и почта слева, телеграм справа; правовые ссылки слева, реквизиты
справа; строка копирайта. Вордмарк подвала — тот же элемент, что в шапке
(Literata 600, «PRO» акцентом, подложка при наведении), кегль 21px вместо 23px;
конвертер выдаёт ему те же классы взаимодействия, что и шапочному.
Верхний отступ 56px, высота 297 → 203 пикселя. Ниже 768px строки складываются
в столбец. Все ссылки подвала — самостоятельные цели нажатия высотой 44px.

### 5. MainPage — аспирантам (12 секций)
Порядок: хедер → hero → две бегущие строки → «Проблема» → «Преимущества» (4 УТП
в карточках, маркер: глиф `pd-*` 21px в подложке `#ECF1F8`, под ним номер) →
«Услуги» (два ряда по 3 карточки с натюрмортами + плитки) → «Стоимость» → «Этапы» (карусель) →
«Команда» (орбиты) → «Вопросы» → «Заявка» → подвал.
Блок «Стоимость» поднят выше трека этапов: он начинается на 4,5 экране при 1440
и на 8 при 390 — до правки был на 5,8 и 9,8. Шаг между секциями **96px везде**,
подблоки — 48px через hairline.

**Меню интерактивно:** активный раздел определяется по прокрутке (верх секции ≤120px),
получает цвет `#14161C`, `aria-current` и акцентное подчёркивание 2px, выезжающее
слева направо (`transform-origin: left; scaleX(0)→scaleX(1)`, 220ms). Слушатель с
`{ passive: true }`, снимается в `componentWillUnmount`.

**Бегущие строки:** дубликат массива для бесшовности помечен `aria-hidden`, анимация
останавливается по `:hover` и `:focus-within` через
`animation-play-state: paused !important`. Подробности и причина
`!important` — раздел «Interactions & Behavior» и решение Р-68.

**«Проблема»:** сетка `1fr auto 1fr`. Слева облако из 7 pill-чипов с болями поверх
приглушённого красного ореола — `radial-gradient(circle closest-side, …)` в 11 стопов,
`filter: blur(40px)`; **десять стопов и closest-side обязательны** — иначе видны
границы полос. Это единственный радиальный градиент на сайте, и он несёт не объём,
а ореол боли. Спиральный пунктир `stroke-dasharray="5 7"`. В центре — акцентная
стрелка, сужающаяся слева направо втрое. Справа — карточка и трек этапов.

**Трек этапов** интерактивен: 6 шагов, клик меняет состояние (пройденные — «Одобрено»,
текущий — «В работе», следующие — «Впереди»), различие цветом текста и полосы,
**не прозрачностью** — она снижает контраст ниже порога. Прокручивается стрелками
44×44, 4 в кадре.

**Стоимость** решена не карточками: таблица сроков по направлениям работы
(«от 2 месяцев», «от 14 дней»), под ней три фактора сметы и нумерованный список
того, что нужно прислать. Денежных значений нет — см. «Что осталось незакрытым».

### 6. StudentsPage — студентам (7 блоков)
Первый экран (паддинг 64/48) → «Над дипломом работает команда» (орбиты) → состав
работ (8 плиток разной ширины с натюрмортами) → смета со **свёрнутым блоком
нормоконтроля** (7 причин возврата с кафедры) → «Как устроена работа от черновика
до защиты» → вопросы → заявка. Смета начинается на 3,4 экране при 1440 и на 5,8
при 390.
H1 Literata `clamp(30px,3vw,42px)`, интерлиньяж 1.4: «Диплом, собранный
**под требования вашей кафедры**».
Переключатель «Где вы сейчас» перестраивает недельный план работ; логика пересчёта на
календарь сохранена, но поле даты удалено по решению заказчика — ветка недостижима.

### 7. BusinessPage — бизнесу (7 блоков)
Первый экран → бегущие строки → «Над задачей работает проектная команда» (орбиты) →
**шкала УГТ 1—9** → пять пакетов → каталог 52 позиции → шесть механизмов → права
и конфиденциальность → заявка. Пакеты начинаются на 3,1 экране при 1440 и на 4,4
при 390.

**Шкала УГТ** — несущий элемент: девять уровней по ГОСТ Р 58048-2017, при выборе
перестраиваются три колонки (что делаем, документы на выходе, финансирование) и строка
связи с подходящим пакетом; клик прокручивает к его карточке. На уровнях 1–2 страница
прямо говорит «для инструментов внедрения рано». Документацию ведём **до УГТ 7** —
на 8 и 9 конструкторскую и серийную готовит предприятие.

**Каталог** — семь направлений, 52 позиции (15+12+8+7+6+3+1). Раскрытое направление
показывает вводную строку, получателя документа, чередующиеся строки позиций
(`#F8FAFC` через одну) и подвал с нормой и ссылкой на заявку. Это же число стоит
на первой плашке посадочной, и каталог — её носитель.

**Пакеты:** карточки разной ширины в сетке из 6 колонок — 01 и 02 по 4 и 2, 03 и 04
по 3, 05 полосой на 6. Выводятся строго по порядку номеров: нумерация несёт смысл
последовательности от техзадания к постановке прав на баланс.

### 8. DesignSystem — справочник (6 разделов)
01 Токены · 02 Каркасы (сайт и рабочий интерфейс) · 03 Компоненты (6 на сайте,
13 в рабочем интерфейсе) · 04 Состояния (по три у таблицы, сетки, списка) ·
05 Данные для примеров (предельные значения) · 06 Приёмка.
Раздел 01 переписан под новую палитру и несёт блок «Гарнитуры — ровно три».
Режим канваса (`<meta name="design_doc_mode" content="canvas">`). **В публикацию не идёт.**

Ветвь «Рабочий интерфейс» — задание на личный кабинет и CRM: плотный UI на тех же
токенах, вертикальный шаг 4/8/12/16/24, строка таблицы 44–48px, кегль 13–14px,
акцентные панели решаются плотностью и рамкой.

### 9—11. Правовые документы
Колонка 72ch, липкое оглавление на десктопе (раскрыто) и свёрнутое на узком экране —
начальное состояние определяется через `matchMedia`, при печати разворачивается.
Сквозная нумерация 4.2.1. Без карточек, плашек и подсветок: выразительные средства —
колонка, интерлиньяж и нумерация. `@media print` убирает шапку, оглавление и подвал.
Переведены на новую палитру и гарнитуры; **юридический текст, нумерация разделов,
пометка «Проект документа» и даты редакций не менялись ни на символ**.

**Статус — проект до согласования юристом.** В шапке стоит пометка «Проект документа».

## Interactions & Behavior
- **Единая кривая** `cubic-bezier(.2,0,.2,1)`, длительности 180–260ms. Других нет.
- **Hover карточек** — только цвет рамки. Подъём, масштаб и смена тени запрещены.
- **Hover кнопок** — только цвет фона (`#14161C → #262A33`).
- **Focus** — `*:focus-visible { outline: 2px solid #14417A; outline-offset: 2px }`,
  никогда не отключается, всегда отличается от hover.
- **Карусели** — `scrollLeft` + CSS `scroll-behavior`; замер страниц после монтирования
  и на resize через `requestAnimationFrame`.
- **Плитки услуг** — раскрытие связано по ряду через состояние `offerRow`, а не
  CSS-hover: иначе высота ряда не выравнивается. Работает и по фокусу с клавиатуры.
- **prefers-reduced-motion** — `* { transition: none !important; animation: none !important }`.
- Формы: submit гасится, показывается плашка `role="status"` с переводом фокуса.
  Плашка помечена `data-sent="<имя формы>"` и `tabindex="-1"`; обработчик макета
  переводит на неё фокус после успешного ответа сервера. Отправку выполняет
  `app/src/lib/submit-lead.ts`, состав полей и валидация макетом не задаются.
- **Цели нажатия** — 44px по высоте у всех самостоятельных ссылок и кнопок,
  включая подвал, вордмарк и «Перейти к содержанию». Исключены три случая:
  ссылка внутри строки текста (исключение WCAG 2.5.8 для inline-целей), скрытая
  за кромкой ловушка для роботов и подпись согласия — 36px (Р-62). Подпись
  осталась целью во всю ширину колонки; 36px в полтора раза больше минимума
  AA (2.5.8 — 24×24), а 44px разрывали группу условий пустотой в 26 пикселей.

**Бегущие строки.** Останавливаются при наведении и при фокусе. Правило
паузы обязано нести `!important`: анимация задана инлайновым сокращением
`animation`, а оно выставляет `animation-play-state: running` прямо в
атрибуте `style` и перебивает таблицу стилей — без `!important` пауза не
срабатывает вообще. Секция бегущей строки несёт `tabindex="0"`, иначе
`:focus-within` не срабатывает и остановить движение с клавиатуры нельзя
(WCAG 2.2.2, уровень A). Решение Р-68.

**Картинка превью ссылки.** `og:image` объявляется в каждом маршруте, а не
в общем макете: Next заменяет родительский блок `openGraph` целиком.
Описатель один — `app/src/components/og-cover.ts`. В сборке витрины должен
быть выставлен `NEXT_PUBLIC_SITE_URL`, иначе в статику уезжает `localhost`.

## State Management
```
StartPage:    sentTop, sentBottom, contactTop, contactBottom, direction,
              scopePage, grtPage, scopePages, grtPages, scopeFill, grtFill
MainPage:     open, sent, contact, stepPage, stepFrac, planPage, planPages,
              offerOpen, trackAt, navOpen, navActive, svcSel
StudentsPage: situation, open, sentTop, sentMain, offerOpen, normOpen,
              contactTop, contactBottom
BusinessPage: trl, open{}, allOpen, sentTop, sentMain, highlight,
              contactTop, contactBottom
Документы:    toc
```
Данных с сервера нет — контент статический. Формы требуют реального эндпойнта.

## Design Tokens

Палитра переведена на новую айдентику 05.09.2026 — решения Р-51 и Р-53.
Прежние
значения (сирень `#7B2BE0`, подсветка `#DDC4FA`, мята `#3DDCA0`) со всех
семи страниц сняты и в вёрстку не возвращаются.

**Правило.** Ни одного цвета, радиуса, тени, кегля и длительности вне этой
таблицы. Значение, которого здесь нет, сначала вносится сюда с обоснованием,
и только потом попадает в макет.

### Цвета

Контраст замерен по WCAG 2.1 (формула относительной яркости), значения
округлены до сотых. Порог для текста мельче 18px — 4.5:1.

#### Поверхности
| Токен | Значение | Роль |
|---|---|---|
| `surface-site` | `#FFFFFF` | фон страницы (с `!important` на `html` и `body`) |
| `surface-quiet` | `#FAFBFC` | чередование строк каталога, самая тихая заливка |
| `surface-app` | `#F6F7F9` | подложка секции и вложенного блока |
| `surface-accent` | `#ECF1F8` | подложка акцентного блока |
| `accent-deep` | `#0D2B52` | плотная акцентная панель, текст на ней белый |
| `surface-inverse` | `#14161C` | тёмная поверхность |

#### Границы
| Токен | Значение | Роль |
|---|---|---|
| `border` | `#E3E7EC` | рамка карточки и поля |
| `border-field` | `#DDE2E9` | рамка поля ввода в покое |
| `divider` | `#EFF1F4` | внутренний разделитель, hairline |
| `accent-edge` | `#B4C9E5` | рамка акцентного блока |
| `accent-edge-soft` | `#D0DDEE` | рамка блока на подложке `surface-accent` |
| `edge-neutral` | `#C4CAD4` | нейтральная кромка карточки `inset 0 -3px 0` |

#### Текст — графит
| Токен | Значение | На `#FFFFFF` | На `#F6F7F9` | На `#ECF1F8` | Роль |
|---|---|---|---|---|---|
| `ink` | `#14161C` | 18.08 | 16.87 | 15.93 | заголовки, значения, суммы |
| `ink-secondary` | `#3D4450` | 9.81 | 9.15 | 8.64 | основной текст, описания |
| `ink-muted` | `#5C6474` | 5.95 | 5.55 | 5.24 | подписи и моно-лейблы; **светлее не бывает** |
| `ink-inverse` | `#FFFFFF` | — | — | 14.15 на `#0D2B52` | текст на плотной панели |
| `ink-on-deep` | `#CFDDEE` | — | — | 10.26 на `#0D2B52` | пояснение на плотной панели |

`ink-muted` на `accent-edge-soft` `#D0DDEE` даёт 4.32, на `accent-mark`
`#D8E4F3` — 4.62; ниже порога или впритык, поэтому
приглушённый текст на этих подложках не ставится.

#### Акцент — один, глубокий синий
| Токен | Значение | На `#FFFFFF` | Роль |
|---|---|---|---|
| `accent` | `#14417A` | 10.16 | ссылки, фокус, активные состояния, полосы прогресса |
| `accent-press` | `#0F3260` | 12.76 | наведение по ссылке и нажатие |
| `accent-deep` | `#0D2B52` | 14.15 (белым по нему) | заливка плотной панели |
| `accent-deep-hover` | `#0A2145` | 15.95 (белым по нему) | наведение на плотную панель |
| `accent-deep-active` | `#081A38` | — | нажатие на плотную панель |
| `accent-soft` | `#2A63B4` | 5.93 | круглые метки, диаграммы, декоративные заливки |
| `accent-mark` | `#D8E4F3` | — | подсветка слова в заголовке, один заголовок на страницу |

#### Кнопка
| Токен | Значение | Роль |
|---|---|---|
| `button` | `#14161C` | заливка pill-кнопки |
| `button-hover` | `#262A33` | наведение |

#### Статусы — цвет не единственный носитель смысла
| Роль | Фон | Рамка | Текст | Контраст текста к фону |
|---|---|---|---|---|
| Успех | `#E6F9F1` | `#5FE6B6` | `#0E4E3C` | 8.83 |
| Ошибка | `#FAE7E5` | `#F0C9C3` | `#8E2C22` | 7.57 |

Мята сохранена **только** в плашке успеха — иначе пришлось бы
переизобретать состояние, которое читается без обучения. Красный статус
ошибки не менялся. Рядом со цветом всегда стоит слово: «Заявка принята»,
текст причины отказа.

#### Семья «боль» — блок «Проблема» на `MainPage`
| Токен | Значение | Роль |
|---|---|---|
| `pain-ink` | `#8E2C22` | текст чипа с болью, тот же, что у ошибки |
| `pain-glyph` | `#B4564B` | глиф в чипе |
| `pain-edge` | `#F0C9C3` | рамка чипа, та же, что у ошибки |
| `pain-halo` | `rgba(178,58,47,…)` | приглушённый ореол, 11 стопов, `closest-side` |

Семья выведена из цвета ошибки, нового оттенка не вводит.

#### Статусы рабочего интерфейса (раздел 02 справочника, в публикацию не идут)
| Статус | Фон | Текст |
|---|---|---|
| ТЗ согласовано | `#ECF1F8` | `#14417A` |
| На приёмке | `#FDF3E0` | `#7A5312` |
| Правки | `#FBEDE7` | `#8A3D22` |

### Градиенты
**Не применяются.** Три градиента под 152° (лаванда, мята, серая) сняты
вместе с прежней палитрой: объём выражается плотностью заливки, рамкой и
одной тенью. Градиенты остаются только там, где они не украшение, а
инструмент: маски затухания у бегущих строк и каруселей
(`linear-gradient(90deg,transparent,#000 …)`), маска-виньетка у орбит и
ореол в блоке «Проблема».

### Тени — три формулы
- Уровень 0: без тени.
- Плоская карточка: `0 1px 2px rgba(20,22,28,.05)`
- Карточка: `0 6px 14px rgba(20,22,28,.06), 0 1px 2px rgba(20,22,28,.05)`
- Внутренняя цветная кромка: `inset 0 -3px 0 <цвет>`

Объёмная панель `0 8px 20px` снята вместе с градиентами. Световые кромки
`inset 0 1px 0 rgba(255,255,255,…)` — **запрещены**.

### Типографика
- **Literata** — антиква, заголовки h1—h4 и вордмарк; начертания 400/500/600.
- **Inter** — гротеск, абзацы, подписи, поля, кнопки; 400/500/600/700.
- **JetBrains Mono** — лейблы, номера, даты, суммы, статусы, обозначения
  норм; 400/500/600, всегда `tabular-nums`.

Обе основные гарнитуры подключаются через `next/font/google` с
`subsets: ['latin','cyrillic']` и отдаются с собственного домена. Внешних
CDN и файлов шрифтов в репозитории нет. Имена семейств в макетах совпадают
с теми, что регистрирует `next/font`, поэтому связывание через переменную
не требуется.

- Кегли: 12, 13, 14, 15, 17, 19, 20, 22, 26, 28 + `clamp()` для H1.
  **Минимум 12px.** Прежний нижний кегль 11px снят решением Р-56: он не проходил
  требование приёмки «ноль текстов мельче 12px». Замена сплошная — 11px на
  страницах не осталось.
- Интерлиньяж — **коэффициентами**: 1.24 / 1.4 / 1.5 / 1.6. Пиксельных
  значений быть не должно: они не переживают смену кегля.
- `font-variant-numeric: tabular-nums` во всех числах.
- Колонка: заголовки max 34ch, текст 46–72ch.

### Отступы и геометрия
- Шаг 8px. Между секциями 96px, подблоки 48px, контейнер 1220px, паддинг 30px (20px на 480).
- Радиусы: 14px (карточки), 10px (поля, вложенные блоки), 6px (подсветка),
  999px (pill), 50% (круг). Других не вводить.
- Брейкпоинты 1024 / 768 / 480.

## Placeholders
- Слоты изображений заполнены: пять портретов и шесть натюрмортов лежат в
  репозитории (см. «Assets»). Пустых слотов не осталось; `PhotoSlot` сохраняет
  подписанную заглушку на случай, если файл потеряется при сборке.
- `avatar-*.svg` — 12 силуэтов под блок отзывов. Блок удалён, файлы оставлены
  до решения по отзывам и на страницах не используются.
- В правовых документах сроки (5 рабочих дней на замечания, 10 на претензию, 3 года
  режима конфиденциальности, 1 год хранения данных заявителей) выбраны предположительно —
  подтвердить у юриста.
- Наименования сервисов (хостинг, почта, аналитика, платёжный) в политике описаны
  словами без названий — подставить из договоров.

## Assets
- `assets/icons/` — два набора, оба подключаются CSS-маской и поэтому наследуют
  цвет. **Проверять наличие файла перед использованием** — иначе маска не
  отрисуется.
  - служебный: Lucide, перештрихованный на `stroke-width="1.5"`, 30 глифов —
    стрелки, галочка, часы, щит, почта, телефон;
  - смысловой: `pd-*.svg`, 16 собственных глифов под формулировки карточек
    (решения Р-61 и Р-64). Та же сетка 24×24, обводка 1,5, скругления концов и стыков.
    Новый глиф этого набора рисуется по тем же правилам, иначе наборы разъедутся.
- `assets/art/svc-*.webp` — натюрморты услуг: рукопись в переплёте, стопка листов,
  разворот журнала, чертёж, стол перед защитой, папка с бланками заявки
  (`svc-grants.webp`, решение Р-69). Формат 700×214 (кроп 3,27:1),
  подставляются фоном с `background-size: cover`. Прежние `svc-*.svg` оставлены
  в репозитории, но со страниц не подключаются.
- `app/public/photos/*.webp` — шесть снимков: `curator` 480², `editor` 340²,
  `expert` 480², `postgrad`, `student`, `business` по 760². Каждый файл не
  меньше двойного размера своего слота. Три последних перегенерированы решением
  Р-60 в документальной манере: 35-мм плёнка, зерно, только естественный свет,
  человек занят делом и в объектив не смотрит. Они показывают три разные группы
  посетителей — аспиранта в лаборатории, студентку в читальном зале, инженера
  у стены с чертежами. `expert.webp` — крупный план из того же кадра, что и
  `business.webp`: в круге 178px общий план не читается. Карта «слот → снимок»
  и точка кадрирования — в `app/src/components/photos.ts`.
- **Все одиннадцать изображений синтезированы** моделью `nano_banana_2` (Higgsfield)
  и не изображают конкретных людей. Приписывать им имя, должность или цитату
  запрещено: подпись — только роль. Промпты генерации приведены в решениях Р-53
  и Р-60 в `docs/DECISIONS.md`.
- `design/assets/` и `app/public/assets/` синхронизируются **вручную**: скрипта
  переноса нет. Расхождение уже случалось (`send.svg` лежал только в приложении,
  Р-64), поэтому новый файл кладётся в оба каталога одной командой, а после
  правок каталоги сверяются: `diff -r design/assets app/public/assets`.
- `image-slot.js`, `support.js`, `ds-base.js` — рантайм среды прототипирования.
  **В репозитории их нет**, хотя `<script src>` в макетах на них ссылается: это
  окружение Claude Design, а не часть проекта. Практическое следствие: открытый
  напрямую `.dc.html` покажет разметку и стили, но слоты изображений останутся
  пустыми, а логика класса не оживёт. Рабочий вид страницы смотрят в приложении
  или в витрине. В продакшене `<image-slot>` заменяется компонентом `PhotoSlot`,
  описание для `alt` берётся из `data-alt`.
- Логотип — **текстовый вордмарк**, не картинка. Растрового логотипа в проекте
  нет: `logo.png` удалён решением Р-67 как не имеющий ссылок.

## Правила, которые нельзя нарушать
1. Никаких эмодзи — ни в интерфейсе, ни в копирайте.
2. Тон академический: без «под ключ», «напишем за вас», «гарантия защиты»,
   «повысим оригинальность», «100% результат», превосходных степеней.
3. Логотипы Scopus / WoS / Антиплагиат как знаки доверия запрещены.
4. Каждое оценочное утверждение снабжено носителем — договор, документ, срок, процедура.
5. Ни одного обещания решения диссовета, кафедры, комиссии, редакции, фонда,
   Роспатента или налогового органа. Предмет обязательства — соответствие требованиям
   на дату подачи.
6. Год начала работы сервиса — **2016**, везде, включая копирайт.
7. Максимум два фоновых мотива на страницу.
8. Объём градиентом не выражается. Уровень задаётся плотностью поверхности,
   рамкой и одной тенью из трёх формул раздела «Тени».
9. Ни один текст мельче 18px не светлее `#5C6474` — это нижняя граница
   отношения 4,5:1 к фактическому фону.
13. Сгенерированному лицу нельзя приписать имя, должность или цитату.
14. Цель нажатия — 44px, кроме ссылки внутри строки текста и подписи согласия
    (36px, решение Р-62). Ниже 24px не опускать ни при каких условиях: это
    минимум WCAG 2.5.8 уровня AA.
10. Галочки согласия **не предзаполняются**: с 01.09.2025 согласие оформляется отдельным
    документом, предзаполненная отметка согласием не считается.
11. Согласие на обработку данных и согласие на рекламу — **разные галочки**.
12. Первичная консультация — безвозмездная, с предварительной оценкой. Скидки за
    регистрацию не обещаем.

## Публикация
Репозиторий: `klemmerpavel-beep/Scientific-Consulting-Service_Web`, ветка `main`.

Раздел переписан 21.08.2026. Прежний план — семь готовых `.html` на GitHub
Pages — отменён: сайт собирается приложением Next.js, страницы отдаются по
адресам `/`, `/main`, `/students`, `/business`, `/offer`, `/privacy`,
`/consent`, а `.html`-имена остались только внутри макетов как цели ссылок
и переписываются конвертером. Причина в приёме заявок: статике негде хранить
заявку, а требование ч. 5 ст. 18 152-ФЗ обязывает держать базу в России.
Обоснование — решение Р-05 в `docs/DECISIONS.md`.

Макеты в `design/` остаются источником вида и поведения. Правится макет,
а не сгенерированный компонент: сборка в CI проверяет, что перенос
воспроизводим.

## Что осталось незакрытым
1. Согласовать три правовых документа с юристом и снять пометку «Проект
   документа».
2. Галочка согласия на рекламные сообщения — правило 11 требует отдельную,
   решение за заказчиком (Р-10).
3. Площадка размещения — подготовка в `deploy/` и `docs/DEPLOY.md` ждёт
   выбранного сервера. Уведомление Роскомнадзора подаётся до открытия сайта.
4. **Цены не опубликованы.** Решение Р-03 предписывало показать порядок
   «от N ₽», но утверждённого прайса нет ни в репозитории, ни в материалах
   заказчика. Вместо суммы на всех трёх профильных страницах стоит таблица
   сроков и перечень того, из чего складывается смета. Цифра без источника —
   обещание, а не факт, поэтому решение остаётся неисполненным до прайса.
5. Отзывы: блок удалён, возвращается только с реальными текстами и письменным
   согласием авторов (флаг согласия и флаг раскрытия имени — отдельные поля).
   Иллюстрации к нему делаются заново: прежние силуэты удалены решением Р-67.
6. Личный кабинет и CRM — макетов нет. Задание: раздел 02 справочника
   дизайн-системы и `docs/briefs/`; обязательные значения в заданиях
   приведены к действующей айдентике решением Р-67.
7. Рантайма среды прототипирования (`image-slot.js`, `support.js`,
   `ds-base.js`) в репозитории нет, хотя макеты на него ссылаются. Открытый
   напрямую `.dc.html` покажет разметку и стили, но слоты изображений
   останутся пустыми, а логика класса не оживёт. Рабочий вид смотрят в
   приложении или в витрине.

Закрыто: семь страниц собраны и переходы работают; одиннадцать изображений стоят
в слотах (Р-11, Р-12, Р-53); палитра, гарнитуры и вычистка шаблонности —
Р-51…Р-57; уплотнение, репортажная съёмка маршрутов, собственный набор
пиктограмм и строки согласия — Р-58…Р-62. Посадочная страница принята
заказчиком 06.09.2026 и зафиксирована — Р-63. Пустоты и пиктограммы
профильных страниц, чистка неиспользуемых ресурсов и перегенерация
натюрморта «Докторские диссертации» — Р-64 и Р-65; разбор вёрстки трёх
профильных страниц — Р-66; сверка документов с фактом, чистка ресурсов и
мёртвых правил CSS — Р-67; пауза бегущей строки, доступность её с
клавиатуры и картинка превью ссылки — Р-68; карточки преимуществ, второй
ряд услуг с натюрмортами и лаконичный подвал страницы «Аспирантам» — Р-69.
