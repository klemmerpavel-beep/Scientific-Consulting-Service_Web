# Handoff: ProDisser — сайт научного консалтинга

## Overview
Восемь поверхностей научно-консультационного сервиса **ProDisser** (коммерческое
обозначение ООО «РУСДРОН»): пять публичных страниц, внутренний справочник дизайн-системы
и три правовых документа.

| Файл | Что это | Публичный адрес |
|---|---|---|
| `StartPage.dc.html` | посадочная страница для всех — общий вход | `/` → `index.html` |
| `MainPage.dc.html` | аспирантам, соискателям и молодым учёным | `main.html` |
| `StudentsPage.dc.html` | студентам, магистрантам и специалистам | `students.html` |
| `BusinessPage.dc.html` | бизнесу и стартап-командам (НИОКР, ИС, гранты) | `business.html` |
| `DesignSystem.dc.html` | справочник дизайн-системы, **в публикацию не идёт** | — |
| `OfferPage.dc.html` | публичная оферта, 16 разделов | `offer.html` |
| `PrivacyPage.dc.html` | политика обработки персональных данных, 15 разделов | `privacy.html` |
| `ConsentPage.dc.html` | согласие на обработку персональных данных, 10 разделов | `consent.html` |

В папке `baseline/` — снимки утверждённых версий для сверки и откатов, актуальны
на 21 августа 2026. `ProDisser - посадочная страница.html` — автономная сборка
посадочной, работает офлайн.

**Навигация.** С посадочной три карточки блока «С чего начнём вашу работу» ведут на
`main.html`, `students.html`, `business.html`. Обратно — логотип в шапке. Правовые
документы связаны ссылками в подвалах всех страниц и у чекбоксов согласия в формах.

Ключевое позиционирование, от которого зависит весь копирайт: **компания не пишет работы
за клиента**. Продукт — экспертиза и сопровождение. Любая формулировка, читаемая как
«напишем за вас», — дефект. Прямое «мы не пишем» тоже не используется: тон остаётся
лояльным, роль описывается через действия сервиса.

## About the Design Files
Файлы — **дизайн-референсы на HTML**, прототипы задуманного вида и поведения, а не
продакшн-код для копирования. Задача — **воспроизвести макеты в среде целевой кодовой
базы** (React, Vue, Next.js — что принято в проекте), опираясь на её паттерны. Если среды
нет, выбрать фреймворк и реализовать в нём.

Формат: Design Component (`.dc.html`) — шаблон с плейсхолдерами `{{ }}`, класс логики
`Component extends DCLogic` и **инлайновые стили**. Инлайн — требование среды
прототипирования, не рекомендация к продакшену: переносите значения в CSS-модули,
styled-components или Tailwind-конфиг.

**Одно исключение, которое обязано сохраниться.** `grid-template-columns` и `display`
вынесены из инлайна в правила классов (`pd-`, `sp-`, `bp-`) внутри `<style>` в
`<helmet>` — инлайн перебивает медиазапрос по специфичности, и на узких экранах
раскладка не перестраивается. Эта ошибка уже была допущена и исправлена; при переносе
в CSS-подход она исчезает сама, но проверьте результат на 390px.

## Fidelity
**High-fidelity.** Цвета, типографика, отступы, состояния и тексты — финальные.
Воспроизводить попиксельно. Палитра переведена на цвета РуВики; все значения ниже.

## Screens / Views

### 1. StartPage — посадочная
Контейнер 1220px, паддинг 30px, фон `#FFFFFF` (задан на `html` и `body` с
`!important` — иначе бумажный фон дизайн-системы `#FAF9F6` просвечивает полосой внизу).

**Фоновый слой** — `z-index:0`, контент на `z-index:1`. Два мотива, больше вводить нельзя:
- Орбиты вокруг звезды: SVG 640×580, эллипсы r=120/196 (обводка `#E8DEF3`), r=262
  (`#F1EAF8`), четырёхлучевая звезда `#DDC4FA`, точки-планеты `#DDC4FA` r=6 и
  `#3DDCA0` r=4. Две сцены: правая сверху (1280px, top −290) и левая ниже (900px, top 820).
- Точечная сетка `radial-gradient(#DDD2EA 1px, transparent 1.2px)`, шаг 34px, opacity .4,
  маска-градиент сверху и снизу; вертикальные hairline-линии `#EEE8F5` по краям контейнера.

**Хедер.** Вордмарк `PRO` `#7B2BE0` + `DISSER` `#16121C`, Onest 700, 28px,
letter-spacing −.012em. Подсветка при наведении `rgba(221,196,250,.45)`, при нажатии
`.62`, радиус 10px. Справа — pill-кнопка «Личный кабинет» (`#16121C`, hover `#2E2440`).

**Hero.** Сетка `1fr / 1fr`, gap 40px.
- H1 Onest 400, `clamp(28px,2.8vw,40px)`, line-height 1.24, `#1B1622`, max-width 19ch:
  «ProDisser — научное сопровождение аспирантов, соискателей и докторантов», где
  «ProDisser» в плашке-подсветке `#DDC4FA`, радиус 6px, `box-decoration-break: clone`.
- Форма: pill-переключатель «E-mail / Телефон» (активная `#16121C` + белый текст), поле
  54px, радиус 10px, рамка `#E6DEEF`, фокус `#7B2BE0`; кнопка «Оставить заявку».
  Две галочки согласия — **не предзаполнены** (по 152-ФЗ предзаполненная галочка
  согласием не считается), рядом ссылки на `privacy.html` и `offer.html`.
- Правая колонка: два круглых слота под фото — сиреневый 178px (`#DDC4FA`) и мятный
  122px (`#5FE6B6`), второй опущен на 123px. Фото ч/б (`grayscale(1)` на внутреннем
  слое), цвет раскрывается при наведении; подложка всегда цветная. Подписи по роли,
  центрированы по ширине кружка.
- Три плашки метрик, отступ сверху 98px — **объёмный уровень**: лаванда
  `linear-gradient(152deg,#E0CBFB,#CBA6F7)`, мята `linear-gradient(152deg,#7BEDC6,#3DDCA0)`,
  серая `linear-gradient(152deg,#FFFFFF,#F4EFF8)` + рамка. Радиус 14px, min-height 128px.
  Значения без чисел: «По этапам», «Команда», «2016».

### 2. StartPage — «С чего начнём вашу работу»
Три карточки-ссылки, сетка `.pd-routes`, gap 16px. Белый фон, рамка `#ECE5F3`,
радиус 14px, внутренняя кромка снизу `inset 0 -3px 0` с чередованием
`#7B2BE0 → #3DDCA0 → #BFB4CE`, тень `0 1px 2px rgba(22,18,28,.05)`.
Фотополоса 355px на подложке `#DDC4FA` / `#5FE6B6` / `#EFE9F5`. При наведении на
**всю карточку** (правила `.pd-routes a:hover .pd-routebg` / `.pd-routeimg`) подложка
уходит в прозрачность, фото раскрывается в цвет — переход 220ms.
Заголовки-результаты: «Довести диссертацию до защиты», «Сдать диплом и опубликовать
статью», «Защитить разработку и получить грант».

### 3. StartPage — карусели
Две карусели по 6 карточек: «Что входит в сопровождение» (4 в кадре) и «Гарантии
и документы» (3 в кадре). `scroll-snap-type: x mandatory`, `scroll-behavior: smooth`
на треке. **Прокрутка задаётся присваиванием `scrollLeft`** — не `behavior:'smooth'`
в JS, иначе не гасится по `prefers-reduced-motion`. Число страниц меряется в
`componentDidMount` и на resize через `requestAnimationFrame`. Полоса прогресса:
заполнение = видимая доля + прогресс. Трекам заданы `tabindex="0"` и `aria-label`.

### 4. StartPage — CTA и подвал
Две панели, `.pd-cta`, gap 24px — объёмный уровень. Мятная «Стандарт качества
ProDisser»: градиент 152°, иконка щита, моно-лейбл `#0E4E3C`, шесть белых pill-чипов.
Сиреневая «Оставьте заявку»: подсветка на слове «безвозмездно», тот же переключатель.
Подвал (паддинг 122px сверху): копирайт «© 2016–2026 ProDisser — Научный Консалтинг.
Работаем по всей России», строка реквизитов, ссылки на три документа, телефон и почта.

### 5. MainPage — аспирантам (12 секций)
Порядок: хедер → hero → две бегущие строки → «Проблема» → «Преимущества» (4 УТП) →
«Услуги» (3 карточки с иллюстрациями + плитки, 23 позиции) → «Этапы» (карусель) →
«Команда» (орбиты) → «Стоимость» → «Вопросы» → «Заявка» → подвал.
Шаг между секциями **96px везде**, подблоки — 48px через hairline.

**Меню интерактивно:** активный раздел определяется по прокрутке (верх секции ≤120px),
получает цвет `#16121C`, `aria-current` и сиреневое подчёркивание 2px, выезжающее
слева направо (`transform-origin: left; scaleX(0)→scaleX(1)`, 220ms). Слушатель с
`{ passive: true }`, снимается в `componentWillUnmount`.

**Бегущие строки:** дубликат массива для бесшовности помечен `aria-hidden`, анимация
останавливается по `:hover` и `:focus-within` через `animation-play-state: paused`.

**«Проблема»:** сетка `1fr auto 1fr`. Слева облако из 7 pill-чипов с болями поверх
приглушённого красного ореола — `radial-gradient(circle closest-side, …)` в 11 стопов,
`filter: blur(40px)`; **десять стопов и closest-side обязательны** — иначе видны
границы полос. Спиральный пунктир `stroke-dasharray="5 7"`. В центре — сиреневая
стрелка с градиентом, сужающаяся слева направо втрое. Справа — карточка и трек этапов.

**Трек этапов** интерактивен: 6 шагов, клик меняет состояние (пройденные — «Одобрено»,
текущий — «В работе», следующие — «Впереди»), различие цветом текста и полосы,
**не прозрачностью** — она снижает контраст ниже порога. Прокручивается стрелками,
4 в кадре.

### 6. StudentsPage — студентам (7 блоков)
Первый экран (паддинг 64/48) → «Над дипломом работает команда» (орбиты) → «Как устроена
работа от черновика до защиты» → состав работ (8 карточек + полный перечень 15 позиций
по кнопке) → смета со **свёрнутым блоком нормоконтроля** (7 причин возврата с кафедры) →
вопросы → заявка.
H1 `clamp(30px,3vw,42px)`: «Диплом, который **примут с первого раза**».
Переключатель «Где вы сейчас» перестраивает недельный план работ; логика пересчёта на
календарь сохранена, но поле даты удалено по решению заказчика — ветка недостижима.

### 7. BusinessPage — бизнесу (7 блоков)
Первый экран → бегущие строки → «Над задачей работает проектная команда» (орбиты) →
**шкала УГТ 1—9** → пять пакетов → каталог 52 позиции → шесть механизмов → права
и конфиденциальность → заявка.

**Шкала УГТ** — несущий элемент: девять уровней по ГОСТ Р 58048-2017, при выборе
перестраиваются три колонки (что делаем, документы на выходе, финансирование) и строка
связи с подходящим пакетом; клик прокручивает к его карточке. На уровнях 1–2 страница
прямо говорит «для инструментов внедрения рано». Документацию ведём **до УГТ 7** —
на 8 и 9 конструкторскую и серийную готовит предприятие.

**Каталог** — семь направлений, 52 позиции. Раскрытое направление показывает вводную
строку, получателя документа, чередующиеся строки позиций (`#FCFAFE` через одну)
и подвал с нормой и ссылкой на заявку.

**Пакеты:** карточки разной ширины в сетке из 6 колонок — 01 и 02 по 4 и 2, 03 и 04
по 3, 05 полосой на 6. Выводятся строго по порядку номеров: нумерация несёт смысл
последовательности от техзадания к постановке прав на баланс.

### 8. DesignSystem — справочник (6 разделов)
01 Токены · 02 Каркасы (сайт и рабочий интерфейс) · 03 Компоненты (6 на сайте,
13 в рабочем интерфейсе) · 04 Состояния (по три у таблицы, сетки, списка) ·
05 Данные для примеров (предельные значения) · 06 Приёмка.
Режим канваса (`<meta name="design_doc_mode" content="canvas">`). **В публикацию не идёт.**

Ветвь «Рабочий интерфейс» — задание на личный кабинет и CRM: плотный UI на тех же
токенах, вертикальный шаг 4/8/12/16/24, строка таблицы 44–48px, кегль 13–14px,
объёмные плашки не применяются.

### 9—11. Правовые документы
Колонка 72ch, липкое оглавление на десктопе (раскрыто) и свёрнутое на узком экране —
начальное состояние определяется через `matchMedia`, при печати разворачивается.
Сквозная нумерация 4.2.1. Без карточек, плашек и подсветок: выразительные средства —
колонка, интерлиньяж и нумерация. `@media print` убирает шапку, оглавление и подвал.

**Статус — проект до согласования юристом.** В шапке стоит пометка «Проект документа».

## Interactions & Behavior
- **Единая кривая** `cubic-bezier(.2,0,.2,1)`, длительности 180–260ms. Других нет.
- **Hover карточек** — только цвет рамки. Подъём, масштаб и смена тени запрещены.
- **Hover кнопок** — только цвет фона (`#16121C → #2E2440`).
- **Focus** — `*:focus-visible { outline: 2px solid #7B2BE0; outline-offset: 2px }`,
  никогда не отключается, всегда отличается от hover.
- **Карусели** — `scrollLeft` + CSS `scroll-behavior`; замер страниц после монтирования
  и на resize через `requestAnimationFrame`.
- **Плитки услуг** — раскрытие связано по ряду через состояние `offerRow`, а не
  CSS-hover: иначе высота ряда не выравнивается. Работает и по фокусу с клавиатуры.
- **prefers-reduced-motion** — `* { transition: none !important; animation: none !important }`.
- Формы: submit гасится, показывается плашка `role="status"` с переводом фокуса.
  Валидация и отправка — на стороне разработчика.

## State Management
```
StartPage:    sentTop, sentBottom, contactTop, contactBottom, direction,
              scopePage, grtPage, scopePages, grtPages, scopeFill, grtFill
MainPage:     open, sent, contact, stepPage, stepFrac, planPage, planPages,
              offerOpen, trackAt, navOpen, navActive, svcSel
StudentsPage: situation, open, sentTop, sentMain, offerOpen, normOpen,
              contactTop, contactBottom
BusinessPage: trl, open{}, allOpen, sentTop, sentMain, highlight,
              contactTop, contactBottom
Документы:    toc
```
Данных с сервера нет — контент статический. Формы требуют реального эндпойнта.

## Design Tokens

### Цвета — палитра РуВики
| Роль | Значение |
|---|---|
| Фон страницы | `#FFFFFF` (с `!important` на `html` и `body`) |
| Текст основной | `#16121C` · заголовки `#1B1622` · тёмный `#120E19` |
| Текст вторичный | `#4A4157` · `#584A6B` · `#3A3247` |
| Текст приглушённый | `#6B6178` — **светлее не бывает**, порог 4.5:1 к фактическому фону |
| Акцент фиолетовый | `#7B2BE0`, нажатие `#5E17B8`, светлее `#9E51E8` / `#8B3AE3` |
| Плашка-подсветка | `#DDC4FA` · подложка `#F4EAFE` · `#E9D6FC` · `#E5D3F7` |
| Мята | `#3DDCA0` · светлая `#5FE6B6` / `#7BEDC6` · тёмная `#1FA97B` · текст `#0E4E3C` |
| Кнопки | `#16121C`, hover `#2E2440` |
| Границы | `#EAE3F2` · `#ECE5F3` · `#EEE7F4` · hairline `#F2ECF7` |
| Поверхности | `#F8F5FC` · `#F6F1FB` · `#FCFAFE` · `#F9F6FC` |
| Нейтральная кромка | `#BFB4CE` |
| Успех | фон `#E6F9F1`, рамка `#5FE6B6`, текст `#0E4E3C` |
| Ошибка | фон `#FAE7E5`, рамка `#F0C9C3`, текст `#8E2C22` |
| Звёзды рейтинга | `#F0A92B` |

### Градиенты — только под 152°
- Лаванда: `linear-gradient(152deg,#E0CBFB 0%,#CBA6F7 100%)`
- Мята: `linear-gradient(152deg,#7BEDC6 0%,#3DDCA0 100%)`
- Серая: `linear-gradient(152deg,#FFFFFF 0%,#F4EFF8 100%)`

Применяются **только** к плашкам метрик и двум панелям CTA. Больше нигде.

### Тени — три формулы
- Уровень 0: без тени.
- Плоская карточка: `0 1px 2px rgba(22,18,28,.05)`
- Карточка: `0 6px 14px rgba(22,18,28,.06), 0 1px 2px rgba(22,18,28,.05)`
- Объёмная панель: `0 8px 20px rgba(22,18,28,.18)`
- Внутренняя цветная кромка: `inset 0 -3px 0 <цвет>`

Световые кромки `inset 0 1px 0 rgba(255,255,255,…)` — **запрещены**.

### Типографика
- **Onest** — интерфейс и заголовки; **JetBrains Mono** — лейблы, номера, даты, суммы,
  статусы, обозначения норм. Montserrat и другие внешние шрифты убраны.
- Кегли: 11, 12, 13, 14, 15, 17, 19, 20, 22, 26, 28 + `clamp()` для H1.
  **Минимум 11px**, и только для моно-лейблов.
- Интерлиньяж — **коэффициентами**: 1.24 / 1.4 / 1.5 / 1.6. Пиксельных значений быть
  не должно: они не переживают смену кегля.
- `font-variant-numeric: tabular-nums` во всех числах.
- Колонка: заголовки max 34ch, текст 46–72ch.

### Отступы и геометрия
- Шаг 8px. Между секциями 96px, подблоки 48px, контейнер 1220px, паддинг 30px (20px на 480).
- Радиусы: 14px (карточки), 10px (поля, вложенные блоки), 6px (подсветка),
  999px (pill), 50% (круг). Других не вводить.
- Брейкпоинты 1024 / 768 / 480.

## Placeholders
- Слоты изображений пусты: 2 портрета в hero посадочной, 3 фотополосы в блоке
  направлений, портреты и фото в блоках FAQ и заявок на внутренних страницах,
  12 аватаров отзывов (сейчас векторные силуэты-заглушки).
- В правовых документах сроки (5 рабочих дней на замечания, 10 на претензию, 3 года
  режима конфиденциальности, 1 год хранения данных заявителей) выбраны предположительно —
  подтвердить у юриста.
- Наименования сервисов (хостинг, почта, аналитика, платёжный) в политике описаны
  словами без названий — подставить из договоров.

## Assets
- `assets/icons/` — набор Lucide, перештрихованный на `stroke-width="1.5"`, 30 глифов.
  Подключаются CSS-маской, поэтому наследуют цвет. **Проверять наличие файла перед
  использованием** — иначе маска не отрисуется.
- `assets/art/svc-*.svg` — иллюстрации услуг: изометрические тома в переплёте, палитра
  сирень / лаванда / мята, мягкая тень. `avatar-*.svg` — силуэты на фирменных подложках.
- `image-slot.js` — веб-компонент слота изображения (drag-and-drop). Локально доработан:
  атрибут `transparent` убирает серую подложку и пунктирную рамку. В продакшене
  заменяется на `<img>`; описание для `alt` лежит в `data-alt`.
- Логотип — **текстовый вордмарк**, не картинка.
- `support.js`, `ds-base.js` — рантайм среды прототипирования, в продакшен не переносятся.

## Правила, которые нельзя нарушать
1. Никаких эмодзи — ни в интерфейсе, ни в копирайте.
2. Тон академический: без «под ключ», «напишем за вас», «гарантия защиты»,
   «повысим оригинальность», «100% результат», превосходных степеней.
3. Логотипы Scopus / WoS / Антиплагиат как знаки доверия запрещены.
4. Каждое оценочное утверждение снабжено носителем — договор, документ, срок, процедура.
5. Ни одного обещания решения диссовета, кафедры, комиссии, редакции, фонда,
   Роспатента или налогового органа. Предмет обязательства — соответствие требованиям
   на дату подачи.
6. Год начала работы сервиса — **2016**, везде, включая копирайт.
7. Максимум два фоновых мотива на страницу.
8. Объём (градиент + тень) — только плашки метрик и панели CTA.
9. Ни один текст мельче 18px не светлее `#6B6178`.
10. Галочки согласия **не предзаполняются**: с 01.09.2025 согласие оформляется отдельным
    документом, предзаполненная отметка согласием не считается.
11. Согласие на обработку данных и согласие на рекламу — **разные галочки**.
12. Первичная консультация — безвозмездная, с предварительной оценкой. Скидки за
    регистрацию не обещаем.

## Публикация
Репозиторий: `klemmerpavel-beep/Scientific-Consulting-Service_Web`, ветка `main`.

Раздел переписан 21.08.2026. Прежний план — семь готовых `.html` на GitHub
Pages — отменён: сайт собирается приложением Next.js, страницы отдаются по
адресам `/`, `/main`, `/students`, `/business`, `/offer`, `/privacy`,
`/consent`, а `.html`-имена остались только внутри макетов как цели ссылок
и переписываются конвертером. Причина в приёме заявок: статике негде хранить
заявку, а требование ч. 5 ст. 18 152-ФЗ обязывает держать базу в России.
Обоснование — решение Р-05 в `docs/DECISIONS.md`.

Макеты в `design/` остаются источником вида и поведения. Правится макет,
а не сгенерированный компонент: сборка в CI проверяет, что перенос
воспроизводим.

## Что осталось незакрытым
1. Согласовать три правовых документа с юристом.
3. Площадка размещения — подготовка в `deploy/` и `docs/DEPLOY.md` ждёт
   выбранного сервера. Уведомление Роскомнадзора подаётся до открытия сайта.
4. Галочка согласия на рекламные сообщения — правило 11 требует отдельную,
   решение за заказчиком (Р-10).
5. Личный кабинет и CRM — задание в разделе 02 справочника дизайн-системы,
   макетов нет.
6. Отзывы: блок удалён, возвращается только с реальными текстами и письменным
   согласием авторов (флаг согласия и флаг раскрытия имени — отдельные поля).

Закрыто: семь страниц собраны и переходы работают; пять снимков стоят
в слотах (Р-11, Р-12).
