# Свод решений

Сто одиннадцать решений, принятых по проекту с августа по сентябрь 2026
года, сведённые к существу. Полный журнал с разбором каждого решения — во
внутреннем репозитории; здесь остаётся то, что нужно знать человеку,
продолжающему работу над кодом.

Формат ссылки на решение по всему проекту — `Р-NN`.

---

## Устройство проекта

| № | Решение |
|---|---|
| Р-05 | Стек: Next.js App Router, React, Prisma, PostgreSQL. Приложение самодостаточное, разворачивается контейнером |
| Р-13 | Площадка только российская: база с данными граждан России физически в России, ч. 5 ст. 18 152-ФЗ |
| Р-25 | Раскладка навигации живёт в классе, а не в атрибуте `style`: правило под размер экрана не может перебить инлайновый стиль |
| Р-33 | Правило под размер экрана пишется с `!important` — по той же причине |
| Р-36 | Накат схемы базы — отдельный слой образа: приложение не стартует, пока схема не применена |
| Р-37 | Резервная копия проверяется до замены предыдущей, а не после |
| Р-50 | Библиотеки анимации не подключаются. Движение задаётся средствами CSS |

## Приём заявок и персональные данные

| № | Решение |
|---|---|
| Р-04 | Заявка сначала сохраняется в базу, потом доставляется. Запись без доставки — задержка ответа; доставка без записи — потерянный клиент и отсутствующий журнал согласия |
| Р-30 | Адрес заявителя берётся из `X-Real-IP`, а не из `X-Forwarded-For`: первый элемент второго задаёт сам отправитель |
| Р-31 | Политика содержимого и сроки доставки: восемь секунд на канал, отказ канала не роняет приём |
| Р-40 | Форма показывает, что отправка идёт, и объявляет результат голосом программы чтения с экрана |
| Р-41 | Правовые тексты не описывают того, чего сайт не делает |
| Р-42 | У каждого правового документа есть дата редакции |
| Р-110 | Форма отзыва не собирает персональных данных: только роль и текст. Нет данных — нет и обязанности брать согласие |
| Р-111 | Отзывы выгружаются в CSV и ведутся реестром на диске заказчика; прямой записи в облако из приложения нет |
| Р-109 | Правовые тексты согласованы, пометка «Проект документа» снята. Цены на сайте не публикуются. Отзыв принимается формой и публикуется только с разрешения автора |
| Р-10 | Отметка согласия на рекламные сообщения — открытый вопрос к юристу. Колонка в базе и поле в схеме разбора есть, видимой отметки в формах нет |

Состав полей форм приведён к направлениям решением Р-96 — один раз и до
открытия сайта. С этого момента состав, порядок, валидация, тексты галочек
согласия и версия текста согласия заморожены: на них завязан журнал согласий
по 152-ФЗ, и изменение состава полей после появления настоящих заявок
разрывает журнал.

## Айдентика и оформление

| № | Решение |
|---|---|
| Р-51 | Палитра: графитовый текст, светлая основа, один глубокий синий акцент |
| Р-52 | Шрифтовая пара: Literata в заголовках, Inter в тексте; JetBrains Mono в служебных подписях |
| Р-53 | Изображения людей генерируются моделью и не персонифицируются: подпись только по роли, без имени |
| Р-106, Р-113 | Отзывы — настоящие, с Авито, приведены дословно; публикуются имя и дата, фамилии и фотографии с площадки — нет. Отзыв, называющий предметом работу за клиента, не публикуется |
| Р-114 | Счётчик посещаемости стартует только после согласия посетителя; редакция раздела политики о веб-аналитике переключается тем же признаком, что и сам счётчик |
| Р-115 | Страница «адрес не найден» строится из макета и оформлена как остальные страницы; разметка организации не выводится в витрине |
| Р-116 | Перенос строк объявлен одним правилом на все макеты: заголовки выравниваются по строкам, абзацы не оставляют одинокого слова |
| Р-117 | Домен `prodisser.ru` куплен; виртуальный хостинг заменяется облачным сервером: проекту нужны своя PostgreSQL, постоянный процесс, свои заголовки безопасности и Docker |
| Р-118 | Стадия зависимостей образа получает схему Prisma до установки; сборка образа проверяется в CI, а не на боевом сервере |
| Р-119, Р-120 | Развёртывание: `listen ... http2` вместо отдельной директивы (nginx 1.24); запрос очистки уходит в psql через ввод, иначе кавычки вокруг «Lead» теряются |
| Р-121 | Сайт открыт 13.09.2026 на prodisser.ru: облачный сервер в России, сертификат Let's Encrypt, ночные копии и очистка по сроку хранения в расписании |
| Р-125 | Заявки выгружаются в CSV, пока не подключена почта; файл содержит персональные данные и в облако не выкладывается — в отличие от выгрузки отзывов |
| Р-126 | Кабинет убирается из сборки витрины: раздел весь серверный, статический вывод его не собирает |
| Р-127 | Восстановление резервной копии проверяется скриптом: временная база, сверка состава, удаление в любом исходе |
| Р-128 | Развёртывание дополнено кабинетом: переменные в описании сервиса, материалы работ на хосте, архив материалов рядом с дампом |
| Р-129 | Рассылка уведомлений кабинета вынесена в скрипт: cron не читает deploy/.env, и заголовок с секретом уходил пустым |
| Р-130 | Приёмочный прогон кабинета перед выкатом: форма одобрения заявки не помещалась в телефон — сетка и список выбора поправлены |
| Р-131 | Первая учётная запись кабинета заводится скриптом на сервере: иначе после выката войти было бы некому |
| Р-132 | Выгрузки и заведение учётной записи запускаются отдельным разовым образом: в рабочем нет ни исходников, ни скриптов |
| Р-133 | Перенос книги заказов: тридцать исторических написаний типа сводятся к шести позициям справочника; естественный ключ строки — дата, ФИО, тип и стоимость; историческому клиенту заводится карточка без учётной записи |
| Р-134 | Витрина потерь показывает обе величины: расчётные 125 000 ₽ по остановленным работам и 365 000 ₽ из брифа — расхождение выводится, а не сглаживается |
| Р-135 | Аналитика практики: шесть вкладок на собственных графиках SVG, палитра — синий из таблицы токенов; библиотека графиков не подключается |
| Р-136 | Журналы действий и доступа к файлам с фильтрами и выгрузкой; удаление данных субъекта затирает персональные поля и объекты хранилища, сохраняя учётные суммы |
| Р-137 | Артборд кабинета — воспроизводимый снимок облика работающего экрана, а не источник истины: код пишется вручную, снимок снимается скриптом |
| Р-138 | Экраны кабинета выложены в витрину снимками: живой раздел серверный, а артборды открываются по ссылке без сервера — данные на них вымышлены |
| Р-139 | Прототип кабинета: связанный обход экранов по ролям вместо набора отдельных снимков; заодно логотип кабинета приведён к логотипу сайта |
| Р-140 | Кабинет сведён к двум ролям — клиент и руководитель; экран клиента отвечает на вопрос «что с моей работой», облик выправлен по дизайн-системе |
| Р-141 | Прототип наполняется настоящей книгой заказов в обезличенном виде; действующие работы берутся по жёлтой заливке, а не по тексту статуса |
| Р-142 | Остаток аудита облика закрыт: шкала графиков разведена по различимым цветам с подписью долей, поля получили подписи, повторы вёрстки сведены в общий модуль |
| Р-143 | Клиент не видит исполнителя нигде, включая авторство файлов и событий; экраны этапа, материалов и переписки доведены до общего вида |
| Р-144 | Прототип кабинета публикуется второй площадкой, не зависящей от `github.io`: переносимая копия, где каждая ссылка указывает на файл |
| Р-145 | Короткий разговор с куратором показывается на карточке работы; лента переписки сведена в общий компонент, прочтение по-прежнему отмечает только экран переписки |
| Р-146 | Зелёный и красный оставлены исходу действия, как на сайте; состояния кабинета оформляются нейтралью и одним акцентом и называются словом |
| Р-147 | Облик просмотрен снимками экранов: повторы и дефекты подписи устранены, а правила «клиент не видит исполнителя» и «зелёный и красный — только исход» проверяются машиной |
| Р-148 | Аудит по своду правил UI/UX: уровень заголовка отделён от кегля, кнопка гаснет на время отправки, подписи графиков прорежены, снимок перестал отставать от кода |
| Р-149 | Третья плашка — менеджер: ведёт работы, где он куратор; разграничение сделано выборкой, передача работы — право руководителя |
| Р-150 | Четвёртая плашка — эксперт: назначенные работы и собственное вознаграждение; переписки с клиентом нет, договор поручения назван причиной закрытого доступа |
| Р-151 | Выкат идёт сам после зелёной проверки: работу делает скрипт на сервере, откат возвращает образ и коммит, ключ привязан к одной команде |
| Р-152 | Формы кабинета приведены к одному строю: Form, FormRow, FormActions и FileField вместо вёрстки на каждом экране; колонки карточки заявки сравнялись, кнопки встали на одну линию |
| Р-153 | Кабинет до настройки закрывается сам: при незаданных SESSION_SECRET, CABINET_STORAGE_DIR или базе раздел показывает «готовится» вместо ошибки сервера |
| Р-154 | Недоставленное уведомление видно руководителю: экран очереди со сводкой и повтором отправки, строка в «Требует внимания»; ненастроенный канал попыток не расходует |
| Р-155 | Таблица заявок выгружается на российское хранилище, а не в Google: в заявке персональные данные, и вывоз их за границу требует согласия каждого заявителя и уведомления РКН |
| Р-156 | Итоги по годам: введённое руководителем и посчитанное кабинетом стоят рядом, расхождение показывает полноту переноса истории |
| Р-157 | Очередь заявок листается по двадцать с указанием общего числа: прежде показывались первые пятьдесят и ничего о трёхстах остальных |
| Р-158 | Служебные экраны приведены к одному строю форм: двадцать восемь ручных форм сведены к общим частям, идентификатор поля получил область, служебный контур собран за пунктом «Управление» |
| Р-159 | Все заявки видны разделом кабинета с отбором и выгрузкой в браузер: личный Диск любой цены не даёт договора поручения обработки |
| Р-160 | Выкат не зависит от ограничения Docker Hub: базовый образ забирается один раз с повторами и помечается локально, сборка в реестр не ходит |
| Р-161 | Доставка заявок с сайта видна на экране очереди: отказ канала прежде читался только в журнале контейнера; названия страниц и состояний сведены в один перечень |
| Р-162 | Одобрение заявки ставит клиенту письмо в очередь той же транзакцией; ссылки входа в письме нет — она живёт пятнадцать минут и к прочтению была бы мертва |
| Р-163 | Вход не обещает письма, которого не будет: ненастроенная почта называется прямо, исход отправки пишется в журнал по факту |
| Р-164 | Экран входа занимает страницу целиком: порядок входа тремя пунктами и блок помощи вместо серой строки; плашки ролей из обвязки прототипа сняты |
| Р-165 | Сквозной аудит кабинета по дизайн-системе: цифры выровнены по разряду, радиусы и знаки приведены к набору, акцент по одному на экран, заголовки таблиц связаны со столбцами; пять новых правил облика |
| Р-166 | Вход — только форма: своды правил и помощи сняты, центрирование перенесено в каркас, страница встала ровно в окно |
| Р-167 | Ответ «что сейчас и что от меня» поднят выше темы работы: длинная тема отжимала его за нижний край телефона; предельные значения проверены стендом |
| Р-168 | Доступность кабинета проверена машиной: axe-core по 84 экранам на двух ширинах; таблицы с прокруткой стали достижимы с клавиатуры, ссылка внутри текста подчёркнута; два новых машинных правила |
| Р-169 | Экран заказа перестроен панелью и помещается в окно: готовность, план, материалы и переписка сразу; прокручиваются колонки, а не страница. Перечень заказов даёт состояние работы без захода внутрь |
| Р-56 | Нижний кегль поднят с 11 до 12 px |
| Р-57 | Цель нажатия — 44 px, кроме ссылки внутри строки текста |
| Р-61 | Собственный набор пиктограмм в карточках вместо готовых наборов |
| Р-43 | Движение — две вещи и ничего больше: появление секции и отклик на действие |
| Р-38, Р-39 | Правила наведения и нажатия объявляются важными; отдача на нажатие обязательна для всего, что нажимается |
| Р-47 | Снимок не растягивается выше своего разрешения |
| Р-32 | Подсветка — один заголовок на страницу |
| Р-95 | Карточка отзывается на наведение рамкой основного акцента |
| Р-97 | Палитра объявляется токенами `--pd-…` в `:root`, а не литералами. Исключение — презентационные атрибуты SVG: `var()` в них не работает |
| Р-98 | Новый цвет заводится только под роль, которой нет. Блок подтверждения отправки одинаков во всех формах: `ok-bg` + `ok-border` + `ok-ink` |
| Р-102 | Правка, меняющая структуру разметки, а не значения свойств, принимается только по снимку экрана: программные проверки её не ловят по устройству |
| Р-99 | Интерлиньяж только коэффициентом (1.24 / 1.4 / 1.5 / 1.6); скругления 6 / 10 / 14 / 999px / 50 %; отступы кратны 2 px; заголовок раздела 22 px на всех страницах |

Действующая дизайн-система целиком описана в `DESIGN-SYSTEM.md`: палитра с
ролями, шкала кеглей, скругления, тени, движение, компоненты, а также долг
системы и правила её расширения.

## Содержание страниц

| № | Решение |
|---|---|
| Р-02 | Источник правды по позиционированию — бриф заказчика, а не домыслы |
| Р-03 | Цены на сайте не публикуются: сумма называется после разбора задачи. Вместо суммы — состав сметы и сроки |
| Р-44 | Посадочная обращается ко всем трём направлениям сразу, а не выбирает одно |
| Р-28 | Этапы работы вместо обратного отсчёта: срок считается от подписания договора |
| Р-46 | Этапы — один план, а не пять карточек |
| Р-49 | Маршрут переключается, а не разворачивается списком |
| Р-45 | Шесть механизмов поддержки видны сразу, без раскрытия |
| Р-48 | Нумерация в каталоге — внутри направления, а не сквозная |
| Р-35 | Повторяющаяся оговорка выносится из карточек в одну подпись под блоком |
| Р-54 | Плашки первого экрана несут проверяемые величины, а не рекламные обещания |
| Р-104 | Лид первого экрана — один тезис. Условия договора читаются там, где человек к ним готов, а не в первом абзаце |

## Приёмки

| № | Что принято | Точка отката |
|---|---|---|
| Р-63 | Посадочная, 06.09.2026 | `accepted/landing-2026-09-06` |
| Р-72 | «Студентам», 07.09.2026 | `accepted/students-2026-09-07` |
| Р-79, Р-81 | «Компаниям», 09.09.2026 | `accepted/business-2026-09-09-r81` |
| Р-84 | Съёмка людей и профили ролей, 09.09.2026 | `accepted/photos-2026-09-09` |
| Р-85 | «Аспирантам», 09.09.2026 | `accepted/postgrad-2026-09-09` |
| Р-87 | Аудит и правки по рекомендациям, 12.09.2026 | `accepted/audit-2026-09-12` |
| Р-105 | Посадочная после правок ритма и шапки, 12.09.2026 | `accepted/landing-2026-09-12` |
| Р-106, Р-107 | Блок отзывов на трёх профильных страницах, 12.09.2026 | `accepted/reviews-2026-09-12` |
| Р-95, Р-96 | Подсветка блоков, порядок разделов «Компаниям», формы по направлениям, 12.09.2026 | `accepted/forms-2026-09-12` |

Ветки приёмок заморожены: в них не коммитят и их не двигают.

## Аудиты и проверки

| № | Решение |
|---|---|
| Р-67 | Аудит материалов, чистка кода и ресурсов |
| Р-75 | Сплошной аудит семи страниц и сверка документов с фактом |
| Р-86 | Аудит критических недочётов: кегль полей форм поднят до 16 px против принудительного масштабирования вьюпорта в Safari на iOS; две нормативные ссылки на странице «Компаниям» вели на отменённые редакции ГОСТ |
| Р-88 | Проверка перед выпуском: закрыты критическая и четыре высоких уязвимости зависимостей, выключен неиспользуемый оптимизатор изображений, удалены четыре неиспользуемые производственные зависимости, исправлена ловушка для роботов, отклонявшая заявку вместо пометки |
| Р-100 | Проверка перед развёртыванием: уязвимости зависимостей закрыты через `overrides`, витрина получила `robots.txt`, карта сайта перестала противоречить запрету на обход |
| Р-89 | Подготовка репозитория к публикации: закрыт путь утечки резервных копий с персональными данными, исправлена карта сайта, отдававшая относительные адреса, убраны сторонние и устаревшие материалы, описана дизайн-система |
| Р-90 | Условия использования и уведомление о правах третьих лиц: файлы `LICENSE` и `NOTICE` |
| Р-91 | Разбор очереди обновлений; отключено и удалено стороннее развёртывание за пределами России |
| Р-92 | Мажорные обновления Next 16 и TypeScript 7; проверка витрины добавлена в CI |
| Р-93 | Уменьшенное движение учитывается на правовых страницах |
| Р-94 | Перенос упущенных файлов развёртывания: временный узел nginx, срок хранения заявок |

Приёмочный набор, который гоняется перед каждой фиксацией: axe-core на семи
страницах, переполнения на семи ширинах от 360 до 1440 px, битые снимки,
ответы 4xx и 5xx, заявки со всех восьми форм, обход клавиатурой, LCP, CLS, вес,
обе сборки, воспроизводимость переноса макетов.

## Правила, действующие постоянно

1. Правится макет `design/*.dc.html`, а не сгенерированный компонент.
   Компоненты перезаписываются при каждом переносе, CI проверяет
   воспроизводимость.
2. Каждое решение записывается с разбором, а не только с результатом: через
   полгода важно, почему сделано так.
3. Принятое состояние закрепляется веткой `accepted/…` — точкой отката,
   которую не двигают и не удаляют.
4. Бэкенд не меняется правками вида: схема, миграции, `app/src/app/api/` и
   `app/src/lib/` — только отдельным решением с явной причиной.
5. Зависимости не добавляются без отдельного решения.
6. Юридические страницы правятся только по прямому указанию заказчика и
   уходят юристу на проверку; без такого указания в них меняются только
   палитра и шрифты. Ограничение снималось дважды: Р-109 и Р-114.

---

Порядок дальнейших работ и незакрытые вопросы — в `PLAN.md`.
Как вносить правки — в `../CONTRIBUTING.md`.
