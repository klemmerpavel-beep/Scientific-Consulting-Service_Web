# Принятые решения

Источник правды по спорным вопросам проекта. Любая правка на любом носителе
сверяется с этим файлом. Изменение решения фиксируется здесь с датой.

Основание — аудит третьего круга от 21.08.2026, разделы §4 и §7.

---

## Р-01 · Год начала работы

**Решение: перевести утверждение на команду, а не на юридическое лицо.**

Формулировка: «научный коллектив работает с 2016 года» и производные от неё.
Подтверждается стажем специалистов, а не датой регистрации ООО.

Прямо запрещено:
- «Компания работает с 2016 года» — юрлицо зарегистрировано 02.06.2025
- «с 2001 года» — вариант из КП, не подтверждается ничем
- «© 2016–2026 ProDisser» в копирайте — заменяется на «© 2026 ProDisser»

Метрика «2016» на посадочной и строки-факты на трёх внутренних страницах
переписываются под формулу коллектива.

## Р-02 · Источник правды по позиционированию

**Решение: главный источник — сайт.**

Действующая формула: *исследование ведёт автор, сервис отвечает за экспертизу
и сопровождение*. Технико-коммерческое предложение переписывается под сайт,
а не наоборот.

Из КП в проект **не переносятся**:
- «работа под ключ» и любые синонимы
- «безукоризненное качество», «превосходный результат», «гарантии уникальны»
  и прочие превосходные степени
- «скидка до 10 000 ₽ за обращение в срок действия предложения»
- метрика «3 000+ специалистов» и «260 специалистов» — числа расходятся
  между источниками и ничем не подтверждены

Правила тона из `design/HANDOFF.md`, раздел «Правила, которые нельзя нарушать»,
остаются в силе целиком.

## Р-03 · Цены

**Решение: публикуем нижнюю границу без верхней.**

Формат: «от N ₽» по ключевым позициям, рядом — оговорка, что итоговая смета
фиксируется в договоре после разбора материала.

Источник цифр — прайс КП. Позиции с вилкой публикуются по нижнему краю вилки.
Верхняя граница на сайт не выносится.

Сохраняется существующий блок трёх факторов ценообразования: специальность,
тип работы, тема.

## Р-04 · Приём заявок

**Решение: три канала одновременно.**

1. **Своя база на сервере в РФ** — основное хранилище. Требование ч. 5 ст. 18
   152-ФЗ. Эта же база становится источником данных для админ-панели на
   следующем витке.
2. **Telegram** — мгновенное уведомление ответственного о новой заявке.
3. **Письмо на корпоративную почту** — дублирующий канал и архив.

Журнал согласий ведётся вместе с заявкой: метка времени, IP, версия текста
согласия, состояние каждой галочки.

Внешняя почта `info.prodisser@gmail.com` из контура приёма заявок выводится —
см. Р-06.

## Р-05 · Стек

**Решение: Next.js (App Router) + TypeScript + PostgreSQL.**

Обоснование: один проект закрывает текущий объём и следующий виток.
Публичные страницы отдаются статически, приём заявок работает в том же
приложении, личный кабинет и админ-панель встают сверху без переписывания.
Разворачивается на российском VPS в контейнере, за обратным прокси.

Отвергнутый вариант: статика без сборщика плюс отдельный микросервис форм.
Дешевле сейчас, но личный кабинет становится вторым проектом со своей
авторизацией, своей вёрсткой и своим деплоем.

## Р-06 · Домен, хостинг, почта

**Решение: подбирается и обосновывается на этапе 5, покупка за заказчиком.**

Обязательные условия:
- провайдер состоит в реестре хостинг-провайдеров Роскомнадзора
- база с персональными данными физически расположена в РФ
- корпоративная почта на собственном домене вместо внешнего сервиса
- уведомление Роскомнадзора об обработке персональных данных подаётся
  до открытия сайта

Домен `prodisser.ru` фигурирует в материалах, но не куплен.

## Р-07 · Кнопка «Личный кабинет»

**Решение: заменить на «Оставить заявку».**

Якорь `#request` остаётся прежним и становится честным. Шапка посадочной
приходит к тому же виду, что у трёх внутренних страниц.

Кнопка входа в кабинет возвращается вместе с продуктом, не раньше.

## Р-08 · Фотографии

**Решение: слоты сохраняются, реальные снимки предоставляет заказчик.**

Пятнадцать слотов остаются в макетах. До получения файлов в них стоят
осмысленные подписи — служебная строка «Drop an image» на основной странице
убирается в любом случае.

Условие публикации каждого портрета — письменное согласие изображённого лица.
Стоковые фотографии не используются: страница продаёт персональную
ответственность конкретного специалиста.

---

## Порядок этапов

| Этап | Содержание | Состояние |
|---|---|---|
| Э0 | Аудит и фиксация решений | выполнен 21.08.2026 |
| Э1 | Доработка дизайна в связке с Claude Design | выполнен 21.08.2026 |
| Э2 | Ревизия UI, UX и наполнения | выполнен 21.08.2026 |
| Э3 | Формы, кнопки и приём заявок | выполнен 21.08.2026 |
| Э4 | Продакшн-код | выполнен 21.08.2026 |
| Э4б | Публикация в GitHub | выполнена 21.08.2026 |
| Э5 | Размещение с доступом из России | подготовлено, ждёт площадки |

Личный кабинет и административная панель начинаются после того,
как сайт открыт. Задание на них — раздел 02 справочника дизайн-системы.

---

## Р-09 · H1 двух страниц (принято 21.08.2026 в ходе этапа 2)

**Решение: обещания решения третьей стороны в заголовках сняты.**

Было: «Диплом, который **примут с первого раза**» и «Оформить разработку
так, чтобы её **приняли и защитили**». Обе формулировки обещают решение
кафедры, комиссии, фонда или Роспатента, и обе стояли внутри подсветки —
в самом заметном месте страницы. Правило 5 проекта это запрещает.

Стало: «Диплом, собранный **под требования вашей кафедры**» и «Оформить
разработку **по требованиям приёмки, гранта и патента**».

Правка редакторская: если формулировка не устраивает маркетингово,
подбирается другая — но вернуть обещание результата нельзя.

## Р-10 · Согласие на рекламные сообщения — открытый вопрос

Правило 11 проекта требует отдельную галочку на рекламные сообщения.
Сейчас её нет ни в одной из семи форм. Следствие: писать заявителям
что-либо, кроме ответа по их заявке, нельзя.

Поле `marketingOptIn` в базе заведено и готово принимать значение.
Решение о добавлении галочки за заказчиком.

## Р-11 · Фотографии: цвет вместо чёрно-белого (принято 21.08.2026)

**Решение: снимки публикуются в цвете, обесцвечивание снято.**

Было: все пять портретов выводились через чёрно-белый фильтр, цвет
проступал только при наведении на карточку направления. Заказчик
попросил цветные снимки — фильтр убран во всех одиннадцати обёртках
на четырёх макетах вместе с правилом `.pd-routeimg` на посадочной.

Следствие для наведения: карточка направления больше не «раскрашивается».
Отклик на наведение остался за подъёмом карточки и тенью.

## Р-12 · Расстановка снимков по слотам

**Решение: размер оригинала определяет, в какой слот идёт снимок.**

Пришло пять вырезанных портретов с прозрачным фоном, и они сильно
разного размера:

| Файл | Оригинал | Кто на снимке |
|---|---|---|
| `student.webp` | 652×682 | студентка с ноутбуком |
| `postgrad.webp` | 572×784 | автор с переплетённой работой |
| `business.webp` | 549×549 | менеджер проекта |
| `curator.webp` | 380×290 | куратор, погрудный кадр |
| `editor.webp` | 171×149 | научный редактор |

Правила расстановки:

1. **`editor.webp` не ставится в крупные карточки.** В блоке «Вопросы»
   на странице аспирантов он растягивался в 6,9 раза и заметно мылил.
   Его место — круглые портреты 112–130 px, там растяжение не выше 1,5.
2. **В узкий высокий слот идёт вертикальный снимок.** Слот «Вопросов»
   на странице аспирантов — 268×654. Квадратный `business.webp`
   срезало бы по локти, вертикальный `postgrad.webp` входит целиком.
3. **На одной странице — четыре разных человека.** Повтор лица внутри
   страницы читается как нехватка материала.
4. **Потолок растяжения — 2×** при удвоенной плотности точек.
   Фактический максимум после расстановки: 1,6 (полоса «Бизнесу» на
   посадочной), в карточках 1,3–1,5.

Если заказчик пришлёт оригиналы большего размера, растяжение уйдёт
само: соответствие «слот → файл» живёт одной строкой в
`app/src/components/photos.ts`.

## Р-13 · Только российские площадки (принято 22.08.2026)

**Решение: рабочий сайт, база и почта размещаются в России. Зарубежные
площадки из плана исключены.**

Отменяет подготовленный демонстрационный стенд на Vercel с базой Supabase.
Причины отказа, названные заказчиком и подтверждённые разбором:

1. **Персональные данные уезжали бы за границу.** Заявка — это имя и
   контакт. Бесплатная база под Vercel встаёт в США или ЕС, что прямо
   расходится с ч. 5 ст. 18 152-ФЗ.
2. **Бесплатный тариф Vercel запрещает коммерческое использование.**
   Сайт действующего ООО под него не подходит.
3. **Доступ из России к `vercel.app` не гарантирован** — то самое
   требование, ради которого площадка и выбиралась.

Бюджет на размещение: до 1000 ₽/мес. Этого достаточно для VPS
российского провайдера из реестра хостинг-провайдеров Роскомнадзора.

**Исключение — витрина проекта.** Статические страницы на GitHub Pages
без сервера, без базы и без приёма заявок: собирать там нечего, хранить
нечего, требование о локализации не затрагивается. Витрина закрыта от
поисковиков и удаляется после открытия рабочего сайта. Подробности —
`docs/PREVIEW.md`.

Признак `NEXT_PUBLIC_PREVIEW` и статический вывод остаются в проекте:
они не мешают рабочей сборке и дают показывать проект по ссылке
в любой момент.

## Р-14 · Вход в личный кабинет (принято 22.08.2026)

**Решение: ссылка на почту как основной путь, пароль как альтернатива.**

Клиент вводит почту и получает ссылку для входа. Кто хочет пароль —
заводит его в кабинете и дальше входит парой «почта + пароль».

Следствия для проектирования, которых не было в первом задании:
- экраны регистрации пароля, смены и восстановления;
- хранение хешей паролей (Argon2id), а не самих паролей;
- ограничение частоты попыток входа и запросов ссылки;
- срок жизни ссылки и одноразовость: повторный переход не пускает.

SMS отвергнут: требует платного шлюза и договора с оператором,
3–5 ₽ за сообщение, при заходе раз в неделю выгоды не даёт.

## Р-15 · Очерёдность работ (принято 22.08.2026)

**Решение: сначала запуск сайта, потом админ-панель, потом кабинет.**

Обоснование порядка: заявки уже сохраняются в базу, но смотреть их можно
только в Telegram и почте — админ-панель закрывает этот пробел раньше,
чем кабинет, которым пока некому пользоваться.

## Р-16 · Уточнение к Р-06: домен, почта, уведомления

Состояние на 22.08.2026:

- **Домен не куплен.** `prodisser.ru` фигурирует во всех материалах —
  проверяется на занятость, при отказе подбираются запасные.
- **Нужна почта на своём домене.** Внешний `info.prodisser@gmail.com`
  из контура приёма заявок выводится (Р-04), в макетах он остаётся
  плейсхолдером до получения рабочего адреса.
- **Нужен Telegram-бот** для мгновенного уведомления о заявке.
  Код принимает `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`, бот заводится
  через @BotFather.

Развёртывание идёт по письменной инструкции: доступ к серверу остаётся
у заказчика, команды выполняет он.

## Р-17 · Кадрирование фотографий по измерениям (принято 22.08.2026)

**Решение: положение и масштаб фигуры считаются из содержимого файла,
а не подбираются множителем.**

Прежний способ — «фигура во столько-то раз выше слота» — дал разъезд в
трёх карточках направлений: головы срезало сверху, студентка уехала
вправо, лица вышли разного размера. Причина в самих снимках, а не в
вёрстке: люди стоят в кадре по-разному.

Измерения по альфа-каналу файлов:

| Файл | Верх фигуры | Ширина головы | Смещение головы от центра кадра |
|---|---|---|---|
| `curator.webp` | 11 | 146 | +0,1 % |
| `editor.webp` | 5 | 57 | −2,0 % |
| `postgrad.webp` | **3** | 161 | −0,7 % |
| `student.webp` | 10 | 214 | **+12,5 %** |
| `business.webp` | 20 | 151 | +0,1 % |

Два числа объясняют весь разъезд. У аспиранта над головой три пикселя
запаса — любое увеличение срезает причёску. У студентки голова смещена
на 12,5 % вправо, потому что она держит ноутбук слева; центрирование по
слоту уводило её вбок.

Правило кадрирования:

1. **Масштаб задаётся требуемой шириной головы.** В соседних карточках
   лица выходят одного размера независимо от крупности исходников.
2. **Центр головы сводится с центром слота.** Сдвиг постоянный
   в пикселях, поэтому не разъезжается при изменении ширины.
3. **Сверху остаётся заданный воздух** либо низ снимка ложится на
   нижнюю кромку — в зависимости от блока.

Измерения живут в `app/src/components/photos.ts` рядом со снимком.

## Р-18 · Подвал приведён к одному виду (принято 22.08.2026)

Подвал на трёх страницах направлений был сеткой из трёх колонок с
четырьмя блоками. «Контакты» переносились на вторую строку и повисали
под самой высокой колонкой — в подвале зияло около двухсот пикселей
пустоты. Сверху добавлялось ещё до ста семидесяти шести пикселей отступа
из трёх слагаемых.

Стало: четыре колонки в одну строку, воздух над подвалом равен шагу
между секциями, шаг ссылок сокращён с пятидесяти пикселей до тридцати
восьми при сохранении цели нажатия выше порога 24×24 (WCAG 2.5.8).
Высота подвала: 604 → 343–351 пикселя.

Добавлено перестроение, которого не было: на 1024 подвал складывается
в две колонки, на 560 — в одну. Прежде на 390 четыре колонки сжимались
до сорока четырёх пикселей.

Страница «Бизнесу» приведена к соседним: иконки у телефона и почты,
блок реквизитов под описанием.

> Изменено решениями Р-69, Р-70 и Р-74: на всех трёх профильных страницах
> подвал переведён на строение посадочной — три строки без сетки колонок.
> Описанные здесь четыре колонки в проекте больше не используются.

## Р-19 · Основания фигур на одной кромке (принято 22.08.2026)

**Решение: ширина головы в карточках направлений — 103 пикселя.**

Заказчик потребовал, чтобы у трёх фигур основания легли на одну линию,
как у аспиранта. При прежних 84 пикселях этого не выходило: студентка не
доставала до кромки 62 пикселя, бизнес-эксперт — 31, и под ними
виднелась пустая подложка. Ряд «скакал».

Причина в разной крупности съёмки. На высоту головы приходится длины
тела: у аспиранта 4,8, у бизнес-эксперта 3,5, у студентки 3,1. Значит
одновременно уравнять и размер лица, и уровень основания можно только
подбором одного числа — ширины головы.

Расчёт по слоту 374×355 при воздухе над головой 30:

| Ширина головы | Аспирант | Студентка | Бизнес |
|---|---|---|---|
| 84 | до кромки | не хватает 62 | не хватает 31 |
| 95 | до кромки | не хватает 27 | до кромки |
| **103** | **до кромки** | **до кромки** | **до кромки** |
| 110 | шире плашки на 17 | до кромки | шире плашки на 26 |

103 — наименьшее значение, при котором все три фигуры доходят до нижней
кромки и ни одна не выходит за ширину плашки. Ниже кромки фигура
обрезается подложкой: так и задумано, человек входит в карточку снизу.

## Р-20 · Галочки согласия стоят вплотную (принято 22.08.2026)

Две галочки — на обработку данных и на оферту с политикой — стояли на
минимальной высоте строки 44 с зазором 8–10. Между ними получалось за
пятьдесят пикселей, и пара читалась как два разных блока вместо одного
согласия из двух пунктов.

Стало: высота строки 28, зазор 2, квадратик прижат к первой строке
подписи. Порядок вертикальный на всех формах проекта.

Цель нажатия остаётся выше порога 24×24 из WCAG 2.5.8: нажимается вся
подпись целиком, а не квадратик, и высота строки 28 при ширине во весь
текст. Проверено axe на семи страницах при 1440/768/390 — ноль
нарушений.

## Р-21 · Телефон сервиса (принято 22.08.2026)

**Решение: +7 495 456 43 23.**

Заглушка `8-000-000-00-00` заменена на всех страницах. Номер выводится
ссылкой `tel:+74954564323` — с телефона по нему звонят нажатием, а не
переписыванием.

В снимках `design/baseline/` заглушка остаётся: это записи утверждённых
версий для сверки и откатов, их не правят задним числом.

## Р-22 · Кадрирование карточек направлений — по фигуре (принято 22.08.2026)

**Решение: в трёх карточках направлений фигура занимает одну и ту же
область и центрируется целиком.**

Отменяет часть Р-19. Выравнивание по ширине головы давало равные лица,
но фигуры уходили за нижнюю кромку на разную глубину: каждого человека
срезало по своему месту, и ряд читался как три случайных кадра.

Стало: высота фигуры одинакова (325 при слоте 355), верх на 30 пикселях,
основание ложится на нижнюю кромку. Центрируется центр фигуры, а не
головы, — поэтому человек стоит посередине плашки.

Одна поправка к правилу. Аспирант снят длиннее остальных: на высоту
головы у него приходится 4,8 длины тела, у студентки 3,1, у
бизнес-эксперта 3,5. При равной высоте фигуры его голова выходила в
полтора раза мельче. Поэтому для него задано, какую часть фигуры
показывать — 531 пиксель из 781, то есть 3,3 длины головы, средняя
крупность двух остальных снимков ряда. Остаток уходит за кромку и
обрезается подложкой.

Итог: лица 93–103 пикселя вместо 67–103, основания на одной линии,
каждая фигура по центру своей плашки.

## Р-23 · Фон посадочной: линии по краям убраны (принято 22.08.2026)

Две вертикальные линии `#EEE8F5` шли по краям контейнера через всю
страницу сверху донизу. В редакторе макетов они читались как
направляющие, в браузере — как случайно прочерченная рамка вокруг
содержимого.

Убраны. Из фоновых мотивов остались орбиты с маской затухания — правило
«максимум два мотива на страницу» соблюдается с запасом.

## Р-24 · Кадрирование считается в долях слота (принято 23.08.2026)

**Решение: высота фигуры задаётся в процентах высоты слота, сдвиги — в
процентах собственного размера снимка. Пиксельных значений в правилах
кадрирования больше нет.**

Уточняет Р-17 и Р-22. Пиксели работали, пока слот не менял размер. На
узком экране слот сжимается, а фигура — нет: она вылезала за кромку или,
наоборот, повисала в пустоте. Проценты пересчитываются вместе со слотом,
поэтому одно и то же правило годится на 1440 и на 390.

Запись `figShown` осталась в пикселях исходника — это свойство файла, а
не слота: какую часть фигуры вообще показывать.

## Р-25 · Раскладка навигации живёт в классе, а не в атрибуте style (принято 23.08.2026)

**Решение: `display` и раскладка шапки задаются правилом класса.
Инлайновый стиль на этих элементах запрещён.**

Инлайновый стиль сильнее правила класса, в том числе внутри
медиазапроса. Из-за этого `display:flex` на `<nav>` перебивал
`.pd-nav[hidden]{display:none}`: на телефоне меню не пряталось, ссылки
ложились поверх заголовка, кнопка заявки уходила за край экрана. Та же
ошибка повторилась на странице студентов и там же нашлась в правиле
полей `.sp-pad`, которое не работало ни на одной секции.

Следствия, кроме самого `display`: атрибут `hidden` не ставится на
широком экране, где панель видна, — иначе программа чтения с экрана
пропускает навигацию целиком; шапка поднимается над содержимым
(`z-index`), иначе раскрытая панель просвечивает насквозь; порог
медиазапроса совпадает с порогом появления кнопки меню — 768.

## Р-26 · «Обратный отсчёт»: две настройки и сжатие маршрута (принято 23.08.2026)

**Решение: блок настраивается двумя ответами — ситуация и дата защиты.
При коротком сроке маршрут сжимается, а не обрезается с начала.**

Блок назывался отсчётом, но отсчитывать было нечем: поле даты из макета
пропало, а расчёт срока, предупреждение о коротком сроке и половина
подписей остались мёртвым кодом. Дата возвращена, и всё, что от неё
считается, работает.

Прежний расчёт при коротком сроке брал последние N шагов маршрута. Это
давало ложь: человеку, который не написал ни строчки, предлагалось
отдать работу научному руководителю. Теперь маршрут сжимается —
разбор в начале, авторская строка про основную работу в середине и
последние шаги. Меньше недели до защиты — вместо плана честный разбор
за день: посмотрим и прямо скажем, что успеваем.

Правило Р-09 держится и здесь: сокращённый состав прямо назван
сокращённым, полный объём в короткий срок не обещается.

Читаемость. Десять строк подряд с повторной подписью «Вы» и «Команда» в
каждой — это стена. Стало: строка итога из трёх чисел (полный маршрут,
недель до защиты, успеваем или нет), подписи колонок один раз сверху,
недели с номерами и датами, по умолчанию видно четыре и кнопка
«Показать ещё N недель».

## Р-27 · Снимки в блоках «Вопросы» и «Заявка» — по доле высоты (принято 23.08.2026)

**Решение: в обоих блоках фигура занимает 94 % высоты слота и
центрируется целиком. Кадрирование по ширине головы больше не
применяется.**

Отменяет остаток Р-19 для этих блоков. Равная ширина головы уравнивает
лица в соседних карточках одного ряда — там это и нужно. В одиночном
слоте она даёт обратное: снимки разной крупности приводятся к одной
голове, и фигура получается тем мельче, чем крупнее снят человек. На
странице студентов это оставляло в карточке заявки 240 пикселей пустоты,
а в блоке «Вопросы» фигура шириной 291 стояла в слоте шириной 222 и
резалась по краям.

Заодно исправлено соответствие снимка подписи: под подписью «куратор со
степенью» стояла студентка, в блоке заявки на странице студентов —
аспирант с диссертацией. Снимок должен показывать того, кто назван
рядом.

## Р-28 · Этапы работы вместо обратного отсчёта (принято 23.08.2026)

**Решение: блок на странице студентов показывает основные этапы работы
для выбранной ситуации. Даты не спрашиваем и не называем: ни поля даты,
ни календарных чисел в строках.**

Отменяет Р-26. Отсчёт требовал двух вводов и выдавал таблицу из десяти
недель с числами вида «23.08—29.08». Числа выглядели обязательством,
которого никто не давал: маршрут строился от сегодняшнего дня, а не от
договора, и первая же пауза со стороны студента делала весь календарь
неверным. Считать срок до защиты на странице тоже нечестно — реальный
объём правок виден только после разбора работы.

Осталось одно действие: выбрать свой случай. Ниже — от четырёх до пяти
карточек этапа: номер, название, ориентировочный срок этапа и две
колонки «Вы» и «Мы». Над ними строка итога (сколько этапов и сколько
занимает маршрут целиком) с пояснением, чем этот случай отличается.
Строка помечена role="status", поэтому смена случая проговаривается
программой чтения с экрана.

Сроки этапов — величины из практики, а не расчёт; в подписи под блоком
прямо сказано, что точные даты фиксируются в договоре после разбора.

## Р-29 · Список «Срок сдачи» открывается на нейтральном варианте (принято 23.08.2026)

**Решение: первым в списке стоит «Срок пока не назначен».**

Список открывался на варианте «До защиты меньше месяца». Тот, кто поле
не трогал, отправлял заявку с чужим сроком — и разговор начинался с
ложной срочности. Нейтральный вариант первым сохраняет поле необязательным
и не приписывает заявителю того, чего он не говорил.

## Р-30 · Адрес заявителя берётся из X-Real-IP (принято 23.08.2026)

**Решение: приложение доверяет только заголовку X-Real-IP; nginx выставляет
его из $remote_addr, а X-Forwarded-For больше не дополняет клиентский
список.**

X-Forwarded-For приходит от клиента, и `$proxy_add_x_forwarded_for` лишь
дописывает адрес к тому, что прислали. Приложение брало первый элемент —
то есть значение, которое отправитель задаёт сам. Отсюда два следствия:
ограничение частоты обходится сменой заголовка, а в журнал согласий
попадает чужой адрес. Журнал согласий — документ, который придётся
предъявлять, и адрес в нём должен быть настоящим.

Приложение слушает только 127.0.0.1:3000 и доступно снаружи исключительно
через nginx, поэтому X-Real-IP подделать неоткуда: proxy_set_header
затирает всё, что прислал клиент. Если заголовка нет, берём последний
элемент X-Forwarded-For — его дописывает ближайший прокси.

## Р-31 · Политика содержимого и сроки доставки (принято 23.08.2026)

**Решение: сервер отдаёт Content-Security-Policy; SMTP получил те же
восемь секунд ожидания, что и Telegram; значения, идущие в заголовки
письма, чистятся от переводов строки.**

Политика запрещает чужие сценарии, кадры и объекты, встраивание сайта в
чужую страницу и отправку форм на сторонний адрес. `'unsafe-inline'`
оставлен для стилей и сценариев осознанно: оформление живёт в атрибутах
style (так перенесены макеты), а Next вставляет в разметку встроенные
вставки для оживления страницы. Одноразовый код через middleware убрал бы
и это, но перевёл бы все страницы в динамическую отрисовку. Текст
посетителя ни в одну страницу не попадает, вставить сценарий через
содержимое некуда.

Доставка идёт до ответа странице, поэтому зависший SMTP держал бы
открытым и запрос заявителя. Перевод строки в имени заявителя попадал в
тему письма — это приём подмены заголовков.

## Р-32 · Подсветка — один заголовок на страницу (принято 23.08.2026)

**Решение: сиреневая подсветка остаётся в заголовке первого экрана и
ровно в одном заголовке раздела — том, что несёт главное обещание
страницы. Остальные заголовки набираются простым текстом.**

Посадочная, задавшая стиль проекта, не подсвечивает заголовки разделов
вовсе. На внутренних страницах подсветка стояла на всех: 8 из 8 у
аспирантов, 6 из 6 у студентов, 7 из 7 у бизнеса, — и почти каждый раздел
нёс одинаковую правую подпись с подчёркиванием. Приём, повторённый без
исключений, перестаёт быть выделением и читается как штамп. Восемнадцать
подсветок сняты, две подписи, пересказывавшие заголовок, убраны.

## Р-33 · Правило под размер экрана пишется с !important (принято 23.08.2026)

**Решение: любое правило в @media, которое перебивает свойство, заданное
в атрибуте style, объявляется с !important.**

Развитие Р-25. Проверка показала, что `.pd-pad`, `.bp-pad` и `.doc-pad`
были мертвы на шести макетах из семи: секции несут инлайновый
`padding:0 30px`, и правило класса до них не доходило. На экране 390
пикселей поля оставались по 30 вместо 20 — шестая часть ширины уходила
в пустоту.

Тем же порядком починено меню на странице «Компаниям»: инлайновый
`display:flex` перебивал `display:none`, меню не пряталось и ссылки лезли
поверх заголовка. Раскладка перенесена в класс, добавлена кнопка меню —
как на страницах аспирантов и студентов.

Заодно из кода убрано кадрирование по ширине головы: после Р-27 его не
использует ни один слот. Вместе с ним ушли замеры головы у всех снимков.

## Р-34 · Переключатель способа связи стоит перед полем (принято 23.08.2026)

**Решение: кнопки «E-mail / Телефон» располагаются в разметке и на экране
раньше поля контакта, которому они задают смысл.**

В форме первого экрана на странице «Компаниям» они стояли под полем: человек
заполнял «Ваш e-mail» и только потом обнаруживал, что мог оставить телефон.
Порядок обхода с клавиатуры повторял ту же ошибку. Управляющий элемент,
меняющий назначение поля, всегда идёт до поля.

## Р-35 · Повторяющаяся оговорка выносится из карточек в подпись (принято 23.08.2026)

**Решение: общая для всех карточек оговорка пишется один раз — в подписи
раздела, а не в шаблоне карточки.**

Пять карточек пакетов заканчивались одной и той же фразой «Точный срок
фиксируется календарным планом после разбора материала»: она была вшита в
шаблон и повторялась дословно пять раз подряд. Повтор не добавлял
достоверности, а читался как машинная заливка. Теперь у каждой карточки
остаётся своя строка «зачем», а общая оговорка стоит один раз в подписи
раздела.

Тем же решением закрыт список «Что нужно»: он открывался на конкретной
услуге, и заявка от того, кто поля не трогал, приходила с чужой задачей.
Первым стоит нейтральный вариант — как в Р-29.

## Р-36 · Накат схемы — отдельный слой образа (принято 23.08.2026)

**Решение: миграции накатывает слой `migrator` с полными зависимостями;
рабочий образ схему больше не содержит.**

Разовый запуск миграций выполнялся в рабочем образе командой
`npx prisma migrate deploy`. Проверка показала, что в рабочем образе командной
строки prisma нет: Next оставляет в `standalone` только модули, которые
требует `server.js`, а prisma вызывается из скриптов npm и в трассировку
не попадает. Команда полезла бы за пакетом в реестр npm прямо на сервере
во время развёртывания — неизвестной версией и при условии, что у сервера
есть доступ к реестру. Это первый шаг установки, и он не должен зависеть
от сети и от того, какая версия окажется свежей.

Слой `migrator` собирается из тех же зависимостей, что и сборка, несёт
каталог `prisma` и вызывает `npx --no-install`: если пакета нет на месте,
команда падает сразу, а не тянет его из сети.

## Р-37 · Резервная копия проверяется до замены предыдущей (принято 23.08.2026)

**Решение: дамп пишется во временный файл, проверяется на целостность и
размер и только потом становится дневной копией; старые копии удаляются
после этого.**

В `/bin/sh` нет `pipefail`. Прежний скрипт вёл дамп конвейером сразу в gzip:
при отказе `pg_dump` конвейер всё равно завершался успешно, на диск ложился
пустой архив, а `find -delete` тут же удалял копию тридцатидневной давности.
Отказ выяснился бы при первой попытке восстановления — то есть тогда, когда
копия и нужна.

Заодно исправлено имя базы: `pg_dump` брал его из окружения cron, где
переменных из `deploy/.env` нет, и при изменённых значениях снимал копию
не той базы. Теперь имя берётся из окружения самого контейнера.

## Р-38 · Правила наведения и нажатия объявляются важными (принято 23.08.2026)

**Решение: переносчик макетов ставит `!important` в правилах `:hover`,
`:focus-visible` и `:active`.**

Развитие Р-25 и Р-33, третий случай той же причины. Оформление в макетах
живёт в атрибуте `style`, а он сильнее любого класса. Правила наведения
переносчик выносил в обычные классы — и ни одно из них не срабатывало.
Замер в браузере: у кнопки заявки фон до наведения и после — один и тот же
`rgb(22,18,28)`. Так было на всех страницах и на всех 33–38 состояниях
наведения: в исходнике страница выглядела живой, в браузере была
неподвижной.

## Р-39 · Отдача на нажатие обязательна для всего, что нажимается (принято 23.08.2026)

**Решение: у каждой кнопки и ссылки-кнопки есть состояние нажатия;
компактные сжимаются до 97 %, строки во всю ширину приглушаются до 82 %.**

Состояние нажатия не было объявлено нигде, кроме логотипа. Палец нажимал —
ничего не происходило до самой отправки запроса.

Сжатие на 3 % годится для таблеток и кнопок с иконкой. Для строки списка
во всю ширину оно сдвигало бы края на два десятка пикселей и выглядело бы
дёшево, поэтому там отдача даётся приглушением.

Переход перечисляет ровно те свойства, которые меняются в состояниях, плюс
`transform`: 180 мс, кривая та же, что во всём проекте. При включённом
«уменьшить движение» переходы отключаются целиком, но изменение вида
остаётся — оно происходит мгновенно.

## Р-40 · Форма показывает, что отправка идёт (принято 23.08.2026)

**Решение: кнопка на время отправки приглушается и получает курсор
ожидания; подтверждение и ошибка появляются с плавным всплытием.**

Кнопка отправки блокировалась атрибутом `disabled`, но выглядела ровно так
же: на медленной связи человек не понимал, нажалась ли она, и жал ещё раз.
Поле ввода в фокусе меняло цвет рамки рывком — добавлено мягкое кольцо и
переход. Плашка «Заявка принята» возникала мгновенно; теперь всплывает за
220 мс. Правило появления привязано к вставке элемента, поэтому блоки,
которые уже на странице, ничего не переигрывают при обновлении содержимого.

## Р-41 · Правовые тексты не описывают того, чего сайт не делает (принято 23.08.2026)

**Решение: утверждения в Политике сверяются с поведением сайта замером,
а не с намерением.**

Проверка в браузере: после обхода трёх страниц сайт не ставит ни одного
файла cookie, `localStorage` и `sessionStorage` пусты, счётчиков нет.
При этом Политика утверждала, что «Сайт использует файлы cookies»,
включала «идентификаторы файлов cookies» в состав данных посетителей,
объясняла порядок отказа от них и перечисляла «Сервис веб-аналитики»
среди лиц, которым поручена обработка, — рядом с собственным пунктом
о том, что аналитика не используется.

Ложное описание обработки в Политике хуже отсутствия описания: оно
расходится и с действительностью, и со сведениями в уведомлении
уполномоченного органа, которые должны совпадать с опубликованным
текстом. Разделы приведены к тому, что происходит на самом деле:
сведения о посещении фиксируются только в журналах сервера. При
подключении счётчика Политика дополняется — это записано в её тексте.

Тем же порядком снято утверждение «Оператор состоит в реестре
операторов»: уведомление ещё не подано. Пункт возвращается после
подачи и появления записи в реестре.

## Р-42 · У каждого документа есть дата редакции (принято 23.08.2026)

**Решение: оферта, политика и согласие несут раздел с датой редакции и
версией; версия согласия совпадает со значением, которое записывается
в журнал вместе с заявкой.**

Оферта и Политика обещали, что новая редакция размещается «с указанием
даты редакции», но действующая редакция даты не имела: определить, какая
версия действовала на дату акцепта, было невозможно, хотя обе ссылаются
на это в своих текстах. В Согласии дата стояла — 10 августа, — а в
журнал заявок писалась версия от 21 августа. Дата документа и значение
`CONSENT_VERSION` теперь одно и то же.

Заодно заполнен пропуск в оферте: «Срок рассмотрения претензии и
направления ответа составляет со дня её получения» — в предложении не
было числа.

## Р-43 · Движение — две вещи и ничего больше (принято 23.08.2026)

**Решение: раздел проявляется при подходе к нему, вверху идёт полоса
прочтения. Ни закрепления экранов, ни параллакса, ни счётчиков.**

Научный консалтинг продаётся доверием, и хореография прокрутки работает
против него: она читается как продуктовый лендинг. Правило «оживлять
один-два элемента на экран» здесь важнее эффектности.

Появление объявлено так, что скрыть содержимое невозможно ни при каком
стечении обстоятельств. Скрытие включается только при выполненных
сценариях (класс `pd-js` на корне) и только при разрешённом движении.
Проверка идёт по положению кромки на каждом кадре прокрутки, а не
наблюдателем пересечений: наблюдатель не срабатывает на разделах,
проскочивших мимо окна при переходе по якорю или прыжке в конец
страницы, и они остаются невидимыми навсегда — это и показала первая
версия. Замер: после прыжка в конец страницы скрытых разделов ноль на
всех семи страницах.

Первый экран не анимируется: по объявлению человек попадает именно на
него, и появиться он должен мгновенно. На правовых документах появление
разделов выключено — шестнадцать пунктов сплошного текста, движение на
каждом было бы шумом; полоса прочтения там остаётся.

Побочное следствие для проверок: axe меряет контраст по фактическому
цвету, и раздел, пойманный в середине проявления, даёт десятки ложных
нарушений. В прогоне после прокрутки нужна пауза на завершение
перехода — без неё проверка показывает 12 «проблем» на ровном месте.

## Р-44 · Посадочная обращается ко всем трём направлениям (принято 23.08.2026)

**Решение: заголовок, подводка и заголовок вкладки называют аспирантов,
студентов и компании; в шапке появились ссылки на три направления.**

Реклама ведёт на главную, а оттуда человек выбирает своё направление.
Заголовок при этом обещал сопровождение «аспирантов, соискателей и
докторантов» — то есть одного направления из трёх: пришедшие по
объявлению студент и компания читали, что они сюда не относятся. Шапка
не содержала ни одной ссылки, и выбрать направление можно было только
прокрутив страницу до карточек.

Заодно исправлен порядок в обеих формах посадочной: кнопка отправки
стояла раньше галочек согласия, которые она требует. Согласие идёт до
действия, а не после него.

## Р-45 · Шесть механизмов видны сразу (принято 23.08.2026)

**Решение: механизмы работы разложены сеткой 3×2 вместо горизонтальной
карусели.**

На экране было видно три из шести: половина гарантий пряталась за
стрелкой на странице, вся задача которой — показать, чем обеспечено
обещание. Карусель была сделана добротно, но экономила место там, где
места хватало. Вместе с ней снята обслуживавшая её логика прокрутки.

## Р-46 · Этапы работы — один план, а не пять карточек (принято 24.08.2026)

**Решение: этапы на странице студентов собраны в одну панель со строками
и нитью между номерами.**

Пять отдельных карточек занимали около девятисот пикселей по высоте ради
двух строк текста в каждой: рамка, тень, отступы и заголовок повторялись
пятикратно, а между колонками «Вы» и «Мы» зияла пустота в треть ширины.
Замер после переделки: панель 367 пикселей вместо примерно 900, весь
раздел 765 вместо 1030.

Строка несёт номер, название, ориентировочный срок и обе колонки в одну
строку. Слева идёт нить — вертикальная линия между кружками номеров:
взгляд спускается по ней, как по календарному плану. Это и есть язык
предмета: план работ, а не набор карточек.

## Р-47 · Снимок не растягивается выше своего разрешения (принято 24.08.2026)

**Решение: перед тем как поставить снимок в слот, сверяется его исходный
размер с размером слота на экране.**

Замер по всем страницам: `editor.webp` имеет 171 пиксель по ширине, а в
новой карточке вопросов на странице «Компаниям» показывался на 321 —
запас 0,53, то есть браузер растягивал картинку почти вдвое. Это и было
видно на снимке экрана.

Карточка перестроена: вместо панели с большим снимком — компактная
карточка с портретом 72 пикселя, а сам снимок заменён на `business.webp`
(549 пикселей). Запас 7,6 — резкость с избытком.

Остальные слоты держатся в пределах от 1,06 до 3,1. Ниже единицы нет
ничего. Значения от 1,0 до 1,5 означают мягкость на экранах с двойной
плотностью; закрывается только заменой файлов исходников, размеры
названы в отчёте.

## Р-48 · Нумерация в каталоге — внутри направления (принято 24.08.2026)

**Решение: позиции нумеруются от единицы в каждом направлении.**

Нумерация была сквозной по всему каталогу: раскрытая вторая группа
начиналась с шестнадцатого номера, хотя в её заголовке написано
«12 позиций». Читатель видел несогласованность там, где её быть
не должно.

## Р-49 · Маршрут переключается, а не разворачивается списком (принято 24.08.2026)

**Решение: этапы на странице студентов — интерактивная дорожка: номера с
рельсом, под ней панель выбранного этапа.**

Список из пяти строк был короче пяти карточек, но оставался списком: чтобы
понять, что происходит на четвёртом этапе, нужно было прочитать все
предыдущие. Дорожка показывает весь маршрут целиком — все названия этапов
видны сразу, — а подробности «Вы» и «Мы» переключаются по нажатию. Ничего
существенного не спрятано: скрыты только две строки описания невыбранных
этапов.

Раздел занял 606 пикселей против 765 у списка и примерно 1030 у карточек.

Смена ситуации сбрасывает выбор на первый этап: иначе после переключения
случая открытым оставался четвёртый этап чужого маршрута.

Две ошибки, найденные проверкой: на узком экране названия под номерами
скрыты, и у кнопок не оставалось имени — программа чтения с экрана
называла их «кнопка» (пять нарушений axe); имя задано явно. И высота
кнопки на телефоне падала до тридцати пикселей — меньше минимальной
цели нажатия; поднята до сорока четырёх.

## Р-50 · Библиотека анимации не подключается (принято 24.08.2026)

**Решение: движение остаётся на переходах CSS и одном наблюдателе
прокрутки. GSAP и ScrollTrigger в проект не вводятся.**

Принципы, по которым сделано движение, взяты из руководства по
производительности GSAP и применены дословно: анимируются только
`transform` и `opacity`, `will-change` стоит единственно на полосе
прочтения, чтение и запись в проверке прокрутки разделены, обработчики
снимаются при размонтировании.

Сама библиотека не нужна. ScrollTrigger оправдан там, где анимация
привязана к прокрутке — закрепление, параллакс, покадровая перемотка; всё
это сознательно исключено (Р-43). Подключить семьдесят килобайт ради
подъёма на четырнадцать пикселей и смены прозрачности значило бы
замедлить первый экран, на который ведёт реклама, ради приёма, который
уже работает без них.

Отдельно про `canvas-design`: он делает статичные изображения — плакаты и
PDF, — и к вёрстке сайта отношения не имеет.

## Р-51 · Палитра переведена на графит и один глубокий синий (принято 05.09.2026)

**Решение: светлая база, графитовый текст, ровно один акцент — `#14417A`.
Сиреневый `#7B2BE0`, подсветка `#DDC4FA` и мятный `#3DDCA0` как акценты
сняты со всех семи страниц.**

Отменяются прежние решения о палитре и о шрифтовой паре. Р-50 остаётся
в силе целиком.

Причина не в моде. Сиренево-мятная гамма читается как гамма ученического
сервиса, а страница обещает экспертизу с учёной степенью и договор с
приёмкой по этапам. Разрыв между обещанием и оформлением — то, из-за чего
посетитель закрывает сайт до блока цен.

Что сделано:
- палитра сведена с примерно девяноста значений до тридцати семи;
- три градиента под 152° удалены целиком; объём выражается плотностью
  поверхности, рамкой и одной тенью из трёх формул;
- мятный сохранён единственным исключением — статус успеха у форм
  (`#E6F9F1` / `#5FE6B6` / `#0E4E3C`), чтобы не переизобретать состояния;
- красный статус ошибки сохранён как есть.

Замер: ноль нарушений контраста на семи страницах, порог 4,5:1 для текста
мельче 18px, 3:1 для крупного. Нижняя граница серого текста — `#5C6474`.

Оттенок акцента выбран заказчиком из двух замеренных вариантов.

## Р-52 · Шрифтовая пара — Literata и Inter (принято 05.09.2026)

**Решение: антиква Literata в заголовках, гротеск Inter в тексте,
JetBrains Mono в лейблах, номерах, датах и обозначениях норм.**

Все три подключаются через `next/font/google` с
`subsets: ['latin','cyrillic']` и отдаются с собственного домена. Внешних
CDN и файлов шрифтов в репозитории нет.

Пара выбрана заказчиком из трёх предъявленных на образце заголовка H1 и
абзаца в шестьдесят символов. Literata рисовалась для длинного чтения с
экрана и имеет полную кириллицу с настоящими, а не подставленными
глифами; Inter — нейтральный гротеск без характера, который не спорит с
антиквой.

Побочный эффект, который пришлось учесть: глифовый бокс Literata выше
строки примерно на 1,48em. Инлайновая плашка-подсветка в заголовке при
интерлиньяже 1,2 вылезает за свою строку и режет соседнюю. Там, где
подсветка стоит не на первой строке, интерлиньяж поднят до 1,4, а
вертикальный паддинг плашки убран.

## Р-53 · Изображения генерируются, лица не персонифицируются (принято 05.09.2026)

**Решение: векторные заглушки заменены десятью сгенерированными
изображениями в WebP. Подпись под портретом — только роль.**

Модель — `nano_banana_2` из каталога Higgsfield. Названной в задании
«Nano Banana 2 Pro» в каталоге нет; ближайшая доступная запись обслужена
как `nano_banana_flash`.

Пять портретов (`curator` 480², `editor` 340², `postgrad`, `student`,
`business` по 760²) и пять натюрмортов (700×214). Каждый файл не меньше
двойного размера своего слота.

Изображения синтезированы и **не изображают конкретных людей**. Поэтому
требование письменного согласия изображённого лица (Р-08) к ним не
относится, но и приписать им имя, должность или цитату нельзя: подпись —
только роль. Блок отзывов не возвращается.

Единая оптика задана общей частью промпта: рассеянный дневной свет с
одной стороны, холодная нейтральная температура, светло-серый фон,
неглубокая резкость, без надписей и логотипов.

Промпты (общая часть приведена один раз, далее — предметная):

> Общая часть: `natural documentary photograph, soft diffused daylight
> from one side, cool neutral white balance, light grey seamless
> background, shallow depth of field, no text, no logos, no watermarks,
> muted deep blue and graphite colour accents`

1. `curator` — `waist-up portrait of a scientific consultant in a dark
   navy blazer holding a bound manuscript, calm confident expression,
   looking at camera`
2. `editor` — `waist-up portrait of an academic editor in a plain shirt
   holding printed pages with pencil marks, attentive expression`
3. `postgrad` — `postgraduate researcher standing with a thick bound
   dissertation and notes, university corridor out of focus behind`
4. `student` — `student with a laptop and printed draft chapters, working
   posture, quiet concentration`
5. `business` — `project engineer in front of a technical drawing,
   holding a rolled blueprint, industrial setting out of focus`
6. `svc-dissertation` — `still life: thick bound dissertation in dark blue
   cloth binding on a desk, fountain pen and paper clips beside it`
7. `svc-doctoral` — `still life: two heavy bound volumes standing upright
   on a desk, archive boxes out of focus behind`
8. `svc-articles` — `still life: open academic journal spread with
   two-column typeset text and a figure, reading glasses at the edge`
9. `svc-diploma` — `still life: stack of printed thesis chapters bound
   with a plastic spine, ruler and pencil on top`
10. `svc-defense` — `still life: lectern with a printed report, water
    glass and a pointer, empty auditorium out of focus`

Файлы кладутся в `app/public/photos/` (портреты) и одновременно в
`design/assets/art/` и `app/public/assets/art/` (натюрморты): каталоги
макета и приложения синхронизируются вручную, скрипта переноса нет.

Замена вырезанных по контуру фигур на полнокадровые снимки убрала из
`photos.ts` кадрирование по альфа-каналу: измерения `figTop`/`figH`/`figCx`
и два правила кадрирования больше не нужны, всё сводится к
`object-fit: cover` с одной точкой привязки.

Один недостаток остаётся: `svc-doctoral.webp` вышел светлее остальных и в
мелком слоте читается слабо. Перегенерация упирается в сеть — сервис
отдаёт файлы с хоста, закрытого политикой прокси.

> **Закрыто решением Р-65 (07.09.2026).** Кадр перегенерирован, обход
> сетевого ограничения найден. Абзац выше сохранён как запись состояния на
> момент принятия Р-53.

## Р-54 · Плашки первого экрана несут проверяемые величины (принято 05.09.2026)

**Решение: каждая из трёх плашек посадочной ссылается на носитель факта
внутри сайта.**

- «52 позиции в семи направлениях» — сверено с каталогом `BusinessPage`:
  15+12+8+7+6+3+1. Ссылка ведёт в каталог.
- «6 этапов» — сверено с треком этапов `MainPage`. Ссылка ведёт в трек.
- «с 2016» — формулировка Р-01, про научный коллектив, а не про
  юридическое лицо. Ссылка ведёт в блок команды.

Числа без носителя, «2001» и «компания работает с…» остаются запрещёнными.

## Р-55 · Блок цен поднят выше на всех трёх профильных страницах (принято 05.09.2026)

**Решение: блок цен начинается не позже шестого экрана при ширине 1440 и
не позже десятого при 390, и на него ведёт ссылка из меню страницы.**

Замер до и после правки, в пикселях от верха документа:

| Страница | Блок | 1440 до | 1440 после | 390 до | 390 после |
|---|---|---|---|---|---|
| `/main` | Стоимость | 4310 | 3147 | 7957 | 5883 |
| `/students` | Смета | 2775 | 2170 | 5042 | 4011 |
| `/business` | Пакеты | 2478 | 1853 | 3889 | 2904 |

Третья причина ухода посетителя — «чтобы узнать порядок цен, нужно
сначала оставить контакты» — снимается тем, что блок виден без заявки и
достижим из меню одним нажатием.

## Р-56 · Нижний кегль поднят с 11px до 12px (принято 05.09.2026)

**Решение: 11px из шкалы удалён, все моно-лейблы переведены на 12px.**

Требование приёмки — ноль текстов мельче 12px — прямо противоречило
прежней шкале, где 11px был нижним кеглем для лейблов. Требование
доступности весомее сохранения шкалы: 11px с трекингом .04em на
светло-сером фоне читается плохо и на телефоне, и на большом экране.

Замена сплошная: 231 объявление на восьми макетах. Остальная шкала,
коэффициенты интерлиньяжа, радиусы, кривая и длительности не тронуты.

## Р-57 · Цель нажатия — 44px, кроме ссылки внутри строки (принято 05.09.2026)

**Решение: все самостоятельные ссылки и кнопки подняты до 44px по
высоте. Исключены два случая.**

Подняты: вордмарк, «Перейти к содержанию», кнопка заявки в шапке, ссылки
подвала, телефон и почта, ссылки на правовые документы, кнопка раскрытия
каталога и 43 пункта оглавлений трёх правовых документов (было 33,6px).
Липкое оглавление после этого занимает 844px из 900 и в окно помещается.

Исключены и записаны как исключения, а не как непройденная проверка:
- ссылка внутри строки текста («согласие на обработку персональных
  данных», «оферту», «политику») — исключение WCAG 2.5.8 для inline-целей;
  сделать её 44px значило бы разорвать строку;
- скрытая за кромкой окна ловушка для роботов (`company_website`,
  `tabindex="-1"`, `aria-hidden`) — не цель нажатия.

Флажок согласия остаётся 16×16: целью нажатия служит подпись, и её высота
поднята до 44px.

> **Изменено решением Р-62 (06.09.2026).** Подпись согласия опущена с 44px
> до 36px: при 44px две строки согласия разделяла пустота в 26 пикселей и
> группа условий читалась как несвязанные поля. 36px — в полтора раза
> больше минимума WCAG 2.5.8 уровня AA. Для всех остальных целей правило
> 44px действует без изменений.


## Р-58 · Первый экран, карточки и подвал уплотнены (принято 06.09.2026)

**Решение: посадочная перестроена по пяти замечаниям заказчика. Ниже —
что именно и почему.**

**Текст первого экрана сокращён.** Снят надзаголовок «Экспертиза и научное
сопровождение исследований»: он повторял заголовок и отодвигал форму на
полтора десятка пикселей вниз. Подводка сведена с двух предложений к
одному, правовая врезка — с трёх строк к двум, подпись к форме — с двух
строк к одной. Смысл сохранён целиком: предмет договора, отказ обещать
решение диссовета, безвозмездность консультации остались.

**Основание плашек выровнено по галочкам согласия.** Раньше плашки
стояли на подобранном отступе `margin-top: 88px` и уезжали при любой
правке текста. Теперь колонки первого экрана тянутся на общую высоту,
плашки прижаты к низу, а нижний отступ правой колонки равен высоте кнопки
отправки. Основание плашек и низ строки «Принимаю оферту и политику»
совпадают в пиксель и остаются совпадающими при смене длины текста.
Портреты получили `margin-top: auto` и встали по середине свободной
высоты — иначе между ними и плашками зияло двести пятьдесят пикселей.

**Изображения трёх карточек направлений перегенерированы.** Прежние три
снимка были поясными портретами в одной обстановке и не различали группы:
посетитель не понимал, какая карточка его. Новые показывают среду и
типаж — молодой учёный в лаборатории у доски с выкладками, студентка в
читальном зале с ноутбуком и распечатками, руководитель проекта в
инженерном офисе с чертежом. Единая оптика сохранена общей частью
промпта: рассеянный дневной свет с одной стороны, холодная нейтральная
температура, неглубокая резкость, приглушённые синий и графит, без
надписей и логотипов.

Промпты (после общей части):

1. `postgrad` — `waist-up shot of a young male postgraduate researcher in
   his late twenties standing in a university research laboratory, plain
   shirt and dark knitted cardigan, holding a thick bound dissertation
   against his chest, laboratory bench with instruments and a whiteboard
   covered in equations blurred behind him, face in the upper third`
2. `student` — `waist-up shot of a female university student in her early
   twenties standing in a library reading room, light sweater with a
   backpack strap over one shoulder, holding a closed laptop and a stack
   of printed draft chapters, tall bookshelves and reading tables blurred
   behind her, face in the upper third`
3. `business` — `waist-up shot of a company project director in his
   forties standing in a modern engineering office, dark navy suit jacket
   over an open-collar shirt without a tie, holding a tablet and a rolled
   technical drawing, glass partitions and a pinned engineering drawing
   blurred behind him, face in the upper third`

Изображения синтезированы и не показывают конкретных людей; правило Р-53
о подписи только по роли действует и здесь.

**Карточки каруселей уменьшены по высоте.** Паддинг 24/24/24 → 18/20/20,
внутренний шаг 12 → 10px, номер 17 → 15px, отбивка нижней строки
16 → 12px. Карточка стала ниже примерно на двадцать пикселей; полоса
перестала растягивать страницу без пользы.

**Подвал перестроен.** Было: два блока по краям, пустая середина, верхний
отступ 96px, высота около 370 пикселей. Стало: три равные колонки —
«Контакты», «Документы», «Мы в сети», — волосяная линия и нижняя строка
«копирайт — реквизиты». Верхний отступ 56px, высота 297 пикселей. Ниже
1024px колонки складываются в две, ниже 480px — в одну. Цели нажатия
44px сохранены везде.

Проверено после правок: ноль нарушений axe и контраста на семи страницах
и трёх ширинах, ноль переполнений, заявки со всех четырёх форм доходят до
базы, блоки цен остались на прежних экранах.


## Р-59 · С первого экрана сняты две справочные надписи (принято 06.09.2026)

**Решение: правовая врезка «Предмет договора…» и подпись «Консультация
занимает 20 минут…» убраны с первого экрана посадочной.**

После Р-58 левая колонка всё ещё была длиннее правой примерно на сто
пятнадцать пикселей: заголовок и форму разделяли два справочных абзаца,
которые ничего не продают и читаются как мелкий шрифт договора. Первый
экран — это место, где посетитель решает, доверять ли; уточнения он
прочтёт ниже.

Ни один факт не потерян:
- безвозмездность консультации и предварительная оценка объёма — в панели
  «Оставьте заявку» на той же странице;
- предмет договора и отказ обещать решение диссовета — в публичной оферте,
  раздел о предмете, и в блоке «Стоимость» профильных страниц.

Правило 5 свода «Правила, которые нельзя нарушать» требует не обещать
решение диссовета — оно и не обещается. Требования печатать оговорку на
первом экране в своде нет.

Замер после правки: основание плашек и низ строки «Принимаю оферту и
политику» совпадают в пиксель (739 и 739), высота первого экрана
уменьшилась с 937 до 822 пикселей.



## Р-60 · Снимки маршрутов переведены в репортаж (принято 06.09.2026)

**Решение: три снимка в карточках «С чего начнём вашу работу»
перегенерированы в документальной манере; из кадра «бизнес» вырезан
крупный план для круглого слота на странице «Бизнесу».**

Прежняя серия (Р-53, Р-58) читалась как порождение генеративной модели:
студийный ровный свет, вычищенная кожа, симметричная композиция, взгляд
в объектив, стерильный фон без следов работы. На сайте научного
консалтинга такой кадр обесценивает остальное — если снимок очевидно
синтетический, посетитель переносит это подозрение на цифры и сроки.

Что изменено в постановке задачи модели. Вместо портрета «в интерьере»
запрошен репортажный кадр: 35-мм плёнка, зерно, только естественный свет
из окна, приглушённый цвет, ретуши нет, кожа с неровностями. Человек
занят делом и снят в три четверти со спины или сбоку, в объектив не
смотрит и не улыбается. В кадре рабочий беспорядок — реактивы, кабели,
листы на стене, бумажный стакан. Композиция намеренно смещена от центра,
глубина резкости малая, кадрирование неидеальное.

Соответствие карточек аудитории (требование заказчика — «профили точнее
указать людей на картинках»):
- «Довести диссертацию до защиты» — аспирант у лабораторного стола с
  тетрадью записей;
- «Сдать диплом и опубликовать статью» — студентка в читальном зале над
  разложенными распечатками;
- «Защитить разработку и получить грант» — инженер сорока лет у стены с
  чертежами в проектном бюро.

Модель — `nano_banana_2` (Higgsfield), соотношение 1:1, выдача приведена
к 760×760 WebP, качество 50. Полные тексты запросов:

```
Candid reportage photograph, shot on 35mm film, Kodak Portra, visible film
grain, natural window light only, no studio lighting, no retouching, natural
uneven skin texture, muted desaturated colours. A postgraduate researcher in
his late twenties stands at a cluttered laboratory bench, three-quarter turned
away from the lens, looking down at an open notebook he is annotating with a
pen. He wears a worn grey sweater over a shirt. Real working clutter: reagent
bottles, a rack of pipettes, coffee mug, cables, printed pages taped to the
wall. Slightly off-centre composition, shallow focus, imperfect framing,
documentary feel. No eye contact with camera, no smile, no text, no logos
```

```
Candid reportage photograph, shot on 35mm film, Kodak Portra, visible film
grain, natural window light only, no studio lighting, no retouching, natural
uneven skin texture, muted desaturated colours. A university student in her
early twenties sits at a library desk seen slightly from the side, leaning
over printed pages spread in front of her, one hand marking a line with a
pencil, laptop half closed beside a paper cup. Bookshelves and other students
out of focus behind. Slightly off-centre composition, shallow focus, imperfect
framing, documentary feel. No eye contact with camera, no smile, no text, no
logos
```

```
Candid reportage photograph, shot on 35mm film, Kodak Portra, visible film
grain, natural window light only, no studio lighting, no retouching, natural
uneven skin texture, muted desaturated colours. A project engineer in his
forties stands at a wall of pinned technical drawings in a working design
office, turned three-quarters away from the lens, studying a detail on a
drawing with a pen in his hand. Rolled sheets, a cluttered desk with monitors
behind him, dark jacket over an open-collar shirt. Slightly off-centre
composition, shallow focus, imperfect framing, documentary feel. No eye
contact with camera, no smile, no text, no logos
```

**Крупный план для круглого слота.** Репортажный общий план в круге 178px
на странице «Бизнесу» не читается: лицо в нём мельче подписи «Отраслевой
эксперт со степенью». Из кадра `business.webp` вырезан фрагмент
(166, 34)—(599, 467) и сохранён отдельным файлом `expert.webp` 480×480.
Это тот же человек в той же обстановке, поэтому карточка «Наука для
бизнеса» на посадочной и портрет эксперта на профильной странице читаются
как один и тот же специалист.

**Попутно исправлено соответствие слота подписи.** Слот `bp-faq-photo`
подписан «Куратор проекта», а получал снимок эксперта; теперь получает
`curator.webp`.

Точки кадрирования пересчитаны под новую композицию: `postgrad` — 20 %,
`student` — 45 %, `business` — 62 %, `expert` — 40 %. В квадратных слотах
(круги) точка не влияет: исходник и слот одного соотношения.

Запрет Р-53 сохраняется: снимки синтезированы, конкретных людей не
изображают и не выдаются за фотографии сотрудников.

**Панели «Стандарт качества» и «Оставьте заявку» уменьшены по высоте.**
Требование заказчика — минус тридцать процентов. Вертикальные внутренние
отступы 18 → 16px, шаг между заголовком и абзацем 10 → 8px, отступ формы
сверху 8 → 6px, шаг внутри формы 12 → 8px. Замер: 549 → 388 пикселей,
минус 29 %. Состав полей, порядок, валидация и тексты галочек согласия не
тронуты — журнал согласий по 152-ФЗ не затрагивается.

**Подвал уплотнён.** Три равные колонки заменены тремя строками: контакты
и телеграм, правовые ссылки и реквизиты, копирайт. Высота 297 → 203
пикселя, минус 32 %. Ниже 768px строки складываются в столбец. Цели
нажатия 44px сохранены.

Проверено после правок: ноль нарушений axe и контраста на семи страницах,
ноль ошибок консоли и ответов 4xx/5xx, заявки со всех четырёх форм
доходят до базы с верным `source`, перенос макетов воспроизводим. Высота
посадочной 3071 → 3053 пикселя.


## Р-61 · Собственный набор пиктограмм в карточках (принято 06.09.2026)

**Решение: карточки «Что входит в сопровождение», «Гарантии и документы»
и список преимуществ получили пиктограммы из собственного набора
`pd-*.svg`; кнопка нижней формы поднята; строка согласия на первом экране
перестала переноситься.**

**Почему не Lucide.** В проекте уже есть служебный набор Lucide (стрелки,
галочка, часы, щит) — он несёт навигацию и статусы. Смысловые карточки
требуют другого: знака, который читается как утверждение о работе, а не
как элемент управления. Двенадцать глифов нарисованы под конкретные
формулировки карточек и построены на общей сетке 24×24, толщина обводки
1,5, скругления концов и стыков — те же, что у служебного набора, поэтому
наборы соседствуют без разнобоя.

| Файл | Карточка | Знак |
|---|---|---|
| `pd-degree` | Куратор со степенью | розетка учёной степени с лентами |
| `pd-gost` | Оформление под требования диссовета | линейка рядом с листом |
| `pd-editing` | Научная редактура и языковая поддержка | корректорская каретка вставки между строк |
| `pd-stages` | Этапы с приёмкой | лестница этапов с галочкой |
| `pd-team` | Проектная команда | узел и три исполнителя |
| `pd-tracker` | Понятно, на каком этапе работа | чек-лист: два этапа сданы, третий в работе |
| `pd-contract` | Договор об оказании услуг | лист с подписью |
| `pd-refund` | Возврат по заявлению | круговая стрелка вокруг платежа |
| `pd-nda` | Соглашение о неразглашении | замок с закрытой строкой |
| `pd-estimate` | Техническое задание | расчётная панель |
| `pd-stagepay` | График этапов | три ячейки, первая принята |
| `pd-revision` | Регламент правок | правка карандашом по строкам |

Глифы подключаются CSS-маской, как весь набор, поэтому наследуют цвет
карточки: в карточках с акцентной кромкой — `#14417A`, в приглушённых —
цвет их надзаголовка. Пиктограмма встаёт в одну строку с номером или
надзаголовком: высота карточки не меняется, уплотнение Р-58 сохраняется.

**Список преимуществ на странице «Аспирантам».** В данных `usp` имя
пиктограммы было заложено с самого начала, но в разметку не доходило —
`.map` его отбрасывал. Поле восстановлено. Маркер собран в колонку: глиф
21px в подложке `#ECF1F8` со скруглением 12px, под ним номер. Номер
переведён из акцентного в служебный цвет — иначе колонка спорит сама с
собой за внимание. Высота блока не изменилась (412 пикселей).

**Кнопка «Отправить заявку».** Собственный верхний отступ 16px снят,
остаётся шаг формы 8px. Кнопка поднялась на 16 пикселей, высота панелей
388 → 372.

**Строка согласия на первом экране.** «Даю согласие на обработку
персональных данных» переносилась на вторую строку: форме не хватало
восьми пикселей. Ширина формы 344 → 392px. Высота не изменилась — метка
и в одну, и в две строки укладывалась в цель нажатия 44px, поэтому
совпадение оснований плашек и строки согласия сохранилось: 739 и 739.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений по горизонтали на 1440/1024/768/390, заявки со всех четырёх
форм доходят до базы, перенос макетов воспроизводим. Высота посадочной
3053 → 3044 пикселя.

> **Дополнено решением Р-64 (06.09.2026).** К двенадцати глифам добавлены
> четыре под карточки пакетов страницы «Бизнесу»: `pd-patsearch`,
> `pd-grant`, `pd-asset`, `pd-market`. Набор — шестнадцать знаков.


## Р-62 · Строки согласия сжаты, логотип в подвале (принято 06.09.2026)

**Решение: цель нажатия подписи согласия опущена с 44px до 36px, кнопка
первого экрана поднята, в подвал добавлен логотип.**

**Почему 44px было слишком много.** Подпись согласия набрана в одну
строку высотой 20px, а цель нажатия занимала 44px и текст прижимался к её
верху (`align-items: flex-start`). Между двумя строками согласия
получалось 26 пикселей пустоты — она читалась как разрыв между
несвязанными полями, хотя это одна группа условий. Заказчик указал на
разрыв прямо.

Что сделано: высота строки 44 → 36px, текст выровнен по центру строки
(`align-items: center`), собственный шаг между двумя строками 2px снят,
у флажка убран сдвиг `margin-top:2px`, который поддерживал верхнее
выравнивание. Пустота между строками 26 → 16 пикселей.

**Это отступление от Р-57, и оно осознанное.** Р-57 поднял все
самостоятельные цели до 44px — это уровень WCAG 2.5.5 (AAA). Обязательный
для проекта уровень AA задаёт критерий 2.5.8: минимум 24×24 CSS-пикселя.
Подпись согласия остаётся целью во всю ширину колонки высотой 36px — это
в полтора раза больше требования AA и по-прежнему удобная цель на
сенсорном экране. Правило Р-57 продолжает действовать для всех остальных
целей: кнопок, ссылок подвала, телефона, почты, пунктов оглавлений.
Исключение касается только строки согласия — девять строк на четырёх
страницах.

Флажок 16×16 не менялся: целью служит подпись.

**Кнопка «Оставить заявку» на первом экране.** Снят собственный верхний
отступ 16px — остаётся шаг формы. Кнопка поднялась на 16 пикселей.
Правая колонка первого экрана держит совпадение оснований плашек и строки
согласия резервом `padding-bottom`, равным тому, что стоит под строкой
согласия слева. Резерв пересчитан 83 → 67px. Замер после правок: 721 и
721.

**Логотип в подвале.** Первая строка подвала начиналась с телефона —
подвал не был подписан маркой, и последний экран заканчивался
реквизитами. Вордмарк поставлен первым, телефон и почта сдвинулись
вправо. Разметка и поведение те же, что у логотипа в шапке: Literata 600,
«PRO» акцентом, «DISSER» графитом, при наведении подложка
`rgba(216,228,243,.62)` с переходом 180ms, при нажатии — плотнее.
Конвертер выдал ему те же классы взаимодействия `x1 x2`, что и логотипу
шапки: поведение одно, дублирования правил нет. Кегль 21px вместо 23px —
подвал набран мельче основного текста, и полноразмерный вордмарк спорил
бы с ним за внимание. Ссылка ведёт наверх страницы, подпись для чтения с
экрана отличается от шапочной, чтобы две одинаковые ссылки не сливались.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений на 1440/1024/768/390, заявки со всех четырёх форм доходят до
базы, перенос макетов воспроизводим. Высота посадочной 3044 → 2992
пикселя, высота подвала не изменилась (203).


## Р-63 · Посадочная страница принята (принято 06.09.2026)

**Решение: вид, наполнение и поведение посадочной страницы приняты
заказчиком. Принята сборка коммита `6f60ec8`; принятое состояние
закреплено веткой `accepted/landing-2026-09-06` — она указывает на коммит
с этой записью, который отличается от принятой сборки только
документацией и вида страницы не меняет.**

Что входит в принятое состояние: первый экран, блок маршрутов «С чего
начнём вашу работу», две карусели, панели «Стандарт качества» и
«Оставьте заявку», подвал. Правовые страницы, три профильные страницы и
задания в `docs/briefs/` под приёмку не подпадают — по ним действуют
прежние договорённости.

**Принятые параметры.** Ниже — замеры, снятые на принятой сборке. Они не
украшение записи: если правка меняет одно из этих чисел, это изменение
принятого результата, и его согласовывают отдельно.

| Параметр | Значение |
|---|---|
| Высота документа, 1440 / 1024 / 768 / 390 | 2992 / 3581 / 4619 / 6043 |
| Первый экран, 1440 | 662 |
| Основания плашек и строки согласия, 1440 | 721 и 721 — совпадают |
| Панели «Стандарт качества» и «Оставьте заявку», 1440 | 354 и 354 |
| Карточки маршрутов, 1440 | 546 каждая |
| Подвал, 1440 / 1024 / 768 / 390 | 203 / 241 / 305 / 471 |
| Карточек в каруселях | 12, у каждой пиктограмма |
| Нарушений axe и контраста, семь страниц | 0 |
| Переполнений по горизонтали, четыре ширины | 0 |
| Формы | четыре, все доходят до базы с верным `source` |

**Что означает фиксация.** Копии макетов в `design/baseline/` побайтно
равны рабочим `design/*.dc.html` на момент приёмки — это точка отката.
Ветка `accepted/landing-2026-09-06` восстанавливает всё дерево принятой
версии: макет, сгенерированные компоненты, изображения и набор
пиктограмм. Она заморожена: в неё не коммитят и её не двигают, работа
продолжается в рабочей ветке и в `main`.

Закрепить состояние тегом не удалось: git-ретранслятор среды разработки
принимает `refs/heads/*` и обрывает соединение на `refs/tags/*`
(«unexpected disconnect while reading sideband packet», четыре попытки с
нарастающей паузой). Замороженная ветка даёт ту же неизменяемую точку
отката; когда ограничение снимут, на этот же коммит можно поставить тег
`landing-accepted-2026-09-06`.

Дальнейшие правки посадочной вносятся обычным порядком — через макет и
перегенерацию, — но каждая, меняющая принятые параметры, требует
отдельного согласования и новой записи в этом журнале. Приёмка не
закрывает открытые пункты из раздела «Что осталось незакрытым» в
`design/HANDOFF.md`: они относятся к проекту целиком.

Путь принятой версии: решения Р-51…Р-62. Айдентика (палитра, шрифты),
изображения, уплотнение первого экрана и карточек, репортажная съёмка
маршрутов, собственный набор пиктограмм, строки согласия и логотип в
подвале.


## Р-64 · Профильные страницы: пустоты, пиктограммы, чистка ресурсов (принято 06.09.2026)

**Решение: убрана пустота внутри карточек пакетов, набор `pd-*` доведён
до блока «Пять пакетов», из репозитория удалены одиннадцать векторных
файлов без ссылок, недостающая иконка доложена в каталог макетов.**

**Пустота в карточках пакетов.** Сетка блока стояла на
`align-items: stretch`: карточки в паре растягивались до высоты соседа, а
нижняя строка карточки прижата к её низу через `margin: auto 0 0`. Разница
в числе строк «Документы на выходе» уходила в дыру внутри карточки — у
пакета 01 она достигала 164 пикселей, у пакета 03 — 49. Дыра внутри
рамки читается как недоделанная вёрстка.

Сетка переведена на `align-items: start`. Карточка теперь по высоте равна
своему содержимому: 580 → 430 у пакета 01 и 482 → 447 у пакета 03,
внутренний разрыв везде равен штатным 14 пикселям. Высота секции не
изменилась — её задаёт самая длинная карточка ряда, и это разница
содержания, а не вёрстки: свободное место ушло из-под рамки на фон
страницы, где оно читается как обычный воздух макета.

**Пиктограммы пакетов.** Пять карточек были чистым текстом. Добавлены
глифы того же набора: 01 — лупа над документом (`pd-patsearch`,
патентно-информационный поиск), 02 — лестница этапов с галочкой
(`pd-stages`, работа по этапам с приёмкой), 03 — документ с печатью
(`pd-grant`, заявка по форме конкурса), 04 — щит с узлом (`pd-asset`,
права на разработку), 05 — столбцы с базовой линией (`pd-market`, рынок и
решение о внедрении). Четыре глифа нарисованы заново по правилам Р-61:
сетка 24×24, обводка 1,5, скругления концов и стыков. Знак встаёт в одну
строку с номером, цвет наследуется от номера, высота шапки карточки не
меняется.

Дальше набор не разворачивался, и это тоже решение. Блок «Шесть
механизмов» — таблица с зеброй, знак в строке таблицы её засоряет.
Карточки «Услуги» на странице «Аспирантам» уже несут натюрморт и галочку.
В блоке «Что берём в работу» часть карточек с натюрмортом, часть без:
глиф у одних и картинка у других дали бы разнобой внутри одного ряда.

**Удалены одиннадцать векторных файлов без единой ссылки:**
`assets/art/utp-*.svg` (шесть, вытеснены набором `pd-*`) и
`assets/art/svc-*.svg` (пять, заменены натюрмортами `svc-*.webp`) — из
обоих каталогов, `design/assets/art/` и `app/public/assets/art/`. Перед
удалением каждый файл проверен на ссылки в макетах, компонентах и
инструментах: ноль. История в git сохраняется, вернуть можно в любой
момент. Пункт 10 списка открытых вопросов закрыт.

**Доложен `assets/icons/send.svg`.** Файл лежал только в `app/public/`,
хотя все четыре макета ссылаются на него. При открытии `.dc.html` напрямую
и в автономной сборке иконка телеграма не рисовалась. Каталоги ресурсов
теперь совпадают файл в файл. Пункт 11 закрыт.

**Не удалялись** двенадцать файлов `assets/art/avatar-*.svg`, оставшихся
от снятого блока отзывов: заказчик санкционировал удаление только
`utp-*` и `svc-*`. Ссылок на аватары нет — вопрос об их удалении вынесен
в список открытых.

Приёмка посадочной не затронута: её геометрия после правок прежняя —
высота 2992, основания плашек и строки согласия 721 и 721, подвал 203.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений на 1440/1024/768/390, заявки со всех четырёх форм доходят до
базы, перенос макетов воспроизводим.


## Р-65 · Натюрморт «Докторские диссертации» перегенерирован (принято 06.09.2026)

**Решение: `svc-doctoral.webp` заменён кадром той же серии; открытый
пункт 8 закрыт.**

Прежний файл был не столько светлым, сколько неудачно скадрированным: два
тома стояли вертикально, и в полосу 700×214 попадали тёмный корешок и
белая карточка — предмет не читался, половину кадра занимало пустое поле.
Средняя яркость 164 при разбросе 76 против 128 у `svc-dissertation`: в
ряду карточек «Услуги» соседний натюрморт выглядел снимком, а этот —
засветкой.

Постановка задачи модели повторяет серию (Р-53): натюрморт на светло-сером
столе, мягкий дневной свет слева, малая глубина резкости, приглушённый
холодный цвет, матовые поверхности, без текста, логотипов и людей.
Изменены три вещи: тома лежат стопкой, а не стоят; композиция собрана под
широкую горизонтальную полосу; в кадре добавлен предмет масштаба — перо.
Полный текст:

```
Still life photograph on a pale grey desk, soft daylight from the left,
shallow depth of field, muted cool colour, matte surfaces. Two thick
hardbound doctoral dissertations in dark navy cloth binding stacked one on
the other, filling the middle of the frame, cream page edges catching the
light, a fountain pen resting on top. Grey archive boxes blurred behind.
Composition centred and readable when cropped to a wide horizontal band.
No text, no lettering, no logos, no people.
```

Сгенерировано три варианта, выбран второй: у первого край кадра занимали
очки, третий снова дал вертикальную композицию, не выдерживающую обрезку
в полосу. Модель — `nano_banana_2` (отдана как `nano_banana_flash`),
соотношение 16:9, из середины кадра взята полоса 3,27:1 и приведена к
700×214, WebP качества 62.

Замер после замены: средняя яркость 124 при разбросе 71 — файл встал в
один тон с `svc-dissertation` (128 и 75) и перестал быть выпадающим в
ряду.

Раньше перегенерация упиралась в сеть: сервис отдаёт файлы с хоста,
закрытого политикой прокси. Обход тот же, что для портретов: кадр
скачивается, обрезается и кодируется в песочнице генератора, а в
репозиторий переносится через base64 с проверкой контрольной суммы
(`515940cf9517ae92e204b41ee9cd68dd`).

Файл положен в оба каталога ресурсов; они совпадают файл в файл.


## Р-66 · Разбор вёрстки профильных страниц (принято 07.09.2026)

**Решение: устранены три дефекта вёрстки, найденные измерением, а не на
глаз.**

Посадочная прошла придирчивый разбор и принята; три профильные страницы
такого разбора не проходили. Написаны три проверки, прогнанные на четырёх
ширинах: выход содержимого за скруглённую рамку карточки, одинокое слово
в последней строке заголовка и число знаков в строке абзаца.

**1. Текст выходил за рамку карточек команды.** Все двенадцать карточек
блока «Команда» на трёх страницах имели жёсткое `height: 138px`. Двум из
них содержимого не хватало места: у «Научного редактора» и «Эксперта по
специальности» заголовок переносился на две строки, и последняя строка
описания оказывалась снаружи скругления — текст лежал на фоне страницы
под белой карточкой. Высота заменена на `min-height: 138px`: карточка
растёт под содержимое, остальные десять не меняются, потому что им
хватало.

**2. Одинокое слово в последней строке заголовков.** Четыре заголовка
блока «Преимущества» ломались так, что на вторую строку уходило одно
слово — хуже всего «Соответствие требованиям ВАК и / ГОСТ». Добавлено
`text-wrap: balance`: строки выравниваются по длине. Два заголовка
разделов («Вы работаете не с отдельным человеком, а с системой
экспертизы», «Над дипломом работает команда, а не один исполнитель»)
стояли на `text-wrap: pretty`, который одинокое слово не убирал, —
переведены на `balance`.

Заголовку «Ведём работу *от первой главы до защиты*» не хватало **девяти
пикселей**: `max-width: 34ch` давало 544px при собственной ширине 553px,
и подсветка рвалась так, что «защиты» стояло на второй строке одно.
Ограничение поднято до 38ch — заголовок встал в одну строку, плашка
подсветки целая, высота заголовка 68 → 34 пикселя.

Заголовки первого экрана не трогались: там последняя строка занята
длинным словом («докторантов», «патента»), это нормальный перенос, а не
дефект. Проверка помечает их формально, по числу слов.

**3. Строка длиной 178 знаков.** Абзац под таблицей сроков на странице
«Аспирантам» тянулся на всю ширину блока — 1158 пикселей при кегле 13.
Норма читаемости 45–75 знаков, здесь выходило под сто девяносто. Добавлено
`max-width: 82ch`. После правки ни один абзац на четырёх страницах не
превышает 78 знаков в строке — проверено подсчётом длины текста на число
строк, а не оценкой по ширине.

Все три проверки сохранены в рабочих скриптах и повторяемы.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений и ноль выходов за рамку на 1440/1024/768/390, заявки со всех
четырёх форм доходят до базы, перенос макетов воспроизводим. Принятая
посадочная не затронута: высота 2992, основания плашек и строки согласия
721 и 721, подвал 203.


## Р-67 · Аудит материалов, чистка кода и ресурсов (принято 07.09.2026)

**Решение: документы приведены к фактическому состоянию, из репозитория
удалены ресурсы без ссылок, из макетов — мёртвые правила CSS.**

Проект прошёл шестнадцать решений подряд, и документация местами описывала
состояние, которого уже нет. Это опаснее отсутствия документации: читатель
верит написанному и принимает решения на устаревших данных. Проведена
сплошная сверка утверждений с репозиторием.

### Что расходилось с фактом

**`README.md`.** Таблица этапов обрывалась на 24.08.2026 и не знала ни о
переходе на новую айдентику, ни о повторной приёмке посадочной. Блок
«Проверено» ссылался на коммит `bc68296` и три ширины. Открытый пункт 5
требовал снимков большего разрешения для вырезанных по контуру фигур,
которых нет с решения Р-53: снимки полнокадровые, 760×760. Добавлены два
этапа, обновлён блок проверок (четыре ширины, актуальный набор проверок),
пункт про разрешение снимков заменён на действительно открытый — цены.

**`design/HANDOFF.md`.** Обещал автономную сборку посадочной
`ProDisser - посадочная страница.html` — файла в репозитории нет. Описывал
`image-slot.js`, `support.js` и `ds-base.js` так, будто они лежат рядом с
макетами; на деле `<script src>` в макетах на них ссылается, а файлов нет:
это рантайм среды прототипирования. Практическое следствие записано прямо:
открытый напрямую `.dc.html` покажет разметку и стили, но слоты изображений
останутся пустыми, а логика класса не оживёт. Заметка про расхождение
каталогов ресурсов переписана — расхождение устранено решением Р-64, вместо
констатации дана команда сверки.

**`docs/PREVIEW.md`.** Таблица замеров витрины держала высоты предыдущей
редакции страниц: посадочная 3382 против нынешних 2992. Замеры пересняты,
проставлена дата и оговорка: при расхождении верен сайт, а не таблица.

**`docs/DECISIONS.md`.** Три решения содержали утверждения, отменённые
позже. История не переписывается — под каждым поставлена отсылка:
- Р-53 «недостаток остаётся: `svc-doctoral.webp` светлее остальных» →
  закрыто Р-65;
- Р-57 «цель нажатия 44px» → изменено Р-62 для строки согласия;
- Р-61 «двенадцать глифов» → дополнено Р-64 до шестнадцати.

**`docs/briefs/`.** Задания на личный кабинет и админ-панель описывали
снятую палитру (`#7B2BE0`, `#DDC4FA`, `#3DDCA0`), гарнитуру Onest и кегль
11px — и утверждали, что проект на них свёрстан. Токены заменены
действующими, добавлена ссылка на источник истины. Заметка про
градиентные плашки переписана: градиенты сняты со всего проекта, а не
только неуместны в кабинете. Пункт 8 списка открытых вопросов закрыт.

Задания сами по себе в объём этой итерации не входят — правился только
раздел обязательных значений, чтобы он не противоречил сайту. Состав
экранов, сценарии и требования к интерфейсу не тронуты.

### Что удалено

Каждый файл перед удалением проверен на ссылки в макетах, компонентах и
инструментах — по имени с расширением и без, чтобы поймать обращения через
переменную:

- `assets/art/avatar-*.svg` — двенадцать силуэтов от снятого блока отзывов;
- `assets/logo.png` — растровый логотип; логотип проекта текстовый,
  картинки для него не существует;
- `assets/icons/library-big.svg`, `assets/icons/rotate-ccw.svg` — глифы,
  вытесненные набором `pd-*` и переработкой блоков.

Всего пятнадцать файлов из двух каталогов — тридцать удалений. История в
git сохраняется. После чистки `design/assets` и `app/public/assets`
совпадают файл в файл.

### Что вычищено из кода

Три правила CSS объявлены в макетах, но ни одного элемента с такими
классами на страницах нет: `.bp-reasons` на «Бизнесу», `.pd-needs` на
«Аспирантам», `.sp-div` на «Студентам». Правила сняты вместе с
упоминаниями в медиазапросах. Проверка «объявлено, но не применено»
прогнана по всем восьми макетам — мёртвых классов не осталось.

Исходники приложения проверены отдельно: неиспользуемых модулей нет,
`SiteMotion` и все семь `*.meta.ts` импортируются, сгенерированный клиент
Prisma в репозиторий не попадает.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений и выходов за рамку на четырёх ширинах, заявки со всех четырёх
форм доходят до базы, перенос макетов воспроизводим. Принятая посадочная
не затронута.


## Р-68 · Движение, клавиатура и превью ссылки (принято 07.09.2026)

**Решение: пауза бегущей строки заработала и стала доступна с клавиатуры;
у страниц появилась картинка превью ссылки.**

Проверены четыре измерения, которых проект не касался: уважение к
`prefers-reduced-motion`, обход страниц клавиатурой, скорость загрузки и
экраны уже 390 пикселей.

### Что оказалось в порядке

`prefers-reduced-motion` учтён honestly: при включённой настройке бегущие
строки останавливаются, переходы обнуляются, плавная прокрутка отключается,
а класс появления секций вообще не навешивается — содержимое видно сразу.
Отдельно проверено, что без JavaScript ничего не прячется: класс с
`opacity: 0` добавляет скрипт, в серверной разметке его нет.

Обход клавиатурой: 43 остановки на посадочной, 57—69 на профильных, у
каждой видимая рамка фокуса, ни одной остановки нулевого размера.
Карусели управляются стрелками и клавишей End. Разметка полная: `lang`,
один `h1`, `main`, `header`, `footer`, `nav`, ссылка «Перейти к
содержанию» и канонический адрес на всех семи страницах.

Скорость: LCP 280—308 мс, CLS 0, вес страницы 188—270 КБ. Экраны 320 и
360 пикселей — без переполнений.

### Пауза бегущей строки не работала

В макетах стояло правило
`.pd-ticker:hover .pd-run{animation-play-state:paused}`, но анимация
задана инлайновым сокращением `animation: pd-marquee-l 42s linear infinite`.
Сокращение выставляет `animation-play-state: running` прямо в атрибуте
`style`, а инлайновое объявление сильнее любого правила таблицы стилей.
**Пауза не срабатывала ни разу, ни на одной из трёх страниц** — ни при
наведении, ни при фокусе. Дефект тихий: правило в коде есть, поведения нет.

Добавлен `!important` — единственный способ перебить инлайновое
объявление, не переписывая разметку. Замер: покой `running`, наведение
`paused`, фокус `paused` на всех трёх страницах.

### Пауза не доставалась с клавиатуры

Второе правило, `:focus-within`, не срабатывало по другой причине: внутри
бегущей строки нет ни одного фокусируемого элемента, значит фокус туда
никогда не попадал. Критерий WCAG 2.2.2 (уровень A) требует механизма
остановки для движения, которое идёт дольше пяти секунд; наведение мышью
таким механизмом не считается — оно недоступно тем, кто работает с
клавиатуры.

Секции бегущей строки получили `tabindex="0"`, а их подпись для чтения с
экрана — указание на поведение: «Услуги — бегущая строка, останавливается
при наведении и фокусе». Прибавилась одна остановка обхода на страницу,
рамка фокуса видна, движение замирает.

### Картинки превью ссылки не было

Ни одна из семи страниц не отдавала `og:image`. Сайт зовёт писать в
телеграм, и ссылка на него приходила без картинки — в ленте это выглядит
как обрывок.

Обложка 1200×630 собрана из фирменных элементов: вордмарк, заголовок
антиквой с плашкой подсветки, строка подводки гротеском, орбита из
хедера, внизу — «Автором работы остаётесь вы» и адрес. Отрисована в
браузере на реальной странице сайта, поэтому гарнитуры те же, что в
вёрстке, а не подобранные на глаз. Сохранена в PNG со сжатой палитрой,
59 КБ. WebP не годится: часть сборщиков превью в мессенджерах его не
разбирает и показывает ссылку без картинки.

Обложка одна на весь сайт: заголовок и описание у страниц свои, а картинка
отвечает на вопрос «от кого пришла ссылка», а не «о чём эта страница».

Объявлять её в общем макете оказалось нельзя: Next заменяет родительский
блок `openGraph` целиком, а не дополняет, и каждая страница задаёт свой —
картинка из макета до страниц не доходила. Описатель вынесен в
`app/src/components/og-cover.ts` и подключён в семи маршрутах: имя файла
живёт в одном месте.

Попутно в рабочем процессе витрины проставлен `NEXT_PUBLIC_SITE_URL`. Без
него Next подставлял в `og:image` адрес `localhost`, и в опубликованную
статику уезжала нерабочая ссылка на картинку.

### Что осталось как есть

Снимок `curator.webp` 480×480 отдаётся в слоты 122 и 72 пикселя — вдвое и
втрое больше нужного при двойной плотности. Файл весит 9 КБ; заводить
набор размеров и `srcset` ради семи килобайт дороже, чем оставить.
Замер записан, чтобы решение было видимым, а не забытым.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений на 1440/1024/768/390/360/320, `og:image` отдаётся абсолютным
адресом на всех семи страницах, сборка витрины проходит, перенос макетов
воспроизводим. Принятая посадочная не затронута.

## Р-69 · Страница «Аспирантам»: карточки преимуществ, второй ряд услуг, лаконичный подвал (принято 07.09.2026)

**Решение: блок преимуществ переведён на карточки, второй ряд услуг стал
рядом карточек с натюрмортами, блок «Что прислать» снят, подвал приведён
к строению посадочной страницы.**

Правки касаются только `design/MainPage.dc.html` (публикуется как
`/main`). Принятая посадочная и две другие профильные страницы не
затронуты, замеры Р-63 сохраняются: `labelBottom` 721, `plateBottom` 721,
подвал 203, документ 2992.

### Преимущества: надписи на фоне страницы переведены в карточки

Четыре пункта УТП были строками на белом фоне страницы, разделёнными
верхней линейкой, — тот же приём, что и в перечнях, поэтому блок читался
как список, а не как четыре самостоятельных утверждения. Пункты
поставлены в карточки: рамка `#E3E7EC`, радиус 14, поля 20/22/22, тень
`0 1px 2px`. Расстояние между колонками 48 пикселей заменено на общий
зазор сетки 16 — карточки не расходятся по горизонтали, как расходились
строки. Пиктограмма и порядковый номер остались в левой колонке.

### Второй ряд услуг: карточки с натюрмортами вместо строк

Строение раздела сохранено: заголовок, ряды сетки в три колонки, кнопка
«Полный список услуг». Второй ряд был перечнем ссылок-строк, где первым
пунктом шли монографии — направление, за которым к нам приходят реже
прочих. Он перестроен в такой же ряд карточек, как первый: заголовок,
описание, полоса натюрморта 104 пикселя, срок.

На передний план вынесены «Магистерские диссертации», «Гранты и заявки»
и «Подготовка к предзащите» — три направления, которыми аспирант
занимается рядом с диссертацией. Монографии остались в полном перечне и
на переднем плане больше не стоят.

Для «Грантов и заявок» дописан натюрморт `svc-grants.webp` (700×214,
6,3 КБ) в той же серии: тёмно-синяя папка, бланки с разлиновкой, зажим и
карандаш, рассеянный дневной свет, фон `#F2F4F7`. Первый вариант
отвергнут: генератор напечатал на бланке разборчивый английский
заголовок, чего нет ни в одном из пяти прежних снимков. Второй снят под
скользящим углом — печать читается как серая фактура.

Полный перечень под кнопкой перестроен: из него убраны три вынесенных
наверх направления, монографии сдвинуты с первой позиции. В свёрнутом
виде показываются пять строк и кнопка — шесть ячеек, ровно две строки
сетки, как было.

### Блок «Что прислать, чтобы мы посчитали смету» снят

Заголовок и четыре пункта убраны со страницы вместе с данными
`costNeeds`. Тот же список дословно повторяется в блоке заявки и в
разделе «Как мы работаем»: страница просила одно и то же трижды.

### Подвал приведён к строению посадочной

Подвал профильной страницы был сеткой в четыре колонки с вордмарком
подложкой, описанием услуги, реквизитами, тремя колонками ссылок и
отдельной строкой копирайта — 394 пикселя на 1440. Он повторял
содержимое страницы и спорил с подвалом посадочной, куда ведёт логотип.

Взято строение посадочной: три строки без сетки колонок. Первая —
вордмарк со ссылкой на главную, телефон, почта, подпись про телеграм и
кнопка мессенджера, отделена линейкой. Вторая — три правовые ссылки и
реквизиты моноширинным. Третья — копирайт и переходы на две другие
профильные страницы, которых на посадочной нет и которые сохранены
намеренно: без них с `/main` не попасть на `/students` и `/business`
иначе как через главную.

Вордмарк-подложка, описание услуги и колонка «Меню» сняты: разделы
страницы уже перечислены в шапке, которая видна на всём протяжении
прокрутки.

Высота подвала на 1440 — 225 пикселей против 394 до правки.

| Ширина | Подвал `/main` | Подвал посадочной |
|---|---|---|
| 1440 | 225 | 203 |
| 1024 | 304 | 241 |
| 768 | 368 | 305 |
| 390 | 595 | 471 |

Разница с посадочной — строка переходов на профильные страницы; на 390
её ссылки переносятся и дают три строки по 44 пикселя. Минимальная
высота цели нажатия сохранена (WCAG 2.5.8).

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений по горизонтали и ноль выходов содержимого за рамку карточек
на 1440/1024/768/390, четыре формы доходят до базы, перенос макетов
воспроизводим, `tsc --noEmit` и сборка проходят.

## Р-70 · Страница «Студентам»: состав работ, этапы в два шага, редактура текста, подвал (принято 07.09.2026)

**Решение: состав работ приведён к строению страницы «Аспирантам», блок
этапов разбит на два явных шага с раскрывающимся перечнем, текст переведён
в профессиональный регистр под УТП «Диплом, собранный под требования вашей
кафедры», подвал приведён к строению посадочной.**

Правки касаются только `design/StudentsPage.dc.html`. Принятая посадочная
не затронута: `labelBottom` 721, `plateBottom` 721, подвал 203, документ 2992.

### Состав работ: плитки разного размера заменены равными карточками

Блок был сеткой в четыре колонки, где плитки занимали то одну колонку, то
две, а изображение показывалось полосой 132×52 пикселя внутри строки текста
и только у четырёх позиций из восьми. Разный размер карточек читался как
разная значимость услуг, которой нет, а снимок такого размера не
распознаётся — он воспринимается как посторонний прямоугольник.

Взято строение раздела «Услуги» на `/main`: сетка в три колонки, все
карточки равной ширины и высоты (минимум 232 пикселя), в каждой — пометка,
заголовок, описание, полоса натюрморта 104 пикселя и срок. Два ряда по три
карточки, ниже — плитки полного перечня и кнопка, как на соседней странице.

На передний план вынесены шесть направлений: диплом и ВКР, магистерская
диссертация, нормоконтроль и ГОСТ 7.32, отчёт по практике и НИР, статья для
журнала РИНЦ, подготовка к предзащите. «Доклад и презентация к защите» и
«Перевод статьи на английский» перенесены в начало полного перечня; общее
число позиций — 23, как и было.

Дописаны два натюрморта той же серии: `svc-norm.webp` (рукопись, линейка,
угольник и карандаш — нормоконтроль) и `svc-practice.webp` (папка отчёта со
штампом и конвертом — практика и НИР). Оба 700×214, 5,2 и 6,0 КБ,
печатный текст на них нечитаем и работает как серая фактура.

### Этапы: маршрут виден целиком, а не по одному шагу

Блок был интерактивным, но непонятным. Причины назывались тремя разными
элементами подряд: чипы ситуации, строка-сводка и горизонтальный трек из
пронумерованных кружков. Трек читался как индикатор прогресса, а не как
переключатель, и открывал ровно один этап — маршрут целиком не был виден
ни в одном состоянии. На экранах уже 560 пикселей подписи этапов и вовсе
скрывались правилом CSS, и от блока оставался ряд безымянных кружков.

Взаимодействие сохранено, но названо явно. «Шаг 1 · На какой стадии
работа» — четыре ситуации чипами. Между шагами — сводка маршрута («Ваш
маршрут: 5 этапов, около 6 недель») и пояснение к выбранной ситуации.
«Шаг 2 · Этапы до защиты» — перечень всех этапов маршрута: номер,
название, срок и знак раскрытия. Открыт всегда ровно один этап, в нём —
две колонки «Вы» и «Мы». Смена ситуации по-прежнему пересобирает
перечень целиком.

Что это даёт: маршрут виден весь, любой этап открывается нажатием,
подписи не пропадают на узких экранах, а высота блока не скачет при
переключении. Горизонтальный трек и связанные с ним правила CSS
(`.sp-track`, `.sp-tname`) сняты вместе с данными рельсов и подложек.

### Текст: профессиональный регистр под УТП

УТП страницы — «Диплом, собранный под требования вашей кафедры» — задаёт
и регистр. Из текста убраны разговорные обороты («честно скажет», «говорим
прямо», «переписывать всё заново обычно не нужно»), формулировки сведены к
составу работ и проверяемым обязательствам: кто разбирает работу, что
именно делает, в какой срок. Подводка первого экрана, пояснения к четырём
маршрутам, формулировки этапов и три ответа в блоке вопросов переписаны;
служебная карточка «4 срока» переименована в «4 ситуации» — она ведёт к
блоку этапов, а не к календарю.

Юридические тексты, состав полей форм, тексты галочек согласия и версия
текста согласия не менялись.

### Подвал

Приведён к строению посадочной, как на `/main` (решение Р-69): три строки
без сетки колонок вместо четырёх колонок с вордмарком-подложкой. Высота на
1440 — 225 пикселей. Переходы на `/main` и `/business` сохранены строкой
у копирайта.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений по горизонтали и ноль выходов содержимого за рамку карточек
на 1440/1024/768/390, четыре формы доходят до базы с верным `source`,
перенос макетов воспроизводим, `tsc --noEmit` и сборка проходят.

## Р-71 · «Студентам»: этапы как диаграмма и состав работ, фиксированная высота блока (принято 07.09.2026)

**Решение: блок этапов получил постоянную высоту, полосу долей срока,
перечень маршрута и панель выбранного этапа с составом работ и
результатом. Наполнение расписано для всех 18 этапов четырёх маршрутов.**

Правки касаются только `design/StudentsPage.dc.html`. Принятая посадочная
не затронута: `labelBottom` 721, `plateBottom` 721, подвал 203, документ 2992.

### Высота блока перестала зависеть от выбора

Раскрывающийся перечень (Р-70) менял высоту при каждом нажатии: открытый
этап добавлял две колонки текста, страница под блоком подпрыгивала.
Маршруты из четырёх и пяти этапов давали разную высоту секции, а
пояснение к ситуации занимало то одну строку, то две.

Высота задана в двух местах: строка сводки маршрута получила
`min-height: 76px` (вмещает пояснение любой длины), рабочая зона —
`height: 448px` через класс `.sp-route`. Замер по всем 18 состояниям
четырёх маршрутов: карточка этапов 592 пикселя, секция 1019, внутренней
прокрутки нет ни в одном состоянии. Ниже 1024 пикселей фиксация снимается
(`height: auto`) — на узком экране колонки складываются в одну.

### Инфографика: полоса долей срока

Над рабочей зоной — полоса из сегментов, ширина каждого пропорциональна
длительности этапа в днях (`flex-grow` по числу дней). Видно, что в
маршруте «Ещё не начинал» на основную и практическую части приходится
40 % срока, а разбор задания занимает 4 %. Подписи по краям — «Разбор
работы» и «Защита», по центру общий срок маршрута. Полоса
презентационная (`aria-hidden`): управление вынесено в перечень, иначе
на каждый этап приходилось бы по две остановки обхода с клавиатуры. Ниже
560 пикселей номера в сегментах скрываются, полоса остаётся диаграммой
долей.

### Рабочая зона: перечень слева, состав работ справа

Слева — все этапы маршрута: номер, название, срок; выбранный подсвечен.
Справа — панель выбранного этапа: номер из общего числа, пометка
«Ключевой этап» там, где на этап приходится основной объём, срок,
название, колонки «Вы» и «Мы», плашка «Что входит в этап» и прижатая к
низу строка «Результат этапа».

### Наполнение: 18 этапов расписаны по составу и результату

Прежде этап описывался двумя строками — что делает автор и что делает
команда. Добавлены два поля: состав работ (3—4 пункта) и результат на
выходе. Наполнение разное по маршрутам, потому что стадия работы задаёт
содержание: для не начатой работы первый этап — постановка цели, задач и
плана по главам, для возвращённой с кафедры — разбор листа замечаний с
оценкой объёма по каждому пункту, для срочной — проверка по чек-листу
нормоконтроля и перечень работ, выполнимых к дате защиты.

Ключевыми помечены восемь этапов из восемнадцати — те, на которые
приходится основной объём: работа с введением и первой главой, основная
и практическая части, проблемные разделы, правки по содержанию,
оформление и ссылки, разбор за один день, правки по списку.

### Попутно

Роль `group` на элементе `<ol>` снята: она перебивала роль списка, и axe
справедливо сообщал, что элементы `<li>` оказались вне списка (WCAG 1.3.1).
Кегль 11 пикселей в восьми служебных подписях поднят до 12 — нижняя
граница, принятая в проекте.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений по горизонтали и ноль выходов содержимого за рамку карточек
на 1440/1024/768/390, четыре формы доходят до базы с верным `source`,
перенос макетов воспроизводим, `tsc --noEmit` и сборка проходят.

## Р-72 · Страница «Студентам» принята и зафиксирована (принято 07.09.2026)

**Решение: страница `/students` принята заказчиком 07.09.2026. Принята
сборка коммита `9b1cd8f`; принятое состояние закреплено замороженной
веткой `accepted/students-2026-09-07`.**

Приёмка охватывает `design/StudentsPage.dc.html` и сгенерированный из
него `app/src/components/pages/StudentsPage.tsx`. Посадочная страница
принята отдельно (Р-63) и этой записью не затрагивается; страницы
`/main` и `/business`, правовые документы и задания в `docs/briefs/`
под приёмку не подпадают.

### Принятые параметры

Правка, меняющая любое из этих чисел, — изменение принятого результата:
её согласовывают отдельно и записывают в журнал решений.

| Параметр | Значение |
|---|---|
| Высота документа, 1440 / 1024 / 768 / 390 | 6874 / 7611 / 10240 / 12658 |
| Первый экран (`#works`), те же ширины | 977 / 898 / 1257 / 1531 |
| Блок этапов (`#route`), те же ширины | 1019 / 1290 / 1488 / 1936 |
| Карточка этапов внутри блока, те же ширины | 592 / 863 / 927 / 1068 |
| Подвал, те же ширины | 225 / 304 / 368 / 595 |
| Карточек состава работ с натюрмортом | 6 |
| Плиток полного перечня в свёрнутом виде | 8 + кнопка |
| Ситуаций в шаге 1 | 4 |
| Этапов в маршрутах | 5 / 5 / 4 / 4 |
| Высота карточки этапов во всех 18 состояниях | 592, прокрутки нет |
| Нарушений axe и контраста, семь страниц | 0 |
| Переполнений по горизонтали, четыре ширины | 0 |
| Формы | четыре, все доходят до базы с верным `source` |

### Что означает фиксация

Копия макета в `design/baseline/StudentsPage.baseline.dc.html` побайтно
равна рабочему `design/StudentsPage.dc.html` на момент приёмки — это
точка отката. Ветка `accepted/students-2026-09-07` восстанавливает всё
дерево принятой версии: макет, сгенерированные компоненты, изображения и
набор пиктограмм. Она заморожена: в неё не коммитят и её не двигают,
работа продолжается в рабочей ветке и в `main`.

Закрепление тегом по-прежнему невозможно: git-ретранслятор среды
разработки принимает `refs/heads/*` и обрывает соединение на
`refs/tags/*` (обоснование — Р-63). Поэтому точка приёмки закреплена
веткой, как и для посадочной.

### Что в странице осталось открытым

Цены не публикуются, пока нет утверждённого прайса (Р-03): в смете
названы только сроки. Отзывов на странице нет — блок снят до решения
заказчика. Рекламная галочка в формах не заводится (Р-10).

## Р-73 · Снимок в блоке вопросов на странице «Студентам» (принято 08.09.2026)

**Решение: в блоке вопросов страницы `/students` снимок инженера у стены
с чертежами заменён на кадр преподавателя у доски в аудитории.**

В слоте `sp-faq-photo` стоял `business.webp` — тот же кадр, что и на
карточке маршрута «Компаниям» посадочной страницы. Для блока «Вопросы о
защите, кафедре и научном руководителе» он читался мимо темы: проектное
бюро, чертежи, монитор. Дописан `lecturer.webp` (760×760, 22 КБ):
молодая преподавательница стоит у доски в аудитории, в руке маркер, на
доске нечитаемые формулы, за спиной ряды столов и окна.

Кадр остаётся в документальной манере Р-60 — плёночное зерно,
естественный свет из окна, приглушённые цвета, — но с двумя отступлениями:
человек смотрит в объектив и улыбается. Отступление осознанное: блок
предлагает задать вопрос и ведёт к телефону и почте, а отстранённая
манера остальных кадров этому предложению противоречит. Прочие снимки
серии правило сохраняют.

Снимок синтезирован и не изображает конкретного человека — требование
письменного согласия по Р-08 к нему не относится, выдавать его за
фотографию сотрудника нельзя (Р-53).

Точка кадрирования `center 30%`: слот 480×288 при 1440 обрезает квадрат
сверху и снизу, при 38 % от прежних снимков лицо уходило под верхнюю
кромку. Подпись слота изменена на «Преподаватель у доски в аудитории».
`business.webp` остаётся в проекте: он по-прежнему стоит в карточке
маршрута «Компаниям» на посадочной.

**Принятые параметры Р-72 сохранены до пикселя**: документ
6874 / 7611 / 10240 / 12658, блок этапов 1019 / 1290 / 1488 / 1936,
карточка этапов 592 / 863 / 927 / 1068, подвал 225 / 304 / 368 / 595.
Правка меняет содержимое слота, но не геометрию страницы; снимок в
`design/baseline/` обновлён.

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений и выходов за рамку на 1440/1024/768/390, четыре формы
доходят до базы, перенос макетов воспроизводим, `tsc --noEmit` и сборка
проходят.

## Р-74 · Страница «Компаниям»: пакеты, первый экран, подвал (принято 08.09.2026)

**Решение: карточки пакетов выровнены по рядам, первый экран уплотнён и
основания колонок сведены, подвал приведён к строению посадочной.**

Правки касаются только `design/BusinessPage.dc.html`. Принятые страницы —
посадочная (Р-63) и «Студентам» (Р-72) — не затронуты.

### Пакеты: карточки разной высоты и одна во всю ширину

Сетка из шести колонок раздавала карточкам разный размах — 4, 2, 3, 3 и 6
колонок, — а `align-items: start` не давал им растягиваться по ряду.
Получалась лестница: в первом ряду карточки 434 и 584 пикселя, во втором
450 и 485, под короткими зияла пустота до 150 пикселей. Разный размер
читался как разная значимость предложений, которой нет.

Пятая карточка занимала все шесть колонок. Её строки растягивались на
1160 пикселей — около 150 знаков, вдвое больше комфортной строки; правая
колонка норм уезжала к дальнему краю и переставала соотноситься с
названием документа.

Стало: три карточки по две колонки в первом ряду, две по три во втором.
Выравнивание по ряду вернулось к штатному `stretch`, сноска в каждой
карточке прижата к низу. Замер: 592 / 592 / 592 в первом ряду, 485 / 485
во втором. Ниже 1024 пикселей карточки идут по две в ряд, ниже 768 — по
одной.

### Первый экран: пустота в левой колонке и разъезд оснований

Форма отступала от подводки на 56 пикселей, а правая колонка кончалась на
40 пикселей ниже кнопки «Оставить заявку» — основания колонок не
совпадали, как это сделано на посадочной (Р-58).

Отступ формы сокращён до 36 пикселей, верхний отступ колонки карточек — с
24 до 8, высота карточки — со 132 до 116 пикселей. Замер: низ формы и низ
правой колонки совпали на 790, высота первого экрана 852 → 792.

### Подвал

Приведён к строению посадочной, как на `/main` (Р-69) и `/students`
(Р-70): три строки без сетки колонок вместо четырёх колонок с описанием
услуги и колонкой «Меню» из шести пунктов. Высота на 1440: 508 → 225.
Переходы на `/main` и `/students` сохранены строкой у копирайта. Тем
самым все три профильные страницы получили одинаковый подвал, и
расхождение, отмеченное при Р-69, закрыто.

### Попутно

Снято мёртвое правило CSS `@media (max-width:768px){.bp-trl,.bp-cat,}` —
селектор с висящей запятой и пустым телом. Снят класс `.bp-foot` вместе с
двумя медиазапросами к нему: сетки, которой они управляли, больше нет.

| Параметр | 1440 / 1024 / 768 / 390 |
|---|---|
| Высота документа | 8129 / 8967 / 12747 / 15277 |
| Первый экран | 792 / 796 / 1409 / 1455 |
| Пакеты | 1208 / 1607 / 2458 / 3280 |
| Каталог | 1055 / 1305 / 1434 / 1864 |
| Шкала УГТ | 546 / 644 / 795 / 1003 |
| Подвал | 225 / 304 / 368 / 595 |

Проверено: ноль нарушений axe и контраста на семи страницах, ноль
переполнений по горизонтали и ноль выходов содержимого за рамку карточек
на 1440/1024/768/390, четыре формы доходят до базы с верным `source`,
перенос макетов воспроизводим, `tsc --noEmit` и сборка проходят. Высота
блока УГТ и без правки постоянна на всех девяти уровнях — 546 пикселей.

### Что осталось на решение заказчика

На первом экране рядом стоят два снимка разной манеры: отраслевой эксперт
снят репортажно (Р-60), куратор проекта — студийный портрет из первой
серии (Р-53). Свести их к одной манере можно только заменой портретов
`curator.webp` и `editor.webp`, а они стоят и на принятых страницах —
такая правка меняет принятый результат и согласуется отдельно.

## Р-75 · Сплошной аудит семи страниц и сверка документов с фактом (принято 09.09.2026)

**Решение: проведён сплошной аудит текущего состояния; найдено и устранено
одно расхождение вёрстки, замеры в документах пересняты.**

### Что проверено

Семь страниц на ширинах 1440 / 1024 / 768 / 390, плюс 360 и 320:

| Проверка | Результат |
|---|---|
| axe-core, правила WCAG 2.0/2.1 A и AA | 0 нарушений на семи страницах |
| Контраст текста | 0 нарушений |
| Переполнения по горизонтали | 0 |
| Содержимое за рамкой карточек | 0 |
| Строки длиннее 90 знаков | нет ни на одной странице |
| Битые снимки, ответы 4xx и 5xx | 0 |
| LCP / CLS / вес страницы | 212—236 мс / 0 / 188—270 КБ |
| Обход клавиатурой | 43 остановки на посадочной, 56—66 на профильных |
| Разметка (`lang`, один `h1`, `main`, `header`, `footer`, `nav`, skip-ссылка, `og:image`, канонический адрес) | полная на семи страницах |
| Формы | четыре, все доходят до базы с верным `source` |

### Что исправлено

Кегль подзаголовков на странице «Компаниям» был неоднородным: семь
заголовков второго уровня набраны 22 пикселями, а «Над задачей работает
проектная команда» — 26, потому что блок перенесён со страницы
«Аспирантам», где принят другой кегль. Приведён к 22 пикселям — размеру
остальных заголовков своей страницы. Высота документа не изменилась:
заголовок и прежде занимал две строки.

Такое же расхождение есть на странице «Студентам» — «Над дипломом
работает команда» набран 26 пикселями против 22 у остальных. Страница
принята (Р-72), правка сдвинет её высоту, поэтому она не вносится:
согласуется отдельно.

### Что пересчитано в документах

`docs/PREVIEW.md` держал замеры от 07.09.2026: `/main` 7453, `/students`
6504, `/business` 8890. После решений Р-69…Р-74 фактические высоты —
7295, 6874 и 8129. Таблица пересчитана, дата и диапазон решений
обновлены. В `README.md` добавлен этап А4, обновлён блок «Проверено на
текущем состоянии» — в нём теперь показатели скорости, обхода клавиатурой
и длины строк.

### Что осталось расхождением по решению заказчика

Ритм секций: на «Аспирантам» шаг между секциями 96 пикселей, на
«Студентам» и «Компаниям» — 101 (`clamp(72px, 7vw, 112px)` при 1440).
Свести к одному значению можно, но это сдвинет высоты принятой страницы
«Студентам», поэтому требует согласования.

Манера портретов: отраслевой эксперт снят репортажно (Р-60), куратор
проекта и научный редактор остаются студийными портретами первой серии
(Р-53). На первом экране «Компаниям» они стоят рядом.

## Р-76 · Единая типографика, ритм секций и манера портретов на трёх профильных страницах (принято 09.09.2026)

**Решение: три расхождения, отложенные в Р-75 до согласования, сведены к
одному значению; выпадающий портрет заменён снимком принятой серии.**

### Кегль заголовков второго уровня

На «Студентам» семь заголовков набраны 22 пикселями, а «Над дипломом
работает команда» — 26; на «Аспирантам» все восемь заголовков шли 26
пикселями против 22 на двух других страницах. Единый кегль — 22 пикселя
с межстрочным 1,4: это значение уже принято на «Компаниям» (Р-75) и
преобладает на «Студентам». Заголовок первого уровня остаётся
доминантой первого экрана, соподчинение уровней сохраняется.

Правило по сайту после правки: `h1` — фирменная антиква крупным кеглем,
`h2` — 22/1,4 на трёх профильных страницах, 19—20 на посадочной и
правовых, где секции короче и заголовков больше.

### Шаг между секциями

«Аспирантам» держала фиксированные 96 пикселей, «Студентам» и
«Компаниям» — `clamp(72px, 7vw, 112px)` (101 при ширине 1440).
Оставлено адаптивное правило: на узких экранах оно сжимает шаг до 72
пикселей вместо постоянных 96, то есть даёт выигрыш там, где вертикаль
дороже всего. Отступ подвала приведён к тому же виду —
`clamp(88px, 8vw, 128px)`. Шаг на трёх страницах теперь совпадает
на всех проверенных ширинах.

### Манера портретов

Расхождение снято не переснятием студийной серии, а заменой выпадающего
кадра. `curator.webp` и `editor.webp` стоят в восьми слотах на четырёх
страницах и задают манеру сайта; репортажный `expert.webp` (Р-60) занимал
один слот на «Компаниям» и там же соседствовал с куратором в соседнем
круге. Заменять один снимок дешевле и безопаснее, чем восемь.

Новый `expert.webp` снят в манере первой серии: ровный светло-серый фон
без предметов, мягкий рассеянный фронтальный свет, приглушённая палитра,
тёмно-синий пиджак, папка с пастельными закладками — тот же реквизит, что
у куратора. Кадр подогнан по масштабу головы к `curator.webp`, точка
кадрирования сведена к `center 38%`, как у остальных портретов серии. Вес
9,2 КБ против 10,3 у прежнего снимка, размер прежний — 480×480.

### Побочно устранено

axe нашёл на «Студентам» нарушение `landmark-complementary-is-top-level`:
снимок рядом с формой заявки был обёрнут в `<aside>` внутри секции. Тег
вспомогательный по смыслу, а содержимое блока — только фотография, поэтому
он заменён на `<div>`. Роли ориентиров на странице стали корректными.

### Проверено

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Кегль `h2` и шаг секций на трёх профильных страницах | 22 пикселя и 101 везде |
| Битые снимки, ответы 4xx и 5xx | 0 |
| `tsc --noEmit`, сборка, воспроизводимость переноса | зелёные |

Высоты документов при 1440: `/main` 7327 (было 7295), `/students` 6874
(без изменений), `/business` 8129 (без изменений).

Состав полей форм, тексты согласий и правовые страницы не затрагивались.

## Р-77 · «Компаниям»: действие в карточках пакетов, плотность механизмов, блок вопросов (принято 09.09.2026)

**Решение: страница доработана в трёх местах — там, где путь читателя
обрывался, где композиция была разрежена и где строение расходилось с
двумя другими профильными страницами.**

### Карточки пакетов заканчивались тупиком

Пять карточек описывали состав работ, сроки и документы на выходе и
завершались строкой смысла — без перехода к действию. Читатель, выбравший
пакет, возвращался к навигации или к форме внизу самостоятельно. При этом
в соседней секции каталога переход уже был («Обсудить это направление»),
то есть паттерн на странице существовал и не применялся там, где нужен
больше всего.

В каждую карточку добавлена ссылка «Обсудить пакет» на форму `#request` —
той же манеры, что в каталоге: акцент `#14417A`, Inter 13/600, стрелка,
цель нажатия 44px. Ссылка стоит отдельной строкой под строкой смысла: при
раскладке в строку она вставала справа только в широких карточках (span 3)
и переносилась вниз в узких (span 2), давая разнобой внутри одного блока.

Пять ссылок ведут в одну форму, поэтому каждой задан свой `aria-label`
вида «Обсудить пакет «НИОКР под сдачу»»: без него скринридер читает пять
одинаковых ссылок подряд и список ссылок страницы становится бесполезен.

### Таблица механизмов была разрежена

Сетка `minmax(0,1.5fr) minmax(0,1fr)` давала первой колонке 660 пикселей
при тексте, ограниченном 52ch (≈420 пикселей): между описанием механизма и
носителем обязательства оставалось около 180 пикселей пустоты в каждой из
шести строк. Носитель к тому же центрировался по вертикали
(`align-self:center`), из-за чего пара «утверждение — чем закреплено»
читалась как два независимых столбца.

Сетка приведена к `minmax(0,1.18fr) minmax(0,1fr)`, текст механизма — до
60ch, носитель выровнен по верху строки (`align-self:start`) и расширен до
46ch. Секция стала на 4 пикселя ниже, плотность выросла без потери
воздуха между строками.

### Блок вопросов расходился с двумя другими профильными страницами

На «Аспирантам» и «Студентам» левая карточка блока вопросов — кадр во всю
ширину (`rect`, `cover`, минимум 288 пикселей) с подвалом «Вопрос не из
списка?» и контактами. На «Компаниям» на её месте стоял круглый портрет
72 пикселя с подписью «Куратор проекта» — единственное такое место на
сайте. Строение приведено к общему: тот же кадр во всю карточку, тот же
подвал, та же сетка `minmax(0,.86fr) minmax(0,1.14fr)`.

Снимок — `business.webp` (инженер у стены с чертежами), уже стоящий в
карточке маршрута на посадочной. Переиспользование кадра между посадочной
и блоком вопросов профильной страницы — принятая практика: так же устроен
`postgrad.webp` на «Аспирантам».

Побочный эффект: круглый слот 72 пикселя под снимок 480×480 был последним
случаем избыточного размера изображения, кроме одного оставшегося
(`curator.webp` в круге 122 пикселя на том же экране).

### Переход от шкалы УГТ к пакету

Плашка «Подходящее предложение» под карточкой уровня заканчивалась
подписью «перейти к карточке» — моноширинный шрифт 12px, строчные, без
стрелки. Это ключевой мост между шкалой готовности и предложениями, и он
выглядел слабее, чем обычная подпись. Приведён к манере действия:
«Перейти к пакету», Inter 13/600, акцент, стрелка.

### Проверено

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Четыре формы | доходят до базы с верным `source` |
| Обход клавиатурой «Компаниям» | 70 остановок (было 65), у каждой видимая рамка |
| LCP / CLS / вес «Компаниям» | 232—296 мс / 0 / 187 КБ |
| `tsc --noEmit`, сборка, воспроизводимость переноса | зелёные |

Высота документа при 1440: 8344 против 8129 — прибавка от пяти ссылок в
карточках и от кадра в блоке вопросов.

Состав полей формы, тексты согласий и правовые страницы не затрагивались.

## Р-78 · «Компаниям»: шкала УГТ, изображения в карточках, интерактив механизмов (принято 09.09.2026)

**Решение: блок УГТ перестроен и перестал скакать при переключении, три
блока страницы получили изображения и одинаковые размеры карточек,
механизмы переведены из таблицы в интерактивные карточки.**

### Почему шкала УГТ скакала

Карточка уровня меняла высоту при каждом переключении: на уровнях 1—2
появлялась плашка «Для инструментов внедрения рано» (её нет на остальных
семи), число пунктов в трёх колонках менялось от двух до четырёх, а
содержимое подменялось мгновенно, без перехода. Замер по девяти
состояниям до правки: 396, 397, 418, 428, 437, 442, 444, 457, 475 —
разброс 79 пикселей, и вся страница под блоком прыгала на эту величину.

Устранено тремя приёмами.

**Постоянная строка статуса.** Плашка есть на каждом уровне, меняется
только её содержание: на 1—2 предупреждение «рано», на 3—7 «Документы
этого уровня — наша зона», на 8—9 «Дальше документацию ведёт
предприятие». Это и убирает скачок, и снимает пустоту: раньше на семи
уровнях из девяти место под плашкой просто не использовалось. Граница
ответственности до этого была зафиксирована только в справочнике и на
странице нигде не проговаривалась.

**Фиксированная высота.** `.bp-trlcard{min-height:500px}` — по самому
объёмному состоянию (уровень 8). После правки все девять состояний дают
ровно 500 пикселей, высота документа не меняется. Ниже 1024 пикселей
ограничение снимается: там раскладка перестраивается в две колонки.

**Плавная смена содержимого.** Тело карточки получает CSS-анимацию
появления 260 мс. Анимация перезапускается только при смене имени, поэтому
заданы два одинаковых по эффекту набора ключевых кадров, и класс
чередуется по чётности выбранного уровня. Библиотеки анимации не
подключались — решение Р-50 в силе. Под `prefers-reduced-motion: reduce`
анимация выключается.

### Шкала стала дорожкой

Девять независимых полос по 4 пикселя заменены непрерывной дорожкой с
заливкой до центра выбранной точки (`width` с переходом 320 мс) и точками
уровней: пройденные залиты акцентом, выбранный получает кольцо и
увеличивается с 10 до 14 пикселей, будущие остаются серыми. Шкала
готовности читается как шкала, а не как девять кнопок.

### Код блока переписан

`trlData` был массивом кортежей из десяти позиций, и логика обращалась к
ним по индексу: `cur[7]` — текст предупреждения, `cur[9]` — подпись
пакета. Данные переведены в объекты с именованными полями. Обращения вида
`cur[7]` из кода ушли.

### Изображения и одинаковые размеры карточек

**Пакеты.** Полоса натюрморта 96 пикселей в шапке каждой карточки,
`flex: 0 0 96px` — она не растягивается вместе с карточкой. Все пять
карточек одной высоты (`.bp-pack{min-height:734px}`) независимо от ряда:
до правки ряд из трёх карточек был 734 пикселя, ряд из двух — 627.

**Каталог.** Миниатюра 44 пикселя в строке каждого из семи направлений.
Раскрытая часть получила фиксированную высоту 460 пикселей и три колонки
позиций вместо двух: в каталоге от 1 до 15 позиций, и без этого «Научная
функция внутри компании» раскрывалась втрое ниже, чем «НИОКР». Заливка
строк через одну снята: в трёх колонках соседние столбцы оказывались в
противофазе и полосы читались шахматной доской.

**Механизмы.** Таблица «механизм — носитель обязательства» заменена
сеткой из шести карточек: миниатюра 52 пикселя, номер, заголовок, текст и
раскрывающаяся строка «Чем закреплено». Высота карточки — 382 пикселя во
всех состояниях: место под раскрытый текст зарезервировано, поэтому сетка
не прыгает при нажатии. Кнопка раскрытия у каждой карточки своя, и её
`aria-label` называет механизм — иначе шесть кнопок «Чем закреплено»
неразличимы на слух.

### Изображения

Восемнадцать натюрмортов (5 полос 700×192 для пакетов, 7 миниатюр 150×150
для каталога, 6 для механизмов) синтезированы моделью `nano_banana_2`
(Higgsfield) в манере серии Р-53: рассеянный дневной свет с одной стороны,
холодный баланс, светло-серый фон, малая глубина резкости. Насыщенность
приглушена на 14 % при сборке — генерации выходили теплее принятой
палитры.

Первая серия была забракована и перегенерирована: модель рисовала на
документах читаемые английские надписи («TECHNICAL SPECIFICATION
DOCUMENT», «PATENT SEARCH REPORT»), что нарушает правило «без надписей и
логотипов». В промпт добавлено требование чистых листов: «All paper
completely blank: no text, no letters, no numbers, no writing anywhere».

### Снимок в блоке вопросов

`business.webp` (инженер у стены с чертежами) заменён на `blueprints.webp`
— мужчина в костюме у рабочего стола с развёрнутыми чертежами и папками
документов. Кадр отвечает предмету страницы точнее: разговор идёт о
договоре, приёмке и правах, а не о производственной площадке. Точка
кадрирования `center 56%` — при `center 40%` чертежи на столе уходили за
нижнюю границу карточки.

### Проверено

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Высота карточки УГТ во всех девяти состояниях | 500 пикселей, документ не смещается |
| Высота карточек пакетов | 734 у всех пяти |
| Высота карточек механизмов | 382 в свёрнутом и раскрытом состоянии |
| Высота раскрытого направления каталога | 460 у всех семи |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Четыре формы | доходят до базы с верным `source` |
| Обход клавиатурой «Компаниям» | 76 остановок, у каждой видимая рамка |
| LCP / CLS / вес «Компаниям» | 268 мс / 0 / 245 КБ |
| `tsc --noEmit`, сборка, воспроизводимость переноса | зелёные |

Высота документа при 1440 — 9002 против 8344: прибавка от полос в
карточках пакетов, миниатюр и зарезервированной высоты раскрытий.

Состав полей формы, тексты согласий и правовые страницы не затрагивались.

## Р-79 · Приёмка страницы «Компаниям» (принято 09.09.2026)

**Решение: страница `/business` принята заказчиком. Принятое состояние
закрепляется замороженной веткой `accepted/business-2026-09-09`.**

Порядок тот же, что у посадочной (Р-63) и «Студентам» (Р-72): ветка
указывает на коммит с записью о приёмке, в неё не коммитят и её не двигают.
Копия макета в `design/baseline/BusinessPage.baseline.dc.html` побайтно
равна рабочему `design/BusinessPage.dc.html` на этот момент.

### Принятые параметры

Замеры при ширине 1440. Правка, меняющая любое из этих чисел, —
изменение принятого результата: она согласуется отдельно и записывается в
журнал решений.

| Параметр | Значение |
|---|---|
| Высота документа | 9002 |
| Шаг между секциями | 101 (`clamp(72px, 7vw, 112px)`) |
| Отступ подвала | `clamp(88px, 8vw, 128px)`, высота подвала 225 |
| Кегль заголовков второго уровня | 22 пикселя во всех восьми |
| Первый экран | 792 |
| Шкала УГТ | секция 723, карточка уровня 500 во всех девяти состояниях |
| Пять пакетов | секция 1599, карточка 734 у всех пяти, полоса натюрморта 96 |
| Каталог | секция 1180, раскрытое направление 460 у всех семи |
| Шесть механизмов | секция 894, карточка 382 в свёрнутом и раскрытом состоянии |
| Команда | 631 |
| Права и конфиденциальность | 437 |
| Вопросы | 626 |
| Заявка | 739 |
| Изображений на странице | 3 снимка в разметке и 18 натюрмортов фоном |

### Состояние на момент приёмки

| Проверка | Результат |
|---|---|
| axe-core, правила WCAG 2.0/2.1 A и AA | 0 нарушений |
| Контраст текста | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Форма | доходит до базы с источником `business` |
| Обход клавиатурой | 76 остановок, у каждой видимая рамка фокуса |
| LCP / CLS / вес | 236 мс / 0 / 245 КБ |
| `tsc --noEmit`, обе сборки, воспроизводимость переноса | зелёные |

Принята сборка коммита `2d09abd`.

### Что приёмка не закрывает

Приёмка касается только `BusinessPage.dc.html`. Под неё не подпадают
правовые страницы (`/offer`, `/privacy`, `/consent`), посадочная,
«Аспирантам», «Студентам» и задания в `docs/briefs/`.

Открытыми остаются вопросы, отнесённые в README к «не нашей части работы»:
цены на странице не показаны, поскольку утверждённого прайса нет
(решение Р-03), а правовые документы держат пометку «Проект документа» до
согласования с юристом.

Два замечания по вёрстке, о которых заказчик уведомлён и которые он принял
в текущем виде:

- миниатюры в карточках механизмов (52 пикселя) и в строках каталога
  (44 пикселя) мелкие: предметы в них читаются как пятна. Укрупнение до
  64 пикселей или замена миниатюры полосой поверх карточки увеличит высоту
  блоков и потребует пересмотра принятых чисел;
- фиксированная высота карточек даёт пустоту там, где содержимого меньше:
  у механизмов с коротким текстом и у направлений каталога с одной-тремя
  позициями. Это плата за одинаковый размер карточек, заявленный
  требованием заказчика.

### Страница «Аспирантам» остаётся непринятой

Из трёх профильных страниц формально не принята одна — `/main` (решения
Р-69, Р-76). Порядок её приёмки тот же и требует отдельного решения.

## Р-80 · «Компаниям»: плотность карточек и блок прав в карточках (принято 09.09.2026)

**Решение: искусственные резервы высоты сняты — размер карточек задаётся
содержимым; блок «Права и конфиденциальность» переведён из общего серого
поля в шесть карточек. Принятые параметры Р-79 пересматриваются.**

Правка меняет числа, принятые решением Р-79 накануне. Основание —
замечание заказчика к принятой странице: в карточках каталога и
механизмов до 40 % высоты занимала пустота.

### Откуда бралась пустота

Решением Р-78 карточкам задавались фиксированные высоты, чтобы сетка не
прыгала при раскрытии: `.bp-mechcard{min-height:382px}` держала место под
раскрытую строку «Чем закреплено», `.bp-catbody{min-height:460px}`
уравнивала направления каталога с 1 и 15 позициями, `.bp-pack` — ряды из
трёх и двух карточек. Приём убирал скачок, но платой стала пустота у
всякой карточки короче самой длинной.

### Как решено теперь

Равную высоту задаёт сетка, а не заданное число:
`grid-auto-rows: 1fr` тянет ряды до самой высокой карточки в них. Высота
определяется содержимым, карточки остаются одного размера, а резерв
исчезает. Правило применено к механизмам, пакетам и карточкам прав.

Раскрытая часть каталога переведена на высоту по содержимому. Направление
с восемью позициями больше не растягивается до размера пятнадцати;
одинаковыми остаются свёрнутые строки, а они и так одной высоты.

Плата за отказ от резерва: раскрытие строки «Чем закреплено» теперь
увеличивает высоту ряда механизмов. Для аккордеона это ожидаемое
поведение, и оно дешевле постоянной пустоты.

Механизмы стали ниже на 76 пикселей (382 → 306), каталог — на 38 (1179
против 1217 при раскрытом первом направлении). Остаток разницы между
карточками одного ряда — свойство содержимого: у трёх механизмов текст на
строку длиннее.

### Блок прав переведён в карточки

Шесть пунктов лежали в общем сером поле в две колонки, разделённые только
линиями: заголовки и абзацы сливались, блок читался сплошным текстом.

Переведён в шесть карточек 3 × 2 в общем строе страницы: пиктограмма
21 пиксель в подложке 38, номер, заголовок антиквой, текст. Нижняя
внутренняя кромка `inset 0 -3px 0` — тот же приём, что у карточек
маршрутов на посадочной и у пакетов.

Первая карточка — акцентная (`#0D2B52`, текст белый и `#D8E4F3`): тем же
приёмом выделена первая плашка первого экрана. Разница задаётся плотностью
и рамкой, не градиентом — правило палитры соблюдено. Акцент ставит на
первое место главный факт блока: исключительные права переходят заказчику.

Наведение задано статичным правилом `border-color:#B4C9E5`. Подстановка
в `style-hover` конвертером не переносится — он предупреждает об этом и
выдаёт правило без обработки; при первой попытке правило наведения
потерялось молча в сгенерированном компоненте.

### Пересмотренные принятые параметры

| Параметр | Было (Р-79) | Стало |
|---|---|---|
| Высота документа | 9002 | 8964 |
| Карточка механизма | 382 | 306 |
| Секция механизмов | 894 | 743 |
| Раскрытое направление каталога | 460 у всех семи | по содержимому |
| Секция каталога | 1180 | 1179 |
| Права и конфиденциальность | 437, сплошное поле | 551, шесть карточек по 210 |

Не изменились: шаг секций 101, подвал 225, кегль заголовков 22, первый
экран 792, шкала УГТ 723 и карточка уровня 500 во всех девяти состояниях,
пакеты 1599 и 734 у всех пяти, команда 631, вопросы 626, заявка 739.

### Проверено

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Форма | доходит до базы с источником `business` |
| Обход клавиатурой | 76 остановок, у каждой видимая рамка |
| LCP / CLS / вес | 284 мс / 0 / 246 КБ |
| `tsc --noEmit`, сборка, воспроизводимость переноса | зелёные |

Копия макета в `design/baseline/` обновлена. Ветка
`accepted/business-2026-09-09` остаётся указывать на состояние Р-79: она
заморожена и не двигается; действующие принятые параметры — таблица выше.

## Р-81 · Повторная приёмка страницы «Компаниям» после Р-80 (принято 09.09.2026)

**Решение: состояние страницы после правок Р-80 принято заказчиком.
Принятое состояние закрепляется замороженной веткой
`accepted/business-2026-09-09-r81`.**

Приёмка Р-79 состоялась до замечания о плотности карточек; решение Р-80
пересмотрело часть принятых чисел. Настоящее решение закрепляет
результат: ветка `accepted/business-2026-09-09` остаётся историческим
слепком состояния Р-79 и не двигается, действующая точка отката —
`accepted/business-2026-09-09-r81`.

Копия макета в `design/baseline/BusinessPage.baseline.dc.html` побайтно
равна рабочему `design/BusinessPage.dc.html`. Принята сборка коммита
`458f301`.

### Принятые параметры

Замеры при ширине 1440. Правка, меняющая любое из этих чисел, — изменение
принятого результата: согласуется отдельно и записывается в журнал.

| Параметр | Значение |
|---|---|
| Высота документа | 8964 |
| Шаг между секциями | 101 (`clamp(72px, 7vw, 112px)`) |
| Отступ подвала / высота подвала | `clamp(88px, 8vw, 128px)` / 225 |
| Кегль заголовков второго уровня | 22 пикселя во всех восьми |
| Первый экран | 792 |
| Бегущая строка | 64 |
| Шкала УГТ | секция 723, карточка уровня 500 во всех девяти состояниях |
| Пять пакетов | секция 1599, карточка 734 у всех пяти, полоса натюрморта 96 |
| Каталог | секция 1179, раскрытая часть по содержимому |
| Шесть механизмов | секция 743, карточка 306 у всех шести |
| Команда | 631 |
| Права и конфиденциальность | секция 551, шесть карточек по 210 |
| Вопросы | 626 |
| Заявка | 739 |
| Изображений | 3 снимка в разметке и 18 натюрмортов фоном |

Высоту карточек задаёт содержимое через `grid-auto-rows: 1fr`, а не
заданное число: правка текста в карточке меняет высоту ряда, и это
ожидаемое поведение, а не расхождение с принятым состоянием. Числа выше
фиксируют результат при текущих формулировках.

### Состояние на момент приёмки

| Проверка | Результат |
|---|---|
| axe-core, правила WCAG 2.0/2.1 A и AA | 0 нарушений |
| Контраст текста | 0 нарушений |
| Переполнения на 1440 / 1024 / 768 / 390 / 360 / 320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Форма | доходит до базы с источником `business` |
| Обход клавиатурой | 76 остановок, у каждой видимая рамка фокуса |
| LCP / CLS / вес | 232 мс / 0 / 246 КБ |
| `tsc --noEmit`, обе сборки, воспроизводимость переноса | зелёные |

### Что осталось за рамками приёмки

Разница высот внутри ряда механизмов — около 40 пикселей у карточек 02 и
05. Это не резерв, а свойство текста: у трёх механизмов описание на строку
длиннее. Убрать её можно только правкой формулировок, и заказчик принял
текущий вид.

Раскрытие строки «Чем закреплено» сдвигает содержимое под блоком. Это
следствие отказа от резерва по требованию заказчика; поведение принято.

Приёмка касается только `BusinessPage.dc.html`. Правовые страницы,
посадочная, «Аспирантам», «Студентам» и задания в `docs/briefs/` под неё
не подпадают. Из трёх профильных страниц формально не принята одна —
«Аспирантам» (`/main`).

## Р-82 · Переходы с плашек первого экрана не работали в витрине (принято 09.09.2026)

**Решение: исправлен скрипт сборки витрины — ссылки с якорем получают
префикс базового пути наравне с остальными.**

Заказчик сообщил, что три плашки первого экрана посадочной — «52 позиции»,
«6 этапов» и «с 2016» — никуда не ведут, и предложил либо поставить
заглушку, либо снять переходы.

### Разбор

Переходы заданы верно и работают в приложении: `/business#catalog`,
`/main#process`, `/main#team`. Целевые якоря на страницах есть — секции
`catalog`, `process` и `team` с `scroll-margin-top`.

Дефект возникал только в статической витрине на GitHub Pages. Она лежит по
адресу с базовым путём `/Scientific-Consulting-Service_Web`, поэтому
`app/scripts/build-preview.mjs` после экспорта дописывает префикс ко всем
внутренним ссылкам. Правило искало точное совпадение `href="/main"` —
с закрывающей кавычкой сразу после маршрута. В ссылке
`href="/main#process"` после маршрута идёт решётка, совпадения нет,
префикс не дописывался, и переход уходил в корень домена
`https://klemmerpavel-beep.github.io/main#process` — на несуществующий
адрес.

Заглушка и снятие переходов не рассматривались: ссылки ведут на реальные
разделы, ломалась только публикация витрины.

### Правка

В список правил добавлена пара для ссылок с якорем, поставленная перед
общим правилом. После пересборки витрины плашки ведут на
`/Scientific-Consulting-Service_Web/main/#process` и далее по списку.

### Проверено

Обход всей витрины: 116 внутренних ссылок, битых нет; все якоря переходов
существуют на страницах назначения. Ни одна другая ссылка на префикс не
жаловалась — дефект касался только трёх плашек, единственных ссылок с
якорем между страницами.

| Проверка | Результат |
|---|---|
| Внутренние ссылки витрины | 116, битых 0 |
| Якоря межстраничных переходов | 3, все существуют |
| axe-core на семи страницах | 0 нарушений |
| Переполнения на шести ширинах | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 |
| `tsc --noEmit`, обе сборки | зелёные |

Макеты и компоненты не менялись: правка целиком в скрипте сборки витрины.
Принятые параметры страниц не затронуты.

## Р-83 · Съёмка людей: маршруты, блоки вопросов и профили ролей (09.09.2026)

**Решение: пересняты три карточки маршрутов и три блока вопросов; под
карточками ролей на трёх профильных страницах поставлены фигуры людей без
фона.**

Заказчик поставил три задачи подряд: обновить снимки карточек маршрутов на
посадочной, заменить снимки в блоках вопросов на всех страницах и вывести
за карточками ролей профили людей, соответствующих ролям, — без фона,
основанием под блоками.

### Съёмка маршрутов

Задание заказчика дословно: молодой учёный — в приемлемом костюме с
учебниками в руке; студентка — весёлая, вокруг раскрытый ноутбук и бумаги,
в процессе работы, смотрит в камеру; бизнес — серьёзный человек в костюме
с бумагами в руке, на фоне научная разработка. Требование ко всем трём:
светлые и релевантные.

Съёмка задаёт манеру, которая противоречит части решения Р-60: там три
снимка маршрутов были переведены в документальную манеру, где человек занят
делом и в объектив не смотрит. Р-83 отменяет эту часть Р-60 для карточек
маршрутов и блоков вопросов. Обоснование: оба блока зовут к разговору —
карточка маршрута ведёт на профильную страницу, блок вопросов предлагает
написать или позвонить, — а отстранённый кадр разговору противоречит.
Отстранённая манера сохранена там, где она уместна: круглые портреты
первого экрана показывают человека за работой, а не в диалоге.

| Слот | Сюжет | Кадрирование |
|---|---|---|
| `sp-route-1` | молодой учёный в костюме, стопка книг, библиотека | `center 24%` |
| `sp-route-2b` | студентка за ноутбуком и конспектами, смотрит в объектив | `center 40%` |
| `sp-route-3` | руководитель в костюме с папкой, за ним лабораторный стенд | `center 28%` |

На первом снимке студентки модель нарисовала на крышке ноутбука логотип
Apple. Чужой товарный знак на снимке коммерческого сайта — дефект: он
читается как указание на партнёрство, которого нет. Логотип снят
ретушью — медианным фильтром 27×27 по области 76×80 px; крышка ноутбука
имеет ровный градиент, следов правки не осталось.

### Блоки вопросов

Три сюжета в единой манере: человек смотрит в объектив и открыт к
разговору.

| Страница | Слот | Сюжет | Кадрирование |
|---|---|---|---|
| Аспирантам | `mp-faq-photo` | научный руководитель за столом с рукописью | `center 34%` |
| Студентам | `sp-faq-photo` | преподаватель у доски в аудитории | `center 30%` |
| Компаниям | `bp-faq-photo` | руководитель проекта с чертежами в переговорной | `center 30%` |

Слот `mp-faq-photo` до правки показывал снимок маршрута `POSTGRAD` —
тот же кадр стоял в карточке маршрута на посадочной. Повтор снят: под блок
вопросов заведён отдельный снимок `adviser.webp`.

### Профили ролей под карточками

Блок команды на трёх профильных страницах устроен одинаково: четыре
карточки ролей на орбите вокруг круга «Заказчик». Под каждой карточкой
поставлена фигура человека соответствующей роли — в полный рост, без фона.

| Роль | Файл | Сюжет |
|---|---|---|
| Менеджер | `assets/crew/manager.webp` | мужчина в тёмно-синем пиджаке, планшет и папка |
| Методолог | `assets/crew/method.webp` | женщина в светлом жакете, планшет с чистым листом |
| Научный редактор | `assets/crew/editor.webp` | мужчина в очках и кардигане, стопка чистых листов |
| Эксперт по специальности | `assets/crew/expert.webp` | женщина в технической куртке, деталь в руке |

На «Студентам» четвёртая роль называется «Куратор по специальности» — сюжет
тот же, отдельный снимок не заводился.

**Композиция.** Первая версия ставила фигуру в полный рост под карточкой.
Заказчик забраковал: ноги под карточкой читаются как отдельная картинка, а не
как основание блока. Принятая композиция — фигура **за** карточкой: снимок
обрезан по бедро, увеличен, голова и торс поднимаются над верхней кромкой
карточки, руки с папкой приходятся ровно на эту кромку, и карточка читается
как предмет в руках. Ног в кадре нет: срез приходится на карточку и закрыт ею.

**Разметка.** Карточка роли разделена надвое: `.XX-crew` стала обёрткой,
которая держит только положение на орбите и ширину, а рамка, фон, тень и
состояние наведения ушли на вложенную `.XX-crewcard` с `z-index:1`. Фигура
идёт первым элементом обёртки и позиционируется абсолютно
(`top:-166px`, `height:210px`) — так вынос над карточкой постоянен и не
зависит от её высоты, которая меняется с длиной текста роли. Отрицательный
`z-index` не используется: порядок в разметке уже кладёт фигуру под карточку,
а `z-index:-1` увёл бы её ещё и под кольца орбиты.

**Запас высоты.** Верхние карточки орбиты стоят на 7—14% её высоты, и фигура
поднимается выше квадрата с пропорцией 648×650. Орбите добавлен
`margin-top:156px`; в раскладке до 1024px, где орбита превращается в сетку,
запас снят: там фигура идёт в потоке с отрицательным нижним отступом и место
резервирует сама.

**Размеры.** Высота фигуры 210px на десктопе, 190px до 1024px, 168px до 480px.
Файлы 212—260 × 440px, 11,5—13,2 КБ каждый: двукратный запас по высоте под
экраны с удвоенной плотностью. Фигуры декоративны — роль названа в самой
карточке, — поэтому у них пустой `alt` и `aria-hidden`.

**Отделение от фона.** Первая серия снималась на белом фоне: заливка по
связной области с допуском 9% съедала белую рубашку и халат, а допуск 15%
оставлял пятна фона. Серия переснята на равномерном хромакее, и маска
построена арифметикой каналов: `G − max(R, B)` с порогом 8% и размытием
границы 0,7px. Одежда, кожа и бумага в кадре зелёного не содержат, поэтому
маска отделяет фон без связности и без потерь на светлых тканях.

### Проверено

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440/1024/768/390/360/320 | 0 |
| Битые снимки | 0 из 28 на четырёх страницах |
| Ответы 4xx и 5xx | 0 |
| Заявки со всех четырёх форм | 200, записи в базе |
| Обход клавиатурой | 43 / 56 / 66 / 76 остановок, у каждой видимая рамка |
| LCP | 172—240 мс |
| CLS | 0 |
| Вес страницы | 235—274 КБ |
| `tsc --noEmit`, обе сборки, витрина | зелёные |

Бэкенд, состав полей форм, тексты согласий и правовые страницы не
затронуты.

## Р-84 · Приёмка съёмки людей и профилей ролей (принято 09.09.2026)

**Решение: состояние Р-83 после правки композиции принято заказчиком и
закреплено веткой `accepted/photos-2026-09-09` (коммит `1adfcdf`).**

Приёмка касается того, что изменило решение Р-83: съёмки людей в карточках
маршрутов на посадочной, снимков в блоках вопросов на трёх профильных
страницах и фигур ролей в блоке команды. Остальное наполнение страниц под
неё не подпадает и остаётся в состоянии прежних приёмок — Р-63 (посадочная),
Р-72 («Студентам»), Р-79 и Р-81 («Компаниям»).

### Что закреплено

| Блок | Состояние |
|---|---|
| Карточки маршрутов на посадочной | три снимка, человек смотрит в объектив |
| Блок вопросов на «Аспирантам» | научный руководитель за столом с рукописью |
| Блок вопросов на «Студентам» | преподаватель у доски |
| Блок вопросов на «Компаниям» | руководитель проекта с чертежами |
| Блок команды на трёх страницах | четыре фигуры за карточками ролей |

### Принятая композиция блока команды

Первая версия ставила фигуру в полный рост под карточкой и была
забракована: ноги под карточкой читались как отдельная картинка. Принято
второе решение — фигура за карточкой, снимок обрезан по бедро, голова и торс
над верхней кромкой, руки на самой кромке. Параметры на 09.09.2026:

| Параметр | Значение |
|---|---|
| Высота фигуры | 210 / 190 / 168px (десктоп / до 1024px / до 480px) |
| Вынос над карточкой | 166px, задан `top` и от высоты карточки не зависит |
| Запас над орбитой | `margin-top:156px`, в раскладке до 1024px снят |
| Исходники | 212—260 × 440px, 11,5—13,2 КБ |

### Точка отката

`accepted/photos-2026-09-09` — состояние на момент приёмки. Прежние точки
отката остаются: `accepted/landing-2026-09-06`, `accepted/students-2026-09-07`,
`accepted/business-2026-09-09-r81`; ветка `accepted/business-2026-09-09` —
слепок «Компаниям» до правок Р-80.

### Проверено на момент приёмки

| Проверка | Результат |
|---|---|
| axe-core на семи страницах | 0 нарушений |
| Переполнения на 1440/1024/768/390/360/320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 из 28 снимков |
| Заявки со всех четырёх форм | 200, записи в базе |
| Обход клавиатурой | 43 / 56 / 66 / 76 остановок, у каждой видимая рамка |
| LCP / CLS / вес | 172—240 мс / 0 / 235—274 КБ |
| Перенос макетов воспроизводится | да |
| `tsc --noEmit`, обе сборки, витрина | зелёные |

Из трёх профильных страниц целиком по-прежнему не принята одна —
«Аспирантам» (`/main`): под Р-84 подпадают только её блок вопросов и блок
команды.

## Р-85 · Приёмка страницы «Аспирантам» (принято 09.09.2026)

**Решение: страница `/main` принята заказчиком целиком и закреплена веткой
`accepted/postgrad-2026-09-09` (коммит `c1f8df9`).**

Это последняя из четырёх публичных страниц с содержательным наполнением,
остававшаяся без формальной приёмки. Три остальные приняты ранее: посадочная
— Р-63, «Студентам» — Р-72, «Компаниям» — Р-79 и Р-81. Съёмка людей и
профили ролей по всем четырём — Р-84.

### Что принято

Страница в состоянии после решений Р-51…Р-84: первый экран с двумя
круглыми портретами, метрики, разбор задачи, услуги, маршрут этапов, блок
команды с фигурами ролей, стоимость, блок вопросов с научным руководителем,
форма заявки и подвал. Правовые страницы под приёмку не подпадают: они
остаются в состоянии «Проект документа» до согласования юристом.

### Замеры на момент приёмки

| Проверка | Результат |
|---|---|
| axe-core | 0 нарушений |
| Переполнения на 1440/1024/768/390/360/320 | 0 |
| Битые снимки, ответы 4xx и 5xx | 0 из 8 снимков |
| Заявка с формы | 200, запись в базе с источником `postgrad` |
| Обход клавиатурой | 56 остановок, у каждой видимая рамка |
| LCP / CLS / вес | 240 мс / 0 / 235 КБ |
| Высота страницы на 1440 | 7483 |
| Перенос макета воспроизводится | да |

### Точки отката

Приняты все четыре страницы, у каждой своя ветка:

| Страница | Ветка |
|---|---|
| Посадочная | `accepted/landing-2026-09-06` |
| «Студентам» | `accepted/students-2026-09-07` |
| «Компаниям» | `accepted/business-2026-09-09-r81` |
| «Аспирантам» | `accepted/postgrad-2026-09-09` |
| Съёмка людей по всем страницам | `accepted/photos-2026-09-09` |

Ветка `accepted/business-2026-09-09` остаётся слепком «Компаниям» до правок
Р-80 и точкой отката больше не служит.

### Что это закрывает

Этап А (айдентика и наполнение публичных страниц) завершён. Дальнейшая
работа перечислена в `docs/PLAN.md`: критический путь до открытия сайта
лежит вне вёрстки — площадка, домен, правовые документы, уведомление
Роскомнадзора.

## Р-86 · Аудит критических недочётов и сверка нормативных ссылок (принято 12.09.2026)

**Решение: проведён сквозной аудит семи страниц по десяти направлениям;
устранены две критические находки, ещё две вынесены на решение юриста.
Отдельно сверены годы редакций стандартов на «Компаниям»: две ссылки вели
на недействующие редакции.**

Аудит шёл строго по этапам, на каждом — перечень находок с пометкой
«критично / рекомендация», правка только критичного, отдельный коммит.
Границы были заданы заранее: критичным считались контраст ниже WCAG AA,
поломка вёрстки на 360—768 px, неработающая форма, недоступность основного
действия с клавиатуры, отсутствие или дублирование `title` и `description`,
битые ссылки, расхождение правового текста с содержанием формы согласия.

### Ошибка в перечне страниц, найденная по ходу

Страница для аспирантов размещена по адресу `/main`, а не `/postgrad`. В
первоначальном списке проверки стоял `/postgrad` — маршрута с таким именем
нет, сервер отдаёт 404. Все инструментальные прогоны первого этапа семь раз
проверяли страницу ошибки вместо содержательной страницы, то есть «axe-core
0 нарушений на семи страницах» относилось к шести страницам и одной 404.
После обнаружения `/main` перепроверена целиком: axe-core 0, контраст 0 пар
ниже порога, переполнений нет на шести ширинах, заявка проходит, кнопка
отправки достижима табом за один шаг. Вывод для будущих проверок: перечень
адресов берётся из `sitemap.xml`, а не из имени компонента — компонент
называется `PostgradPage`, маршрут `/main`, и это расхождение легко принять
за адрес страницы.

### Что исправлено

| Находка | Правка | Коммит |
|---|---|---|
| Кегль абзацев основного текста 15 px, интерлиньяж 24—25 px | 16 px и 26 px на семи страницах; попутно 51 неразрывный пробел в заголовках и `max-width:72ch` на `routeNote` | `0c3cfa6` |
| Восемнадцать видимых полей форм с кеглем 13—15 px | 16 px | `3021b59` |

Вторая правка — не косметика. Safari на iOS принудительно масштабирует
вьюпорт при фокусе в поле с кеглем менее 16 px и не возвращает исходный
масштаб после потери фокуса: вёрстка на ширинах 360—768 px уезжает вправо, и
пользователь дозаполняет заявку на съехавшей странице. Состав полей, порядок,
валидация, тексты галочек согласия и версия текста согласия не затронуты.

### Что вынесено на решение юриста

Оба расхождения относятся к критическим по условиям аудита, но неустранимы
без нарушения одного из двух запретов — не менять правовой текст и не менять
состав полей формы.

| Расхождение | Существо |
|---|---|
| `/consent`, раздел 9 | описывает отметку «Согласен получать информационные и рекламные сообщения»; такого поля нет ни в одной из четырёх форм |
| `/offer`, п. 4.1 | определяет акцепт как заявку «с проставлением отметки о согласии с условиями настоящей оферты»; отметка `terms` есть только в двух формах на посадочной, на `/main`, `/students` и `/business` — только `consent` |

Это те же два вопроса, что уже стоят в `docs/PLAN.md`, раздел 2. Аудит
подтвердил их инструментально и уточнил охват: расхождение по оферте
касается трёх страниц из четырёх.

### Сверка нормативных ссылок «Компаниям»

Задача из `docs/PLAN.md`, раздел 3 — единственная в списке без условия
старта. Проверены все шесть нормативных ссылок страницы; две вели на
недействующие редакции.

| Было | Стало | Основание |
|---|---|---|
| ГОСТ 15.101 | ГОСТ Р 15.101-2021 | приказ Росстандарта от 24.08.2021 № 784-ст, введён 01.11.2021 взамен ГОСТ 15.101-98 |
| ГОСТ Р 15.011 | ГОСТ Р 15.011-2024 | приказ Росстандарта от 13.02.2024 № 208-ст, введён 01.03.2024 взамен ГОСТ Р 15.011-96 |
| ГОСТ 7.32 | ГОСТ 7.32-2017 | редакция подставлена: ранее была помечена маркером «не подставлено по памяти» |
| ГОСТ Р 15.301 | ГОСТ Р 15.301-2016 | год проставлен, редакция действует |
| ПП РФ № 327, п. 7 ст. 262 НК РФ, ФСБУ 14/2022 | без изменений | действуют |
| ГОСТ Р 58048-2017 | без изменений | замены не имеет, проверено повторно |

Замена ГОСТ Р 15.011-96 на редакцию 2024 года — не только смена года:
стандарт сменил наименование и область, с «СРПП. Патентные исследования» на
«Интеллектуальная собственность. Патентные исследования». Для страницы,
которая продаёт патентные исследования корпоративному заказчику, ссылка на
отменённую редакцию читается как признак неактуальной экспертизы.

Комментарий с источниками в `design/BusinessPage.dc.html` переписан: годы
редакций внесены с реквизитами приказов и датой сверки, пункт про ГОСТ 7.32
снят из списка неподставленного. В списке остались наименования программ
фондов поддержки и сроки ведомственных процедур.

Ссылка ГОСТ Р 7.0.11—2011 на посадочной проверена отдельно — действует.
Упоминания ГОСТ 7.32 на «Студентам» намеренно оставлены без года: там это
разговорная отсылка в тексте услуги, а не нормативная ссылка в перечне
требований к документам.

### Замеры после правок

| Проверка | Результат |
|---|---|
| axe-core 4.10.2, наборы wcag2a/2aa/21a/21aa/22aa, семь страниц | 0 нарушений |
| Контраст по текстовым узлам, семь страниц | 0 пар ниже порога |
| Переполнения на 360/390/414/480/768/1024 | горизонтальной прокрутки нет |
| Обрезка подписей со стандартами на 360/768/1280 | нет |
| Внутренние адреса и якоря | 10 адресов, 100 якорей, битых нет |
| `title`, `description`, `canonical`, `<h1>` | уникальны, по одному на страницу |
| Заявка с формы `/main` при 1280 и 390 | 200, статус объявлен, поля очищены |
| Перенос макета воспроизводится | да |

### Рекомендации, оставленные без правок

Кегль 118 блоков пояснительного текста в карточках (97 при 13 px, 18 при
12 px, 3 при 14 px); четыре блока со строкой длиннее 90 знаков; вынос палитры
в семантические токены (`#14417A` и `#14161C` по 373 вхождения инлайном);
непрозрачность `.55` на кнопке в состоянии отправки вместо явного цвета;
неоформленный `::placeholder`; три близких серых и три близких голубых фона
одной роли; отсутствие клавиатурной паузы у бегущих строк (WCAG 2.2.2 —
остановка работает только по наведению и `:focus-within`, а фокусируемых
элементов внутри строки нет); подписи полей только через `placeholder` и
`aria-label`; пять разных формулировок ссылки на одну форму.

Ни одна из них не входит в перечень критичного и не блокирует открытие сайта.

## Р-87 · Приёмка аудита и правок по рекомендациям (принято 12.09.2026)

**Решение: результаты аудита критических недочётов, сверка нормативных
ссылок и первая группа правок по рекомендациям приняты заказчиком и
закреплены веткой `accepted/audit-2026-09-12`.** Последняя содержательная
правка вёрстки — коммит `da847c6`; вершина ветки — коммит `13b9de7` с
настоящей записью.

Разбор самого аудита — Р-86. Настоящее решение фиксирует принятое состояние
и подводит черту под работой по вёрстке, начатой после приёмки четырёх
страниц.

### Что принято

| Правка | Коммит |
|---|---|
| Кегль абзацев основного текста 15 → 16 px, интерлиньяж 24—25 → 26 px, 51 неразрывный пробел в заголовках, предел строки для примечания маршрута | `0c3cfa6` |
| Кегль восемнадцати полей форм 13—15 → 16 px против принудительного масштабирования вьюпорта в Safari на iOS | `3021b59` |
| Нормативные ссылки «Компаниям»: ГОСТ Р 15.101-2021 взамен 15.101-98, ГОСТ Р 15.011-2024 взамен 15.011-96, годы редакций ГОСТ 7.32-2017 и ГОСТ Р 15.301-2016 | `e598abf` |
| Кегль пояснительного текста, 209 объявлений: 13 → 14 px и 12 → 13 px с сохранением пропорции интерлиньяжа; предел 72 знака в двух блоках; контраст кнопки в состоянии отправки 4,02 → 6,68:1; явный цвет подсказки в полях | `da847c6` |

Правовые страницы под приёмку не подпадают: их юридический текст не
менялся ни на символ и остаётся в состоянии «Проект документа» до
согласования юристом. Изменения на них коснулись только кегля — как и
разрешает граница «палитра и шрифты».

### Замеры на момент приёмки

| Проверка | Результат |
|---|---|
| axe-core 4.10.2, наборы wcag2a/2aa/21a/21aa/22aa, семь страниц | 0 нарушений |
| Контраст по текстовым узлам, семь страниц | 0 пар ниже порога |
| Переполнения на 1440/1024/768/480/414/390/360 | горизонтальной прокрутки нет |
| Обрезка текста на 360/390/768/1280 | нет |
| Строк длиннее 85 знаков при замере по фактической ширине глифов | нет |
| Битые снимки, ответы 4xx и 5xx | 0 |
| Заявки со всех семи форм | 200, статус объявлен, поля очищены |
| Обход клавиатурой посадочной | 43 остановки, у каждой видимая рамка |
| LCP / CLS | 72 мс / 0 |
| Вес уникальных ресурсов по семи страницам | 382 КБ |
| Обе сборки: боевая и витрина | собираются |
| Перенос макетов воспроизводится | да |

### Точки отката

| Состояние | Ветка |
|---|---|
| Посадочная | `accepted/landing-2026-09-06` |
| «Студентам» | `accepted/students-2026-09-07` |
| «Компаниям» | `accepted/business-2026-09-09-r81` |
| «Аспирантам» | `accepted/postgrad-2026-09-09` |
| Съёмка людей по всем страницам | `accepted/photos-2026-09-09` |
| Аудит и правки по рекомендациям | `accepted/audit-2026-09-12` |

### Что осталось незакрытым

Два расхождения правовых страниц с составом форм — раздел 9 согласия
описывает отметку рекламной рассылки, которой в формах нет, и п. 4.1 оферты
определяет акцепт через отметку принятия оферты, которая есть только в двух
формах из семи. Оба неустранимы силами вёрстки: любое исправление меняет
либо юридический текст, либо состав полей и версию текста согласия. Вопросы
поставлены юристу через `docs/PLAN.md`, раздел 2, и решаются до открытия
сайта, а не после: менять состав полей формы, когда в базе появились
настоящие заявки, значит разрывать журнал согласий по 152-ФЗ.

Рекомендации аудита, оставленные без правок: вынос палитры в семантические
токены; слияние трёх близких серых и трёх близких голубых фонов одной роли;
видимые подписи полей вместо `placeholder` и `aria-label`; единый словарь
ссылок на форму вместо пяти формулировок. Ни одна не блокирует открытие.

Ошибочная рекомендация о клавиатурной паузе бегущих строк снята: секции
тикеров имеют `tabindex="0"`, а `:focus-within` срабатывает и на самом
элементе, поэтому требование WCAG 2.2.2 выполняется без правок.

### Что это закрывает

Работы по вёрстке публичной части завершены. Дальнейшие задачи из
`docs/PLAN.md`, раздел 3, ждут решений заказчика: прайс, домен и площадка,
уведомление Роскомнадзора, ответ юриста по составу согласий.

## Р-88 · Финальная проверка перед выпуском (принято 12.09.2026)

**Решение: проверка перед выпуском проведена по всему проекту, а не только
по вёрстке; четыре критических недочёта устранены коммитом `b247cf3`.**

Предыдущие проверки (Р-86, Р-87) смотрели на страницы. Эта смотрела на то,
что уезжает на сервер: зависимости, поверхность сетевых запросов, обработчик
заявки, секреты, миграции, конвейер сборки.

### Критические недочёты и что сделано

**1. Уязвимости зависимостей.** `npm audit` показывал одну критическую и
четыре высоких. Устранены поднятием версий:

| Пакет | Было | Стало | Что закрыто |
|---|---|---|---|
| next | 15.5.23 | 15.5.25 | GHSA-p293-qw3h-jr36 и GHSA-2xp9-vwfh-vxw4 — удалённое выполнение кода, второе через оптимизатор изображений |
| nodemailer | 9.0.5 | 10.0.9 | четыре высоких, в том числе обход проверки домена получателя и квадратичный разбор адресов |
| sharp | 0.35.3 | 0.35.4 | уязвимости libheif |
| fast-uri | 3.1.5 | 4.1.4 | подмена узла и подделка серверных запросов |

Проверка API nodemailer при переходе на 10.x: проект использует только
`createTransport` и `sendMail` с обычными полями, сигнатуры не изменились;
письмо уходит, сборка и типы зелёные.

Осталась связка `mysql2` под `prisma` — одна высокая и одна умеренная.
Устранение требует отката prisma до 6.x, то есть слома совместимости, а сам
драйвер MySQL в проекте на PostgreSQL не загружается ни разу: подключение
идёт через `@prisma/adapter-pg`. Оставлено сознательно.

**2. Открытый оптимизатор изображений.** Страницы перенесены из макетов и
ставят снимки обычным тегом `img`; `next/image` не используется нигде. При
этом рабочая сборка держала открытым `/_next/image` и пропускала файлы через
`sharp` — то есть именно через ту библиотеку и тот путь, к которым относится
критическая рекомендация по безопасности Next. Обращение к внешним адресам
оттуда уже было закрыто отсутствием `remotePatterns`, но обработка запросов
шла. Оптимизатор выключен: `images: { unoptimized: true }` в рабочей ветке
конфигурации, адрес отвечает 404. Витрина не затронута — там он и так был
выключен статическим выводом.

**3. Четыре неиспользуемые производственные зависимости.**
`@astryxdesign/core`, `@astryxdesign/cli`, `@astryxdesign/theme-neutral` и
`@stylexjs/stylex` были добавлены 11.09.2026 отдельным коммитом и не
упоминаются ни в одном файле проекта: ни импорта, ни конфигурации, ни
упоминания в документации. Для продакшена это чужой код в производственной
ветке зависимостей без единого потребителя — лишняя поверхность цепочки
поставок, лишнее время установки и полторы тысячи строк в `package-lock`.
Удалены. Заодно восстановлено правило проекта: зависимость не добавляется
без явного решения.

**4. Ловушка для роботов отклонялась схемой.** Поле `company_website`
описано в `lead-schema.ts` правилом `max(0)`, и заполненная ловушка получала
422 с именем поля в ответе. Замысел был противоположный: комментарий к
`looksAutomated` прямо говорит «не отклоняем молча: помечаем заявку и всё
равно сохраняем — ложное срабатывание не должно стоить живого клиента».
Схема срабатывала раньше, поэтому ветка `company_website` в `looksAutomated`
была мёртвым кодом.

Последствия были двойные. Робот получал точный адрес своей ошибки и узнавал,
на каком поле попался. Живой заявитель, которому автозаполнение подставило в
скрытое поле адрес организации, упирался в неустранимый отказ на поле,
которого не видит, — с английским текстом «Invalid input» на сплошь русском
сайте, и с переводом фокуса на элемент с `aria-hidden` за границей экрана.
Схема больше поле не отклоняет: заявка сохраняется со статусом `SPAM`,
ответственного не тревожит, заявитель видит обычное подтверждение.

Состав полей, порядок, валидация контакта и согласия, тексты галочек и
версия текста согласия не затронуты: журнал согласий по 152-ФЗ не
разрывается.

### Дополнительно: секреты в журнале доставок

Текст ошибки доставки пишется в таблицу `Delivery` и оттуда попадает в
админ-панель. Сообщение сетевого отказа умеет включать адрес запроса, а в
адресе Telegram лежит токен бота; сообщение SMTP умеет включать строку
авторизации. Перед записью секреты вырезаются: подстановкой значений из
окружения и образцом `bot<цифры>:<ключ>` на случай, когда токен в окружении
уже сменили.

### Что проверено и оказалось в порядке

| Область | Результат |
|---|---|
| Секреты в репозитории | `.env` не отслеживается, в истории ключей нет, образцы без значений |
| Переменные окружения | все рабочие переменные есть в `.env.example` |
| Заголовки безопасности | CSP, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` отдаются; HSTS добавляет nginx по `docs/DEPLOY.md` |
| Обработчик заявки | 422 без согласия и при кривом контакте, 400 на мусор вместо JSON, 503 при отказе базы, ограничение частоты 15 за 5 минут, адрес берётся из `X-Real-IP` |
| Подмена почтовых заголовков | переводы строк вырезаются из темы и адреса ответа |
| Миграции | расхождения со схемой нет |
| Проверка живости | `/api/health` отвечает и проверяет базу |
| Боевое окружение | `robots.txt`, `sitemap.xml` и canonical указывают на рабочий домен, `index, follow` |
| Вёрстка | axe-core 0, контраст 0 пар ниже порога, 4xx и 5xx нет, битых снимков нет, 10 адресов и 100 якорей отвечают, 43 остановки табом с видимой рамкой, LCP 80 мс, CLS 0 |

### Поправка к прежней записи

В Р-86 и в `docs/PLAN.md` отдельная отметка согласия на рассылку значилась
как задача, затрагивающая бэкенд. Это неточно: колонка `marketingOptIn` в
таблице `Lead` и поле `marketing` в схеме разбора уже есть, клиент их уже
отправляет. Чтобы закрыть расхождение с разделом 9 согласия, нужны только
видимая отметка в формах и новая версия текста согласия — миграция базы не
требуется.

### Рекомендации без правок

Скрипт `npm run lint` неработоспособен: `next lint` объявлен устаревшим и
без конфигурации ESLint уходит в интерактивный вопрос. В CI он не вызывается,
на сборку не влияет. Настройка ESLint означает новые зависимости и делается
отдельным решением.

Пакет `prisma` (командная строка) лежит в `dependencies`, а не в
`devDependencies` — из-за этого в производственное дерево попадает `mysql2`.
Перенос уберёт оставшиеся предупреждения аудита, но затронет
`postinstall: prisma generate` на сервере, поэтому делается вместе с
проверкой развёртывания, а не перед самым выпуском.

Canonical посадочной отдаётся без завершающей косой черты, а в карте сайта
она с косой чертой. Поисковые системы приводят такие адреса к одному виду.

## Р-89 · Подготовка репозитория к публикации и опись дизайн-системы (принято 12.09.2026)

**Решение: репозиторий приведён к состоянию, пригодному для открытой
публикации; действующая дизайн-система описана в `docs/DESIGN-SYSTEM.md`.**

Работа шла четырьмя параллельными направлениями: код, документация, опись
оформления, готовность к публикации. Ниже — что найдено и что сделано.

### Убрано как стороннее и устаревшее

| Что | Объём | Почему |
|---|---|---|
| `.agents/skills/**` и `skills-lock.json` | 58 файлов, 472 КБ | Пакет методичек из стороннего репозитория `jakubkrehel/skills`, попавший в проект отдельным коммитом. Ни одного файла лицензии внутри; перераспространение чужих текстов в публичном репозитории без указания их условий недопустимо. Инструментарий агента, а не код проекта — восстанавливается установкой из источника |
| `design/baseline/*.baseline.dc.html` | 8 файлов, 632 КБ | Каталог описан в `HANDOFF.md` и `README.md` как побайтно равный рабочим макетам. Проверка показала расхождение во всех восьми файлах: правки Р-51…Р-88 в снимки не переносились. Роль точки отката полностью выполняют замороженные ветки `accepted/*`, а расходящаяся копия вводит в заблуждение |
| `tools/audit.mjs`, `tools/shot.mjs`, `tools/slice.py` | 3 файла | Первые два содержат абсолютные пути из чужой рабочей сессии (`/tmp/claude-0/-home-user-deeptech-lab/…`) и вне той машины неработоспособны; третий требует PIL и нигде не вызывается. В публичном репозитории читаются как утечка рабочего окружения |
| `app/undefined/cat_03.png` | 1 файл | Каталог с именем `undefined` — результат подстановки неопределённой переменной в путь записи |
| Скрипт `npm run lint` | — | `next lint` объявлен устаревшим, конфигурации ESLint в проекте нет: команда уходит в интерактивный вопрос и не завершается. Неработающая команда в публичном репозитории хуже её отсутствия |

Наборы значков и изображений **не трогались**: проверка на живых страницах
показала, что из 42 значков отрисовываются 38, а оставшиеся четыре
(`arrow-down`, `file-text`, `scale`, `star`) упомянуты в макетах либо в
справочнике дизайн-системы. Иллюстрации `art/*`, портреты `photos/*` и фигуры
`crew/*` подставляются динамически и используются полностью.

### Закрыт путь утечки персональных данных

`.gitignore` не закрывал `deploy/backups/`, при том что `deploy/backup.sh`
кладёт туда дампы таблицы заявок, а `docker-compose.yml` монтирует каталог в
контейнер. Один `git add -A`, выполненный на боевом сервере, опубликовал бы
базу заявок с персональными данными. Добавлено правило `deploy/backups/*` с
исключением для `.gitkeep`. Заодно закрыты `undefined/`, настройки редакторов,
отчёты инструментов и временные файлы.

История проверена: файлов окружения с настоящими значениями в неё никогда не
добавляли, шаблонов токенов и паролей нет.

### Исправлены дефекты кода

**Карта сайта отдавала относительные адреса.** `sitemap.ts` подставлял пустую
строку вместо незаданного `NEXT_PUBLIC_SITE_URL` и выдавал `<loc>/main</loc>`.
Относительный адрес делает `sitemap.xml` недействительным целиком — поисковик
не берёт из него ни одной страницы. Разрешение адреса вынесено в
`app/src/lib/site-url.ts`: одно правило для разметки, robots и карты, адрес
приводится к виду без завершающей косой черты, при незаданной или испорченной
переменной возвращается `null`. Карта в этом случае пуста, но действительна —
проверено сборкой с пустой переменной.

**Текст ограничения частоты жил в двух редакциях** — в маршруте и в клиенте.
Вынесен в `RATE_LIMIT_MESSAGE` рядом со схемой, общей для обоих концов.

Прочее: снят `any` в разборе ответа сервера, убраны неиспользуемые
`SubmitState`, `emptyState` и объявление модуля `*.scss`, сняты экспорты типов,
которые никто не импортирует, из конвертера убран вырожденный тернарник и
повторная инициализация состояния. Перенос макетов после правки конвертера
даёт побайтно тот же результат.

### Конвейер и развёртывание

- `ci.yml`: добавлены `permissions: contents: read` и `concurrency` с отменой
  предыдущего прогона; добавлен шаг сверки `design/assets` с
  `app/public/assets` — каталоги синхронизируются вручную, и расхождение
  раньше обнаруживалось только пустым квадратом на странице.
- `.github/dependabot.yml`: еженедельные обновления npm и ежемесячные —
  действий CI. Причина прямая: проверка Р-88 нашла критическую уязвимость Next
  и четыре высоких в nodemailer, накопившиеся за три недели.
- `deploy/nginx.conf`: `listen 443 ssl http2` заменено на `listen 443 ssl` плюс
  директиву `http2 on` — начиная с nginx 1.25 прежняя форма выдаёт
  предупреждение.

Разбор показал, что `deploy/.env.deploy.example` не содержит `DATABASE_URL`
намеренно: строка подключения собирается в `docker-compose.yml` из
`POSTGRES_*`. Правка не требуется.

### Файлы публичного репозитория

Добавлены `CONTRIBUTING.md`, `SECURITY.md`, шаблон запроса на слияние, два
шаблона задач с отсылкой к порядку сообщения об уязвимостях, `CODEOWNERS`.

`CONTRIBUTING.md` начинается с главного правила проекта: правится макет, а не
сгенерированный компонент. Без этого первый же внешний запрос на слияние
ломается о проверку воспроизводимости переноса, причём без объяснения причины.
Там же таблица соответствия макетов и компонентов с явно отмеченным
расхождением `MainPage.dc.html` → `PostgradPage.tsx` → маршрут `/main`.

### Дизайн-система

`docs/DESIGN-SYSTEM.md` — опись действующего оформления, собранная подсчётом
по семи макетам: палитра с ролями и частотой, шкала кеглей, скругления, тени,
движение, контрольные точки, полтора десятка повторяющихся компонентов,
значки и слоты снимков. Отдельным разделом — правила расширения системы на
личный кабинет и админ-панель.

Прежний справочник `design/DesignSystem.dc.html` разошёлся с практикой, и
расхождения зафиксированы как долг: заявлено семь кеглей против пятнадцати
фактических, три формулы тени против шести, тень второго уровня объявлена с
прозрачностью `.07` против применяемой `.06`, ритм секций объявлен как 96 px
против фактического `clamp(72px,7vw,112px)`, отчёт приёмки утверждает «цветов
вне токенов — 0» при семнадцати таких цветах. Канон объявлен по факту вёрстки:
страницы приняты заказчиком и работают, справочник — нет.

### Документация приведена в соответствие

Исправлены утверждения, разошедшиеся с репозиторием: «четыре формы» вместо
семи в README, «две формы из шести» вместо семи в плане, ссылка на
несуществующий отчёт аудита в инструкции по размещению, побайтное равенство
снимков `baseline/` в трёх местах `HANDOFF.md`, утверждение о непринятой
странице «Аспирантам» вопреки Р-85, адреса страниц в виде `main.html` вместо
маршрутов. Замеры в README заменены на числа Р-88. В README добавлены ссылка
на витрину, требования к окружению, таблица обязательных переменных, команды
проверки, аннотация на английском и колонка «компонент» в таблице адресов.

### Решения заказчика по трём открытым вопросам (12.09.2026)

**Лицензию пока не ставим.** Следствие принимается явно: **репозиторий не
переводится в публичный до появления файла лицензии.** Без него публикация не
даёт читателю никаких прав — код остаётся «все права защищены», — а само
отсутствие лицензии читается как незавершённость. Вся прочая подготовка
выполнена, и перевод в публичный останется одним действием, как только
лицензия будет выбрана. Тем же файлом закрывается уведомление об условиях ISC
для двадцати шести значков Lucide.

**Журнал решений закрывается, наружу идёт выжимка.** Создан
`docs/DECISIONS-SUMMARY.md` — свод восьмидесяти девяти решений по существу,
без внутренней кухни, приёмочной переписки и незакрытых правовых рисков в
открытом виде. Полный `docs/DECISIONS.md` остаётся в репозитории, пока тот
закрыт; при переводе в публичный он переносится в закрытый репозиторий, а
выжимка остаётся. Порядок перенесён в `docs/PLAN.md`.

**Руководство по хостингу перенесено со сверкой.** `docs/HOSTING.md` (611
строк, одиннадцать шагов от выбора площадки до выпуска сертификата и правовых
действий после покупки) взято из неслитой ветки
`claude/scientific-consulting-service-web-42nebo` и приведено в соответствие:
сняты конкретные рублёвые цены как быстро протухающие и читающиеся рекламой,
в проверку после запуска добавлен ответ 404 от `/_next/image`, к резервным
копиям добавлено предупреждение о правиле в `.gitignore`. Отдельной оговоркой
отмечено, что сведения о домене приведены на 31.08.2026 и записи DNS могли
измениться — перед шагом 6 состояние проверяется фактически. Разделение ролей
закреплено перекрёстными ссылками: `HOSTING.md` — инструкция для владельца,
`DEPLOY.md` — инженерная сторона, при расхождении в командах верен `DEPLOY.md`.

### Что осталось нерешённым

**Лицензия.** Файла лицензии нет. Без него публикация формально не даёт
никаких прав: код остаётся «все права защищены», и открытым репозиторий не
становится. Выбор за заказчиком и в вёрстке не решается.

**Значки Lucide.** Двадцать шесть значков общего назначения — перештрихованные
глифы Lucide под лицензией ISC, которая требует сохранять текст лицензии при
распространении. Текста в репозитории нет. Закрывается разделом в файле
лицензии одновременно с предыдущим пунктом.

**`docs/HOSTING.md` из ветки `claude/scientific-consulting-service-web-42nebo`.**
Ветка содержит четыре неслитых коммита от 31.08.2026, среди них пошаговое
руководство на 611 строк «Как купить хостинг и опубликовать сайт» — от выбора
площадки до выпуска сертификата и правовых действий после покупки. В `main`
оно не попало. `docs/DEPLOY.md` его не заменяет: это инструкция для инженера,
а не для владельца, покупающего хостинг. Перенос требует сверки с текущим
состоянием (Node 22, prisma 7, самодостаточная сборка) и делается отдельной
задачей.

**Публиковать ли журнал решений целиком.** `docs/DECISIONS.md` — 266 КБ
внутренней работы: разбор площадок, приёмки, критика подрядных решений,
незакрытые правовые риски заказчика в открытом виде. Ценность для
преемственности высокая, но это рабочая переписка. Решение за заказчиком.

### Приёмка

Результат принят заказчиком 12.09.2026 и закреплён веткой
`accepted/repo-2026-09-12`. Принятое состояние — коммит `33359ce`, слияние
запроса № 32: репозиторий очищен, дизайн-система описана, руководство по
хостингу перенесено, файлы публичного репозитория добавлены.

Приёмка не отменяет условия из раздела выше: **репозиторий остаётся закрытым
до появления файла лицензии.**


## Р-90 · Условия использования и уведомление о правах третьих лиц (12.09.2026)

**Решение: репозиторий получает проприетарные условия использования —
исходный код открыт для чтения, но не является свободным программным
обеспечением. Права третьих лиц вынесены в отдельный файл `NOTICE`.**

Прежнее решение «лицензию пока не ставить» (Р-89) снято: без файла лицензии
публикация не даёт читателю никаких прав и одновременно не защищает
правообладателя, потому что молчание читается по-разному в разных
юрисдикциях. Отсутствие лицензии — это не отказ от выбора, это выбор
наихудшего варианта по умолчанию.

### Почему не свободная лицензия

Разбирались три варианта: проприетарная пометка, MIT и Apache 2.0.

MIT и Apache разрешают взять содержимое целиком. Для обычного кода это
приемлемо, но здесь в репозитории лежит не только код: макеты несут
фирменный стиль, а `design/OfferPage.dc.html`, `PrivacyPage.dc.html` и
`ConsentPage.dc.html` — юридические документы конкретного юридического лица
с его реквизитами и обязательствами. Свободная лицензия на такой набор
означала бы разрешение копировать чужую оферту вместе с ОГРН и ИНН. Это
вредит и правообладателю, и тому, кто скопирует.

Выбрана проприетарная пометка с явно разрешённым чтением. Она сохраняет
смысл публикации — показать работу, дать основу для преемственности, вести
разработку кабинета на виду — и не создаёт разрешения, которого заказчик не
давал.

### Что разрешено и что нет

Разрешено без отдельного согласия: смотреть, читать, изучать, делать
локальную копию для ознакомления, цитировать со ссылкой, сообщать об ошибках
и уязвимостях. Не разрешено без письменного согласия: использовать в
собственных продуктах, распространять, создавать производные произведения,
использовать обозначение «ProDisser» и фирменный стиль для сторонних услуг.

Отдельным абзацем оговорено, что копирование правовых текстов создаёт риск
прежде всего для копирующего: документы содержат реквизиты и обязательства
ООО «РУСДРОН».

Текст двуязычный, русская версия имеет преимущественную силу. GitHub
автоматически такую лицензию не распознаёт и покажет её как «Other» — это
ожидаемо и не является ошибкой.

### Права третьих лиц

Файл `NOTICE` перечисляет всё, что принадлежит не проекту.

**Значки.** Двадцать шесть значков общего назначения основаны на наборе
Lucide с изменённой на 1.5 толщиной штриха. Лицензия Lucide составная:
девять из использованных значков — `arrow-down`, `arrow-left`,
`arrow-right`, `check`, `clock`, `external-link`, `frown`, `help-circle`,
`x-circle` — входят в перечень производных от проекта Feather и
распространяются под MIT (© 2013—настоящее время Cole Bemis); остальные
семнадцать — под ISC (© 2026 Lucide Icons and Contributors). Обе лицензии
требуют сохранять текст при распространении, поэтому оба текста приведены
дословно. Шестнадцать значков с префиксом `pd-` — собственная разработка и
под условия `NOTICE` не подпадают.

**Гарнитуры.** Literata, Inter и JetBrains Mono под SIL Open Font License
1.1. Файлы шрифтов в репозитории не хранятся: подключаются на сборке через
`next/font/google` и попадают только в собранный сайт. OFL встраивание в
веб-страницы допускает.

**Зависимости** распространяются на условиях своих правообладателей и в
репозитории не хранятся.

**Изображения** созданы генеративными моделями по заказу правообладателя;
люди на них синтезированы и не изображают конкретных лиц.

### Что это разблокирует

Условие «репозиторий остаётся закрытым до появления файла лицензии», принятое
в Р-89, выполнено. Перевод в публичный — действие в настройках GitHub,
доступное только владельцу; со стороны репозитория препятствий больше нет.

## Р-91 · Разбор очереди обновлений и находка стороннего развёртывания (12.09.2026)

**Решение: пять обновлений от Dependabot приняты после проверки; обнаружено
подключённое к репозиторию развёртывание Vercel, противоречащее Р-13.**

### Обновления

| Запрос | Что | Как проверено |
|---|---|---|
| № 37 | prisma 7.9.1 → 7.10.0, zod 4.4.3 → 4.5.4, `@types/node`, `@types/react-dom` | Прогнано локально: типы, рабочая сборка, состояние миграций, заявки со всех семи форм, axe-core, обход клавиатурой. Всё чисто |
| № 35 | `actions/checkout` 4 → 7 | Проверка в CI использует само действие: зелёный прогон и есть проверка |
| № 34 | `actions/setup-node` 4 → 7 | То же |
| № 33 | `actions/deploy-pages` 4 → 5 | CI это действие не задействует. Принято под наблюдением: прогон витрины после слияния проверяется отдельно |
| № 36 | `actions/upload-pages-artifact` 3 → 5 | То же |

Два последних приняты не вслепую: прогон витрины от 12.09.2026 предупреждал,
что `actions/deploy-pages@v4` работает на Node 20, который объявлен
устаревшим, и раннер принудительно запускает его на Node 24. Обновление
убирает предупреждение, а не создаёт риск.

Связка `mysql2` под `prisma` в версии 7.10.0 не изменилась — предупреждение
аудита, оставленное в Р-88 сознательно, остаётся в силе.

### Находка: развёртывание Vercel

При разборе запросов в проверках обнаружены `Vercel Preview Comments` и
успешное развёртывание на `vercel.com/deep-tech-lab/scientific-consulting-service-web`.
К репозиторию подключено приложение Vercel, собирающее предпросмотр на каждый
запрос на слияние.

Это прямо расходится с принятым порядком:

1. **Р-13 и ч. 5 ст. 18 152-ФЗ.** Площадка выбрана только российская. Vercel —
   инфраструктура за рубежом.
2. **`docs/HOSTING.md`, шаг 1.** Раздел «Чего не делать» называет Vercel в
   перечне недопустимых площадок поимённо.
3. **Практический риск.** Пока в настройках Vercel не задан `DATABASE_URL`,
   заявка с предпросмотра не сохранится и уйдёт в ошибку. Но стоит кому-то
   добавить туда строку подключения «чтобы проверить» — и персональные данные
   заявителей окажутся в базе за пределами России. Второй риск проще:
   предпросмотр открыт по ссылке и, если в нём не задан
   `NEXT_PUBLIC_DEMO_STAND=1`, страницы индексируются и перебивают будущий
   боевой адрес.

Отключение приложения выполняется в настройках GitHub и Vercel и доступно
только владельцу учётной записи. Со стороны репозитория сделать это нельзя.

**Выполнено 12.09.2026.** Заказчик отключил приложение Vercel в настройках
GitHub (Integrations → Applications). Проверка: следующий запрос на слияние
после отключения не получил ни проверки `Vercel Preview Comments`, ни
развёртывания предпросмотра — сборка предпросмотров прекращена.

Отключение со стороны GitHub закрывает источник новых развёртываний, но не
удаляет проект в Vercel и уже собранные копии сайта. Пока проект существует,
последнее удачное развёртывание продолжает отвечать по адресу вида
`*.vercel.app`, то есть копия страниц остаётся доступной с зарубежной
площадки. Поэтому в план внесён второй шаг — удалить сам проект в кабинете
Vercel; до этого пункт 4а не считается закрытым.

**Закрыто полностью 12.09.2026.** Заказчик удалил проект в кабинете Vercel.
Копии страниц на зарубежной площадке не осталось, источник новых
развёртываний закрыт ранее отключением приложения в GitHub. Пункты 4а и 4б
плана выполнены.

Отдельно фиксируется для будущих сессий: возврат к Vercel, Netlify, Cloudflare
и любой другой зарубежной площадке в этом проекте невозможен без пересмотра
Р-13 и раздела 9 политики обработки персональных данных. Требование ч. 5
ст. 18 152-ФЗ о размещении базы на территории России касается не только базы:
страницы, принимающие персональные данные, отдаются оттуда же, где эти данные
собираются. Предложение «поднять предпросмотр на Vercel» выглядит безобидным
и потому опасно — именно так оно и появилось в проекте в первый раз.

## Р-92 · Мажорные обновления: Next 16 и TypeScript 7 (12.09.2026)

**Решение: приняты `next` 15.5.25 → 16.3.4, `typescript` 6.0.3 → 7.0.2 и
`actions/configure-pages` 5 → 6. Порядок слияния имел значение: по
отдельности TypeScript 7 ломает сборку.**

### Найденная зависимость между обновлениями

Dependabot прислал два мажорных обновления отдельными запросами, и по
отдельности они несовместимы. Сборка на TypeScript 7 с Next 15.5.25 падает с
прямым сообщением от самого Next:

> TypeScript 7.0.2 is not supported by this version of Next.js. The TypeScript
> 7 native compiler does not provide the JavaScript compiler API that Next.js
> requires. Install TypeScript 6 or upgrade to a Next.js v16.2.11 or later.

Причина в том, что TypeScript 7 — это переписанный на другом языке
компилятор, у которого нет прежнего программного интерфейса; Next опирался
на него и научился обходиться без него только с версии 16.2.11.

Проверка в CI отработала верно: запрос с TypeScript 7 получил красную сборку
и в основную ветку сам по себе не попал бы. Поучительна другая деталь: у себя
на машине первая же проверка дала «сборка успешна» — потому что рабочая
сборка подхватила кэш `.next` от предыдущей версии. Настоящая ошибка
проявилась только на сборке витрины, где кэша не было. Вывод для дальнейших
проверок мажорных обновлений: перед проверкой каталог `.next` удаляется, иначе
результат недостоверен.

### Как проверялось

Каждое обновление прогонялось локально, а не принималось по зелёной галочке:

| Состав | Результат |
|---|---|
| TypeScript 7 на Next 15.5.25 | сборка витрины падает, см. выше |
| Next 16.3.4 на TypeScript 6 | всё чисто |
| Next 16.3.4 и TypeScript 7 вместе | всё чисто |

Полный набор на связке Next 16 и TypeScript 7: проверка типов, рабочая
сборка, сборка витрины, самодостаточный сервер поднимается, `/api/health`
отвечает `{"ok":true,"db":true}`, оптимизатор изображений по-прежнему
отдаёт 404, заявки со всех семи форм принимаются, axe-core 0 нарушений,
контраст 0 пар ниже порога, строк длиннее 85 знаков нет, обход клавиатурой
43 остановки с видимой рамкой, LCP 100 мс, CLS 0, перенос макетов
воспроизводится.

### Порядок слияния

Сначала Next 16, затем ветка запроса с TypeScript 7 обновлялась от новой
основной, дожидалась зелёного прогона и только потом сливалась. Обратный
порядок оставил бы основную ветку красной.

### Побочное подтверждение

Ни один из этих запросов не получил проверок Vercel — отключение приложения,
выполненное заказчиком, держится.

### Побочная правка: `app/tsconfig.json`

Next 16 при первой же сборке переписывает конфигурацию TypeScript под себя:
меняет `"jsx": "preserve"` на `"react-jsx"` и добавляет
`.next/dev/types/**/*.ts` в перечень включаемых файлов. Изменение
зафиксировано в репозитории в том виде, в каком его делает сам Next, включая
перенос значений массивов на отдельные строки. Иначе после каждой сборки
рабочее дерево оказывалось бы грязным, а конфигурация в репозитории —
не той, которую Next считает правильной.

### Рекомендация

Добавить `npm run build:preview` в проверку CI. Сейчас конвейер собирает
только рабочую сборку; в этом случае ошибку поймала и она, но сборка витрины
работает без кэша и ловит подобные расхождения надёжнее.

## Р-93 · Уменьшенное движение на правовых страницах и проверка витрины в CI (12.09.2026)

**Решение: правило `prefers-reduced-motion` на трёх правовых страницах
приведено к тому же виду, что на содержательных; сборка витрины добавлена в
конвейер проверок.**

### Уменьшенное движение

Опись дизайн-системы зафиксировала расхождение: на четырёх содержательных
страницах правило отключает и переходы, и анимации, а на `/offer`,
`/privacy` и `/consent` — только переходы:

```
@media (prefers-reduced-motion:reduce){*{transition:none!important}
```

При этом на всех трёх объявлена анимация `pd-appear` длительностью 220 мс.
Пользователь, включивший в системе уменьшение движения, получал её вопреки
своей настройке. Добавлено `animation:none!important`.

Проверено в браузере с `reducedMotion: reduce`: на всех семи страницах ноль
элементов с активной анимацией и ноль с ненулевой длительностью перехода.
Правило касается только оформления и юридического текста не затрагивает.

### Сборка витрины в CI

Конвейер собирал только рабочую сборку. Решение Р-92 показало, чем это
плохо: рабочая сборка подхватывает кэш `.next`, а сборка витрины идёт
статическим выводом и без кэша — именно она поймала несовместимость
TypeScript 7 с Next 15, когда рабочая отрапортовала успех. Шаг добавлен
после сборки, до проверки воспроизводимости переноса.

## Р-94 · Перенос упущенного: временный узел nginx и срок хранения заявок (12.09.2026)

**Решение: в основную ветку перенесены `deploy/nginx-bootstrap.conf` и
`deploy/retention.sh` — файлы, на которые ссылается перенесённое ранее
руководство `docs/HOSTING.md`, но которых в репозитории не было.**

### Как обнаружилось

Разбор трёх оставшихся неслитых коммитов ветки
`claude/scientific-consulting-service-web-42nebo` показал, что перенос
руководства решением Р-89 был неполным. Я взял `docs/HOSTING.md`, но не взял
два файла, на которые оно ссылается в четырёх местах. Владелец, следующий
инструкции, упирался бы в отсутствующий файл на шаге 8.2 — том самом, где
выпускается сертификат и назад дороги нет.

Проверка теперь механическая: все упоминания `deploy/*` в руководстве сверены
с содержимым каталога, расхождений не осталось.

### `deploy/nginx-bootstrap.conf`

Разрывает замкнутый круг первого запуска. В рабочем `nginx.conf` прописаны
пути к сертификату, которого до первого выпуска не существует, и nginx с
таким файлом не стартует. А certbot, чтобы выдать сертификат, должен
получить ответ по адресу `http://prodisser.ru/.well-known/acme-challenge/…`,
то есть nginx уже должен работать. Временный узел поднимает только порт 80 и
отдаёт каталог проверки, без единого упоминания SSL. По корню отвечает 404, а
не показывает сайт: принимать заявку с персональными данными по незащищённому
соединению нельзя. После выпуска сертификата заменяется на рабочий файл.

### `deploy/retention.sh`

Существеннее. Политика обработки персональных данных, п. 7.1, опубликована и
обещает: данные заявителей хранятся не более одного года с даты обращения,
если договор не заключён. До сих пор это обещание нигде не исполнялось —
записи лежали бы в базе бессрочно. Обещание в опубликованном тексте без
исполнения в коде — это не техническая недоработка, а расхождение с
собственной политикой, которое видно проверяющему в первой же выгрузке из
базы.

Скрипт ставится в расписание на понедельник и удаляет заявки старше
двенадцати месяцев. Записи со статусом `CONTRACTED` не трогает: по ним
договор заключён и действует п. 7.2 — срок договора и три года после;
их удаление требует оглядки на бухгалтерские сроки и автоматом не делается.
Журнал доставок уходит вместе с заявкой по связи `ON DELETE CASCADE`.

Порядок намеренный: сначала посчитать, потом удалить, число записать в
журнал. Молчаливое удаление персональных данных нельзя проверить ни через
день, ни через год. Если база не ответила числом, скрипт завершается с
ошибкой и ничего не удаляет.

Оговорка о точности формулировки: политика говорит «с даты последнего
обращения», скрипт считает от `createdAt` записи. Для повторного обращения
создаётся новая запись со своей датой, поэтому каждая удаляется через год
после собственной подачи — это соответствует смыслу пункта.

### Что осталось в старой ветке

Из четырёх неслитых коммитов содержательного больше ничего нет: остальные
правки — прежние редакции `README.md`, `docs/DEPLOY.md` и правовых страниц,
переписанные в основной ветке позднее и подробнее. Ветку можно удалять; до
явного распоряжения оставлена на месте.

## Р-95 · Подсветка блоков акцентом и перенос блока команды на «Компаниям» (принято 12.09.2026)

**Решение: рамка при наведении на любой блок переведена на основной акцент
`#14417A`; блок команды на странице «Компаниям» перенесён на второе место,
сразу после первого экрана.**

### Подсветка по периметру

Правило наведения было несогласованным: 21 блок подсвечивался бледной
рамкой `#B4C9E5`, 20 — акцентной `#14417A`, ещё несколько — `#D8E4F3`. Один
и тот же жест давал разный отклик на соседних карточках.

Приведено к одному: **наведение на блок красит рамку в `#14417A`** — тот же
акцент, что у ссылки, значка и кольца фокуса. Переведено 35 правил:

| Что | Было | Стало |
|---|---|---|
| Карточка с тенью подъёма | `#B4C9E5` | `#14417A` |
| Карточка без тени | `#D8E4F3` | `#14417A` |
| Карточка прав на «Компаниям» | `#B4C9E5` | `#14417A` |
| Плашки первого экрана со ссылкой на раздел | `#B4C9E5` | `#14417A` |

Толщина рамки не менялась — 1 px: увеличение сдвинуло бы содержимое блока на
пиксель при каждом наведении. Тень подъёма и фон при наведении оставлены как
были: подсветка добавляет к ним обводку, а не заменяет их.

Не тронуты два правила, где рамка меняется вместе с заливкой и обводка
акцентом читалась бы как ошибка: кнопка раскрытия оглавления на правовых
страницах (`#D8E4F3`) и синяя кнопка действия, у которой при наведении и
рамка, и фон становятся `#0A2145`.

Проверено вживую: на четырёх содержательных страницах 19 типов блоков с
правилом наведения, все дают `rgb(20, 65, 122)`.

### Порядок разделов на «Компаниям»

Блок команды стоял восьмым — после шкалы УГТ, пакетов, каталога и
механизмов. Для корпоративного заказчика вопрос «кто это делает» идёт сразу
после «что вы делаете»: пакеты и каталог убеждают только того, кто уже
поверил в исполнителя. Блок перенесён на второе место, сразу после первого
экрана и перед бегущей строкой.

Новый порядок: первый экран → команда → бегущая строка → шкала УГТ →
пакеты → каталог → механизмы → права → вопросы → заявка.

Перенос сделан целиком, вместе с орбитой портретов и фигурами ролей; разметка
не переписывалась, изменилось только положение раздела в потоке. Отбивка
`clamp(72px,7vw,112px)` у раздела своя, поэтому ритм между секциями
сохранился.

## Р-96 · Формы заявок: своя для каждого направления (принято 12.09.2026)

**Решение: состав форм пересобран под направления. Короткая форма везде
одна и та же — имя и контакт. Полная спрашивает сверх этого только то, что
меняет ответ именно для этой аудитории.**

### Что было не так

Формы складывались по одной вместе со страницами, и общей логики у них не
образовалось:

| Форма | Состав до правки |
|---|---|
| Посадочная, обе | контакт |
| Аспирантам | имя, контакт |
| Студентам, короткая | контакт |
| Студентам, полная | тема, специальность, срок, имя, контакт |
| Компаниям, короткая | имя, контакт, что нужно |
| Компаниям, полная | организация, имя, контакт, что нужно, срок |

Три несогласованности. Первая: на посадочной и «Студентам» коротка форма
просила только контакт — оператор перезванивал, не зная, как обратиться.
Вторая: у «Аспирантам» не было ни одного поля, специфичного для направления,
хотя именно там разговор определяют шифр специальности и дата защиты. Третья:
на «Студентам» форма начиналась с темы работы — человека спрашивали о работе
раньше, чем о нём самом.

### Правило

**Короткая форма** стоит в первом экране или врезке, её задача — получить
возможность связаться, а не квалифицировать заявку. Везде одинаково: имя и
контакт, оба обязательные.

**Полная форма** стоит в разделе «Заявка» и добавляет к ним поля, без которых
нельзя дать содержательный ответ именно этой аудитории. Порядок всегда один:
сначала о человеке, потом о задаче.

| Направление | Полная форма | Почему эти поля |
|---|---|---|
| Посадочная | имя, контакт | аудитория ещё не выбрана, спрашивать нечего |
| Аспирантам | имя, контакт, специальность или шифр, дата защиты или срок | по шифру подбирается куратор со степенью в той же области; дата защиты задаёт весь календарный план |
| Студентам | имя, контакт, тема работы, специальность, срок сдачи | тема и специальность определяют исполнителя, срок — возможность взяться |
| Компаниям | организация, имя, контакт, что нужно, срок или дедлайн конкурса | договор заключается с организацией, поэтому она идёт первой; тип работы задаёт пакет |

### Что изменилось в разметке

- Посадочная, обе формы: добавлено имя.
- Аспирантам: добавлены специальность и дата защиты, оба необязательные.
- Студентам, короткая: добавлено имя.
- Студентам, полная: блок «имя + способ связи + контакт» поднят наверх, перед
  темой работы; поле срока переименовано с `term` на `deadline`.
- Компаниям: без изменений. Организация первой — нормальный порядок для
  корпоративной формы, а состав уже соответствует правилу.

Обязательными остаются только имя, контакт и согласие. Поля направления
необязательные: человек, который не хочет их заполнять, не должен упереться
в форму.

### Почему это не рвёт журнал согласий

Правило «состав полей форм заморожен» действует с оговоркой, записанной в
`docs/PLAN.md`: правки формы делаются один раз и **до открытия**, потому что
менять состав после появления настоящих заявок значит разрывать журнал
согласий. Сайт не открыт, настоящих заявок нет — это тот самый момент.

Существеннее другое: **ни новых колонок, ни новой версии текста согласия не
потребовалось.** Раздел 4 согласия перечисляет категории собираемых данных —
имя, контакт, организация, тема работы, специальность, тип работы, срок сдачи
или дата защиты, описание разработки, срок подачи на конкурс, содержание
сообщения. Все поля новых форм входят в этот перечень, а колонки под них уже
есть в таблице `Lead`. Перестановка и добавление полей внутри объявленного
перечня категорий не меняет ни того, на что даётся согласие, ни его версии
`2026-08-21`.

Граница, которую нельзя перейти без юриста: в перечне упомянута должность
субъекта, но колонки под неё в базе нет. Поле должности потребовало бы
миграции — и потому не добавлялось.

### Проверено

Все семь форм отправляются и доходят до базы с верным составом: заявка с
«Аспирантам» несёт специальность и дату защиты, с «Студентам» — тему,
специальность и срок, с «Компаниям» — организацию, тип работы и срок.
Обязательность полей соблюдается, подтверждение объявляется, поля очищаются.
axe-core 0 нарушений, горизонтальной прокрутки нет ни на одной из шести
проверенных ширин, обход клавиатурой посадочной — 45 остановок с видимой
рамкой (было 43: прибавились два поля имени).

### Приёмка Р-95 и Р-96

Подсветка блоков акцентом, перенос блока команды на «Компаниям» и новый
состав форм приняты заказчиком 12.09.2026. Принятое состояние закреплено
веткой `accepted/forms-2026-09-12`.


---

## Р-97 · Палитра вынесена в семантические токены (12.09.2026)

**Решение: цвета объявляются один раз в блоке `:root` каждого макета и
подставляются через `var(--pd-…)`; литералом остаётся только то, где `var()`
физически не работает или где значение ещё не имеет роли в системе.**

### Почему это делалось до кабинета, а не после

Опись дизайн-системы (Р-89) первой строкой долга зафиксировала: палитра
задана литералами. Семь макетов держали около 1700 упоминаний цвета, и смена
одного оттенка означала правку в сотнях мест с риском пропустить часть.
Личный кабинет и админ-панель (`docs/briefs/`) наследуют разделы 1—8
справочника целиком — то есть унаследовали бы ту же проблему в двойном
объёме, причём в интерфейсе, где к палитре добавятся статусы заявки и
платежа. Дешевле вынести цвета один раз сейчас, чем разбирать это в двух
приложениях позже.

### Что сделано

В начало первого блока `<style>` каждого из семи макетов добавлен блок
`:root` с 22 токенами. Он попадает в компонент дословно как строка `css`,
поэтому объявления действуют глобально на странице. Роли токенов
перечислены в `docs/DESIGN-SYSTEM.md`, раздел 2; имена токенов добавлены в
таблицы 2.1 и 2.2 рядом со значениями.

| Макет | Замен |
|---|---|
| `BusinessPage.dc.html` | 434 |
| `StudentsPage.dc.html` | 431 |
| `MainPage.dc.html` | 366 |
| `StartPage.dc.html` | 310 |
| `ConsentPage.dc.html` | 53 |
| `OfferPage.dc.html` | 39 |
| `PrivacyPage.dc.html` | 39 |
| **Итого** | **1672** |

Правовые страницы затронуты только в этой части: юридический текст,
нумерация разделов, пометка «Проект документа» и даты редакций не изменены
ни на символ. Ограничение по ним — палитра и шрифты — соблюдено буквально:
менялись именно объявления цвета.

### Что осознанно осталось литералами

**66 презентационных атрибутов SVG** — `fill`, `stroke`, `stop-color`,
записанные атрибутом, а не свойством CSS. `var()` в презентационном атрибуте
не разрешается: атрибут не является объявлением CSS, подстановка переменной
там не производится, значение просто теряется и элемент отрисовывается
чёрным. При автоматической замене эти места защищались временными метками,
чтобы не попасть под общий проход. Распределение: «Студентам» 27, «Аспирантам»
17, «Компаниям» 13, посадочная 9; на правовых страницах таких мест нет.

**48 вхождений 13 различных значений вне палитры** — близкие серые и
голубые фоны одной роли, зелёная рамка блока успеха, два случайных оттенка.
Они не получили токенов сознательно: токен обязан обозначать роль, а не
просто прятать значение. Заводить `--pd-gray-3`, `--pd-gray-4`, `--pd-gray-5`
под три оттенка, которые должны быть одним, значит закрепить долг вместо
того, чтобы его закрыть. Оставленные литералы прямо видны в разметке и
перечислены в разделе 9 справочника отдельной строкой; они снимутся вместе
со сведением шкал.

### Как доказана визуальная идентичность

Правка такого объёма опасна не тем, что сломается сборка, а тем, что один
оттенок тихо изменится и это заметят через месяц. Поэтому проверка велась не
глазами, а сличением вычисленных стилей.

До замен снят эталон: на семи страницах для каждого элемента дерева
считывались `color`, `backgroundColor`, четыре `border*Color`, `outlineColor`,
`boxShadow`, `fill`, `stroke` — **3926 элементов**. После замен и повторного
переноса макетов снимок снят заново тем же скриптом и сличён поэлементно:
**0 расхождений из 3926**. Браузер отдаёт вычисленное значение, то есть
`var(--pd-accent)` в снимке выглядит как `rgb(20, 65, 122)` — подмена
переменной на неверное значение дала бы расхождение немедленно.

Отдельно перепроверено наведение: скрипт, читающий правила через
`getPropertyValue`, после токенизации стал возвращать `var(--pd-accent)`
вместо цвета и перестал находить совпадения — это дефект проверки, а не
разметки. Скрипт исправлен, подсветка блоков акцентом (Р-95) подтверждена
вживую на всех четырёх страницах.

### Проверено

`typecheck` и обе сборки зелёные; перенос макетов воспроизводится; axe-core 0
нарушений на семи страницах; ответов 4xx и 5xx нет, битых снимков нет; 45
остановок при обходе клавиатурой с видимой рамкой; LCP 100 мс, CLS 0; контраст
— ни одной пары ниже порога; заявки со всех семи форм принимаются; витрина
собирается, объявления токенов присутствуют в выводе.

### Что это даёт дальше

Смена любого оттенка айдентики — правка одной строки в семи макетах вместо
сотен мест. Тёмная тема, если она понадобится кабинету, становится
переопределением 22 значений, а не переверсткой. Правило раздела 10
справочника — «новый цвет заводится только под новую роль» — теперь
проверяемо механически: цвет вне `:root` виден поиском.

---

## Р-98 · Сведение близких оттенков и единый блок подтверждения (12.09.2026)

**Решение: 48 вхождений тринадцати значений, оставшихся литералами после
Р-97, разобраны по ролям. Девять значений сведены к действующим токенам,
четыре получили собственные токены. Цветов вне блока `:root` в разметке
больше нет.**

### Почему это не механическая замена

Р-97 намеренно не трогал эти значения: токен обязан обозначать роль, а не
прятать значение, и заводить `--pd-gray-3`, `--pd-gray-4`, `--pd-gray-5` под
три оттенка, которые должны быть одним, значит закрепить долг. Разбор
потребовал по каждому значению ответить, что именно оно обозначает.
Ответы разделились на две группы.

### Сведено к действующим токенам

| Литерал | Вхождений | Токен | Почему именно этот |
|---|---:|---|---|
| `#F1F3F6` | 4 | `--pd-surface-quiet` | Фон переключателя и неактивной кнопки — та же роль «тихая подложка» |
| `#FAFBFC` | 6 | `--pd-surface-quiet` | Фон тихой карточки и зебра таблицы |
| `#DDE2E9` | 6 | `--pd-border` | Рамка поля ввода, неактивная точка шкалы |
| `#D0DDEE` | 9 | `--pd-accent-mark` | Рамка и фон акцентной карточки на фоне `accent-tint` |
| `#CFDDEE` | 5 | `--pd-accent-edge` | Вторичный текст на плотном синем — роль, уже закреплённая за `accent-edge` |
| `#EDF0F4` | 1 | `--pd-accent-mark` | Подложка третьей карточки маршрута: две соседние уже голубые |
| `#EDF2F8` | 1 | `--pd-accent-tint` | Третий шаг шкалы граней рядом с `accent-mark` и `accent-edge` |
| `#B4564B` | 1 | `--pd-err-ink` | Пиктограмма внутри плашки проблемы с рамкой `err-border` |
| `#5FE6B6` | 3 | `--pd-ok-border` | См. ниже |

Сдвиг оттенка нигде не превышает 11 единиц на канал. Единственное изменение
с последствиями для доступности — текст `#CFDDEE` → `#B4C9E5` на плотном
синем: контраст падает с 7,3:1 до 6,0:1, то есть остаётся выше порога AA
с запасом. Проверено сплошным обходом, а не расчётом на бумаге: пар ниже
порога на семи страницах нет.

### Получили собственные токены

| Токен | Значение | Роль |
|---|---|---|
| `--pd-art-line` | `#E5EBF2` | Средние окружности фонового рисунка, вертикальные направляющие макета, подложка круглого снимка |
| `--pd-art-mark` | `#D3DEEC` | Знаки рисунка и пунктирное кольцо |
| `--pd-art-dot` | `#D8DEE6` | Точечный узор фона |
| `--pd-accent-soft` | `#2A63B4` | Круглая подложка снимка и диаграммы; никогда не текст |

Первые три не сведены к шкале акцента сознательно. Те же значения стоят
литералами в презентационных атрибутах SVG того же рисунка — 5 и 18 мест
соответственно, — где `var()` не работает. Сведи их к `accent-tint` и
`accent-mark`, и пунктирное кольцо разошлось бы по цвету с окружностями,
которые оно продолжает: рисунок распался бы на части. Отдельный токен
фиксирует, что значение обязано совпадать с атрибутом SVG, и делает эту
связь видимой тому, кто будет менять палитру.

### Блок подтверждения отправки

Раздел 2.3 справочника требовал не смешивать семантику с акцентом и тут же
объявлял каноном блока успеха голубую рамку `#D8E4F3`. Противоречие
разрешено в пользу правила: **успех получает собственную рамку в зелёной
шкале.**

Значение выведено, а не подобрано. Рамка ошибки `#F0C9C3` отличается от
своего фона `#FAE7E5` понижением светлоты на 8,6 % и насыщенности на 7,7 %.
То же преобразование, применённое к фону успеха `#E6F9F1`, даёт `#C5EEDD`.
Блок успеха и блок ошибки теперь различаются только тоном, а не построением.

Заодно устранён разнобой: из семи блоков подтверждения три несли рамку
`#5FE6B6`, четыре — голубую `#D8E4F3`, один — белый фон вместо зелёного.
Теперь все семь одинаковы.

Проверка блока подтверждения возможна только отправкой формы: до отправки он
скрыт, и сличение вычисленных стилей его не видит. Отправлены все семь форм
на четырёх страницах — семь блоков, у всех фон `rgb(230,249,241)` и рамка
`rgb(197,238,221)`.

### Что осталось литералами

66 презентационных атрибутов SVG — по причине, разобранной в Р-97, — и 22
вхождения `#000` в масках (`mask-image`, `-webkit-mask`), где чёрный
обозначает не цвет, а полную непрозрачность маски. Заменять его токеном
значит выдать за цвет то, что цветом не является.

### Проверено

Сличение вычисленных стилей до и после: **57 элементов из 3926, 109
изменённых свойств — все до одного из таблиц выше**, ни одного постороннего.
Контраст: пар ниже порога на семи страницах нет. Отправка: семь форм из семи
принимаются, блоки подтверждения единообразны. axe-core 0 нарушений;
ответов 4xx и 5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на
одной из проверенных ширин; 45 остановок при обходе клавиатурой с видимой
рамкой; подсветка блоков акцентом 4/4, 5/5, 5/5, 5/5; LCP 80 мс, CLS 0;
`typecheck` и сборка зелёные; перенос макетов воспроизводится.

Блок `:root` побайтно одинаков во всех семи макетах и содержит 27 токенов.

---

## Р-99 · Типографический ритм, скругления и шаг сетки (12.09.2026)

**Решение: интерлиньяж задаётся только коэффициентом, набор скруглений
закрыт пятью значениями, отступы кратны шагу 2 px, заголовок раздела имеет
один кегль на всех семи страницах.**

Четыре строки долга дизайн-системы закрываются одним заходом: они правятся в
тех же объявлениях и разделять их значит трижды пересобирать и трижды
проверять одно и то же.

### Интерлиньяж: 119 пиксельных значений сведены к четырём коэффициентам

Пиксельный интерлиньяж не масштабируется вместе с кеглем. Пока кегль не
трогают, разница незаметна; при первой же правке размера строка либо
слипается, либо расходится, и дефект всплывает не там, где его внесли.

Каждое из 119 объявлений стояло в паре со своим кеглем, то есть было
настоящим интерлиньяжем текста, а не высотой бокса — это проверено сплошным
сопоставлением, а не выборкой. Пары переведены в ближайший коэффициент из
набора 1.24 / 1.4 / 1.5 / 1.6: `12/16` → 1.4, `13/19` → 1.5, `14/21` → 1.5,
`15/21` → 1.4, `16/26` → 1.6, `17/24` → 1.4, `26/30` → 1.24 и ещё восемь пар.
Максимальное расхождение с прежней высотой строки — 2,2 px у надписи в
центре диаграммы, у остальных не более 0,8 px.

Дополнительно сведены шесть единичных коэффициентов: `1.35` → 1.4 (3),
`1.55` → 1.5 (2), `1` → 1.24 (1). В макетах остались ровно четыре значения:
1.4 — 297, 1.5 — 200, 1.6 — 53, 1.24 — 37.

### Кегли: заголовок раздела — 22 px везде

Одна роль имела три кегля: 19 px на трёх документных страницах и на первом
экране посадочной, 20 px в разделах посадочной, 22 px на остальных четырёх
страницах. Все шесть вхождений 19 px и три `<h2>` с 20 px переведены на
22 px; трекинг заодно приведён к каноническому `-.015em`.

Правовые страницы затронуты только кеглем и трекингом заголовков разделов.
Проверено механически: разметка этих трёх файлов с вырезанными атрибутами
`style` не изменилась ни на символ — ни текст, ни нумерация разделов, ни
пометка «Проект документа», ни даты редакций.

Остальные кегли вне прежней таблицы оставлены и внесены в шкалу: у каждого
своя роль — 20 px заголовок карточки маршрута, 21 / 23 / 27 px вордмарк в
трёх положениях, 26 px надпись в центре круговой диаграммы. Кегль, у
которого есть роль, — часть шкалы, а не отклонение от неё.

### Скругления: набор закрыт

`12px` (7) и `8px` (3) сведены к `10px`, `2px` (6) и `1px` (6) — к `999px`.
Последнее визуально тождественно: полоска высотой 2 px со скруглением 1 px и
та же полоска со скруглением 999 px дают один и тот же полукруглый конец.
Остались 999px (115), 14px (90), 10px (87), 50 % (54), 6px (7).

### Шаг сетки

42 вхождения нечётных величин приведены к шагу 2 px: `padding:7px 14px` (21)
→ `8px 14px`, `gap:9px` (4) → `10px`, `top`/`left:7px` (6) → `8px`,
`padding:5px 11px` (2) → `6px 12px`, `3px` (5) → `4px`, `padding:9px 0` →
`10px 0`, а также две подгоночные величины `123px` → `124px` и `67px` →
`68px`. Значений вне шага в макетах не осталось.

### Что это стоило по высоте

Правка ритма неизбежно меняет геометрию, поэтому измерено, а не оценено на
глаз. Прирост высоты документа: посадочная +9 px (0,3 %), «Аспирантам»
+17 px (0,2 %), «Студентам» 0, «Компаниям» +10 px (0,1 %), документные
страницы +68…+77 px (0,8—1,8 %) — там причина в укрупнении заголовков
разделов с 19 до 22 px. Ни одного схлопнувшегося или разъехавшегося блока:
горизонтальной прокрутки нет ни на одной из семи проверенных ширин от 360
до 1440 px.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
ответов 4xx и 5xx нет, битых снимков нет; 45 остановок при обходе
клавиатурой с видимой рамкой; подсветка блоков акцентом 4/4, 5/5, 5/5, 5/5;
семь форм из семи принимаются, блоки подтверждения единообразны; LCP 76 мс,
CLS 0; `typecheck` и обе сборки зелёные; перенос макетов воспроизводится.

Отдельно о метрике «поле ввода не меньше 16 px»: проверка находит четыре
поля с кеглем 13,3 px на четырёх страницах. Это ловушка для роботов
`company_website` — `tabindex="-1"`, `aria-hidden="true"`,
`autocomplete="off"`, размер 1×1 px за границей экрана. Человек в неё не
вводит, правило Р-86 на неё не распространяется.

---

## Р-100 · Проверка перед развёртыванием (12.09.2026)

**Решение: закрыты обе уязвимости зависимостей, зависимости подняты в
пределах объявленных диапазонов, на витрине появился `robots.txt`, карта
сайта перестала противоречить запрету на обход.**

### Уязвимости

`npm audit` показывал две — обе в `mysql2`, который тянет за собой `prisma`:
подмена плагина аутентификации на `mysql_clear_password` с утечкой пароля в
открытом виде (высокая) и неограниченная распаковка в сжатом протоколе,
дающая отказ в обслуживании (умеренная).

Проект работает на PostgreSQL, драйвер MySQL в рантайме не исполняется, то
есть эксплуатация невозможна. Тем не менее уязвимость закрыта, а не
объяснена: `npm audit fix --force` предлагал `prisma 8.0.0-rc.14` —
предрелиз, в производство такое не ставится. Вместо этого версия
транзитивного пакета поднята через `overrides`: `"mysql2": "^3.24.4"`.
Механизм в проекте уже применяется для четырёх пакетов, новых зависимостей
не появилось. Итог: `npm audit` — 0 уязвимостей.

### Обновления

`npm update` в пределах объявленных диапазонов: Next 16.3.4 → 16.3.5,
React и React DOM 19.2.8 → 19.3.0, Zod 4.5.4 → 4.6.2, типы. Мажорных
переходов нет — по Р-92 они делаются отдельным решением с полной проверкой.
`prisma 8.0.0-rc` не берётся: предрелиз.

### `robots.txt` на витрине отсутствовал

`docs/PLAN.md` утверждал, что стенд закрыт двумя рубежами: мета-тегом
`noindex, nofollow` и `Disallow: /`. Первый рубеж стоял, второго не было
вовсе — и не по недосмотру вёрстки, а по устройству сборки: витрина
собирается статическим экспортом, который не умеет серверные маршруты,
поэтому `build-preview.mjs` намеренно убирает `robots.ts` и `sitemap.ts` на
время сборки. Убирает — и ничего не кладёт взамен.

Робот, который читает только `robots.txt` и не разбирает разметку, видел
стенд открытым. Теперь скрипт витрины дописывает статический
`out/robots.txt` с `User-agent: *` и `Disallow: /`.

Замечу отдельно, чтобы не повторить: первая попытка исправления была
неверной — я снял `force-dynamic` с маршрутов, не заметив, что скрипт
витрины их вообще выносит из дерева. Правка откачена, дело исправлено там,
где оно ломалось.

### Карта сайта противоречила запрету

При `NEXT_PUBLIC_DEMO_STAND=1` `robots.txt` запрещал обход целиком, а
`sitemap.xml` тут же перечислял все семь адресов — то есть подсказывал
роботу ровно то, что запрещал. Теперь на стенде карта пуста и при этом
остаётся действительной.

### Проверено на боевом режиме, а не только на стенде

`NEXT_PUBLIC_*` подставляются на сборке, а не в рантайме: переключить режим
переменной окружения контейнера нельзя, только пересборкой образа. Это
учтено в развёртывании — `NEXT_PUBLIC_SITE_URL` передаётся в
`docker-compose.yml` как аргумент сборки и объявлен обязательным (`:?`),
`NEXT_PUBLIC_DEMO_STAND` в образе не задаётся, то есть боевая сборка идёт в
открытом режиме. Сверено также, что `.env` исключён и из образа
(`.dockerignore`), и из репозитория (`.gitignore`), а Node 22 согласован
между CI, `Dockerfile` и README.

### Проверено

`npm audit` 0 уязвимостей; `typecheck` и обе сборки зелёные; axe-core 0
нарушений на семи страницах; контраст — пар ниже порога нет; ответов 4xx и
5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на одной из
семи ширин; 45 остановок при обходе клавиатурой с видимой рамкой; семь форм
из семи принимаются; LCP 72 мс, CLS 0; перенос макетов воспроизводится;
наборы ассетов синхронны; на витрине `robots.txt` с `Disallow: /` и
`noindex, nofollow` на всех страницах.

---

## Р-101 · Видимые подписи полей, снятие макета-справочника (12.09.2026)

Три решения заказчика по остатку задач, принятые 12.09.2026.

### Видимые подписи полей

Рекомендация аудита исполнена. Уточнение к формулировке, с которой я
выходил к заказчику: подписи отсутствовали не во всех семи формах, а только
в четырёх компактных формах первого экрана. Полные формы «Студентам» и
«Компаниям» видимые подписи имели изначально — 13 полей из 19 уже были
подписаны `<label for>`.

Добавлено 13 подписей: посадочная 4 (две формы по два поля), «Аспирантам» 4,
«Студентам» 2, «Компаниям» 3. Оформление взято у существующих подписей без
изменений: моношрифт 12 px, верхний регистр, трекинг `.06em`, цвет
`--pd-ink-muted`, отбивка 6 px; подпись оборачивает поле, а не стоит рядом.

Почему это не косметика. `placeholder` исчезает в момент, когда человек
начинает печатать, — ровно тогда, когда подсказка нужнее всего: при
исправлении опечатки, при возврате к полю, при переключении между полями в
форме из четырёх строк. Программе чтения с экрана `aria-label` называл поле
верно, а зрячему человеку после первого нажатия клавиши не оставалось
ничего.

Где подпись повторяла подсказку дословно — «Ваше имя», «ФИО»,
«Специальность или шифр», «Дата защиты или срок», — `placeholder` снят:
дублирование заполняет поле серым текстом, который выглядит как введённое
значение. Где подсказка несла формат или пример — контакт, тема работы,
срок — она оставлена. Атрибут `aria-label` у подписанных полей снят: при
видимой подписи он избыточен, а при расхождении текстов вреден.

Состав полей, их порядок, валидация, тексты галочек согласия и версия
согласия не тронуты — журнал согласий по 152-ФЗ не затрагивается. Проверено
не чтением разметки: у всех 19 полей на четырёх страницах подпись видима и
связана атрибутом `for`; заявка с «Аспирантам» со всеми четырьмя
заполненными полями дошла до базы с верными значениями специальности и
срока.

### Макет-справочник снят

`design/DesignSystem.dc.html` (1 021 строка) удалён. Он не участвовал в
сборке и расходился с вёрсткой по всем разделам: заявлял 7 кеглей против 15
фактических, 3 формулы тени против 6, «цветов вне токенов — 0» при 27
токенах и 66 атрибутах SVG. Опасность такого файла не в том, что он
бесполезен, а в том, что он выглядит источником правды: следующий, кто
откроет репозиторий, возьмёт значения оттуда.

Действующее описание системы — `docs/DESIGN-SYSTEM.md`, собранный подсчётом
по макетам и сверенный с живыми страницами. Наброски статусов заказа и
оплаты для будущего кабинета, которые были в снятом файле, остаются
доступны в истории: `git show 65b4021:design/DesignSystem.dc.html`. Брать их
как значения нельзя — только как повод подумать.

Упоминания файла убраны из `README.md`, `CONTRIBUTING.md`,
`design/HANDOFF.md`; в `docs/DESIGN-SYSTEM.md` они переписаны как
исторические.

### Рабочая ветка удалена

`claude/scientific-consulting-service-web-42nebo` удалена. Содержательное из
неё перенесено решениями Р-89 и Р-94 со сверкой; остальные 63 файла — то,
что снято решением Р-89 сознательно: неиспользуемые заготовки SVG,
`design/baseline` с расходящимися снимками, три неработоспособных скрипта с
абсолютными путями из чужой сессии. Последний коммит ветки — `f1cc1f2`.
Ветки приёмок `accepted/*` не тронуты: это точки отката.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
семь форм из семи принимаются, подтверждение единообразно; заполнение всех
полей доходит до базы; ответов 4xx и 5xx нет, битых снимков нет;
горизонтальной прокрутки нет ни на одной из семи ширин; 45 остановок при
обходе клавиатурой с видимой рамкой; LCP 76 мс, CLS 0; `typecheck` и обе
сборки зелёные; перенос макетов воспроизводится.

---

## Р-102 · Откат видимых подписей: раскладка форм сломана (12.09.2026)

**Решение: правка Р-101 в части подписей полей откачена целиком. Макет-справочник остаётся снятым, ветка — удалённой; откат касается только форм.**

### Что сломалось

Подпись добавлялась обёрткой: `<label style="display:flex;flex-direction:column;gap:6px">Подпись<input …></label>`. Образец взят у существующих подписей в полных формах — там он работает. В компактных формах первого экрана он сломал раскладку: поля растянулись на всю высоту строки контейнера, превратившись в прямоугольники высотой в несколько сотен пикселей. На «Компаниям» вдобавок список `<option>` внутри `<select>` вывалился под поле отдельными строками.

Причина в том, что компактные формы построены иначе, чем полные: поля в них — прямые дети сетки с растягиванием по высоте строки, и обёртка, ставшая новым ребёнком сетки, унаследовала это растягивание, передав его полю внутри себя. В полных формах поля лежат в столбце фиксированной высоты, и обёртка ничего не меняла.

### Почему я этого не увидел

Проверка после правки была программной и полной по своему предмету: у всех 19 полей подпись видима и связана атрибутом `for`, axe-core 0 нарушений, контраст в норме, все семь форм доходят до базы с верными значениями, горизонтальной прокрутки нет. Каждая из этих проверок дала верный ответ на свой вопрос — и ни одна не спрашивала, как блок выглядит.

Снимок геометрии, которым проверялся Р-99, здесь не снимался: правка казалась локальной. Между тем именно она меняла структуру дерева, а не значения свойств, — то есть ровно тот случай, где сравнение вычисленных стилей бесполезно, а снимок экрана обязателен.

**Правило на будущее: правка, меняющая структуру разметки, а не значения свойств, принимается только по снимку экрана.** Программные проверки её не ловят по устройству.

### Что сделано

Четыре макета возвращены к состоянию до Р-101 (`65b4021`). Поля снова подписаны `placeholder` и `aria-label`. Проверено снимками всех четырёх компактных форм: высоты 279—353 px, поля в одну строку, переключатель на месте.

Рекомендация аудита остаётся неисполненной и возвращается в список задач. Правильный путь — не оборачивать поле, а класть подпись отдельным элементом рядом, с явным управлением поведением в сетке; делать это следует с предварительным снимком каждой формы.

---

## Р-103 · Бегущая строка после первого экрана, форма в первом экране «Аспирантам» (12.09.2026)

Две правки по указанию заказчика.

### «Компаниям»: бегущая строка сразу после первого экрана

Порядок разделов был hero → команда → бегущая строка. Стал hero → бегущая
строка → команда. Секция перенесена целиком, отступы у обеих сохранены:
у бегущей строки `clamp(80px,7vw,112px)`, у команды `clamp(72px,7vw,112px)`.

Порядок проверен по живой странице, а не по разметке: перечень секций в
порядке отрисовки с координатами — первый экран `top=70`, бегущая строка
`top=964`, команда `top=1117`.

### «Аспирантам»: форма вместо кнопки в первом экране

В первом экране стояла одна кнопка «Записаться на консультацию», уводившая
к форме внизу страницы. Заказчик распорядился поставить форму прямо здесь, а
подпись кнопки привести к «Оставить заявку».

Форма перенесена с «Студентам» — там такая же короткая форма первого экрана
уже работает: переключатель «E-mail / Телефон», имя, контакт, отметка
согласия, кнопка. Состав полей и текст отметки согласия скопированы дословно,
без единого изменения: версия текста согласия `2026-08-21` остаётся прежней,
журнал согласий по 152-ФЗ не затрагивается.

Что потребовалось в коде страницы: второй набор состояния формы —
`contactTabsTop`, `contactTypeTop`, `contactAutoTop`, `contactPlaceholderTop`,
`sentTop`, `submitTop` — по образцу «Студентам», где две формы сосуществуют.
Имя обработчика важно: конвертер оборачивает любой ключ, начинающийся с
`submit`, и берёт имя формы из суффикса, поэтому заявка из первого экрана
уходит с пометкой `top`, а из нижней формы — `request`. Отметка согласия
одна, без отметки оферты: по Р-86 отметка оферты стоит только на посадочной.

Форм на сайте стало восемь. Проверено отправкой: заявка из нового первого
экрана дошла до базы с `source=postgrad`, `form=top`.

### Проверено

Снимками, а не только программно — правило из Р-102: первый экран
«Аспирантам» снят на 1280 и 390 px, поля 48 px, горизонтальной прокрутки нет,
подпись кнопки «Оставить заявку»; порядок разделов «Компаниям» снят перечнем
секций в порядке отрисовки.

Сплошной обход полей: 175 полей на четырёх страницах и семи ширинах, вне
нормы ноль. axe-core 0 нарушений на семи страницах; контраст — пар ниже
порога нет; восемь форм из восьми доходят до базы; ответов 4xx и 5xx нет,
битых снимков нет; горизонтальной прокрутки нет ни на одной ширине; 45
остановок при обходе клавиатурой с видимой рамкой; подсветка блоков акцентом
4/4, 5/5, 5/5, 5/5; LCP 72 мс, CLS 0; `typecheck` и сборка зелёные; перенос
макетов воспроизводится.

---

## Р-104 · Плашки под изображениями, короткий лид, бегущая строка на посадочной (12.09.2026)

Три правки по указанию заказчика.

### «Аспирантам»: плашки перенесены в правую колонку

Три плашки первого экрана — «26 позиций», «6 этапов», «с 2016» — стояли в
левой колонке под формой и вытягивали первый экран вниз. Перенесены в правую
колонку под изображения, как на посадочной: правая колонка стала столбцом с
`min-height:100%`, плашки прижаты к низу через `margin-top:auto`, у сетки
первого экрана `align-items` изменён с `start` на `stretch` — иначе колонке
не к чему прижимать.

### Лид первого экрана — один тезис

| Страница | Было | Стало |
|---|---:|---:|
| «Студентам» | 286 зн., три предложения | 98 зн. |
| «Аспирантам» | 161 зн., два предложения | 102 зн. |
| Посадочная | 140 зн., два предложения | 104 зн. |

«Компаниям» не тронута: там уже одно предложение в 95 знаков.

Сведения, ушедшие из лида, нигде не потеряны — это проверено, а не
предположено: «оплата по этапам» раскрыта отдельным блоком в разделе сметы
(«Следующий этап оплачивается после приёмки предыдущего»), «первичная
консультация не оплачивается» стоит подписью у кнопки отправки и в блоке
заявки. Лид первого экрана обязан читаться за один взгляд; условия договора
читаются там, где человек к ним готов.

### Посадочная: бегущая строка со всеми направлениями

После первого экрана добавлены две строки, идущие навстречу друг другу, — по
образцу «Аспирантам», «Студентам» и «Компаниям», где такие строки уже есть.

Отличие в составе: направления перемешаны, а не идут блоками. Посадочная
обращается ко всем трём аудиториям сразу (Р-44), и список, где сначала шесть
позиций для аспирантов, а потом шесть для бизнеса, читался бы как две разные
страницы, склеенные вместе. Верхняя строка: кандидатские диссертации,
дипломные работы, техническое задание, научные статьи, отчёты по практике,
патентный ландшафт, докторские диссертации, НИОКР под сдачу. Нижняя:
магистерские диссертации, гранты и субсидии, научная редактура, заявка на
изобретение, оформление по ГОСТ, подготовка к защите, программа для ЭВМ,
монографии.

Список удваивается, вторая половина скрыта от чтения вслух через
`aria-hidden` и уезжает ровно на длину первой — шва не видно. Движение
останавливается при наведении и при фокусе (секция имеет `tabindex="0"`), а
общее правило `prefers-reduced-motion: reduce` на этой странице снимает
анимацию целиком — отдельной оговорки не потребовалось.

### Проверено

Снимками: первый экран «Аспирантам» на 1280 и 390 px, бегущая строка
посадочной, порядок секций посадочной перечнем в порядке отрисовки —
первый экран `top=70`, бегущая строка `top=923`, следующий раздел `top=1042`.

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет; 175
полей форм вне нормы ноль; восемь форм из восьми доходят до базы; ответов 4xx
и 5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на одной из
семи ширин; 46 остановок при обходе клавиатурой с видимой рамкой (прибавилась
бегущая строка); LCP 80 мс, CLS 0; `typecheck` и сборка зелёные; перенос
макетов воспроизводится.

---

## Р-105 · Приёмка посадочной: ритм, шапка, личный кабинет (12.09.2026)

### Ритм между блоками поднят на 10 %

Только на посадочной, по указанию заказчика. Значения округлены к шагу 2 px
(Р-99): отбивка бегущей строки `clamp(80px,7vw,112px)` → `clamp(88px,7.7vw,124px)`,
межсекционные `56px` → `62px` (два места) и `64px` → `70px`, отбивка блока
заявки `96px` → `106px`, подвала `48px` → `52px`.

Проверено замером фактических зазоров между блоками на живой странице:
113 px перед бегущей строкой, 62 перед «С чего начнём», 70 перед «Что входит
в сопровождение», 62 перед «Гарантии» — прирост 9,4—10,7 %.

### Меню направлений в шапке заменено кнопкой личного кабинета

Три ссылки — «Аспирантам», «Студентам», «Компаниям» — сняты, на их место
поставлена кнопка «Личный кабинет».

Кнопка **пока никуда не ведёт**: раздела ещё нет, он идёт этапом Л1 в
`docs/PLAN.md`.

Сначала она была сделана отключённой (`disabled`, приглушённый текст, курсор
`not-allowed`) — из соображения, что работающая на вид кнопка, которая ничего
не делает, вводит в заблуждение. Заказчик распорядился иначе: кнопка
активная, обводка чёрная. Исполнено — рамка и текст `--pd-ink`, курсор
`pointer`, при наведении заливка чёрным с белым текстом, при нажатии отдача
`scale(.97)` (Р-38, Р-39). Кнопка получает фокус и участвует в обходе
клавиатурой.

Оговорка остаётся в силе и записана здесь, а не замалчивается: до появления
кабинета нажатие не даёт результата. Как только раздел появится, кнопке
достаточно дать адрес — оформление менять не придётся.

Вместе с меню снят бургер: он существовал только чтобы раскрывать это меню
на узких экранах. Снято и всё, что его обслуживало — состояние `navOpen`,
атрибуты `hidden`/`aria-expanded`, обработчик `toggleNav`, синхронизация
`syncWide` с шириной экрана, четыре правила CSS.

Два следствия проверены, а не приняты на веру.

**Переходы на профильные страницы не потерялись.** Шапка была не
единственным путём: блок «С чего начнём вашу работу» ведёт на все три
страницы карточками, плюс ссылки на «Аспирантам» и «Компаниям» стоят в
плашках первого экрана. Проверено обходом ссылок: `/main`, `/students`,
`/business` отвечают 200.

**Шапка на узких экранах не осталась пустой.** До правки на ширинах до
480 px кнопку «Оставить заявку» прятало правило `display:none` — её роль там
брал на себя бургер. Бургера не стало, и шапка на телефоне превратилась бы в
один логотип. Правило снято: теперь на узком экране видна кнопка заявки, а
скрывается кабинет — из двух кнопок в приоритете та, что ведёт к цели
страницы.

### Приёмка

Посадочная принята заказчиком 12.09.2026. Принятое состояние закрепляется
веткой `accepted/landing-2026-09-12`.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
ответов 4xx и 5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на
одной из семи ширин; 44 остановки при обходе клавиатурой с видимой рамкой
(было 46: ушли три ссылки меню, прибавилась кнопка кабинета); восемь форм из
восьми доходят до базы; LCP 76 мс,
CLS 0; `typecheck` и сборка зелёные; перенос макетов воспроизводится. Шапка
снята на 1280 и 390 px.

---

## Р-106 · Отзывы клиентов: карусель на трёх профильных страницах (12.09.2026)

**Решение: перед подвалом «Аспирантам», «Студентам» и «Компаниям» добавлен
блок отзывов — карусель из пяти карточек, листаемая по кругу.**

### Сразу о главном: тексты — образцы, а не настоящие отзывы

Написанные мной отзывы **нельзя публиковать как настоящие.** Это не
осторожность на всякий случай: вымышленный отзыв на коммерческом сайте —
недостоверная реклама по ч. 7 ст. 5 ФЗ «О рекламе» и нарушение права
потребителя на достоверную информацию (ст. 10 Закона о защите прав
потребителей). ФАС разбирает такие дела по обращению любого лица, включая
конкурента.

Чтобы риск не был скрытым, приняты три меры:

1. Подписи **обезличены**: роль и область, без имён и названий организаций
   («Соискатель учёной степени · технические науки»). Это распространение
   правила Р-53 со съёмки команды на отзывы: снимки синтезированы моделью и
   конкретных людей не изображают, поэтому имя рядом с таким лицом было бы
   выдумкой поверх выдумки.
2. Под заголовком блока стоит видимая строка: «Отзывы обезличены: указаны
   роль и область, без имён и названий организаций».
3. В коде каждой страницы над списком стоит предупреждение, а в `PLAN.md`
   заведена задача: заменить настоящими отзывами до открытия сайта.

Что нужно от заказчика: письменные отзывы клиентов с их согласием на
публикацию. Тогда тексты меняются, а разметка и механика остаются.

### Механика

Пять карточек, положение каждой считается по кругу от текущей:
смещение приводится к диапазону −2…+2, поэтому список листается бесконечно в
обе стороны.

| Смещение | Масштаб | Размытие | Слой |
|---|---|---|---|
| 0 — впереди | 1 | нет | 3 |
| ±1 | 0,88 | `blur(2.5px)` — 25 % от полного | 2 |
| ±2 | 0,76 | `blur(5px)` — 50 % от полного | 1 |

Сцена с `overflow:hidden`, поэтому дальние карточки уходят за край. Движение
— переход `transform`, `filter` 420 мс по кривой проекта; библиотеки анимации
не подключались (Р-50).

**Приглушение сделано размытием и масштабом, а не прозрачностью.** Первая
версия гасила соседние карточки через `opacity`, и axe-core немедленно нашёл
три нарушения контраста: полупрозрачный текст на белом даёт 2,6:1 при
пороге 4,5:1. Прозрачность снята, глубину даёт размытие — контраст остался
полным, а визуально карточки читаются как задний план.

### Доступность

Все карточки, кроме передней, помечены `aria-hidden="true"` и
`pointer-events:none` — программа чтения с экрана видит один отзыв, тот, что
показан. Кнопки листания подписаны, счётчик «2 / 5» объявляется через
`aria-live="polite"`. Блок вложен в `<main>`: первая версия стояла между
`</main>` и `<footer>`, и axe справедливо указал, что содержимое выпало из
ориентиров страницы.

### Снимки

Пять снимков из `app/public/photos/`, которые до сих пор не использовались ни
одной страницей: `adviser`, `business`, `lecturer`, `postgrad`, `student`.
Кадрированы в квадрат 160×160 с индивидуальным смещением — человек в этих
кадрах не по центру, и общая обрезка срезала бы лица. Положены в
`design/assets/people/` и `app/public/assets/people/` побайтно одинаково:
сверка наборов в CI это проверяет. Вес всех пяти — 19 КБ.

Портреты команды (`assets/crew/`) намеренно не использованы: одно и то же
лицо не может быть и куратором проекта, и его клиентом.

**Поправка того же дня.** Первое распределение снимков было ошибочным, и
ошибка нашлась только при сплошной проверке страниц. Я исходил из того, что
каталог `app/public/photos/` не используется, — поиск по макетам не давал
совпадений. На деле снимки подключены через `app/src/components/photos.ts`
как слоты блоков, и каждый уже играет роль: «Куратор проекта», «Научный
редактор», «Отраслевой эксперт», «Студентка присылает работу на разбор».
Совпадения по страницам были: на «Аспирантам» и «Студентам» два лица из
отзывов уже стояли в других ролях, а «Преподаватель у доски» — наша сторона —
оказался бы клиентом, оставившим отзыв.

Снимки перераспределены по фактической занятости каждой страницы:

| Страница | Занято слотами | Лица отзывов |
|---|---|---|
| «Аспирантам» | curator, editor, adviser, student | postgrad, expert, lecturer, blueprints, business |
| «Студентам» | curator, editor, lecturer, student | adviser, postgrad, business, expert, blueprints |
| «Компаниям» | curator, expert, blueprints | business, lecturer, adviser, postgrad, editor |

Пересечений не осталось ни на одной странице — проверено сличением адресов
снимков в блоке отзывов и во всех слотах страницы, а не глазами. Подписи
согласованы с полом на снимке: на «Аспирантам» свободны только мужские кадры,
поэтому «Аспирантка» заменена на «Аспиранта»; на «Студентам» по той же
причине «Магистрантка» стала «Магистрантом», а род в её реплике исправлен.
Аватаров теперь восемь: к пяти прежним добавлены `blueprints`, `expert`,
`editor`.

### Приёмка

Блок отзывов на трёх профильных страницах принят заказчиком 12.09.2026 после
сплошной проверки каждой страницы. Принятое состояние закрепляется веткой
`accepted/reviews-2026-09-12`. Приёмка касается механики, вёрстки и
распределения снимков; тексты остаются образцами и заменяются настоящими
отзывами до открытия сайта — задача 1а в `PLAN.md`.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
листание проверено вживую: пять шагов вперёд возвращают к первому отзыву,
шаг назад с первого даёт пятый; карточки одинакового размера 430×236 на
широком экране и 328×236 на 390 px; горизонтальной прокрутки нет ни на одной
из семи ширин; восемь форм из восьми доходят до базы; ответов 4xx и 5xx нет,
битых снимков нет; 44 остановки при обходе клавиатурой с видимой рамкой;
LCP 80 мс, CLS 0; наборы ассетов синхронны; `typecheck` и сборка зелёные;
перенос макетов воспроизводится.

---

## Р-107 · Отзывы: без счётчика, с рисованными аватарами (12.09.2026)

Три правки по указанию заказчика.

### Счётчик «3 / 5» убран

Число раскрывало, что отзывов ровно пять, — а блок должен читаться как
выборка из многих. Остались две стрелки.

Смена отзыва по-прежнему объявляется программе чтения с экрана, но не
числом: скрытый от глаз элемент `aria-live` называет подпись показанной
карточки — «Отзыв: Соискатель, химия». Без него листание для незрячего
проходило бы беззвучно: карточки помечены `aria-hidden`, и появление новой
ничем себя не выдаёт.

### Крайние карточки выведены в кадр

Смещение дальней пары было 144 % ширины — она уезжала за край почти целиком.
Стало 112 %, соседняя пара сдвинута с 72 % на 62 %. Дальняя карточка заходит
за границу сцены и остаётся видимой примерно на две трети своей свободной
части; ближняя перекрывает её ровно настолько, чтобы читалась глубина.

### Снимки заменены рисованными аватарами

Заказчик потребовал, чтобы изображения не совпадали с уже использованными на
сайте. Вместо подбора других фотографий нарисованы пять векторных аватаров:
круг в акцентной шкале, светлый силуэт, деталь-различитель — стрижка, длинные
волосы, очки, борода, собранные волосы. Манера — как у собственного набора
`pd-*` (Р-61): простая геометрия, скруглённые формы. Пять фонов из палитры:
`#14417A`, `#0D2B52`, `#2A63B4`, `#3D4450`, `#0F3260`; цвета литералами, как
требует Р-97 для содержимого SVG.

Решение снимает и правовой риск, о котором сказано в Р-106: фотографический
портрет рядом с вымышленным отзывом читается как живой человек, которого не
существует. Силуэт этого не обещает. Когда придут настоящие отзывы, аватар
можно заменить фотографией с согласия автора — или оставить, если человек не
захочет показывать лицо.

Каталог `assets/people/` (восемь кадрированных снимков) снят: он появился
двумя решениями раньше и больше не используется. Ссылок на него не осталось,
наборы `design/assets` и `app/public/assets` синхронны. Пять аватаров весят
2,2 КБ против 33 КБ снимков.

### Приёмка

Правки приняты заказчиком 12.09.2026. Ветка `accepted/reviews-2026-09-12`
переставлена на это состояние: она закрепляет блок отзывов целиком — Р-106 и
Р-107 вместе. Тексты остаются образцами; замена настоящими отзывами — задача
1а критического пути в `PLAN.md`.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
листание по кругу в обе стороны, объявление меняется вместе с карточкой;
ответов 4xx и 5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на
одной из семи ширин; восемь форм из восьми доходят до базы; 44 остановки при
обходе клавиатурой с видимой рамкой; LCP 84 мс, CLS 0; наборы ассетов
синхронны; `typecheck` и сборка зелёные; перенос макетов воспроизводится.

---

## Р-108 · Аудит перед выпуском и вывод карточек за край (12.09.2026)

### Крайние карточки отзывов

Сцена карусели резала дальние карточки по своей границе — они обрывались
посреди блока. Обрезка со сцены снята: у корневого элемента страницы стоит
`overflow-x: clip`, и лишнее теперь отсекает край окна, а не рамка блока.
Дальняя карточка видна целиком на 1440 px, на 98 % при 1280 px и уходит за
край экрана на узких ширинах. Горизонтальной прокрутки не появилось ни на
одной из семи проверенных ширин.

### Что нашёл аудит

Механика в порядке: перенос макетов воспроизводится, наборы ассетов
синхронны, `npm audit` — 0 уязвимостей, дизайн-система соблюдена (цветов вне
токенов нет, кегли в шкале, четыре коэффициента интерлиньяжа, радиусы из
набора, нечётных отступов нет).

Расхождения нашлись в документации — она отстала от кода на десяток решений:

| Где | Утверждалось | На деле |
|---|---|---|
| `README.md`, «Проверено» | замеры от 09.09.2026, вес 382 КБ, 43 остановки, семь форм | 12.09.2026, 429 КБ, 44 остановки, восемь форм |
| `README.md`, там же | «next 15.5.25, nodemailer 10.0.9» | Next 16.3.5, React 19.3.0, Zod 4.6.2 |
| `README.md`, п. 6 | «блок отзывов снят, силуэты удалены» | блок вернулся решением Р-106 |
| `design/HANDOFF.md` | «`avatar-*.svg` — 12 силуэтов, блок удалён» | пять рисованных аватаров, блок работает |
| `DESIGN-SYSTEM.md`, разд. 7 | «7 форм», «бегущая строка на трёх страницах», «плашка `7px 14px`» | 8 форм, четыре страницы, `8px 14px` |
| `DESIGN-SYSTEM.md`, разд. 7 | карусели отзывов и кнопки кабинета нет | добавлены с размерами |

Все шесть исправлены по факту, а не по памяти: числа взяты из прогонов
приёмочного набора этого дня, состав компонентов — подсчётом по макетам.

Отдельно отмечено: правки такого рода не ловятся ни одной автоматической
проверкой. CI следит за воспроизводимостью переноса и синхронностью ассетов,
но не за тем, совпадает ли текст описания с тем, что описано. Единственная
защита — сплошная сверка документов с фактом, и её приходится делать руками.

### Проверено

axe-core 0 нарушений на семи страницах; контраст — пар ниже порога нет;
ответов 4xx и 5xx нет, битых снимков нет; горизонтальной прокрутки нет ни на
одной из семи ширин; текст в карточках отзывов не переполняется ни на одной
из пяти проверенных ширин; восемь форм из восьми доходят до базы; 44
остановки при обходе клавиатурой с видимой рамкой; LCP 76 мс, CLS 0;
`typecheck` и сборка зелёные.

---

## Р-109 · Закрытие задач заказчика: правовые тексты, цены, РКН, отзывы, ВК, кабинет (12.09.2026)

Шесть указаний заказчика, полученных подряд.

### Правовые документы согласованы как есть

Пометка «Проект документа» снята с трёх страниц. Юридический текст,
нумерация разделов и даты редакций не тронуты — снят только один `<span>` в
шапке каждого документа.

**Согласование юриста не закрыло два расхождения текста с сайтом.** Их
закрывает не юрист, а правка сайта или текста, поэтому они переписаны в
`PLAN.md` из «вопросов к юристу» в открытые задачи:

1. Пункт 4.1 оферты определяет акцепт через отметку «Принимаю оферту», а
   отметка стоит только на посадочной — в двух формах из восьми. На `/main`,
   `/students` и `/business` акцепт формально не оформлен.
2. Раздел 9 согласия описывает отметку рекламной рассылки, которой на сайте
   нет.

### Цены на сайте не публикуются

Заказчик подтвердил: сумму называет менеджер при разговоре. Решение Р-03
остаётся в силе, задача о прайсе снята, риск «прайс не утверждён к открытию»
закрыт. Вместо суммы на трёх страницах стоят сроки и состав сметы.

### Уведомление Роскомнадзора подано

Задача закрыта, вместе с ней снято условие для счётчика Яндекс.Метрики и
риск «счётчик поставлен до уведомления». Остаётся вернуть в Политику пункт о
внесении в реестр операторов — это правка юридического текста, и делается
она по отдельному указанию.

### Форма отзыва

Кнопка «Написать отзыв» под каруселью раскрывает форму. Заказчик просил два
поля — «кто» и текст. **Полей три: добавлен контакт, и это отступление
сделано намеренно.**

Причина не техническая, а содержательная: опубликовать отзыв без разрешения
автора нельзя, а спросить это разрешение больше негде. Форма прямо об этом
говорит: «Контакт нужен, чтобы уточнить формулировку и получить ваше
согласие на публикацию. Без вашего разрешения отзыв на сайте не появится».
Плюс обязательная отметка согласия на обработку персональных данных — без неё
приём имени и контакта нарушает 152-ФЗ.

Бэкенд не тронут. Отзыв уходит существующим обработчиком заявок: конвертер
берёт имя формы из суффикса ключа, `submitReview` даёт `form: 'review'`, «кто»
ложится в `name`, текст — в `message`. Проверено отправкой с трёх страниц:
записи в базе с верным источником и пометкой `review`.

### Ссылка на сообщество ВКонтакте

Рядом с кнопкой телеграма на четырёх содержательных страницах. Значка ВК в
наборе нет, и рисовать чужой товарный знак от руки в проекте, где все глифы
свои (Р-61), неуместно, — поэтому кнопка несёт монограмму «VK» тем же
шрифтом, что вордмарк.

### Заглушка личного кабинета

Кнопка «Личный кабинет» вела в никуда. Появился маршрут `/cabinet`: шапка и
подвал документной страницы, заголовок «Раздел готовится», объяснение и две
кнопки — на главную и в телеграм. Страница закрыта от индексации
(`robots: index false`): раздел ничего не содержит, в выдаче ему делать
нечего.

Восьмой макет `CabinetPage.dc.html` внесён в перенос CI, в список маршрутов
витрины и в таблицу адресов конвертера — без последнего ссылка вела бы на
`/cabinet.html` вместо `/cabinet`, что и случилось при первой сборке.

### Проверено

axe-core 0 нарушений на **восьми** страницах; контраст — пар ниже порога нет;
горизонтальной прокрутки нет; восемь форм заявок доходят до базы, форма
отзыва принята с трёх страниц; переход по кнопке кабинета ведёт на
`/cabinet`, страница отдаёт `noindex, nofollow`; 45 остановок при обходе
клавиатурой с видимой рамкой; LCP 80 мс, CLS 0; наборы ассетов синхронны;
`typecheck` и сборка зелёные; перенос макетов воспроизводится.

Отдельно: при прогоне проверок сработало ограничение частоты — 15 заявок за
пять минут с одного адреса, ответ 429 с понятным текстом. Защита отработала
верно, это не дефект.

---

## Р-110 · Форма отзыва: всплывающее окно без контакта и согласия (13.09.2026)

**Решение: форма отзыва спрашивает только роль автора и текст; контакт и
отметка согласия из неё убраны; форма показывается всплывающим окном.**

### Что это меняет и чем оплачено

Заказчик распорядился убрать из формы контакт, пояснение к нему и отметку
согласия. Указание исполнено, но следствие надо назвать прямо: **опубликовать
такой отзыв нельзя.** Спросить разрешение автора негде — обратной связи
форма не оставляет. Отзыв в базе становится сигналом «нам написали», а не
готовым к публикации текстом. Задача «настоящие отзывы на сайте» этим не
закрывается: для неё нужен отдельный сбор согласий вне сайта.

### Почему согласие убрать было можно

Не потому, что так распорядились, а потому, что после правки форма перестала
собирать персональные данные. Она спрашивает роль и направление — «соискатель,
технические науки», — и прямо просит не указывать имя. Контакта нет. Нет
персональных данных — нет и обязанности брать согласие по ст. 9 152-ФЗ.

Чтобы это не осталось обещанием на словах, закрыт обходной путь: если поле
контакта всё же придёт в запросе с `form: 'review'` — а прислать его может
только робот, форма такого поля не содержит, — обработчик записывает пустое
значение. Иначе через форму без отметки согласия можно было бы собирать
контакты.

### Правка бэкенда

Схема разбора и обработчик заявки — та часть, которую правки вида не
трогают. Здесь правка сделана намеренно и ограничена одной формой:

- `contact` больше не обязателен на уровне поля; для всех форм, кроме отзыва,
  его требует `superRefine` с прежним текстом ошибки;
- `consent` перестал быть литералом `true` и проверяется там же — тоже для
  всех форм, кроме отзыва;
- у отзыва взамен стал обязателен текст: пустой отзыв отправлять незачем;
- обработчик не сохраняет контакт, пришедший с `form: 'review'`.

Поведение остальных семи форм не изменилось: тексты ошибок, порядок проверок
и состав полей прежние.

### Всплывающее окно

Форма больше не растёт снизу от карусели, а открывается окном поверх
страницы: затемнение, карточка 440 px, заголовок, кнопка закрытия. Закрывается
тремя способами — крестиком, щелчком по затемнению и клавишей Esc; последнее
обязательно, иначе с клавиатуры из окна не выйти.

Здесь нашлась ошибка, стоившая двух сборок: видимостью управлял атрибут
`hidden`, а он скрывает элемент правилом `display:none` из стилей браузера —
инлайновый `display:flex` его перебивает. Окно оставалось в потоке,
невидимое, и перехватывало нажатия по всей странице: кнопка «Написать отзыв»
переставала работать. Правило `[hidden]{display:none!important}` не помогло,
потому что инлайновый стиль сильнее. Видимость переведена на подстановку
значения `display`.

### Проверено

Окно открывается и закрывается всеми тремя способами — проверено вживую;
поля два, отметок согласия нет; отзыв доходит до базы с трёх страниц, в
записи контакт пуст и `consentGiven = false`. axe-core 0 нарушений на восьми
страницах; контраст — пар ниже порога нет; горизонтальной прокрутки нет; 45
остановок при обходе клавиатурой; LCP 80 мс, CLS 0; `typecheck` и сборка
зелёные.

---

## Р-111 · Сбор отзывов в одну таблицу (13.09.2026)

**Решение: отзывы выгружаются в CSV командой `npm run reviews:export`;
реестр ведётся в таблице на диске заказчика, а не в приложении.**

Отзывы лежат в общей таблице заявок с пометкой `form = 'review'`. Читать их
там неудобно: нужен доступ к серверу и знание SQL. Скрипт
`app/scripts/export-reviews.mjs` собирает их в файл со столбцами: дата,
страница, «кто», текст, состояние, заметки, идентификатор. Состояние и
заметки берутся из полей заявки — значит таблицу можно вести как реестр:
опубликован, отклонён, ждёт согласования.

Файл пишется с меткой BOM и переводами строк CRLF: без них таблицы на Windows
показывают кириллицу вопросительными знаками, а строку — одной ячейкой.

**Прямой записи в облако намеренно нет.** Автоматическая отправка в чужой
сервис означала бы ключ доступа на боевом сервере, зависимость от
доступности сервиса и данные, уходящие за пределы контура без отдельного
решения. Выгрузка отдаёт файл — куда его положить, решает заказчик; порядок
описан в `DEPLOY.md`, раздел 5а.

Личных данных в файле нет по устройству формы (Р-110), поэтому облачное
хранение допустимо. Каталог `deploy/exports/` закрыт от индекса git.

### Проверено

Выгрузка отработала на живой базе: девять отзывов, файл открывается таблицей,
кириллица читается, столбцы разделены. Зависимостей не добавлено — скрипт
использует тот же драйвер, что приложение.

---

## Р-112 · Закрытие остатка до открытия: акцепт, рассылка, счётчик, подписи полей (13.09.2026)

**Решение: расхождения сайта с правовыми текстами закрыты правкой сайта;
счётчик посещаемости готов и выключен; подписи полей доведены до общего
приёма.**

Указание заказчика — «закрыть оставшиеся вопросы автоматически». Автоматически
закрывается то, что не требует чужого решения: домен, площадка и почта на своём
домене остаются за заказчиком, правка юридического текста — за юристом.

### 1. Акцепт оферты во всех формах

Отметка «Принимаю оферту и политику» стояла только в двух формах посадочной.
Пункт 4.1 оферты определяет акцепт через направление заявки **с проставлением
отметки о согласии с условиями оферты**, поэтому заявки с шести остальных форм
договора не образовывали: юридически безупречно, но означало, что одно и то же
действие человека на разных страницах имеет разные последствия.

Отметка добавлена во все восемь заявочных форм. Разметка не сочинялась заново:
каждая новая строка — копия соседней строки согласия того же файла с заменой
имени поля и текста. Стили при этом совпадают побайтно, и раскладка не может
поехать оттого, что где-то забыт `grid-column`.

Правки бэкенда не потребовалось: колонка `termsAccepted` и поле `terms` в схеме
разбора существовали с самого начала.

**Серверная проверка отметки намеренно не добавлена.** Отметка обязательна на
клиенте; на сервере её отсутствие не отклоняет заявку, а сохраняется как
`termsAccepted = false`. Так и должно быть: акцепт — факт, который фиксируют, а
не условие приёма обращения. Заявка без акцепта остаётся обращением, договором
она не становится.

### 2. Отметка рекламной рассылки

Раздел 9 согласия описывает отметку «Согласен получать информационные и
рекламные сообщения», которой на сайте не было. Отметка добавлена в четыре
развёрнутые формы — по одной на страницу, — необязательная, с формулировкой
слово в слово из текста согласия.

В компактные формы первого экрана она не ставится: там два поля, и третья
строка отметок стоит дороже, чем даёт. Раздел 9 при этом действует — поле
существует, и согласие на рассылку можно дать сознательно.

Версия текста согласия не меняется: раздел 9 описывал отметку заранее,
`CONSENT_VERSION` остаётся `2026-08-21`. Журнал согласий не разрывается —
состав полей поменялся до появления настоящих заявок.

### 3. Счётчик посещаемости

Техническая часть сделана и выключена. Компонент `app/src/components/Metrika.tsx`
отдаёт счётчик только при заданном `NEXT_PUBLIC_METRIKA_ID`; послабления
политики содержимого для `mc.yandex.ru` выдаются в `next.config.mjs` тем же
условием. Пока номера нет, разметка и заголовки не отличаются от сборки, где
аналитики нет вовсе, — проверено сборкой в обоих состояниях.

**Вебвизор выключен намеренно.** Он записывает содержимое полей формы — имя,
телефон, тему работы — и передаёт третьему лицу. Это отдельная передача
персональных данных со своим основанием и своей строкой в уведомлении
Роскомнадзора; ради сведений о посещаемости такая цена не платится.

Два условия остаются невыполненными, и оба не технические: новая редакция
политики (её раздел «Файлы cookies и веб-аналитика» сейчас утверждает, что
cookies и аналитика не используются) и сверка с поданным уведомлением. Номер
счётчика заводит заказчик на своей учётной записи: счётчик, заведённый
исполнителем, останется у исполнителя вместе со статистикой.

### 4. Подписи полей и порядок в форме «Аспирантам»

Задача о видимых подписях (Р-102) оказалась уже наполовину сделанной: на
«Студентам» и «Компаниям» подписи стоят и раскладку не ломают. Не хватало их в
развёрнутой форме «Аспирантам» — добавлены тем же приёмом, подпись обёрткой.
Приём, сорвавшийся в Р-102, здесь работает, потому что у поля своя высота
52 px и растягиваться по строке сетки ему нечем.

Заодно исправлены две вещи, обнаруженные на снимках, а не в задании:

- Сетка формы была жёстко двухколонной, и на узком экране поля сжимались до
  120 px, обрезая текст подсказки. Переведена на схлопывающуюся —
  `repeat(auto-fit,minmax(200px,1fr))`.
- Кнопка «Отправить заявку» стояла **выше** отметок согласия: человек доходил
  до действия раньше, чем видел условия. Блок кнопки перенесён под отметки —
  порядок теперь одинаков во всех восьми формах.

Подсказки в полях разведены с подписями: подпись называет поле, подсказка
показывает пример заполнения. Дублирующие `aria-label` сняты — при видимой
подписи они перебивали бы её и расходились с видимым текстом (WCAG 2.5.3).

### Проверено

Все восемь заявочных форм заполнены и отправлены на живой базе: заявка доходит,
`consentGiven` и `termsAccepted` истинны везде, `marketingOptIn` истинен там,
где отметка есть, и ложен там, где её нет. Формы сняты на 1280 и 390 пикселях:
растянутых полей нет ни одного, горизонтальной прокрутки нет ни на одной
странице. axe-core — ноль нарушений на семи страницах. Перенос макетов
воспроизводится, наборы ассетов синхронны, витрина собирается.

---

## Р-113 · Настоящие отзывы вместо образцов (13.09.2026)

**Решение: образцы заменены настоящими отзывами клиентов с Авито; из
двенадцати переданных опубликованы восемь, четыре отклонены по содержанию.**

Заказчик передал двенадцать снимков отзывов со своего профиля на Авито за
2024 год. Это закрывает единственный правовой риск, который создавал сам
сайт: пять карточек, написанных исполнителем как образцы, читались как отзывы
реальных клиентов, и никакой пометки об этом на странице не было — признаки
недостоверной рекламы по ч. 7 ст. 5 ФЗ «О рекламе».

### Что отклонено и почему

Четыре отзыва не опубликованы. Не из-за недостоверности — они подлинные, — а
потому что называют предметом работу, выполненную за клиента: «заказывала
вторую часть в магистерской ВКР», «курсовая была готова раньше срока, а
преподаватель поставил отличную оценку», «подняли оригинальность с 10 % до
70 % за день», «лентяям и уклонистам на заметку».

Сайт построен на обратном тезисе: исследование ведёт автор, сервис отвечает
за экспертизу и сопровождение; предмет договора в оферте — научно-
консультационное сопровождение. Такие карточки рядом с прямым «мы не пишем
за вас» читаются как опровержение собственного позиционирования, а отзыв о
повышении оригинальности — как реклама обхода проверки на заимствования.
Первым это заметит не клиент, а конкурент или проверяющий.

Опубликованы восемь, где предметом названы сроки, цена, общение,
сопровождение и проверка на заимствования.

### Как перенесены

Тексты приведены дословно, включая авторскую пунктуацию и опечатки
(«за качественно выполненную работа»): отредактированный отзыв перестаёт быть
отзывом и возвращает нас к тому, от чего уходили. Нормализованы только лишние
пробелы перед знаками препинания и сняты эмодзи — это вёрстка, а не
содержание.

Публикуются имя и дата, фамилии — нет: они не нужны для доверия, а их
перенос — обработка персональных данных без основания. Фотографии профилей с
площадки не копируются вовсе; аватары остаются наши, рисованные. Подпись
карточки называет источник и дату: «Авито · 30 июня 2024» — отзыв
проверяется у первоисточника.

Набор аватаров дорисован с пяти до восьми в том же приёме: круг в акцентной
шкале, светлый силуэт, один различитель. Восьмому различитель дан не на
голове, а на плечах — все причёски уже заняты, а в 56 px новая читалась бы
как край силуэта.

### Один набор на три страницы

Прежнее правило «на каждой странице свои отзывы, релевантные её аудитории»
(Р-106) снято: оно выполнимо только при сочинённых текстах. Настоящие отзывы
пришли от частных клиентов, и разложить их по трём аудиториям, ничего не
выдумав, нельзя. На всех трёх страницах теперь один и тот же набор, заголовок
приведён к «Что говорят клиенты» — прежние «Что говорят компании и
разработчики» стали бы неправдой.

### Проверено

Восемь карточек, ни одна не обрезается по высоте на 1280 и 390 px; обход
карусели по кругу возвращает все восемь имён и замыкается; аватары различимы
в 56 px; перенос макетов воспроизводится, наборы ассетов синхронны.

---

## Р-114 · Политика под счётчик, согласие на cookies, разрешение на публикацию отзыва (13.09.2026)

**Решение: правовые тексты и техника счётчика доведены до состояния, в
котором включение аналитики — это одна переменная, а публикация отзыва с
сайта — отметка самого автора.**

Ограничение «правовые страницы правятся только в палитре и шрифтах» снято
прямым указанием заказчика: тексты готовит исполнитель, юрист их проверяет.
Ограничение на изменение схемы базы снято отдельным ответом на прямой вопрос.

### 1. Реестр операторов

В «Общие положения» добавлен пункт: уведомление направлено в Роскомнадзор
12 сентября 2026 года, сведения об операторе подлежат внесению в реестр.
Закон этого от политики не требует — требование ч. 2 ст. 18.1 152-ФЗ
исчерпывается публикацией самой политики, — но при проверке первым сверяют
именно соответствие текста фактическому положению дел.

### 2. Раздел о веб-аналитике: две редакции в одном макете

Раздел «Файлы cookies и веб-аналитика» существует в двух редакциях, и
выбирает между ними тот же признак, что включает счётчик:
`process.env.NEXT_PUBLIC_METRIKA_ID`. Без номера действует прежний текст
(«cookies не используются, сервисы веб-аналитики не применяются»), с номером
— новый: наименование и реквизиты ООО «ЯНДЕКС», состав собираемых данных,
цель, правовое основание, порядок отзыва согласия, отсутствие трансграничной
передачи и вебвизора. Дата редакции идёт за составом текста: 1.0 от
21 августа против 1.1 от 13 сентября.

Так сделано, потому что альтернативы хуже обеих. Опубликовать новый текст
сразу — значит утверждать про счётчик, которого нет. Оставить старый и
править вручную в день включения — значит однажды забыть и получить
недостоверную политику ровно в тот момент, когда счётчик начал собирать
данные.

В автономном макете `process` не существует, поэтому признак читается через
`typeof process !== 'undefined'`. Проверено, что сервер и браузер выбирают
одну и ту же редакцию: расхождений разметки при оживлении страницы нет.

### 3. Согласие на cookies

Счётчик больше не запускается сам. При включённой аналитике внизу страницы
появляется уведомление с выбором «Разрешить» или «Отказаться»; до выбора со
страниц не уходит ни одного запроса к `mc.yandex.ru`. Выбор хранится в
браузере посетителя под ключом `pd-analytics`, отказ запоминается наравне с
согласием.

Это не дань моде на баннеры. Правовым основанием обработки в новой редакции
политики названо согласие посетителя, выражаемое в этом уведомлении, — без
механизма согласия сам текст политики стал бы неверным.

Уведомление не показывается, пока номер счётчика не задан: без аналитики
спрашивать не о чем.

### 4. Разрешение на публикацию отзыва

В форму отзыва добавлена необязательная отметка «Разрешаю опубликовать отзыв
на сайте без указания имени». Значение уходит в новую колонку
`publishAllowed` и выводится отдельным столбцом в выгрузке реестра.

Миграция — одна строка, `ADD COLUMN ... DEFAULT false`: существующие записи
не трогаются, простоя нет. Хранить разрешение в свободном строковом поле
заявки было бы дешевле, но это правовой признак, и через полгода строка
«публикация разрешена» в поле «Что нужно» читалась бы как ошибка данных.

Отзыв без отметки принимается и остаётся внутренним сигналом: форма не
собирает ни имени, ни контакта (Р-110), поэтому спросить автора задним
числом негде.

### 5. Найдено попутно: модальное окно перекрывалось подвалом

Кнопка «Отправить отзыв» не нажималась: в точке нажатия оказывался подвал.
Секция отзывов и подвал объявлены с одинаковым уровнем наложения `z-index:1`,
а окно с `z-index:40` живёт внутри секции — то есть внутри её контекста
наложения, и выше подвала подняться не могло. Подвал идёт позже в документе
и потому оказывался сверху.

Дефект существовал с Р-110 и не был замечен, потому что кнопка тогда стояла
на 43 пикселя выше и в перекрытие не попадала: добавление отметки сдвинуло её
вниз ровно настолько, чтобы поломка проявилась. Секции отзывов задан уровень
`z-index:20`.

Проверка на «элемент виден» такое не ловит — виден он был всё время. Ловит
только `document.elementFromPoint` в середине кнопки, и эта проверка добавлена
в прогон форм.

### 6. Отзывы от организаций

Отдельный набор отзывов для страницы «Компаниям» не восстанавливается: взять
корпоративные отзывы неоткуда, а сочинить их — вернуться к тому, от чего
ушли в Р-113. Механизм сбора при этом уже настроен: подсказка в форме отзыва
на «Компаниям» просит представиться должностью и отраслью, а не ролью
соискателя. Набор появится, когда наберётся хотя бы три отзыва от юрлиц.

### Проверено

Политика без номера счётчика показывает прежнюю редакцию и версию 1.0, с
номером — новую и версию 1.1; ошибок оживления страницы нет. До выбора в
уведомлении запросов к Яндексу ноль; после отказа — ноль и на других
страницах; после согласия счётчик грузится, уведомление больше не
показывается. Отзыв с отметкой и без неё доходит до базы с верным
`publishAllowed` и пустым контактом; выгрузка показывает столбец «можно
публиковать». Восемь заявочных форм принимаются. axe-core — ноль нарушений
на семи страницах, включая страницы с показанным уведомлением; переполнений
нет на четырёх ширинах.

---

## Р-115 · Страница «адрес не найден», знак сайта, разметка организации (13.09.2026)

**Решение: закрыты три пробела, которые видны только на живом сайте и потому
не попадали ни в один список задач.**

Все три нашлись при финальной сверке перед открытием. Ни один не был заметен
в макетах: страницы 404 в макетах не бывает, знак вкладки в них не виден, а
разметка для поисковика вообще не имеет вида.

### 1. Страница «адрес не найден»

По несуществующему адресу сайт отдавал стандартную страницу Next:
англоязычную, белую, с системным шрифтом — «404: This page could not be
found». На русском сайте научного консалтинга это выглядит как чужая
техническая заглушка.

Сделан макет `design/NotFoundPage.dc.html` из макета заглушки кабинета: та
же шапка, тот же подвал, та же типографика. Второй комплект оформления ради
страницы, которую видят раз в сотню посещений, заводить незачем.

Вместо кнопок «На главную» и «Написать в телеграм» — перечень разделов:
человеку с этой страницы нужен не мессенджер, а тот раздел, в который он не
попал. Ссылка на почту в тексте подчёркнута: без подчёркивания она
отличается от текста абзаца только цветом, а пара `--pd-accent` и
`--pd-ink-secondary` нужного различия не даёт — проверка доступности
отметила это сразу.

Страница закрыта от индексации: индексировать нечего, ходить по ссылкам с
неё поисковику незачем.

### 2. Знак сайта

Иконки не было вовсе: вкладка браузера показывала пустой лист, закладка —
безымянный прямоугольник. Нарисован `icon.svg` — монограмма PD в той же
логике, что подпись в шапке: «P» белым, «D» приглушённым, на акцентном
квадрате. Проверено в 16, 32, 48, 64 и 96 пикселях: в 16 читается.

Из него же собран `apple-icon.png` 180×180 для домашнего экрана — без
скругления, его добавляет сама система, иначе углы срезаются дважды.

### 3. Разметка организации

Добавлен `OrgSchema` — описание организации в JSON-LD: наименование,
реквизиты, адрес, телефон, почта, ссылки на телеграм и ВК. Нужно ровно для
одного: чтобы в карточке организации в выдаче стояли наши сведения, а не
собранные поисковиком по сторонним площадкам. Значения совпадают с подвалом
сайта и с правовыми документами — расхождение здесь поисковик читает как
признак недостоверности, а проверяющий как расхождение сведений.

Услуги и цены в разметке не описаны: цены на сайте не публикуются (Р-03), а
перечислять услуги второй раз в машинном виде значит заводить второй
источник правды рядом со страницами.

**В витрине разметки нет** — и при заданном адресе тоже. Демонстрационный
стенд не должен заявлять организацию с её реквизитами: это второй
«официальный» адрес компании в глазах любого, кто читает разметку.

### Найдено попутно

Приёмочный прогон форм упал восемь раз подряд с «нет ответа». Причина не в
формах: сборка витрины перезаписывает `.next`, и работающий сервер начинает
отдавать таблицу стилей как `text/plain`, а страницы — 500. Порядок проверок
теперь такой: сначала браузерные прогоны на рабочей сборке, витрина
собирается последней.

### Проверено

Страница «адрес не найден» отдаётся с кодом 404, оформлена как остальные
страницы, все ссылки с неё ведут на существующие адреса (200), axe-core без
нарушений на 1280 и 390 px, переполнений нет. Иконки отдаются, монограмма
различима в 16 px. Разметка организации разбирается как JSON и содержит
верные реквизиты; в витрине её нет. Восемь заявочных форм принимаются,
axe-core — ноль нарушений на семи страницах, обе сборки зелёные.

---

## Р-116 · Предрелизный обход выдачи и типографики (13.09.2026)

**Решение: сайт обойдён с точки зрения поисковика и наборщика; найденное
исправлено, проверенное записано.**

Обход механический: для каждой страницы снимаются заголовок, описание,
канонический адрес, обложка ссылки, число заголовков первого уровня, разрывы
уровней, подписи и размеры картинок; отдельно — типографика видимого текста.

### Что оказалось в порядке

Прямых кавычек вместо ёлочек, дефисов вместо тире, двойных пробелов и
пробелов перед знаками препинания на сайте нет ни одного — восемь страниц,
включая правовые. Заголовок первого уровня везде один, разрывов уровней нет,
все картинки с подписями и объявленными размерами, язык страницы объявлен.
Описания уникальны и укладываются в границы выдачи.

Карта сайта в боевом режиме содержит семь страниц и не содержит закрытых:
кабинет и страница «адрес не найден» в неё не попадают. `robots.txt` открыт,
`/api/` запрещён, ссылка на карту стоит. Проверено сборкой с боевыми
переменными: `NEXT_PUBLIC_*` вшиваются на сборке, поэтому иначе проверить
нельзя — стенд остаётся стендом, пока образ не пересобран.

### Что исправлено

**Заголовок оферты** был 74 знака и обрезался в выдаче на середине фразы —
«…договор оказания услуг научного сопро…». Сокращён до 51 знака: «Публичная
оферта ProDisser — договор оказания услуг». Правится только заголовок
вкладки; юридический текст документа не затрагивается.

**Обложка ссылки** отсутствовала у кабинета и у страницы «адрес не найден».
Обе расходятся по мессенджерам не реже прочих: кабинет — как ссылка из
переписки с менеджером, «адрес не найден» — когда кто-то делится битой
ссылкой. Без обложки такая ссылка выглядит чужой.

**Перенос строк** был объявлен точечно и вразнобой: восемь объявлений
`balance` на «Компаниям», восемь `pretty` на «Аспирантам», на правовых
страницах — ни одного. Введено одно правило на все девять макетов:
заголовки первого-третьего уровня выравниваются по строкам, абзацы и пункты
списков не оставляют одинокого слова в последней строке. Точечные объявления
остаются в силе там, где они заданы прямо в элементе, — инлайновое правило
сильнее.

### Что осталось сознательно

Заголовок посадочной — 66 знаков, на грани. Сокращать его значит вычёркивать
одну из трёх аудиторий: «аспирантов, студентов и компаний» — это и есть
предмет страницы. Яндекс, основной поисковик для этого сайта, показывает до
семидесяти с лишним знаков; шесть знаков не стоят потери охвата.

Висячие предлоги в конце строк — 1176 мест по сайту. Расставлять неразрывные
пробелы вручную по такому числу мест бессмысленно, а делать это переносом из
макетов значит учить конвертер разбирать русский текст: цена сильно выше
пользы. Правило `text-wrap: pretty` снимает худшую часть — одинокое слово в
последней строке абзаца.

### Проверено

Восемь страниц обойдены заново: замечаний по выдаче не осталось, кроме
сознательно оставленного заголовка посадочной. axe-core — ноль нарушений на
семи страницах, переполнений нет, формы принимаются, обе сборки зелёные.
Заголовки после смены правила переноса сняты на 1280 и 390 px: числа строк
прежние, раскладка не изменилась.

---

## Р-117 · Домен куплен; виртуальный хостинг заменяется облачным сервером (13.09.2026)

**Решение: домен `prodisser.ru` принят как есть; купленный виртуальный
хостинг для проекта не годится, берётся VPS у того же провайдера.**

### Домен

`prodisser.ru` зарегистрирован у Рег.ру до 31.08.2027, автопродление
включено, зона обслуживается серверами регистратора. Требования соблюдены:
регистратор российский, домен в зоне `.ru`.

Открытый пункт: администратор домена не прошёл идентификацию через Госуслуги.
С 1 сентября 2026 года операции с доменами `.ru`, `.рф` и `.su` без неё
ограничиваются, а нам предстоит менять записи зоны при переезде на сервер.
Действие заказчика, делается в кабинете регистратора.

### Почему виртуальный хостинг не подходит

Куплен тариф Host-0 с панелью ISPmanager — общая среда, где сайты соседствуют
на одной машине. Проекту нужны четыре вещи, которых там нет.

1. **PostgreSQL.** На виртуальном хостинге Рег.ру доступна MySQL. Перевод
   схемы и миграций на MySQL — не настройка, а переписывание слоя данных
   вместе с журналом согласий по 152-ФЗ.
2. **Постоянный процесс приложения.** Сайт — не набор файлов: это работающий
   сервер Node, принимающий заявки. Общая среда рассчитана на другое.
3. **Свои заголовки безопасности.** Политика содержимого, HSTS, запрет
   встраивания в чужую страницу задаются конфигурацией веб-сервера. В общей
   среде её нет, а без этих заголовков сайт с формами открывать нельзя.
4. **Docker.** Развёртывание, обновление и резервные копии описаны через
   него; без него весь порядок из `DEPLOY.md` пришлось бы писать заново под
   конкретную панель.

Оценка переделки под виртуальный хостинг — несколько дней работы, потеря
контроля над заголовками и резервными копиями, и постоянные ограничения
впредь. Облачный сервер — несколько сотен рублей в месяц и ни одной правки
в коде.

### Что берётся вместо

Облачный сервер Рег.ру: 2 vCPU, 2 ГБ памяти, 40 ГБ диска, Ubuntu 24.04,
регион Россия. Провайдер тот же, что у домена, — он в реестре
хостинг-провайдеров Роскомнадзора, серверы в России; оба обязательных
условия из `DEPLOY.md`, раздел 1, выполнены. Размер выбран по замерам
работающего проекта, а не на глаз.

Купленный Host-0 возвращается в течение четырнадцати дней либо остаётся под
почту на домене — почта на нём работает независимо от того, где стоит сайт.

### Порядок с записями зоны

При переезде меняются только записи `A` для `prodisser.ru` и `www`. Записи
`MX` не трогаются: почта продолжает работать на серверах регистратора, пока
указывает на них. Записи вносит заказчик в своей панели — доступ к домену
остаётся только у него.

### Ограничение среды исполнителя

Из среды, где ведётся разработка, наружу нет сетевого доступа: подключиться
к серверу по SSH нельзя. Развёртывание идёт передачей команд блоками —
исполнитель готовит блок, заказчик выполняет и присылает вывод. Порядок и
сами команды уже описаны в `HOSTING.md` и `DEPLOY.md`, разбирать их по ходу
не придётся.

### Отдельно: пароль в переписке

Пароль от панели управления хостингом был прислан в переписку и считается
скомпрометированным независимо от того, кто её читает. Заказчику сказано
сменить его немедленно. Правило на будущее: доступы передаются одноразовым
сообщением или через хранилище паролей, а после настройки сервера меняются.

---

## Р-118 · Образ не собирался на чистой машине; сборка образа добавлена в CI (13.09.2026)

**Решение: стадия зависимостей получает схему Prisma до установки; сборка
образа проверяется в CI, а не на боевом сервере.**

При первом развёртывании на сервере сборка упала на третьей минуте:

```
> prisma generate
Error: Could not find Prisma Schema that is required for this command.
npm error command sh -c prisma generate
failed to solve: process "/bin/sh -c npm ci" did not complete successfully
```

### Что произошло

Стадия `deps` копировала только `package.json` и `package-lock.json`, после
чего запускала `npm ci`. В `package.json` есть `postinstall` с
`prisma generate`, а схемы в этом слое нет — её копируют позже, на стадии
сборки. Prisma 7 в такой ситуации не предупреждает, а завершает установку с
ошибкой, и весь образ не собирается.

Исправление в две строки: `prisma.config.ts` и каталог `prisma` копируются
вместе с манифестами, до `npm ci`. Проверено повторением того же набора
файлов в чистом каталоге — установка проходит, ноль уязвимостей.

### Почему это не поймали раньше

Ни одна проверка не собирала образ. CI ставит зависимости в готовом дереве
исходников, где схема лежит на месте, и сборка приложения проходит. Разница
между «собрать приложение» и «собрать образ» здесь принципиальная: во втором
случае каждый слой видит ровно то, что в него скопировали, и нехватка файла
проявляется сразу.

Поэтому в CI добавлен шаг сборки образа. Он длиннее прочих на несколько
минут, и эти минуты окупаются: дефект такого рода иначе всплывает в день
открытия сайта, на чужой машине, при заказчике.

### Порядок исправления

Правка внесена в репозиторий, слита в `main` и подтянута на сервер
`git pull` — файлы на сервере не правились. Это то же правило, что записано
в `RELEASE.md`: сайт собирается из репозитория, правка на сервере теряется
при первом обновлении.

---

## Р-119 · HTTP/2 в конфигурации nginx: форма, понятная nginx 1.24 (13.09.2026)

**Решение: `listen 443 ssl http2;` вместо отдельной директивы `http2 on;`.**

На боевом сервере переход с временной конфигурации на рабочую упал:

```
[emerg] unknown directive "http2" in /etc/nginx/sites-enabled/prodisser:22
```

Отдельная директива `http2 on;` появилась в nginx 1.25.1. В Ubuntu 24.04
штатный пакет — 1.24, и на нём конфигурация не проходит проверку: сайт не
поднимается вовсе, а на 80-м порту остаётся временная настройка для выпуска
сертификата.

Возвращена форма `listen 443 ssl http2;` — её понимают обе ветки. В 1.25+ она
объявлена устаревшей и печатает предупреждение, но работает. Это дешевле, чем
требовать от площадки конкретную минорную версию nginx или подключать
сторонний репозиторий на боевом сервере ради одной строки.

Проверено на сервере: `nginx -t` проходит, `http://` и `www` отдают 301,
основной адрес отвечает по HTTP/2.

---

## Р-120 · Очистка заявок по сроку хранения не работала: кавычки терялись по дороге (13.09.2026)

**Решение: запрос уходит в psql через ввод, а не параметром `-c`.**

Первый же запуск `retention.sh` на сервере закончился ошибкой:

```
ERROR:  relation "lead" does not exist
LINE 1: SELECT count(*) FROM Lead WHERE status <> 'CONTRACTED' AND c...
2026-09-13T23:00:34+00:00 ОШИБКА: база не ответила числом, удаление не выполнялось
```

### Что произошло

Запрос проходил три уровня кавычек: оболочка хоста, оболочка внутри
контейнера, затем psql. Двойные кавычки вокруг `"Lead"` терялись по дороге —
в сообщении об ошибке имя таблицы уже без них. PostgreSQL приводит
идентификатор без кавычек к нижнему регистру, а Prisma создаёт таблицы с
заглавной буквы, и `lead` не находится.

Экранировать четвёртый раз — путь к следующей такой же ошибке. Запрос
передаётся в psql через стандартный ввод: кавычки при этом никем не
интерпретируются. Проверено дважды — на выводе того, что реально доезжает до
psql, и на настоящей базе: запрос отвечает числом.

### Чем это было опасно

Скрипт исполняет п. 7.1 политики: заявки без заключённого договора хранятся
не более года. Молча он не падал — писал в журнал «ОШИБКА» и выходил с
ненулевым кодом, — но заявки не удалялись бы. Обещание в тексте политики без
исполнения в коде проверяется одним запросом к базе, и расхождение видно
сразу.

Оба этих дефекта — Р-119 и Р-120 — обнаружились в первый час работы боевого
сервера и ни один не мог быть найден раньше: конфигурация nginx и скрипты
расписаний не выполняются ни в сборке, ни в проверках. Это цена того, что
развёртывание делается один раз.

---

## Р-121 · Сайт открыт: prodisser.ru (13.09.2026)

**Решение: работа принята заказчиком, сайт работает на боевом адресе.**

Развёртывание выполнено за один заход передачей команд блоками: из среды
разработки нет сетевого доступа наружу, поэтому команды готовил исполнитель,
выполнял заказчик, вывод разбирался на месте.

### Что стоит

| Слой | Как сделано |
|---|---|
| Домен | `prodisser.ru` у Рег.ру до 31.08.2027, автопродление; зона на серверах регистратора, записи `A` на сервер, `AAAA` для `@` и `www` сняты |
| Сервер | Timeweb Cloud, 2 vCPU / 2 ГБ / 40 ГБ, Ubuntu 24.04.5, Россия; файл подкачки 2 ГБ, межсетевой экран на три порта |
| Приложение и база | Docker Compose: приложение слушает только `127.0.0.1:3000`, PostgreSQL наружу не выставлена |
| Сертификат | Let's Encrypt до 12.12.2026, выпуск `--webroot`, продление настроено пакетом certbot |
| Расписания | ночная копия базы в 03:00, очистка заявок с истёкшим сроком хранения по понедельникам в 03:30 |

### Проверка после открытия

`http://` и `www` отдают 301 на основной адрес, соединение по HTTP/2.
`/api/health` — `{"ok":true,"db":true}`. `robots.txt` открыт с запретом
`/api/` и ссылкой на карту; карта содержит семь страниц с настоящим доменом.
Отдаются все четыре заголовка безопасности, включая политику содержимого и
`Strict-Transport-Security` на год. `/_next/image` — 404: оптимизатор
изображений выключен намеренно (Р-88). Несуществующий адрес — 404 с нашей
страницей (Р-115).

Заявки с двух форм посадочной дошли с `consentGiven` и `termsAccepted`;
отзыв дошёл с `publishAllowed` и без согласия — так и задумано, форма отзыва
персональных данных не собирает (Р-110, Р-114). **В записях сохранён
настоящий адрес посетителя**, а не адрес прокси: заголовок от nginx
разбирается верно, и журнал согласий не пустой.

### Что осталось и почему это не блокировало открытие

Почта `info@prodisser.ru` не заведена: заявки сохраняются в базу, но письма
и дублирующий канал не работают. Это единственный оставшийся пункт
критического пути; он не мешает сайту работать, но мешает узнавать о заявках
вовремя, поэтому закрывается первым.

Три дефекта, найденных при развёртывании, исправлены в тот же час: сборка
образа на чистой машине (Р-118), директива HTTP/2 для nginx 1.24 (Р-119),
кавычки в запросе очистки (Р-120). Ни один не мог быть найден раньше —
конфигурация nginx, скрипты расписаний и сборка образа до этого дня не
выполнялись ни в одной проверке. Сборка образа с тех пор идёт в CI.

---

## Р-122 · Личный кабинет разворачивается внутри этого репозитория (15.09.2026)

**Решение: закрытый контур ProDisser — кабинет клиента и кабинет
руководителя — строится как раздел `/cabinet` этого сайта, а не отдельным
приложением.**

Постановка пришла отдельным комплектом документов (бриф PD-LK-BRIEF-001,
функционал PD-LK-FUNC-002, прототип кабинета клиента) и предполагала
разработку с нуля в репозитории `Scientific-Consulting-Service`. Разбор
показал, что этот репозиторий содержит Telegram-агента SMM и к кабинету
отношения не имеет, а комплект местами описывает состояние проекта до
принятых здесь решений.

### Что из постановки не принято и почему

| Позиция брифа | Принято вместо неё |
|---|---|
| Дизайн-система v1.0: лаванда `#5B5BD6`, мята `#48DCA1`, гарнитура Onest, элемент «ось исследования» | Действующая система сайта: `#14417A`, Literata + Inter + JetBrains Mono. Onest снят решением Р-101, «оси исследования» в системе нет. Кабинет открывается по кнопке с посадочной страницы — расхождение обликов было бы заметно на первом же переходе. Из прототипа берётся композиция экранов: трекер этапов, блок «сейчас от вас требуется», лента событий, список версий |
| Стек на выбор исполнителя, ориентир на серверный рендеринг и PostgreSQL | Стек сайта без изменений: Next.js 16, React 19, TypeScript 7, Prisma 7, PostgreSQL 16. Второй язык означал бы вторую схему доступа к данным и невозможность одной транзакцией связать заявку с сайта и проект кабинета |
| Два канала переписки: «клиент — эксперт» и «клиент — менеджер» | Один канал: клиент — менеджер. Эксперт высказывается комментариями к версиям после модерации. Это усиливает барьер против переманивания, ради которого закрываются контакты, и снимает противоречие с заданием `docs/briefs/ПРОМТ-личный-кабинет.md`, где чата нет вовсе |
| Вход по коду из письма (артборд 01 задания) | Одноразовая ссылка. Пароля нет ни в одном варианте; ссылка избавляет от ручного ввода и от отдельного экрана подтверждения |
| Механика предоплаченного баланса у конкурента | Не применяется. Оплата — транши по договору со статусом и закрывающими документами |

### Модель данных

Схема расширена на 26 моделей. Существующие `Lead` и `Delivery` по составу
полей не изменены: на них держится журнал согласий и замороженные формы
сайта. Добавлены только связь `Lead → Project` и причина отказа, видимая
заявителю (в отличие от `notes`, которая остаётся внутренней).

Отдельной сущности «Заявка» не заводится. Заявка с сайта уже приходит в
`Lead`, и её жизненный цикл выражается существующим `LeadStatus`:
`NEW → IN_PROGRESS → CONTRACTED` либо `DECLINED`. Заявки не удаляются
физически никогда.

Три сквозных правила схемы:

- **Деньги — в копейках целым числом.** Тип с плавающей точкой к деньгам не
  применяется нигде.
- **Версии материалов неизменяемы.** В слое доступа к данным нет функций
  обновления и удаления `MaterialVersion`: только вставка и чтение. Удаление
  материала — это `deletedAt`, объект в хранилище сохраняется.
- **Маржа не хранится.** Она считается как сумма договора за вычетом
  начислений эксперту. У исторических строк, где исполнитель не указан,
  начислений нет, и маржа сама собой равна сумме договора — отдельной ветви
  в коде не потребовалось.

Код проекта `PD-ГГГГ-ННН` выдаётся транзакционным инкрементом счётчика
`ProjectCodeCounter`, а не как `max(code) + 1`: два менеджера, одобряющих
заявки одновременно, не получат один код.

### Права

Критерий приёмки 10.4 брифа требует, чтобы права действовали на уровне
сервера, а не интерфейса. `app/src/lib/cabinet/access.ts` — единственная
точка принятия решения, три механизма по числу типов ограничений в матрице:

1. **Действие** — `can` и `ensure`. Ни один обработчик не проверяет роль
   в своём теле.
2. **Строки** — `scopeProjects` и соседи возвращают фрагмент условия выборки.
   Списки, реестры, выгрузки и аналитика строятся только через них, поэтому
   правило «эксперт видит только назначенные проекты» невозможно забыть
   в отдельно написанном перечне.
3. **Поля** — `presentProject` собирает представление под роль. Контакты
   клиента, начисления и маржа не попадают в объект, который получает
   разметка. Показать телефон нельзя не потому, что он скрыт стилями,
   а потому, что такого свойства у объекта нет.

Договор поручения обработки персональных данных (ч. 3 ст. 6 152-ФЗ) —
техническое условие, а не бумага: у эксперта без заполненного `ndaSignedAt`
выборка проектов пуста, хотя назначение на проект есть.

Матрица закреплена таблицей в `app/tests/access-matrix.test.ts`, буквально
повторяющей матрицу PD-LK-FUNC-002: 45 проверок по четырём ролям, трём
отношениям к объекту и двум нерабочим состояниям учётной записи.
Расхождение кода с матрицей валит сборку.

### Проверки

В проекте до сих пор не было автоматических тестов — приёмка велась вручную
и инструментально. Матрица прав проверяется машиной: перечислить руками
27 действий × 4 роли при каждой правке нереально, а цена ошибки — контакты
клиента у эксперта или маржа у менеджера.

Взят встроенный `node --test` с разбором типов средствами Node 22: ни одной
новой зависимости, стиль повторяет проверки `verify/*.check.js`, уже
принятые в соседнем проекте панели. Шаг `npm test` добавлен в CI сразу после
проверки типов.

### Конвейер вёрстки

Правило «правится макет, а не компонент» сохраняется для девяти страниц
сайта без изменений. Компоненты кабинета пишутся вручную и лежат в
`app/src/components/cabinet/` — каталоге, который `tools/dc-to-tsx.mjs`
не порождает и который не участвует в сверке `git diff` в CI.

Основание: генератор рассчитан на статичные страницы, ставит `@ts-nocheck`
и не знает ни типизированных свойств, ни серверных компонентов, ни выборок
из базы. Экраны кабинета живые. Дорабатывать генератор ради этого — значит
трогать код, от которого зависят девять работающих страниц, ради задачи,
которую он не решает по устройству. Артборды `.dc.html` при этом остаются:
по ним принимается облик каждого экрана.

### Расхождения в исходных данных, оставленные открытыми

Прямой разбор книги заказов `Отработка заказов.xlsx` (55 записей) подтвердил
контрольные суммы брифа: законтрактовано 5 425 000 ₽, получено 4 370 000 ₽,
остаток 1 055 000 ₽. Три величины не подтвердились:

1. **Потери.** Бриф называет 365 000 ₽. По остановленным работам
   недополучено 125 000 ₽. Ту же величину даёт независимый автотест
   соседнего проекта панели. До решения заказчика витрина считает по факту.
2. **Ключ сведения строк.** Функционал предписывает сверку по коду заказа,
   но колонки с кодом в файле нет. Принят естественный ключ: дата, ФИО, тип
   работы, сумма; код присваивается при первой фиксации.
3. **Заливка ячеек.** Зелёная заливка не означает «выполнено»: девять
   зелёных строк имеют статус «в работе». Ведущим признаком принят
   нормализованный текст статуса, заливка — уточняющим; расхождение
   выводится в отчёт предпросмотра, а не разрешается молча.

---

## Р-123 · Экраны кабинета собраны; заглушка раздела снята (16.09.2026)

**Решение: `/cabinet` перестал быть страницей «Раздел готовится» и стал
работающим закрытым контуром.**

### Что появилось

| Маршрут | Экран |
|---|---|
| `/cabinet` | Вход по одноразовой ссылке |
| `/cabinet/enter/<токен>` | Погашение ссылки и открытие сессии |
| `/cabinet/exit` | Выход с отзывом сессии на сервере |
| `/cabinet/projects` | Мои работы: блок «сейчас от вас требуется», карточки проектов с трекером этапов |
| `/cabinet/projects/<код>` | Проект: этапы, лента событий, кто ведёт проект, назначение эксперта |
| `/cabinet/stages/<id>` | Этап: состояние, причина остановки, материалы с версиями, комментарии, согласование |
| `/cabinet/files/<версия>` | Выдача файла с записью в журнал доступа |
| `/cabinet/manage` | Очередь заявок с одобрением и отклонением |

### Заглушка снята вместе с макетом

Макет `design/CabinetPage.dc.html`, порождённый из него компонент и запись
о нём в переносе CI удалены. Держать макет со словами «Раздел готовится»
рядом с работающим разделом незачем: он не был бы виден ни по одному адресу,
а перенос продолжал бы его проверять. Маршрут `/cabinet` сохранил
`noindex`: за входом лежат персональные данные, а форма входа поисковику
бесполезна.

### Как это устроено

Компоненты кабинета написаны вручную и лежат в `app/src/components/cabinet/`
— каталоге, который генератор не порождает и который не участвует в сверке
`git diff` (Р-122). Правило «правится макет, а не компонент» для девяти —
теперь восьми — страниц сайта не изменилось.

Токены, гарнитуры, радиусы и тени взяты из дизайн-системы сайта без
единого нового значения: тот же блок из 27 переменных, Literata на
заголовках, Inter в тексте, JetBrains Mono на служебных метках, акцент
`#14417A`. Общие правила подключаются один раз в разметке раздела, а не
в каждом экране.

Формы работают без единой строки клиентского кода: обычные `<form>` и
серверные действия. Это не аскетизм, а следствие устройства — экран,
который не зависит от выполнения сценариев в браузере, работает и при
медленной сети, и с клавиатуры, и в читалке.

### Что проверено в браузере

Сквозной сценарий пройден на поднятом стенде под тремя учётными записями:

- менеджер входит по одноразовой ссылке, видит заявку с сайта в очереди и
  одобряет её — создаётся проект `PD-2026-001`;
- менеджер заводит этап, назначает эксперта, переводит этап в работу;
- эксперт загружает версию v1 и оставляет комментарий; свой непубликованный
  комментарий он видит помеченным;
- **клиент этого комментария не видит** до решения менеджера — проверено
  на живом экране, а не только выборкой;
- менеджер публикует, клиент видит комментарий и загружает версию v2;
  история версий сохраняется;
- этап уходит на согласование, у клиента он появляется в блоке «сейчас от
  вас требуется», клиент согласует — этап переходит в «завершён», и в
  истории переходов автором записан клиент;
- клиент по адресу очереди заявок оказывается на своих работах;
- **в разметке проекта у клиента и у эксперта нет ни адреса, ни телефона
  клиента**, у менеджера — есть. Контакты не скрыты стилями: их нет
  в объекте, который получает разметка;
- при ширине окна 400 px горизонтального переполнения нет.

### Стенд

Добавлены две команды: `npm run seed:cabinet` наполняет стенд ролями,
справочником типов сопровождения и заявкой в очереди, `npm run issue:link`
выдаёт ссылку входа по адресу почты. Обе отказываются работать с базой,
не похожей на локальную, без явного согласия: наполнять боевую базу
демонстрационными данными нельзя даже по ошибке.

### Мелочи, которые видны только на экране

Кнопка выбора файла оформлена правилом `::file-selector-button` — её текст
задаёт браузер по своей локали и разметкой не управляется, поэтому подпись
к полю стоит рядом. Форма комментария оставлена только у текущей версии:
под каждой прежней она превращала экран в анкету. Числительные в подписи
«N из M этапов» согласуются по правилам языка, а не приписыванием окончания.

---

## Р-124 · Кабинет: переписка, уведомления, операционный контур (16.09.2026)

**Решение: закрытый контур получил канал связи, очередь уведомлений и
инструменты, которыми менеджер ведёт работу изо дня в день.**

### Устранён дефект предыдущей работы

Навигация клиента вела на `/cabinet/request`, а маршрута не было: переход
давал 404. Экран новой заявки собран и пишет в ту же таблицу `Lead`, что и
обращения с сайта, с пометкой `source = "cabinet"`. Публичная схема
валидации при этом не используется: состав её полей заморожен, на нём
держится журнал согласий сайта. Заявитель здесь уже известен по сессии,
согласие принято при первом входе, и спрашивать контакт заново незачем.

### Канал связи — один

Переписка ведётся только между клиентом и менеджером. Эксперт в канал не
входит: его замечания доходят до клиента комментариями к версиям после
модерации. Это и есть технический барьер против переманивания, ради
которого закрываются контакты; канал «клиент — эксперт» его бы обнулил.

Сообщения с телефоном, адресом почты, именем в мессенджере или ссылкой на
мессенджер помечаются, но **не блокируются**. Запрет породил бы обход
(«продолжим там, где начинали»), а пометка даёт менеджеру повод вмешаться.
Пометку видит только менеджер — клиенту знать о ней незачем.

Детектор отделяет контакты от рабочего текста: «в п. 2.3», «ГОСТ Р
58048-2017», «240 000 рублей», «PD-2026-014», «02.09.2027» и диапазоны
страниц проверку проходят, а «+7 999 000-00-00» и «@ivan_petrov» — нет.
Обе стороны закреплены тестами: перечень, который ловит лишнее, приучают
обходить, а не соблюдать.

Непрочитанное считается по отметке на самом сообщении, а не по паре
«сообщение — читатель»: в канале ровно две стороны, различать внутри
стороны нечего. Открытие экрана и есть прочтение.

### Очередь уведомлений

Событие бизнес-логики не отправляет письмо, а кладёт строку в очередь — и
делает это **в той же транзакции**, что и само изменение. Отсюда три
свойства: недоступная почта не откатывает перевод этапа; уведомление не
теряется при падении процесса; недоставленное видно, а не растворяется.

Проверено на стенде, где почта не настроена: три записи не отправились, не
пропали и получили отложенный повтор с причиной отказа. После пяти попыток
запись помечается неудачной, а не перебирается вечно.

Ключ дедупликации делает повторную постановку безвредной — напоминание о
сроке не приходит дважды за сутки. Каналы выбирает получатель; выключенный
канал записи не порождает, обезличенной учётной записи уведомления не
ставятся вовсе.

Рассылку выполняет маршрут, вызываемый по расписанию раз в минуту, рядом с
ночной копией базы и еженедельной очисткой заявок. Отдельного демона не
заводится: при семи типах событий это компонент «на вырост». Маршрут закрыт
общим секретом, а не сессией — у расписания учётной записи нет.

Обращения с сайта в очередь не попадают: их доставляет уже работающий
`notify.ts`, и второе уведомление о том же было бы дублем.

### Telegram

Ссылка привязки устроена как ссылка входа, но с другим назначением, и
назначение проверяется при погашении: перейдя по ссылке привязки в браузере,
войти нельзя; погасив её в боте, нельзя получить сессию. Разделение здесь не
формальность — ссылка уходит в мессенджер, где её видит и пересылает кто
угодно. Потребовалось поле `purpose` в таблице токенов и миграция.

Один аккаунт Telegram — одна учётная запись: при повторной привязке прежняя
снимается, иначе уведомления уходили бы двоим.

Содержание переписки во внешние каналы не уходит: уведомление сообщает
повод заглянуть в кабинет, а не пересказывает сообщение.

### Операционный контур менеджера

Светофор по срокам — три полосы, каждая отвечает на свой вопрос: что уже
сорвано, что сорвётся на этой неделе и где работа стоит из-за клиента дольше
двух недель. Третья полоса нужна отдельно: просрочки там может ещё не быть,
а проект уже не движется.

Реестры клиентов и экспертов; у экспертов отдельным столбцом — договор
поручения обработки персональных данных, потому что без него доступ к
материалам клиента не выдаётся. Там же сводка сообщений с признаком
передачи контактов.

### Терминология

Перечень запрещённых формулировок вынесен в `text-guard.ts` и проверяется
тестом, который проходит по всем исходникам кабинета — экранам, службам,
текстам писем. Перечень намеренно узкий: широкий ловил бы законные обороты
вроде «работа эксперта».

Граница слова в нём задана просмотром назад и вперёд, а не `\b`: в
JavaScript граница слова опирается на латиницу и перед кириллическим словом
не срабатывает вовсе. Первая редакция перечня из-за этого пропускала «под
ключ» и «рерайт» — поймано тестом до того, как попало в ветку.

### Проверено

110 проверок, из них 37 на настоящей базе. Сквозной прогон в браузере:
заявка из кабинета ставит два уведомления менеджерам; сообщение с телефоном
помечается и пометка видна только менеджеру; эксперт на экране переписки
получает 404; перевод этапа в «ждём клиента» ставит уведомление клиенту;
выключение канала в настройках сохраняется; реестр показывает контакты
менеджеру; при ширине 400 px переполнения нет ни на одном новом экране.

---

## Р-125 · Заявки выгружаются в таблицу; файл с персональными данными не покидает контур (16.09.2026)

**Решение: `app/scripts/export-leads.mjs` по образцу выгрузки отзывов, но с
прямым запретом класть результат в облако.**

Почта и Telegram не подключены: заявка сохраняется в базу и больше нигде не
видна. Единственный способ прочитать её — зайти на сервер и написать запрос
на SQL. Это работает ровно один раз, после чего заказчик перестаёт смотреть
заявки вовсе.

Выгрузка повторяет устройство `export-reviews.mjs`: метка BOM и перевод
строк CRLF (иначе таблицы на Windows показывают кириллицу вопросительными
знаками), экранирование по RFC 4180, режимы `--stdout` и «в указанный файл»,
по умолчанию `deploy/exports/leads.csv`. Двадцать столбцов, среди них журнал
согласий: отметка согласия с редакцией текста, акцепт оферты, согласие на
рассылку — то, что предъявляется проверяющему.

Отзывы из выгрузки исключены (`NOT: { form: 'review' }`): у них своя
выгрузка и свой порядок работы (Р-111), а смешивать реестр публикаций с
реестром обращений незачем.

### Чем эта выгрузка отличается от выгрузки отзывов

Отзывы персональных данных не содержат по устройству формы (Р-110), и файл с
ними разрешено вести на облачном диске. **Здесь наоборот: в файле имя,
контакт, организация и текст обращения.** Загрузка такого файла на внешний
сервис — трансграничная передача, которой Политика не допускает, и
обработка за пределами базы на территории России вопреки ч. 5 ст. 18
152-ФЗ. Предупреждение стоит в трёх местах: в шапке скрипта, в выводе после
записи файла и в `DEPLOY.md`. Одного места мало: шапку читает тот, кто
правит скрипт, вывод видит тот, кто запускает, документацию — тот, кто
ищет, как это делается.

Мера временная. Появится почта — заявки будут приходить письмом, и
заглядывать в базу не придётся; выгрузка останется как способ получить
реестр целиком, а не по одному письму.

Проверено на базе с 407 записями: 389 заявок в файле, 18 отзывов отброшены;
все строки разбираются в двадцать ячеек, кириллица и переводы строк внутри
сообщений не ломают разметку.

---

## Р-126 · Витрина перестала собираться после кабинета: раздел убирается вместе с серверной частью (16.09.2026)

**Решение: `src/app/cabinet` убирается на время сборки витрины — там же, где
убираются обработчик заявки, `robots.ts` и `sitemap.ts`.**

Проверка на ветке `main` красная с момента слияния контура кабинета:

```
Error: Failed to collect page data for /cabinet/enter/[token]
Error: Command failed: npx next build
```

Витрина — статический вывод: страницы собираются заранее, сервера нет
(Р-32). Кабинет устроен ровно наоборот: вход по одноразовой ссылке,
обработчики выхода и выдачи файлов, запросы к базе, `dynamic =
'force-dynamic'` на каждом экране. Статический вывод такой раздел не
собирает вовсе — сборка падает на первом же обработчике.

Раньше по адресу `/cabinet` лежала страница из макета, и вопрос не
возникал: витрина собирала обычную страницу-заглушку. Макет удалён вместе с
заглушкой, его место занял настоящий раздел — и то же место в скрипте
витрины осталось незаполненным. Дефект пришёл не из правки витрины, а из
того, что у неё не спросили.

Кабинет убран из перечня маршрутов, которым дописывается префикс подпапки:
в витрине его нет, ссылка с посадочной ведёт на страницу «адрес не найден»
стенда. Витрина показывает публичные страницы; закрытый контур в
демонстрации без сервера смысла не имеет и работать там не может.

Проверено: `npm run build:preview` собирает одиннадцать страниц, `npm run
build` проходит, перенос макетов чист.

---

## Р-127 · Проверка резервной копии выполняется скриптом, а не глазами (16.09.2026)

**Решение: `deploy/restore-check.sh` — восстановление последней копии во
временную базу со сверкой состава и удалением временной базы.**

В списке проверки после открытия стоял пункт «копия восстанавливается на
тестовой базе», и рядом лежала команда на четыре строки с двумя вложенными
вызовами psql. Пункт остался невыполненным: такую команду набирают один раз
и больше не возвращаются к ней, а копия без проверки — предположение, а не
факт. Файл на диске выглядит одинаково и когда дамп полон, и когда он снят с
пустой базы или оборван; `backup.sh` ловит только отказ `pg_dump` и архив
меньше килобайта.

### Что проверяется

Копия распаковывается и накатывается с `ON_ERROR_STOP`, поэтому обрыв на
середине останавливает проверку, а не проходит незамеченным. Дальше три
числа: есть ли таблица заявок и перечень применённых миграций, сколько в
копии заявок, сколько их в боевой базе сейчас. В журнал уходят числа, а не
слово «успешно»: по строке видно, что именно восстановилось.

Заявок в боевой базе обычно больше — они прибывают между снятием копии и
проверкой. Сравнение поэтому одностороннее: меньше заявок, чем в копии,
означает либо оборванный дамп, либо удаление записей, и это предупреждение.

### Чем это опасно, если сделать небрежно

Проверка работает рядом с боевой базой, и две вещи сделаны нарочно. Имя
временной базы содержит отметку времени, а совпадение с боевой проверяется
до начала работы и прекращает её. Удаление временной базы висит на `trap` и
срабатывает при любом исходе, включая падение: иначе на сервере копились бы
базы с персональными данными, вне срока хранения и вне всякого учёта.

Запросы уходят в psql через ввод, а не параметром `-c`, а имя временной базы
передаётся переменной окружения контейнера — по той же причине, по которой
переписан `retention.sh`: кавычки вокруг `"Lead"` теряются между оболочками
(Р-120).

Наличие таблиц проверяется запросом к перечню `information_schema.tables`, а
не попыткой сосчитать в них строки: запрос к несуществующей таблице падает, и
вместо внятного «в копии нет заявок» в журнал попадала бы ошибка синтаксиса.
Первая редакция так и делала — поймано на дампе пустой базы.

### Проверено

Прогон на кластере PostgreSQL 16 с копией настоящей структуры, 407 заявок:
проверка проходит, временная база удаляется. Оборванная копия (первые четыре
килобайта архива) — остановка с ненулевым кодом на ошибке восстановления.
Копия пустой базы — «в копии нет таблицы заявок или перечня миграций».
Несуществующий файл — «копия не найдена». После каждого из четырёх прогонов
временных баз в кластере не остаётся.

Попутно выяснилось, что дамп снимается с владельцами объектов: восстановление
на другой машине требует роли с тем же именем либо ключа `--no-owner`. Внутри
своего сервера вопрос не возникает, но в `DEPLOY.md` это записано — иначе
выяснится в тот единственный момент, когда копия понадобится по-настоящему.

---

## Р-128 · Развёртывание не знало о кабинете: переменные, том материалов, копия файлов (16.09.2026)

**Решение: `deploy/docker-compose.yml` передаёт приложению переменные
кабинета и монтирует каталог материалов на хост; `backup.sh` архивирует этот
каталог рядом с дампом.**

Контур кабинета влит в `main` (Р-122—Р-124), но развёртывание осталось
прежним. Разбор перед выкатом показал три пробела — каждый из них проявился
бы уже на боевом сервере.

### Что было не так

**Переменных кабинета не было в описании сервиса.** Приложение получало
одиннадцать переменных, кабинету нужны ещё пять. Без `SESSION_SECRET` модуль
свёрток бросает исключение при первом же обращении — раздел отвечал бы
пятисоткой, тогда как публичные страницы работают. Проверка ленивая, при
запросе, а не при старте: развёртывание выглядело бы успешным.

Секрет объявлен обязательным (`:?`), остальные четыре — с пустым значением
по умолчанию. Обоснование в разнице последствий: без секрета кабинет
недоступен целиком, а раздел открыт ссылкой с посадочной; без адреса бота
или секрета вебхука не работает один канал уведомлений. Отказ развернуть с
понятным текстом дешевле, чем молчаливая пятисотка в разделе, куда ведёт
кнопка с главной страницы.

**Материалы работ лежали внутри контейнера.** `CABINET_STORAGE_DIR` в
образце указывал `/app/storage` — путь внутри контейнера, без тома. При
первой же пересборке образа (`up -d --build`, то есть при каждом обновлении
сайта) файлы работ исчезли бы. Каталог вынесен на хост, `deploy/storage`, и
закрыт от индекса git: там персональные данные.

**Резервная копия не охватывала материалы.** `backup.sh` снимает дамп базы;
файлы работ в базе не лежат. Восстановление дало бы кабинет, где ссылки на
версии есть, а самих файлов нет. Каталог архивируется той же меткой времени,
что и дамп, и удаляется по тому же сроку в тридцать дней: дамп и материалы
восстанавливаются парой.

### Проверено

Описание сервисов разбирается как YAML, переменные и том на месте. `backup.sh`
прогнан дважды: без каталога материалов — только дамп, с непустым каталогом —
дамп и архив, в архиве сохранены вложенные каталоги с русскими именами.
Правило `.gitignore` проверено `git check-ignore`: файл в `deploy/storage` в
индекс не попадает, пустой каталог остаётся.

Проверить это на сервере из среды разработки нельзя — сети наружу нет, — но
все три пробела видны в описании развёртывания, а не только в бою.

---

## Р-129 · Рассылка уведомлений кабинета вынесена в скрипт: строка в crontab не работала (16.09.2026)

**Решение: `deploy/outbox.sh` вместо строки с `curl` прямо в расписании.**

Порядок развёртывания предлагал такую строку:

```
* * * * * curl -fsS -X POST -H "x-cabinet-cron: $CABINET_CRON_SECRET" http://127.0.0.1:3000/cabinet/api/outbox
```

Cron не читает `deploy/.env`: переменной в его окружении нет, и заголовок
уходил бы пустым. Маршрут в ответ отдаёт «не найдено» — расписание выглядит
исправным, письма и уведомления молчат. Дефект того же рода, что Р-120:
команда верна на бумаге и не работает в бою.

### Почему скрипт, а не подстановка в строке расписания

Прочитать секрет можно и в самой строке, но `. deploy/.env` в оболочке cron
опасен: в файле есть `SMTP_FROM=ProDisser <site@prodisser.ru>`, и угловые
скобки будут поняты как перенаправление ввода. Секрет извлекается одной
строкой через `sed`, как и в остальных скриптах развёртывания.

### Две подробности, найденные на проверке

**Кавычки нельзя снимать через `tr -d`.** Первая редакция удаляла кавычки и
возврат каретки одним набором знаков. `tr` удаляет перечисленное по всей
строке: секрет base64, содержащий букву из набора, приходил бы на сервер
урезанным. Поймано на секрете `rXr+pro/secret=rr` — до правки заголовок не
совпадал. Кавычки снимаются только по краям значения, `sed`-ом.

**Секрет должен быть в ASCII.** Проверка с кириллическим секретом дала
устойчивое «не найдено»: заголовки HTTP переносят кириллицу искажённой.
`openssl rand -base64` даёт нужный набор знаков, и это записано в порядке
развёртывания — иначе заказчик, придумавший «секретную фразу» по-русски,
получил бы неработающую рассылку без единого сообщения об ошибке.

### Что попадает в журнал

Строка пишется, только когда что-то отправлено или произошла ошибка. Вызов
раз в минуту при молчаливом выводе дал бы полторы тысячи строк «ничего не
делалось» в сутки, и настоящая ошибка потерялась бы среди них. Отсутствие
секрета в `.env` — не ошибка, а выключенная рассылка: одна строка и нулевой
код, иначе cron слал бы письмо каждую минуту.

### Проверено

Пять прогонов на поддельном маршруте: нет файла с секретом — «рассылка
выключена», код 0; маршрут не поднят — ошибка, код 1; рассылать нечего —
молчание, код 0; есть что рассылать — строка с ответом; неверный секрет —
ошибка, код 1. Секрет в кавычках и с буквами из прежнего набора `tr`
проходит.

---

## Р-130 · Приёмочный прогон кабинета перед выкатом: форма одобрения не помещалась в телефон (16.09.2026)

**Решение: сетка форм на экране заявок получает `minmax(0,1fr)`, список
выбора — явную ширину.**

Кабинет разработан (Р-122—Р-124) и готовится к первому выкату. Перед тем как
отдавать команды на боевой сервер, контур прогнан целиком на стенде: база с
накатанными миграциями, наполнение `seed:cabinet`, сборка и запуск, обход
браузером под тремя ролями на ширинах 360, 768 и 1440 px.

### Что нашлось

На экране «Заявки на рассмотрении» форма одобрения выходила за край страницы:
при окне 360 px разметка тянулась на 409 px, появлялась горизонтальная
прокрутка. Причина не в самой форме, а в списке выбора типа сопровождения:
`select` не сжимается ниже своего самого длинного варианта
(«Кандидатская диссертация»), а колонка сетки по умолчанию `auto` растягивается
под содержимое. Родительская сетка `.cab-two` на узких экранах честно
переходила в одну колонку — и всё равно оказывалась шире экрана.

Лечение в одну строку на каждую форму: `grid-template-columns: minmax(0,1fr)`
запрещает колонке расти под содержимое, а `width: 100%` с
`box-sizing: border-box` у списка выбора доводит дело до конца. Это тот же
приём, что держит остальные сетки кабинета.

### Что прогон подтвердил

Вход по одноразовой ссылке работает, повторный переход по использованной
ссылке не пускает. Разграничение ролей соблюдается: эксперт с адресов
`/cabinet/manage` и `/cabinet/manage/registry` уводится к своим проектам.
Одобрение заявки создаёт проект — в базе появился `PD-2026-001` со
связанным клиентом. Несуществующий проект отдаёт 404, а не падение. После
правки переливов нет ни на одном экране: проекты, заявки, реестры,
настройки, карточка проекта, переписка — три ширины каждая. Ошибок в
браузерной консоли ни на одном экране.

### Почему правка внесена, хотя кабинет — не наш объём

Кабинет писала другая ветка, и код его страниц в объём этой работы не
входит. Но выкат на боевой сервер идёт отсюда, а горизонтальная прокрутка на
телефоне у первого же экрана, куда попадает менеджер, — дефект того же рода,
что и любой другой перед открытием. Правка занимает три строки и проверена
тем же прогоном, которым найдена.

---

## Р-131 · В кабинет после выката было некому войти: средство завести первую учётную запись (16.09.2026)

**Решение: `app/scripts/grant-role.ts` — заведение учётной записи по адресу
почты с выдачей ссылки входа.**

Подготовка к выкату вскрыла последний пробел. Учётные записи кабинета
создаются двумя путями: наполнением стенда и одобрением заявки. Первое
работает только с адресами `@example.org` и до боевой базы не допускается по
устройству, второе требует уже работающего менеджера. На боевой базе не было
бы ни одной записи, а `issue-link` выдаёт ссылку только существующему
пользователю: кабинет развернулся бы и остался бы закрыт для всех.

Скрипт заводит или обновляет запись по адресу, назначает роль и печатает
ссылку входа. Ссылка нужна отдельно: пока почта не подключена, отправить
письмо некуда, а войти требуется в тот же час.

### Границы

Средство выдаёт полные права в кабинете, поэтому доступно только тому, у кого
есть доступ к серверу: запускается внутри контейнера, из приложения не
вызывается, наружу не смотрит.

Приостановленная запись возвращается в рабочее состояние — этим же скриптом
возвращают доступ. Обезличенная (`ERASED`) не трогается и вход в неё не
выдаётся: обезличивание — исполненное требование субъекта об удалении
данных, и отменять его административным средством нельзя.

### Проверено

Заведение записи с ролью руководителя, повторный запуск с другой ролью
(запись не задвоилась, роль обновилась), отказ при неизвестной роли и при
пустых аргументах. Вход по выданной ссылке проходит, экраны заявок и реестров
открываются, повторный переход по той же ссылке не пускает — «ссылка
недействительна».

---

## Р-132 · Средства для сервера нельзя было запустить: рабочий образ их не содержит (16.09.2026)

**Решение: отдельный слой `tools` и сервис с профилем `tools` — разовый
запуск рядом с работающим приложением.**

Порядок развёртывания предлагал запускать выгрузки и заведение учётной записи
внутри рабочего контейнера:

```
docker compose exec web node scripts/export-leads.mjs --stdout > leads.csv
```

В рабочий образ попадают только `.next/standalone`, `public` и `.next/static`
— ни исходников, ни каталога `scripts`, ни полных зависимостей. Это не
упущение сборки, а её замысел (Р-40): в бою не должно быть ни исходников, ни
инструментов сборки. Команда закончилась бы `Cannot find module`.

Ошибка тянулась с описания выгрузки отзывов и повторилась в выгрузке заявок
и в порядке выката кабинета. Ни одна из трёх команд ни разу не выполнялась:
отзывы читали на стенде, до заявок и кабинета дело не доходило.

### Как сделано

Слой `tools` собран по образцу слоя миграций: отдельный образ, который
поднимается на один запуск и гаснет. Берётся готовый слой сборки — в нём уже
лежат исходники, зависимости и сгенерированный клиент базы; размер значения
не имеет, образ разовый и наружу не смотрит.

Сервису переданы адрес базы, публичный адрес (ссылки входа печатаются
целиком), секрет свёрток и каталоги материалов и выгрузок. Профиль `tools`
держит его вне обычного `up -d`: при обновлении сайта он не поднимается.

Скрипты кабинета написаны на TypeScript и запускаются без сборки — Node 22
снимает типы сам. Переменная `NODE_OPTIONS=--experimental-strip-types`
оставлена на случай сборки, где это ещё за флагом: без неё запуск кончился бы
синтаксической ошибкой на первом же объявлении типа.

### Проверка

Слой собирается в CI отдельным шагом: рабочий образ его не включает, и без
такой проверки отказ вылез бы на сервере ровно в тот момент, когда средство
понадобилось. Это тот же урок, что Р-118: то, что не собирается в проверке,
собирается на боевой машине впервые.

Локально проверить сборку нельзя — в среде разработки нет Docker. Команды
самих скриптов проверены на локальной базе, разбирается только слой образа.

---

## Р-133 · Перенос книги заказов: свод типов, естественный ключ, карточки без учётных записей (16.09.2026)

Практика вела учёт в `Отработка заказов.xlsx` — 55 заказов за период
09.2024–09.2026. Файл перенесён в систему, и на разборе выяснились три
вещи, каждая из которых потребовала решения.

### Статус закодирован заливкой ячейки

Простые разборщики `.xlsx` заливку не отдают, поэтому взят порт разбора из
панели: архив ZIP читается вручную, OOXML — тоже, со стилями и цветом.
Портируемый разбор отдавал приоритет цвету над текстом. Применительно к
этой книге это дало бы 46 закрытых работ вместо фактических 37: зелёным
помечены в том числе девять строк со статусом «в работе».

**Приоритет инвертирован.** Ведущий признак — нормализованный текст
статуса; красная заливка перекрывает его (ею размечены остановленные
работы); зелёная уточняет только там, где текст не разобран. Всякое
расхождение цвета с текстом попадает в отдельный перечень предпросмотра —
на настоящем файле таких строк 17. Молча исправлять не в ту и не в другую
сторону: разметка велась вручную, и выбор между «работа доведена» и
«работа идёт» остаётся за руководителем.

### Кода заказа в книге нет

PD-LK-FUNC-002 предписывает upsert по коду заказа, но колонки с кодом в
файле не существует. Повторность определяется естественным ключом строки:
дата, ФИО, **исходное написание** типа работы и стоимость. Написание, а не
сведённый код: строки 47 и 48 — «Отчет НИР» и «Презентаци по Отчету НИР»
одного клиента, одной даты и одной суммы — сводятся к одной позиции
справочника, но это два разных заказа. Полностью совпадающие строки
различаются порядковым номером повтора. На настоящем файле — 55 уникальных
ключей из 55 строк.

Ключ хранится в `ImportRow.signature`. Повторная загрузка того же файла
даёт 55 строк «уже перенесена» и ни одного нового проекта — проверено и
тестом, и вживую.

### Справочник — шесть позиций

Решение Q20 называло одиннадцать позиций, но перечень с ценами не
предоставлен, а свод исторических написаний естественным образом даёт
шесть: аспирантура, диссертация, консалтинг, НИР и отчёты, статьи,
дипломы. Все 30 написаний книги, включая опечатки «Диссертция»,
«Аспирнтура», «Докторская Диссертаци», сводятся без остатка. Дробление
позиции и привязка псевдонима — ручное действие на экране импорта. Базовые
цены не заводятся: поле, заполненное догадкой, хуже пустого.

### Историческим клиентам учётные записи не заводятся

Создаются только карточки `ClientProfile` без `User`. Адресов почты в книге
нет, согласия на рассылку нет, а заведённая «на всякий случай» учётная
запись — это персональные данные без основания обработки. Вход открывается
поимённо тем, кто продолжает сотрудничество.

Совпадение ФИО дублем не считается: карточки заводятся по каждому
написанию, группы совпадений выводятся отдельным перечнем (на настоящем
файле — 11 групп), сведение выполняется вручную и проставляет
`mergedIntoId`, не удаляя прежнюю карточку — на неё ссылаются журналы.

### Прочее

Код проекта выдаётся по **году заказа**, а не по году переноса: история
должна читаться в своей хронологии. Колонка «Стоимость» трактуется как
сумма договора (трактовка брифа), вознаграждение эксперта при импорте не
восстанавливается. Оплаченное разносится траншем `PAID`, остаток —
траншем `PLANNED`; даты поступления в книге нет, и подставлять дату заказа
вместо неё не стали.

Изменившаяся строка при повторной загрузке правит состояние, сроки и сумму
договора, но не транши: по ним могли пройти ручные правки менеджера, и они
старше книги.

### Проверка стоп-формулировок получила построчную оговорку

Правило свода написаний обязано узнавать «рерайт», хотя предлагать такую
услугу нельзя. Оговорка ставится в той же строке кода, видна в разборе
изменений и на тексты экранов и писем не распространяется.

### Проверено

На настоящем файле вне репозитория: 55 строк, 5 425 000 ₽
законтрактовано, 4 370 000 ₽ получено, 1 055 000 ₽ остаток — сошлось с
эталоном брифа. Замечания: 2 несуществующие даты, 4 срока без года, 1
переплата, 2 закрытые работы с остатком, 11 групп совпадений ФИО, 17
расхождений цвета с текстом; несведённых написаний и неразобранных сроков
нет. Заведено 55 проектов и 37 карточек клиентов; повторная загрузка — ноль
новых проектов.

Сам файл в репозиторий не попадает ни при каких условиях: он содержит
фамилии клиентов и суммы договоров, а репозиторий открыт. Проверки идут по
книге, собираемой `tests/helpers/make-workbook.ts` — настоящий OOXML с
заливкой, вымышленные имена, все дефекты исходника воспроизведены.

Проверок 185 с базой, из них 7 на перенос.

---

## Р-134 · Величина потерь: показываются обе (16.09.2026)

Бриф PD-LK-BRIEF-001 называет потери на остановленных работах в
365 000 ₽. Разбор книги даёт 125 000 ₽ (150 000 − 50 000 и 50 000 −
25 000); ту же величину независимо фиксирует автотест панели
`verify/etl.check.js`. Источник расхождения не установлен.

По решению заказчика витрина потерь показывает расчётные 125 000 ₽ и
сноской — 365 000 ₽ из брифа с пояснением расхождения. Скрывать цифру
брифа нельзя: она названа заказчиком; подставлять её вместо расчётной —
тоже, она не выводится из данных.

---

## Р-135 · Аналитика практики: шесть вкладок на собственных графиках (16.09.2026)

Перенесены расчёты панели `nauchny-konsalting-panel` и её модуль графиков.
Перенесено не всё, и это решение, а не недоделка.

### Что перенесено

Сезонная норма, LTV и сегменты клиентов, концентрация, средний чек и
разброс по типам, циклы по Каплану — Мейеру, дебиторка. Расчёты лежат в
`analytics/metrics.ts` и не знают ни о базе, ни о правах: на входе массив
строк, на выходе числа. Поэтому они проверяются построчно на наборах, где
ответ известен заранее, а выборка — единственное место, где действуют
права, — остаётся в `analytics/data.ts` и строится через `scopeProjects`.

### Что не перенесено

Прогнозная часть панели: бутстрап год-энда, пуассоновские розыгрыши числа
заказов, планировщик слотов, ценовые рекомендации. Она строилась на
допущениях, которых данные не подтверждают: 55 записей за два года не
дают ни распределения спроса, ни устойчивой сезонности. Витрина
показывает факты; прогноз — отдельная работа с явно названной моделью.

### Каплан — Мейер вместо среднего

Медиана срока считается с цензурированием: незавершённые работы не
выбрасываются, а укорачивают кривую. Среднее по закрытым работам
занижало бы срок систематически — длинные проекты в него не попадают,
потому что ещё идут. Там, где кривая до половины не опускается, медианы
не существует, и на экране стоит «более N дней», а не подставленное
число; столбцом такая позиция не изображается вовсе.

### Палитра графиков новых цветов не вводит

Шкала синего из таблицы токенов: `accent-deep`, `accent`, `accent-soft`,
`accent-press`, `accent-edge`, `accent-mark`. Палитра JetLend из панели не
переносится. Библиотека графиков не подключается — правило репозитория и
требование дизайн-системы; геометрия (потолок шкалы, деления, скруглённый
столбец, сглаживание Катмулла — Рома, дуга кольца) вынесена в
`lib/cabinet/charts.ts` и проверяется отдельно от разметки.

Разметка графика собирается элементами React, а не строкой SVG, как в
панели: строковая сборка требует ручного экранирования имён клиентов, а
забытое экранирование — это не косметика, а внедрение разметки.

### График не заменяет таблицу

Под каждым графиком стоит таблица с теми же числами, а под каждой
величиной — пояснение, из чего она получена. Вывод, существующий только
в виде картинки, проверить нельзя.

### Проверено

185 проверок, из них 56 на настоящей базе; 40 новых — на расчётах.
Обход в браузере на перенесённой истории: «Обзор» даёт 5 425 000 ₽ и
4 370 000 ₽, «Клиенты» — 38 клиентов, 11 повторных (29 %), медиану LTV
60 000 ₽ и долю пяти крупнейших 55 %, «Продукты» — три позиции с
разбросом чека выше 40 %, «Сроки» — медиану 92 дня по выпускным работам
при 20 наблюдениях и 13 завершённых, «Потери» — 125 000 ₽ расчётных
рядом с 365 000 ₽ из брифа. Менеджер и эксперт на всех шести маршрутах
переводятся к своим проектам, раздела в их навигации нет. При 400 px
переполнения нет ни на одной вкладке.

---

## Р-136 · Журналы, выгрузка и исполнение требований об удалении (16.09.2026)

### Два журнала, а не один

Журнал действий отвечает на вопрос «кто и что изменил», журнал доступа к
файлам — «кто получил файл клиента». Второй растёт на порядок быстрее,
нужен при разборе утечки, а не правки, и живёт своим сроком хранения.
Сведение их в один означало бы либо потерю подробностей выдачи файлов,
либо журнал действий, в котором ничего не найти.

Выгрузка журнала сама записывается в журнал: получить полный перечень
действий по практике — событие, о котором журнал обязан помнить.

### Значения CSV, начинающиеся с «=», обезвреживаются

Поле, начинающееся со знака равенства, плюса, минуса или собаки,
табличный редактор исполняет как формулу. В журнале такие значения
появятся неизбежно: их пишет не система, а пользователь — в названии
этапа, в имени файла, в теме. Все они предваряются апострофом. Выгрузка
журнала не должна становиться способом выполнить что-то на машине
проверяющего.

### Удаление данных субъекта: модель смешанная

Полное физическое удаление противоречит обязанности хранить первичные
учётные документы. Поэтому персональные данные затираются необратимо, а
учётные величины сохраняются: по сумме договора и коду проекта человека
опознать нельзя.

Тема проекта отнесена к затираемому, хотя формально это не персональные
данные: она пересказывает работу и в узкой области опознаёт автора
надёжнее фамилии.

Тела сообщений заменяются маркером, но строки остаются: удаление строк
рассыпало бы переписку и сделало бы невозможным разбор спора. Строки
версий материалов остаются по той же причине — с пустым именем, пустой
контрольной суммой и проставленной датой изъятия; сам объект из хранилища
удаляется, и это записывается в журнал доступа действием «изъятие».

Порядок операций обязателен: объекты удаляются **до** затирания строк,
иначе ключ объекта теряется и мусор остаётся в хранилище навсегда.
Отсутствующий объект не останавливает исполнение, но попадает в отчёт
отдельным числом — молчать о расхождении нельзя.

Адрес почты стёртой учётной записи заменяется неповторяющимся значением:
поле уникально, и два стёртых клиента иначе столкнулись бы.

В отчёт включена сумма договоров — доказательство, что учёт не пострадал.
Затирание, унёсшее сумму договора, — не исполнение закона, а утрата
первичного учёта.

### Сроки хранения служебных записей

`deploy/retention.sh` дополнен: погашенные ссылки входа старше 30 дней,
истёкшие и отозванные сессии старше 90 дней, попытки входа старше 90 дней.
Эти строки — идентификаторы пользователей без всякой цели обработки.
Журналы не трогаются.

### Кабинет закрыт от индексирования дважды

Мета-тег `noindex` стоял в разметке раздела с первого спринта; добавлено
правило `Disallow: /cabinet/` в `robots.txt`. Робот, читающий только
`robots.txt`, о мета-теге не узнает.

### Проверено

229 проверок, из них 100 на настоящей базе; 8 новых — на исполнении
требования об удалении, включая изъятие настоящего объекта из хранилища.
Обход в браузере: фильтр по виду действия оставляет строки одного вида,
выгрузка отдаёт CSV с меткой порядка байтов и заголовком из девяти
столбцов, менеджер получает 403 на выгрузке и перевод к своим проектам на
обоих журналах и на экране ФЗ-152; при 400 px переполнения нет.

---

## Р-137 · Артборд кабинета — снимок облика, а не источник истины (16.09.2026)

Правило репозитория гласит: правится макет, а не компонент; артборд —
предмет приёмки каждого экрана. Для девяти страниц сайта это работает:
макет `design/*.dc.html` пропускается через `tools/dc-to-tsx.mjs`, и CI
валит сборку, если сгенерированный компонент разошёлся с макетом.

К кабинету правило неприменимо в прежнем виде. Генератор рассчитан на
статичные страницы: он ставит `@ts-nocheck` и не умеет ни типизированных
свойств, ни серверных компонентов, ни выборок из базы. Экраны кабинета
живые — списки проектов, версии файлов, состояния этапов, — поэтому
решением Р-122 код кабинета пишется вручную. Двадцать четыре экрана
написаны и работают.

Оставалось два выхода. Нарисовать под них макеты задним числом — значит
переписать кабинет второй раз в HTML и разойтись с ним при первой же
правке: расхождение никто не заметит, потому что машинной сверки для
кабинета нет. Либо признать, что для кабинета отношение обратное.

**Принято второе.** Артборд кабинета снимается с работающего экрана
скриптом `tools/cabinet-artboards.mjs` и потому не может с ним разойтись.
Источник истины — компонент; артборд нужен для приёмки: открывается в
браузере без сервера, показывается заказчику, хранится в истории и
пересобирается одной командой после каждой правки облика.

Двенадцать артбордов, а не двадцать четыре: артборд заводится на образец
композиции. Экраны, повторяющие уже снятую композицию (реестры, финансы,
пользователи, справочники, вознаграждение эксперта), своих файлов не
получают — набор из двадцати четырёх устарел бы в первый же день и стал
бы декорацией.

### Персональные данные

Снимок экрана — это разметка со всем, что на экране есть, включая ФИО
клиентов и суммы договоров. Репозиторий открыт, поэтому снимать с базы, в
которую перенесена настоящая книга заказов, нельзя. Снимок делается только
с отдельной базы, наполненной `app/scripts/seed-artboards.ts` вымышленными
людьми; и наполнение, и снятие отказываются работать, если в адресе базы
нет слова «artboard».

Вымышленные имена подобраны так, чтобы не пересекаться с настоящими:
первая редакция дала шесть совпадений по распространённым именам и одной
фамилии, имена заменены, сверка повторена до нуля. Сверка машинная и
идёт по настоящему файлу вне репозитория — сами имена ни в репозиторий,
ни в вывод не попадают.

### Воспроизводимость

Повторный запуск даёт побайтно тот же результат. Для этого понадобилось
три вещи: наполнение отсчитывает даты от постоянной точки, а не от
текущего момента; начисления эксперту заводятся однократно, а не при
каждом запуске; идентификаторы записей приводятся к постоянному виду
`id-01`, `id-02` — база выдаёт их заново, и без приведения каждый снимок
отличался бы от предыдущего одними ключами.

Правила `@font-face` из снимка вырезаются: они ведут на файлы сборки,
которых рядом с артбордом нет. Гарнитура берётся из стека — то же
соглашение, что и у макетов сайта, где файлов шрифтов тоже нет.

### Что это не отменяет

Проверка CI «перенос макетов совпадает с исходниками» перечисляет восемь
страниц сайта поимённо и каталога `design/cabinet/` не касается. Правило
«правится макет, а не компонент» для страниц сайта остаётся в силе без
изменений.

### Проверено

Двенадцать артбордов открываются с диска без сервера и без ошибок: токен
акцента читается как `#14417A`, заголовки на месте, горизонтального
переполнения при 1440px нет. Повторный снимок совпадает побайтно.
Совпадений с именами из настоящей книги заказов — ноль.

---

## Р-138 · Экраны кабинета выложены в витрину снимками (17.09.2026)

**Решение: тринадцать артбордов `design/cabinet/` публикуются на GitHub
Pages рядом с витриной сайта; кабинет как работающий раздел туда по-прежнему
не собирается.**

Кабинет влит в `main`, но посмотреть на него нельзя ничем, кроме запущенного
приложения с базой: вход по одноразовой ссылке, серверные сессии, выборки
через матрицу прав. Заказчик смотрит результат по ссылке — так принимались
все девять страниц сайта, — и до выката на боевой сервер такой ссылки у
кабинета не было.

### Почему снимки, а не статическая копия раздела

Статическая копия кабинета означала бы второй контур: предзаписанные
выборки, отдельная сборка, отдельный набор данных, и всё это пришлось бы
поддерживать в согласии с настоящими экранами. Снимки уже есть, они
получены из работающего кода (Р-137) и потому не могут с ним разойтись.
Стоимость выкладки — один модуль сборки витрины, стоимость поддержания —
ноль: артборды пересобираются той же командой, что и раньше.

### Что делается с копией и чего не делается с исходником

`app/scripts/preview-artboards.mjs` кладёт копии в `out/cabinet/artboards/`
и собирает к ним перечень. В копии: маршруты кабинета переписываются
относительными ссылками внутри набора — на нужный артборд, где он есть, и
на перечень, где его нет; формы обезвреживаются, потому что снимок формы
отправлять некуда; дописывается `noindex, nofollow`. Ссылки на страницы
сайта не трогаются — префикс подпапки им допишет общий проход по витрине.

Исходники в `design/cabinet/` не правятся ни одним байтом: они обязаны
совпадать с тем, что выдаёт пересборка снимка, иначе приёмка облика теряет
смысл.

### Персональные данные

Витрина открыта всем, у кого есть ссылка, поэтому выкладывать снимок
рабочей базы нельзя ни при каких условиях. Артборды сняты только с
отдельной базы, наполненной `app/scripts/seed-artboards.ts`: вымышленные
люди, вымышленные суммы, вымышленные темы работ. Ни одной фамилии из
настоящей книги заказов в наборе нет — проверено поиском по каталогу перед
фиксацией снимков. Витрина закрыта от поисковиков: `noindex` на каждом
файле и `Disallow: /` в `robots.txt` (Р-100).

### Чем это ограничено

Снимок не работает: формы не отправляются, фильтры неподвижны, файлы не
скачиваются. Переходы работают в пределах тринадцати экранов; маршруты без
своего артборда — настройки, выдача файла, реестры, финансы, справочники,
удаление данных — ведут на перечень. Живой кабинет требует сервера и базы,
это выкат по `docs/DEPLOY.md`.

### Проверено

Витрина собрана с префиксом подпапки: тринадцать артбордов и перечень на
месте, все 92 относительные ссылки ведут на существующие файлы, битых нет.
Внешних запросов в наборе ноль — ни шрифтов, ни картинок, ни скриптов.
Отправляющих форм не осталось ни в одном файле, `noindex` есть во всех.
`git diff` по `design/cabinet/` после сборки чист: выкладка исходники не
трогает.

---

## Р-139 · Прототип кабинета: связанный обход вместо набора отдельных экранов (17.09.2026)

**Решение: рядом с артбордами выкладывается прототип — те же снимки
настоящих экранов, но снятые обходом по ссылкам, с сохранёнными переходами
и раскладкой по ролям.**

Артборд отвечает на вопрос «как выглядит экран». По набору артбордов
нельзя пройти сценарий: открыть работу из перечня, войти в этап, вернуться
к оплатам, сменить роль и посмотреть ту же работу глазами менеджера.
Заказчик принимает не тринадцать картинок, а раздел, и первое же замечание
на выложенные артборды было именно об этом.

### Обход, а не перечисление

`tools/cabinet-prototype.mjs` поднимает рабочую сборку на базе прототипа,
входит под каждой из четырёх ролей и обходит раздел по ссылкам: что найдено
переходом — то и снято. Маршруты нигде не перечислены руками, поэтому
экран, добавленный в кабинет и связанный ссылкой, попадёт в прототип сам.

Роль меняет и состав разделов, и содержимое одного и того же маршрута —
это матрица прав, а не оформление. Поэтому дерево строится по ролям:
`client/`, `expert/`, `manager/`, `head/`. Точка входа — настоящий экран
входа кабинета; одноразовой ссылки в прототипе нет, писем он не шлёт, и
вместо неё предложен выбор роли.

### Норма на однотипные экраны

Перенесённая книга заказов дала полсотни проектов, и первый обход у
менеджера ушёл целиком в них: до аналитики и переноса очередь не дошла.
Введены две вещи: неповторяющиеся маршруты разбираются раньше однотипных
записей, а на однотипные ставится норма — три проекта, пять этапов, по два
экрана материалов, переписки и оплат, одна загрузка книги. Прототип
показывает устройство раздела, а не содержимое базы.

### Чем прототип не является

Это снимки: формы не отправляются, фильтры и сортировки неподвижны, файлы
не скачиваются, данные не меняются. Маршрут, которого в дереве нет — выдача
файла, выгрузка журнала, экземпляр сверх нормы, — ведёт на начальный экран
роли, а не в никуда. Живой кабинет требует сервера и базы: это выкат по
`docs/DEPLOY.md`.

Прототип и артборды остаются оба и пересобираются независимо. Один служит
приёмке облика экрана, другой — проверке сценария; оба получены из одного
и того же кода и потому не могут с ним разойтись.

### Логотип

Заодно устранено расхождение, которое прототип и обнажил: кабинет показывал
логотип наоборот — `PRO` основным цветом, `DISSER` акцентным, начертанием
500. В шапке сайта наоборот: `PRO` акцентным синим, `DISSER` цветом текста,
Literata 600. Раздел читался как чужой продукт под похожим именем. Написание
приведено к макетам сайта, артборды пересняты.

### Проверено

Прототип собран: 79 экранов в четырёх деревьях. Обход в браузере прошёл
сценарий целиком: вход → выбор роли «Клиент» → «Мои работы» → работа
PD-2026-001 → этап «Расчётная часть» → смена роли на «Руководитель» →
очередь заявок. В собранной витрине 93 страницы кабинета и 1253
относительные ссылки, битых нет; внешних запросов ноль. Проверок 236, из
них 100 на настоящей базе.

---

## Р-140 · Кабинет сведён к двум ролям; облик выправлен по дизайн-системе (17.09.2026)

**Решение: на этой стадии кабинет показывается двумя плашками — клиент и
руководитель; экран клиента перестроен вокруг вопроса «что с моей работой»;
расхождения с дизайн-системой устранены в общих компонентах.**

Кабинет писался под четыре роли и весь объём четырёх спринтов. Заказчик
принимает две: свою и клиентскую. Разделы служебного контура — перенос книги
заказов, журналы, исполнение требований об удалении, пользователи,
справочники, вознаграждение эксперта — убраны из навигации. Маршруты и код
остались рабочими: перенос нужен, чтобы в системе вообще были заказы, а
контур ФЗ-152 — требование закона, а не пожелание.

### Что видит руководитель

Главный экран начинается со сводки: **заказов, выручка, прибыль, к
получению**. Новых расчётов не заведено — величины берут `financeSummary` и
счётчик проектов.

**Прибыль — величина с оговоркой, и оговорка стоит на экране.** Практику вёл
сам руководитель: сторонних исполнителей при переносе книги заказов не
восстанавливали, и честной себестоимости взять неоткуда. Принято:
прибыль = поступления − начисления исполнителям − 10 % накладных расходов.
Ставка объявлена именованной величиной `OVERHEAD_PERCENT`, подпись плитки
называет её прямо. Как только начисления появятся, цифра станет точнее сама,
без правки экрана.

### Что видит клиент

Прежний экран заказа требовал собрать ответ из трёх мест: горизонтальной
полосы этапов, ленты событий и блока «кто ведёт проект». На телефоне он не
собирался вовсе. Порядок теперь такой:

1. **Состояние работы одной строкой** — этап, срок и что требуется от
   клиента. `StatusLine`.
2. **Ход работы** — этапы сверху вниз, с состоянием словом, сроком и
   причиной остановки там, где она возникла. `Roadmap` вместо `Stepper`:
   горизонтальная полоса на шесть колонок читалась только на широком экране
   и передавала состояние почти одним цветом.
3. **Что уже сделано** — лента событий второстепенной колонкой.
4. **Связь и материалы** — переписка с куратором и материалы работы.

Денег клиент не видит: экран оплат ему не показывается. Эксперта тоже —
для клиента работу ведёт **куратор**; слово «менеджер» с клиентских экранов
убрано, событие о назначении исполнителя в клиентскую ленту не попадает.

### Что выправлено в облике

Аудит против `docs/DESIGN-SYSTEM.md` дал перечень расхождений; устранены те,
что живут в общих компонентах и потому чинят все экраны сразу:

| Было | Стало |
|---|---|
| h2 — 21 px | 22 px, как на всех страницах сайта (Р-99) |
| h3 — 500 при 1.24 | 600 при 1.4 |
| Карточка — тень уровня 1, отступ `20px 22px` | уровень 2, `18px 20px 20px` |
| Плашка-метка — `6px 12px` | `8px 14px` |
| Поле ввода — 44 px | 48 px |
| Кнопка — начертание 500, переход 200 мс, нажатие без отклика | 600, 180 мс, `scale(.97)` |
| Вордмарк — 21 px, без отклика и без имени для читалки | 23 px, подложка на наведение, `aria-label` |
| Подсказка поля внутри `<label>` | вынесена, связана `aria-describedby` |
| Галочки системного цвета | `accent-color` акцента |
| `select` без кольца фокуса | правило распространено на `select` |
| Пять сырых цветов и укороченный стек Literata на экране сбоя | токены и общие константы |
| Плитка величины в трёх файлах | `Tile` и `Tiles` в `components/cabinet/ui.tsx` |

Порядок заголовков выправлен: h3 больше не стоит выше h1, разделы клиентских
экранов стали настоящими h2 вместо моно-меток. Ширина рабочей области сведена
к сайтовым 1220 px — правило о 1440 px заводилось под таблицы служебного
контура, которых на видимых экранах не осталось; раздел 10.3 дизайн-системы
исправлен.

### Чего это решение не делает

Эксперт и менеджер остаются в матрице прав, в схеме и в тестах: скрыт
интерфейс, а не контур. Прибыль остаётся оценкой, пока не заведены
начисления. Экраны служебного контура не переоформлялись — они скрыты и
ждут своей очереди.

### Проверено

Проверок 236, из них 100 на настоящей базе. Прототип пересобран на две роли:
37 экранов. Обход в браузере на 1440 и 390 px: переполнения нет, состояние
работы читается с первого экрана, дорожная карта на телефоне разворачивается
в одну колонку.

---

## Р-141 · Прототип показывает настоящие заказы, обезличенные (17.09.2026)

**Решение: прототип наполняется настоящей книгой заказов, пропущенной через
обезличивание; действующими считаются работы, помеченные в книге жёлтой
заливкой.**

Прототип на вымышленной практике не отвечает на вопрос, которым заказчик его
открывает: чем практика занята сейчас. Настоящая книга при этом в открытый
репозиторий не попадает ни при каких условиях — в ней ФИО клиентов, темы
исследований и суммы договоров.

### Что сохраняется и что заменяется

`tools/anonymize-book.mjs` запускается по книге, лежащей **вне репозитория**
(скрипт отказывается работать, если путь ведёт внутрь), и пишет
`app/scripts/data/book.json`. Сохраняются величины и состояния: даты заказов,
сроки, стоимость, оплата, тип сопровождения, признак «действующая».
Заменяются опознаваемые поля: заказчик сводится к первой букве фамилии
(«Заказчик К.»), тема — к обобщённой формулировке по типу работы. Полные
инициалы не годятся: вместе с суммой и сроком они сужают круг до одного
человека.

Свод питает `scripts/seed-artboards.ts`; прототип снимается с него, как и
прежде.

### Что считать действующей работой

Текстовый статус книги для этого не годится: «в работе» стоит у семнадцати
строк, включая полностью оплаченные заказы 2024 года. Заказчик помечает
идущие работы **жёлтой заливкой** — таких шесть, у всех неоплаченный остаток.
Признак берётся из заливки; красная по-прежнему означает остановленную
работу, зелёная — закрытую.

Сводка воспроизводит книгу: 54 заказа (одна строка без суммы и заказчика
отброшена), выручка 4 370 000 ₽, к получению 820 000 ₽. Расхождение с
остатком книги в 815 000 ₽ — та же переплата по одному договору, что
и при переносе: задолженность по проекту не бывает отрицательной.

### Этапы

В книге этапов нет — одна строка на заказ. План работ разворачивается по типу
сопровождения, доля пройденного берётся от доли оплаты. Это то, что менеджер
завёл бы руками при одобрении заявки, и то, ради чего клиент открывает
кабинет.

### Облик: убрано самоописание

Профессиональный инструмент не пересказывает себя. Убраны пояснительные
абзацы под заголовками («Последние события по работе: кто и что сделал»),
дублирующие моно-метки над заголовками о том же, двухстрочные подписи плиток
с арифметикой, двухфразовые пустые состояния. Карточные «полосы сроков» у
руководителя заменены таблицей действующих работ: числа выстроены в колонку
с моноширинными цифрами, знак рубля вынесен в шапку, — колонка чисел нужна
затем, чтобы их сравнивать.

### Проверено

Проверок 236, из них 100 на настоящей базе. Прототип — 41 экран.
Совпадений с фамилиями из книги в `design/` и в своде — ноль.

---

## Р-142 · Остаток аудита облика закрыт (18.09.2026)

**Решение: устранены расхождения, найденные аудитом и остававшиеся после
правок в общих компонентах.**

### Графики были нечитаемы по цвету

Шкала рядов перечисляла шесть оттенков синего подряд, и две соседние
ступени — `--pd-accent` `#14417A` и `--pd-accent-press` `#0F3260` —
различались меньше чем на три процента светлоты. Их не различает ни
дальтоник, ни нормальное зрение на проекторе, а смысл сегмента держался
ровно на этом различии.

Оставлены три ступени синего с шагом светлоты около двадцати пунктов и
нейтральный серый `--pd-ink-muted`: он отличается от синего тоном, а не
светлотой, и потому различим при цветовой слепоте. Новых цветов не
введено — все четыре из таблицы токенов. Сверх четырёх рядов цвета
повторяются, поэтому **доля подписывается прямо в сегменте**: смысл не
держится на одной заливке.

Кегли подписей графиков — 9,5, 10, 10,5 и 11 px — были ниже нижней границы
12 px из раздела 3.2 дизайн-системы; подняты до 12.

Попутно отвергнут промежуточный вариант со штриховкой рядов: узор SVG не
наследует заливку элемента, `currentColor` внутри `<pattern>` разрешается
в контексте самого узора, и сегменты вышли чёрными. Четырёх различимых
цветов и подписи доли достаточно.

### Поля без подписи

Выбор роли и дата подписания договора поручения в управлении
пользователями, ввод исторического написания в справочнике — подпись была
только в placeholder либо в заголовке колонки. Кнопка удаления написания
давала читалке имя «…✕». Всё названо явно через `aria-label`.

### Повторы вёрстки

Объект стилей ячейки таблицы был скопирован в девять экранов и уже
разошёлся по отступам; объект поля — в четыре. Оба сведены в
`components/cabinet/ui.tsx` (`TABLE_CELL`, `TABLE_HEAD`, `TABLE_NUM`),
вкладки аналитики берут их реэкспортом под прежними именами.

Инлайновый `<select>` встречался десять раз, местами без `fontSize` —
Safari на телефоне увеличивает вьюпорт, когда поле мельче 16 px (запрещено
решением Р-86). Заведён компонент `Select` с подписью, высотой 48 px и
кеглем 16; крупные вхождения переведены на него.

`fontFamily: 'monospace'` в справочнике — голое ключевое слово вместо стека
`MONO` — давало другую гарнитуру, чем весь остальной кабинет.

### Мелкие мишени

Хлебная крошка с кодом проекта — единственный путь назад к работе — имела
высоту около семнадцати пикселей. Ей и другим ссылкам-перечням присвоен
класс `cab-mark` с целью нажатия 44 px.

### Проверено

Проверок 236, из них 100 на настоящей базе. В прототипе (41 экран) не
осталось ни одного объявления кегля ниже 12 px. Совпадений с фамилиями из
книги заказов — ноль.

---

## Р-143 · Клиент не видит исполнителя ни на одном экране (18.09.2026)

**Решение: правило «состав привлечённых специалистов клиенту не
показывается» распространено на все экраны, включая авторство файлов,
замечаний и событий.**

Решением Р-140 исполнитель убран с карточки работы: блок «Исполнитель»
скрыт, событие о назначении в клиентскую ленту не попадает. Но правило
обходилось с другой стороны — через авторство. Клиент, открыв этап из
дорожной карты, видел под заголовком `· эксперт Иванов И. И.`, а в перечне
версий и в ленте событий — фамилию человека, о существовании которого ему
знать не нужно.

Заведена `authorName(author, viewer, authorId)` в общем модуле оформления:
себя клиент видит как «Вы», куратора — по имени (с ним он переписывается),
эксперта — как «Специалист практики». Для персонала ничего не меняется:
имена видны как прежде.

Заодно подсказка поля причины остановки перестала говорить клиенту про
«работу эксперта».

### Три экрана доведены до общего вида

Экраны этапа, материалов и переписки не проходили чистку, применённую к
главным экранам решением Р-141, и выбивались из остального кабинета: абзац
под заголовком пересказывал экран, разделы обозначались моно-меткой вместо
заголовка, пустые состояния занимали два предложения. Правила те же:
пояснение остаётся там, где объясняет последствие действия, а не устройство
страницы. Блок согласования этапа («После согласования этап закрывается, и
работа переходит к следующему») сохранён целиком — это ровно такой случай.

### Проверено

Проверок 236, из них 100 на настоящей базе. Прототип пересобран: в
клиентских снимках фамилии исполнителя нет ни одной. В собранной витрине
724 относительные ссылки, битых нет.

---

## Р-144 · Прототип кабинета публикуется второй площадкой (18.09.2026)

**Решение: помимо GitHub Pages прототип выкладывается переносимой копией
на площадку, не зависящую от `github.io`; ссылка на неё приводится наравне
с адресом витрины.**

Витрина публикуется сама и исправно: прогон зелёный, развёртывание
отчиталось, адрес выдан. Открыть его у заказчика не получается — дважды.
Домен `github.io` у российских провайдеров перекрывается регулярно и
независимо от содержимого; проверить это из среды разработки нельзя, выход
к нему закрыт её собственной политикой. Чинить недоступный узел нечем:
дефекта в сборке нет, а ссылка, которая не открывается, ничем не лучше
отсутствующей.

Поэтому вводится вторая площадка. Публикация на GitHub Pages сохраняется
как есть — она не заменяется, а дублируется.

### Переносимая копия: ссылка указывает на файл

Прототип разложен каталогами, и переходы записаны каталожными ссылками
вида `../../head/manage/` — около пятисот на сорок один экран. GitHub Pages
на такой адрес отдаёт `index.html`, поэтому там прототип работает. Ни
другая площадка, ни открытие с диска такого поведения не обещают: половина
переходов упёрлась бы в «страница не найдена».

`tools/cabinet-portable.mjs` собирает копию, ничего не ждущую от сервера:
каталожная ссылка получает явный `index.html`, логотип и пустой адрес в
шапке ведут на начальный экран прототипа, правовые страницы — на
действующий сайт. Артборды собираются тем же кодом, что и для витрины
(`writeCabinetArtboards`), — второго написания одной работы не заведено.
Исходники в `design/cabinet-prototype/` не правятся: они пересобираются
снимком и обязаны совпадать побайтно.

Копия годится сразу для трёх случаев: публикация на стороннем узле,
открытие с диска двойным щелчком и та же выкладка на Pages.

### Чем это ограничено

Вторая площадка частная: ссылка открывается у владельца учётной записи, а
не у любого, кому её переслали. Для показа заказчику этого достаточно и
это строже нынешней открытой витрины; для показа третьим лицам остаётся
адрес Pages.

Прототип не кладётся в `app/public/`: это выложило бы его на prodisser.ru
при ближайшем выкате, а выката по распоряжению заказчика нет.

### Проверено

Обход переносимой копии: 780 внутренних ссылок, все ведут на существующие
файлы; каталожных адресов и ссылок на корень домена не осталось.
Совпадений с фамилиями из настоящей книги заказов — ноль.

---

## Р-145 · Короткий разговор с куратором живёт на карточке работы (18.09.2026)

**Решение: последние сообщения канала и поле ответа показываются прямо на
экране работы; отдельный экран переписки остаётся и хранит её целиком.**

Заказчик формулирует задачу клиентского кабинета одной фразой: клиент,
открыв работу, должен понимать её состояние и иметь короткий канал связи.
Состояние экран показывал, канал — нет: вместо разговора стояла ссылка
«Написать куратору», и чтобы прочитать три строки, требовался переход на
отдельный экран и возврат обратно.

Теперь под ходом работы стоят три последних сообщения и поле ответа.
Отправка возвращает человека на карточку работы, а не уводит в переписку:
поле `back` в форме называет экран, откуда писали, и подставляется не в
адрес, а в выбор из двух — иначе форма стала бы способом увести
пользователя на чужой узел.

Прочитанными сообщения здесь не помечаются. Отметку по-прежнему ставит
открытие самой переписки: три последних строки — не то же, что прочитать
канал, и счётчик непрочитанного не должен обнуляться от одного взгляда на
карточку.

### Лента переписки — один источник оформления

Разметка сообщений понадобилась второй раз, и вместо копии заведён
компонент `Thread` в общем модуле оформления: пузырь, подпись автора
через `authorName` (Р-143), пометка о передаче контактов — для менеджера.
Экран переписки переведён на него же; различаются два места числом
показанных сообщений и видимостью пометки.

### Проверено

Проверок 236, из них 100 на настоящей базе. Артборды и прототип
пересняты: блок виден у клиента как «Переписка с куратором», у
руководителя — как «Переписка с клиентом». Совпадений с фамилиями книги
заказов в снимках — ноль.

---

## Р-146 · Зелёный и красный — только исход действия (18.09.2026)

**Решение: семантическая пара цветов возвращена к той роли, которая у неё
на сайте; состояния кабинета оформляются нейтралью и одним акцентом.**

Заказчик назвал происходящее прямо: химозность в цветах. Разбор подтвердил
претензию и показал источник. Раздел 2.3 дизайн-системы говорит: «Успех» и
«Ошибка» — это семь блоков подтверждения и семь блоков ошибки формы,
семантические цвета не участвуют в оформлении обычных блоков. В кабинете
эта пара разошлась по экранам: срок работы выводился розовым чипом,
подписанный договор — зелёным, пройденный этап — зелёным кружком,
непройденный — красной кромкой, полученная сумма — зелёными цифрами,
причина остановки — розовым блоком. На одном экране сходились красный,
зелёный и два синих, и настоящая ошибка переставала отличаться от
обычной строки.

### Что теперь чем обозначается

| Что | Чем | Было |
|---|---|---|
| Исход действия: получилось / не получилось | `Notice` с тоном `ok` или `error` | то же |
| Состояние работы, этапа, транша, договора | слово в чипе; акцент — у действующего, спокойный тон — у остального | зелёный, красный, два синих |
| Ход этапов | акцент у начатого, глубокий акцент у пройденного, серая кромка у незапланированного | пять цветов |
| Причина остановки этапа | спокойный блок `Notice tone="quiet"` | розовый блок ошибки |
| Суммы и доли | цвет текста, без заливки | зелёные цифры полученного |

Чип получил два тона вместо четырёх: спокойный и акцентный. Смысл при
этом не теряется — состояние и раньше было подписано словом, цвет его
только дублировал.

### Заодно убрано

Горизонтальный трекер этапов `Stepper` снят: он заменён дорожной картой
решением Р-140 и с тех пор не вызывался ни с одного экрана. Мёртвая
подпись ролей на экране материалов удалена — авторов там называет
`authorName` (Р-143). Пояснение об уведомлениях на экране настроек
перестало упоминать эксперта: клиенту его не показывают.

### Проверено

Применений `var(--pd-ok-…)` и `var(--pd-err-…)` в снимках прототипа и
артбордов — ноль; в коде кабинета пара осталась только внутри `Notice`.
Проверок 236, из них 100 на настоящей базе.

---

## Р-147 · Облик проверен по снимкам; два правила переведены в проверки (18.09.2026)

**Решение: экраны кабинета просмотрены снимками на 1440 и 390 px, найденные
повторы и дефекты подписи устранены, а два правила облика теперь проверяет
машина, а не глаз.**

После сведения палитры (Р-146) экраны были сняты браузером с переносимой
копии прототипа и просмотрены как картинки, а не как разметка. Так нашлось
то, чего не видно в коде.

### Что нашлось и устранено

| Найдено | Устранено |
|---|---|
| Клиент видел себя в ленте событий как «Заказчик Г.» | в выборке событий не было идентификатора автора, и `authorName` не узнавала себя; теперь стоит «Вы» |
| Тип работы печатался чипом и тут же заголовком карточки — у всего, что перенесено из книги заказов, это одна строка | чип не выводится, когда дословно повторяет заголовок |
| У работы без этапов рисовалась пустая полоса хода | полоса не выводится, пока этапов нет |
| Там же две строки подряд говорили одно: «План работы ещё составляется» и «Этапы появятся после согласования плана» | осталась одна |
| Клиенту показывали слова «эксперт» в сообщении куратора и в замечании к версии | формулировки исправлены; правило Р-143 держится не только на подписях |

Правка наполнения прототипа сама по себе не доезжала до снимка: переписка
и замечания создавались только при пустой выборке, и снимок показывал
прежнюю редакцию. Теперь обе выборки переписываются заново при каждом
наполнении — текст в скрипте и текст на снимке совпадают всегда.

### Машинные проверки вместо ручного просмотра

`app/tests/cabinet-view.test.ts` проходит по снимкам прототипа и требует
двух вещей: на клиентских экранах не встречается слово «эксперт» ни в
какой форме, и ни на одном экране не применяется пара `--pd-ok-*` /
`--pd-err-*`. Обе проверки до сих пор делались глазами и держались на
внимательности; теперь они падают в CI. Проверок стало 291, из них 100 на
настоящей базе.

---

## Р-148 · Аудит облика по своду правил UI/UX (18.09.2026)

**Решение: экраны проверены по своду правил доступности и интерфейса,
найденные нарушения устранены, а те из них, что поддаются машинной
проверке, закреплены тестами.**

Проверялись пять групп правил: иерархия заголовков, подписи полей, отклик
на действие, читаемость графиков и плотность типографики. Ниже — то, что
нарушалось, и то, чем закрыто.

### Заголовки: уровень и кегль разведены

Семь экранов переходили с `h1` сразу на `h3`: заголовок карточки нужно
было набрать мелко, а единственным способом это сделать был третий
уровень. Для читалки пропуск уровня выглядит как потерянный раздел.

У `Heading` появилась ступень кегля `size`, независимая от уровня
разметки: карточка внутри раздела теперь `h2` с третьей ступенью кегля.
Внешне ничего не изменилось, в разметке пропусков не осталось.

### Отклик на действие

Кабинет весь серверный, и до сих пор нажатие «Отправить» не давало
никакого отклика: страница перерисовывалась, когда действие заканчивалось,
а до тех пор кнопка выглядела нетронутой. Правило `button[disabled]` в
общих стилях было написано под этот случай и ни разу не срабатывало —
отключать кнопку было некому.

Кнопка вынесена в `components/cabinet/Button.tsx` и стала единственной
частью раздела, работающей в браузере: `useFormStatus` гасит её и
объявляет занятой на время отправки. Остальные экраны остаются
серверными.

### Графики: подписи перестали наезжать

На витрине «Законтрактовано по месяцам» два десятка столбцов подписывались
подряд, и подписи сливались в сплошную строку. Теперь подпись выводится
через шаг, вычисленный из ширины столбца, а отсчёт ведётся от последнего
месяца — свежий подписан всегда. Числа над столбцами прорежены тем же
правилом и рисуются отдельным слоем поверх всех столбцов: прежде соседний
столбец ложился на уже написанное число и срезал его.

Там же, на витринах «Средний чек по позициям» и «Медиана срока по
позициям», подписи выключены вправо по границе колонки, и длинное название
позиции уезжало за левый край картинки: строка начиналась с середины слова.
Ширину текста в SVG не измерить, поэтому она оценивается по числу знаков
(при кегле 12 знак кириллицы занимает около 6,8 px); что не помещается,
заменяется многоточием, а полное название остаётся в подсказке и в таблице
под графиком. Колонка подписей на этих двух витринах расширена до 280 px.

### Снимок больше не отстаёт от кода

Обнаружено при проверке: инструменты снимка пересобирали приложение только
при полном отсутствии сборки. Правка экрана попадала в код, но не в
артборд — то есть ровно то, ради чего снимок заведён, не работало. Теперь
сборка считается устаревшей, если любой файл под `src` или `scripts`
свежее её, и пересобирается сама.

### Закреплено проверками

К двум проверкам облика (Р-147) добавлены ещё две: уровни заголовков не
пропускаются и каждое поле подписано — меткой через `for`, обёрткой или
`aria-label`. Проверок стало 371, из них 100 на настоящей базе.

### Что проверено и нарушений не дало

Контраст текста к фону по применяемым парам токенов, цели нажатия не
меньше 44 px, кегль поля не меньше 16 px, горизонтальная прокрутка на
ширинах 390 и 1440 px, таблицы с горизонтальной прокруткой внутри
карточки, обозначение текущего раздела навигации.

---

## Р-149 · Третья плашка: менеджер ведёт свои работы (18.09.2026)

**Решение: роль менеджера выведена в кабинет; он видит работы, где сам
назначен куратором, а распоряжение практикой — деньги, аналитика, раздача
работ — остаётся у руководителя.**

Кабинет был сведён к двум плашкам решением Р-140: заказчик принимал облик
и не хотел смотреть четыре роли сразу. Матрица прав при этом знала
менеджера с первого спринта, и её проверки на него написаны — не хватало
видимости и разграничения между кураторами.

### Что видит менеджер

| Видит | Не видит |
|---|---|
| Свои работы: этапы, материалы, переписку, договор и оплаты по ним | Работы других кураторов — по прямому адресу они не отличаются от несуществующих |
| Контакты своих клиентов, очередь заявок, реестры по своим работам | Прибыль, маржу, начисления эксперту, аналитику, журналы, перенос книги, справочники, пользователей |
| Что требует вмешательства по его работам | Сводку практики и деньги целиком |

Заявки на модерации видны обоим: их разбирает тот, кто дошёл первым, и он
же становится куратором заведённой работы.

### Разграничение сделано выборкой, а не экранами

`scopeProjects` для менеджера возвращает `{ managerId: actor.id }`. Этого
достаточно для перечня работ, карточки, материалов, переписки и всего
прочего, что строится через выборку, — то есть правило нельзя забыть на
отдельно написанном экране.

Два перечня механизм обходили и после сужения стали бы утечкой: светофор
по срокам выбирал этапы запросом без условия проекта, а реестр клиентов —
все карточки. Оба переведены на `scopeProjects`. Это то самое правило, ради
которого механизм заведён: «списки, реестры, выгрузки и аналитика строятся
только через них».

### Главный экран менеджера

У руководителя `/cabinet/manage` начинается со сводки практики; менеджеру
она закрыта проверкой `MARGIN_VIEW`, и экран оставался одной очередью
заявок. Заведён раздел «Требует внимания»: сорванные сроки, работы, ждущие
клиента дольше двух недель, и непрочитанные сообщения — всё по видимым
работам. Руководителю тот же раздел показывается под сводкой: он отвечает
на тот же вопрос и ему.

### Передача работы

Куратором становился тот, кто одобрил заявку, и сменить его было нечем.
Заведено действие `PROJECT_SET_MANAGER` — только руководителю: раздача
работ между кураторами есть распоряжение практикой, а не ведение своей
работы. Смена пишется в журнал действий и в ленту событий проекта, потому
что клиенту меняется тот, кому он пишет.

### Проверено

Проверок 425, из них 104 на настоящей базе. Новый набор `curators.db.test.ts`
проверяет сквозь запрос: менеджер не получает чужую работу ни перечнем, ни
по коду, ни светофором, ни реестром клиентов; после передачи работа
исчезает у прежнего куратора и появляется у нового; менеджеру передача
запрещена, а назначить куратором клиента нельзя.

Заодно устранена чужая хрупкость: две проверки переноса книги считали
работы по всей базе, и параллельно идущий набор мог их уронить — счёт
сужен до их собственных работ.

---

## Р-150 · Четвёртая плашка: эксперт (18.09.2026)

**Решение: роль эксперта выведена в кабинет; он видит назначенные работы и
собственное вознаграждение, переписки с клиентом у него нет и не появится.**

Роли выводились по одной: клиент и руководитель (Р-140), менеджер (Р-149).
Эксперт — последняя из четырёх, и, как и с менеджером, половина работы
лежала в коде с первого спринта: выборка ограничивает его назначенными
работами, экран собственного вознаграждения написан, навигация роли есть.

### Что видит и чего не видит

| Видит | Не видит |
|---|---|
| Назначенные работы, их этапы, сроки, состояние | Чужие работы — по прямому адресу неотличимы от несуществующих |
| Материалы и версии, свои замечания и опубликованные чужие | Непубликованные замечания других, переписку клиента с куратором |
| Собственное вознаграждение: начислено, выплачено, к выплате | Сумму договора, оплаты, маржу, чужие начисления, аналитику, журналы |
| ФИО клиента, вуз, направление — решение заказчика | Телефон и почту клиента: их нет в объекте, который получает разметка |

Переписки у эксперта нет — это не упущение, а технический барьер против
переманивания: он высказывается замечаниями к версиям, и они доходят до
клиента после публикации куратором.

### Договор поручения назван причиной, а не тишиной

Без подписанного договора поручения обработки персональных данных (ч. 3
ст. 6 152-ФЗ) выборка эксперта пуста — доступа к материалам клиента он не
получает, даже будучи назначенным. Экран показывал при этом пустой
перечень, и это читалось как «работ пока нет»: человек ждал назначения,
которого уже дождался. Теперь названа причина и следующий шаг.

### Вознаграждение начисляется вручную

Сумму вводит руководитель по этапу или работе — решение заказчика. Доля по
умолчанию в профиле эксперта (`defaultPayout`) остаётся подсказкой на
будущее и в расчёт не подставляется.

### Проверено

Проверок 469, из них 148 на настоящей базе. Новый набор `expert.db.test.ts`:
без договора поручения эксперт не получает ни одной работы; с договором
видит только назначенную; контакты клиента в объект не попадают; своё
начисление видит, чужое — нет; сводка практики ему отказывает.

Заодно исправлена проверка облика: с появлением четвёртой роли полоса
выбора роли в прототипе поставила слово «Эксперт» на каждую страницу,
включая клиентские. Обвязка прототипа вырезается перед проверкой — она
принадлежит прототипу, а не кабинету.

---

## Р-151 · Выкат идёт сам после зелёной проверки; работу делает скрипт на сервере (17.09.2026)

**Решение: `deploy/update.sh` на сервере и задача `deploy.yml`, которая
подключается по ssh и запускает его.**

Каждое обновление сайта до сих пор означало терминал, ssh и пять команд
руками. Заказчик обновлений не делает вовсе, а исполнитель делает их из
среды, откуда нет сети наружу: обновление превращалось в передачу команд
блоками с разбором вывода. Ни то, ни другое не повторяется дважды в неделю.

### Почему работа в скрипте, а не в описании задачи

Шаги выката лежат в репозитории рядом с остальными скриптами развёртывания и
выполняются одной командой на сервере. Поэтому обновление можно повторить
руками, не разбирая конвейер, — а это именно то, что нужно в тот день, когда
GitHub недоступен. Обратный порядок (шаги внутри описания задачи) давал бы
механизм, работающий ровно в одном месте.

### Порядок и откат

Копия базы, миграции, сборка образа, подъём, проверка здоровья тридцатью
попытками. При отказе возвращается прежний образ и прежний коммит, задача
завершается с ошибкой: сайт не остаётся лежать из-за неудачной правки.

**Откат возвращает код, но не схему базы.** Миграции необратимы, и делать вид,
что это не так, опаснее, чем признать: отсюда правило — миграция пишется так,
чтобы прежняя версия приложения работала с новой схемой. Добавить колонку
можно, переименовать нельзя. Копия базы снимается до миграций — это
единственная точка возврата, если неудачной окажется сама миграция.

Успешный выкат отмечается файлом с версией, и следующий запуск сверяется с
ним, а не с состоянием рабочей копии. Без этого выкат, упавший после
обновления кода — например, на нехватке переменной окружения, — оставлял бы
сервер с новым кодом и старым контейнером, а следующий запуск отвечал бы
«обновление не требуется» и не делал ничего. Поймано разбором первого
настоящего выката, до того как он произошёл: секреты кабинета в `deploy/.env`
ещё не заданы, и подъём контейнера обязан был упасть именно так.

Два выката разом отсекаются дважды: очередью на стороне GitHub и блокировкой
`flock` на сервере. Второй выход — не ошибка конвейера, а отказ с понятной
строкой в журнале.

### Границы доступа

Ключ лежит в чужом сервисе, поэтому он не должен открывать сервер целиком.
Выкат идёт от отдельного пользователя `deploy`, которому разрешён `sudo` ровно
на один файл, а сам ключ в `authorized_keys` привязан к одной команде через
`command=`: даже попав в чужие руки, он не даёт ни оболочки, ни файлов.

Отпечаток сервера хранится секретом и подставляется в `known_hosts`, а не
принимается на веру при первом подключении: иначе подмена узла прошла бы
незамеченной — на другом конце ключ, запускающий обновление.

Приватная половина ключа создаётся на сервере и оттуда копируется в настройки
репозитория самим заказчиком; после этого файл затирается. В переписке ключ
не появляется — то же правило, что для паролей (Р-117).

Правило проверено практикой в тот же день: первый ключ был показан в переписке
вместе с выводом команды и потому заменён. Поэтому в порядке развёртывания
рядом с командой `cat` стоит прямое предупреждение, а не одна строка в конце
раздела: вывод команды копируют целиком, вместе с тем, что в нём оказалось.

### Проверено

Скрипт прогнан на площадке с подставными `docker`, `curl` и отдельным
репозиторием, четыре случая: обновление не требуется — выход без действий;
обновление прошло — порядок вызовов ровно тот, что задуман; здоровье не
ответило — прежний образ возвращён, коммит откачен, код выхода ненулевой; два
запуска разом — второй отбит блокировкой.

Описание задачи разбирается как YAML. Проверить подключение по ssh из среды
разработки нельзя — сети наружу нет; это выяснится на первом настоящем
выкате, и потому задача написана так, чтобы падать с внятным сообщением на
каждом шаге: нет секретов — сказано, каких именно; не отвечает сайт снаружи —
сказано, каким кодом.

В проверку добавлен разбор скриптов развёртывания оболочкой: они не участвуют
в сборке, и опечатка в них выясняется на сервере в тот момент, когда скрипт
понадобился.

---

## Р-152 · Формы кабинета приведены к одному строю (18.09.2026)

**Решение: расстояния, раскладка и положение кнопок в формах задаются тремя
общими частями, а не переписываются на каждом экране.**

Формы писались по мере появления экранов, и разнобой копился: расстояние
между полями 8, 12, 16 или 20 px; сетка полей то `minmax(180px,1fr)`, то
`minmax(200px,1fr)`; кнопка то прижата к полям, то отбита пустым блоком.
Заказчик указал на это прямо: в карточке заявки левая колонка была шире
правой, а кнопки «Одобрить» и «Отклонить» стояли на разной высоте.

### Три части вместо повторов

| Часть | Что задаёт |
|---|---|
| `Form` | колонка полей с единым шагом 16 px и высотой в размер колонки |
| `FormRow` | ряд полей, который сам переносится на узком экране; один порог — 200 px |
| `FormActions` | блок действий, прижатый к низу: кнопки соседних форм стоят на одной линии |

Заодно заведено `FileField`: своя связка подписи и `input[type=file]` стояла
на трёх экранах в трёх написаниях. Самодельный `<select>` на экране новой
заявки и на назначении исполнителя заменён общим `Select` — он стоял рядом
с компонентом, который для того и написан, и отличался отступами и высотой.

### Что это дало на карточке заявки

Колонки сравнялись по ширине: решение одобрить и решение отклонить
равнозначны, и перекос читался как небрежность. Обе кнопки встали на одну
линию независимо от числа полей в колонке. Цель нажатия у флажка «применить
шаблон этапов» доведена до 44 px.

### Чего правка не коснулась

Формы экрана оплат, кроме договора, а также управление пользователями и
справочниками остались на прежней вёрстке: они служебные и переводятся
следующим заходом. Разнобоя между ними и остальным кабинетом это не
прибавило — пороги и шаг там те же, что были.

---

## Р-153 · Кабинет до настройки закрывается сам, а не падает (18.09.2026)

**Решение: каркас раздела спрашивает, заданы ли настройки, и при их
отсутствии показывает страницу «Раздел готовится» вместо ошибки сервера.**

Решением Р-151 выкат стал автоматическим: слияние в `main` обновляет
боевой сервер. Кабинет уезжает туда вместе с остальным сайтом, и его
настройки — отдельная работа на сервере. До неё раздел не просто не
работал бы: `SESSION_SECRET` требуется для свёртки токенов, материалы
негде хранить без `CABINET_STORAGE_DIR`, выборки невозможны без базы —
любой из трёх случаев давал пятисотую ошибку на живом домене.

Теперь `cabinetReadiness()` читает три переменные, и каркас раздела при
незаданных показывает страницу с тем, что человеку делать дальше. Имена
настроек выводятся только на стенде разработки: на боевом сайте это
подсказка о внутреннем устройстве, и постороннему она не адресована.

Порядок открытия кабинета от этого не меняется — он описан в `docs/DEPLOY.md`,
раздел 5в, и остаётся отдельным действием: задать настройки, применить
миграции, завести первую учётную запись.

### Проверено

`app/tests/readiness.test.ts`: пустое окружение называет все три настройки;
секрет короче тридцати двух знаков не считается заданным; у каждой
настройки есть пояснение. Проверок 412 без базы.

---

## Р-154 · Недоставленное уведомление видно руководителю (18.09.2026)

**Решение: состояние очереди уведомлений выведено отдельным служебным
экраном для руководителя; ненастроенный канал перестал считаться
отказом.**

Очередь работает со второго спринта, и модуль `outbox.ts` в собственном
заголовке обещает три свойства: недоступный SMTP не откатывает перевод
этапа, уведомление не теряется при падении процесса, недоставленное видно
руководителю. Первые два исполнялись, третье — нет: письмо, не ушедшее
после пяти попыток, переходило в состояние `FAILED` с причиной в
`lastError`, и не видел его никто. Человек ждал бы письма, которого нет.

Теперь `outboxDigest()` считает состояния очереди и отдаёт последние
отказы, экран `/cabinet/manage/outbox` показывает их таблицей, а действие
«Отправить ещё раз» возвращает строку в очередь с обнулённым счётчиком
попыток: причина отказа устраняется снаружи — адрес исправлен, ящик
настроен, — и прежние попытки к новой отправке отношения не имеют.

**Кто видит.** Только руководитель, правом `AUDIT_VIEW`: в очереди видны
адресаты по всем работам, и это та же служебная кухня, что и журналы.
Менеджеру она не нужна — решение заказчика. Пункта меню экран не получает
по правилу Р-140: служебные разделы в навигации скрыты. Вместо этого в
разделе «Требует внимания» появляется строка «Уведомления не доставлены:
N» — только у руководителя и только при ненулевом счётчике. Экран не висит
в меню, но и не теряется.

**Ненастроенный канал отказом не считается.** До этой правки пустой SMTP
давал обычную неудачу: пять попыток, и строка выгорала в `FAILED`. Вся
накопившаяся очередь перегорела бы до того, как ящик `info@prodisser.ru`
завели. Теперь причина «канал не настроен» попыток не расходует: строка
остаётся в ожидании, проверяется раз в час и уходит сама, как только
настройки появятся. Отдельного состояния для этого не заводится — причина
в `lastError` называет положение дел, и сводка считает такие строки
отдельно.

**Чего это не делает.** Доставка по-прежнему не проверена: без почтового
ящика и токена бота проверить можно только очередь и её состояния. Первые
настоящие отказы экран покажет, когда они появятся. Вызов маршрута
рассылки по расписанию — часть выката (`docs/DEPLOY.md`).

### Проверено

`app/tests/outbox.db.test.ts`: ненастроенный канал не расходует попытки и
оставляет причину; настоящий отказ (снятая привязка Telegram) наращивает
счётчик и кончается пометкой неудачи; сводка считает состояния и выводит
отказавшую строку; повтор возвращает её в очередь с нулём попыток;
менеджер получает отказ и на сводку, и на повтор. Проверок 476, из них 151
на настоящей базе.

---

## Р-155 · Таблица заявок — на Яндекс Диске, а не в Google (19.09.2026)

**Решение: `deploy/yandex-upload.sh` выгружает заявки и отзывы в таблицы на
российском хранилище раз в час; выгрузка в Google не делается.**

Заказчик попросил, чтобы новые заявки сами складывались в таблицу на Google
Диске и он мог их смотреть. Приём заявок и так автоматический — заявка
попадает в базу сразу; речь шла о месте, где их удобно просматривать.

### Почему не Google

В заявке — имя, контакт, организация, тема работы. Это персональные данные,
и их выгрузка в Google означала бы трансграничную передачу в страну, которой
нет в перечне обеспечивающих адекватную защиту прав субъектов. Для такой
передачи требуется отдельное согласие каждого заявителя (ч. 4 ст. 12
152-ФЗ), уведомление Роскомнадзора **до начала** передачи и новая редакция
Политики, которая сейчас прямо утверждает обратное. Ответственность — от
административной по ст. 13.11 КоАП до уголовной по ст. 272.1 УК за
незаконный оборот с передачей за рубеж.

Заказчику предложены четыре варианта с разной ценой: российское хранилище,
раздел в кабинете, Google без имён и контактов, Google целиком со всеми
тремя предварительными шагами. Выбран первый.

### Что сделано

Скрипт собирает те же две выгрузки, что делаются вручную (Р-125, Р-111), и
кладёт их на Диск по WebDAV. Файл перезаписывается целиком: это всегда
полный список, а не приращение — так не бывает пропусков и не нужно хранить
состояние «что уже выгружено».

Отсутствие доступа — не ошибка, а выключенная выгрузка: одна строка в журнал
и нулевой код, иначе cron слал бы письмо каждый час. Отказ Яндекса по логину
или паролю, неудачная сборка выгрузки и отказ при загрузке файла — три разных
сообщения: по журналу видно, на чём именно остановилось.

Создание папки не считается ошибкой, когда она уже есть: WebDAV отвечает на
такой `MKCOL` кодом 405, и только другой ответ прекращает работу.

### Условие включения, которое не в коде

Хранилище — третье лицо, и обработка ему поручается по договору (ч. 3 ст. 6
152-ФЗ). Личный Диск для этого не подходит: там пользовательское соглашение,
а не договор поручения. Нужен тариф для организаций — он же закрывает второй
незакрытый пункт, почту на домене.

Перечень поручений в Политике придётся дополнить пунктом об облачном
хранилище: сейчас там хостинг, почта, платёжный сервис и бухгалтерия.
Это правка юридического текста, она идёт через юриста и делается по
отдельному указанию (правило действует с Р-109). Поэтому выгрузка написана
выключенной: пока поля пусты, скрипт ничего не делает.

### Проверено

Площадка с подставными `docker` и `curl`, пять случаев: доступ не задан —
«выгрузка выключена», код 0; Яндекс ответил 401 — названо, что не принят
логин или пароль; папка уже есть (405) — обе таблицы загружены, в журнале
число строк; Диск не принял файл — названо, какая таблица не загрузилась,
код 1; выгрузка не собралась — в журнале последняя строка ошибки сборки.

---

## Р-156 · Итоги по годам: введённое и посчитанное стоят рядом (19.09.2026)

**Решение: экран «Итоги по годам» показывает в одной строке две величины —
введённую руководителем и посчитанную кабинетом, — и расхождение между ними.**

Заказчик ведёт выручку и прибыль по годам в отдельной книге и просил
перенести это в кабинет, оговорив: первое время цифры вносит сам, формат
вывода ещё уточняется. Кабинет при этом знает только те работы, что прошли
через него, — за прошлые годы там пусто.

### Почему не одно число

Свести введённое и посчитанное в одну величину значило бы спрятать главное:
какая часть года ведётся в кабинете, а какая держится на отдельной книге.
Пока история не перенесена целиком, расхождение — это не погрешность, а
показатель полноты переноса: чем оно меньше, тем больше работы идёт через
кабинет. Когда оно дойдёт до нуля, отдельная книга перестанет быть нужна —
и это будет видно из таблицы, а не из ощущений.

Год попадает в сводку, если он есть хотя бы в одном источнике: строка с
введённой цифрой и без работ в кабинете так же законна, как обратная.

### Как считается

Выручка года — оплаченные транши, отнесённые к дате оплаты, а не к дате
договора: деньги приходят в тот год, когда пришли. Расходы — выплаты
экспертам с той же привязкой к дате. Прибыль — разница. Суммы хранятся в
копейках целыми числами, как и все деньги кабинета: тип с плавающей точкой
к деньгам не применяется.

Правка года заменяет прежние величины — строка на год одна, ключ по году.
Введённые цифры уходят в журнал действий с автором и датой: спорное число
через год нужно уметь отнести к тому, кто его внёс.

### Что проверено

Стенд, руководитель: пустой экран показывает приглашение; два ввода дают две
строки, годы по убыванию; повторный ввод года заменяет строку, а не
добавляет вторую; год вне границ (1999) отклонён; переливов нет при 360, 768
и 1440; менеджер уводится на свои работы, неизвестный посетитель — на вход;
ошибок в браузере нет.

### Историческая выручка: запасная дата

Перенос книги заказов создаёт оплаченные поступления **без даты оплаты**: в
книге её не вели, и подставлять выдуманную при переносе отказались намеренно
(Р-133). Считая выручку года строго по дате оплаты, сводка показала бы ноль
за все прошлые годы, а расхождение равнялось бы введённым величинам целиком —
таблица оказалась бы бесполезной ровно там, где нужна.

Поэтому год определяется по первой доступной дате: оплаты, затем договора,
затем начала работы. Данные при этом не правятся — подстановка живёт в
расчёте, а не в базе, и подпись под таблицей называет число таких
поступлений. Суммы, у которых нет ни одной даты, в сводку не входят и
показаны отдельной строкой с указанием, что делать.

### Проверка, отменившая собственную правку

Первый прогон показал, что после сохранения на экране остаются прежние
цифры, и в действие была добавлена принудительная переоценка страницы.
Повторная проверка с корректным сравнением (суммы разделяются неразрывным
пробелом, и поиск по обычному пробелу их не находил) показала, что экран
обновляется и без неё: дефекта не было, был неверный замер. Строка снята —
код не должен содержать лечения несуществующей болезни, а комментарий рядом
с ней утверждал бы неправду.

---

## Р-157 · Очередь заявок листается, а не обрывается на пятидесятой (19.09.2026)

**Решение: очередь на экране «Заявки на рассмотрении» разбита на страницы по
двадцать, с общим числом и перелистыванием.**

Выборка брала первые пятьдесят заявок и молчала об остальных. С открытия
сайта накопилось четыреста с лишним обращений: руководитель, впервые
открывший кабинет, увидел бы полсотни карточек и не узнал бы, что за ними
есть ещё триста с лишним. Потери данных здесь нет — заявки лежат в базе, — но
есть потеря доверия к экрану: очередь, которая молча обрывается, хуже
очереди, честно сообщающей свою длину.

### Почему двадцать, а не пятьдесят

Каждая заявка на этом экране — не строка, а карточка с двумя формами:
одобрить с выбором типа работы либо отклонить с причиной. Пятьдесят таких
карточек — это полторы сотни полей ввода на странице; двадцать умещаются в
разумную прокрутку и грузятся заметно быстрее.

Порядок — от старых к новым: очередь разбирается с головы. Первой видна самая
давняя заявка, а не последняя пришедшая.

### Края

Страница за пределами перечня — не ошибка: ссылку могли сохранить, а заявки
за это время разобрать. Показывается последняя существующая страница.
Нечисловое значение в адресе приводит к первой странице. Оба случая проверены
и не дают ни падения, ни пустого экрана.

### Проверено

Стенд на настоящем объёме: 258 заявок в очереди, тринадцать страниц. Первая
страница — карточки с первой по двадцатую, третья — с сорок первой по
шестидесятую, `?page=999` — последняя страница с восемнадцатью карточками,
`?page=пять` — первая. Перелистывание показывает «Предыдущие» и «Следующие»
только там, где им есть куда вести. Переливов нет при 360 и 1440, ошибок в
браузере нет, страница открывается за 0,6—0,7 секунды.

---

## Р-158 · Служебные экраны приведены к одному строю форм (19.09.2026)

**Решение: формы служебных экранов собираются теми же частями, что и
клиентские; идентификатор поля получает область; служебный контур собран
за одним пунктом навигации «Управление».**

Решением Р-152 формы сведены к `Form`, `FormRow`, `FormActions` и
`FileField` — но только на клиентских и производственных экранах.
Служебные в тот заход не вошли, и к этому дню в них стояло двадцать восемь
собственных форм и два десятка полей, набранных вручную. Объект стиля поля
был объявлен четырьмя копиями, и они разошлись: `manage/users` и
`manage/directory` давали поле 44 px с отступом `8px 12px`,
`manage/audit` и `manage/erasure` — `10px 14px`, а общее поле кабинета —
48 px и `12px 14px`. Решением заказчика высота приведена к 48 px везде:
строки таблиц в справочнике и учётных записях стали выше, и это принято
сознательно — второй ступени в дизайн-системе не заводится.

### Дефект, а не косметика: повторяющиеся идентификаторы

`Field` и `Select` складывали идентификатор из одного имени поля. Форма,
повторённая в перечне, давала столько одинаковых идентификаторов, сколько
строк: `<label for>` ведёт к первому совпадению, и читалка называла не то
поле, которое человек правит. Дубли стояли в снимках прототипа:
`id="amount"` и `id="title"` на экране оплат, `id="file"` на экране этапа.

Введена необязательная **область**: идентификатор складывается из области
и имени, подсказка получает свой от него же. Уникальность выводится из
того, что уже уникально, — имена полей внутри формы различны по
определению, область различна по строке перечня.

Отвергнуто: `useId()` — поля серверные, а перевод их в клиентские отправил
бы в браузер каждый контрол раздела; счётчик в модуле — снимки прототипа
перестали бы быть побайтно воспроизводимыми, а на этом держится приёмка
облика; голый необязательный идентификатор — склейку строки пришлось бы
делать вызывающей стороне, и ошибку нечем было бы поймать.

### Почему проверки этого не ловили

Четыре машинных правила облика ходят по снимкам прототипа, а снимки
снимаются обходом по ссылкам. Служебные разделы убраны из навигации
решением Р-140, входящих ссылок на них не было нигде — и правила облика на
них не работали вовсе. Разнобой накопился ровно там, где его никто не
проверял.

Поэтому заведён экран `/cabinet/manage/tools` — «Управление»: перечень
служебных разделов, каждый со строкой пояснения. Пункт меню один, и каждая
роль видит в нём ровно то, на что имеет право: реестры открыты обеим
штатным ролям, остальное — руководителю. **Р-140 этим не отменяется**:
служебная кухня по-прежнему не занимает места в первом ряду навигации,
но перестаёт быть доступной только по набранному вручную адресу. Побочное
следствие, ради которого это и делалось: обход находит служебные экраны по
ссылкам, и правила облика начали на них работать — прототип вырос с 71
экрана до 81.

### Что добавилось в общие части

Оформление кнопки переехало в токены: `Button.tsx` помечен `'use client'`,
и всё, что из него брали, уезжало в браузер — кнопке-ссылке и кнопке
выхода состояние отправки не нужно. `Form` получил строй в ряд (форма
внутри ячейки таблицы) и отправку адресом (форма-фильтр журналов). Поля —
область, скрытую подпись и ширину; выбор файла — перечень расширений.
Заведены `Checkbox` (подпись оборачивает поле, идентификатор не нужен),
`ButtonLink` (четыре места набирали вид кнопки заново по полтора десятка
строк), `Tabs` (одна полоса вкладок стояла в двух написаниях — у аналитики
и у журналов) и третий тон кнопки для чипа снятия написания.

### Проверено

Два новых правила запирают работу. Пятое правило облика: идентификаторы на
экране не повторяются — оно видит только то, что попало в снимок, поэтому
область внутри перечня остаётся предметом разбора, а не проверки. Новый
набор `cabinet-markup.test.ts` ходит по исходникам (прецедент —
`text-guard.test.ts`): на экранах кабинета нет ни собственной формы, ни
поля помимо скрытого, ни вида кнопки, набранного вручную. Вернуть ручную
разметку молча больше нельзя.

Проверок 683 против 479, из них 64 на настоящей базе. Прототип пересобран:
81 экран, совпадений с фамилиями книги заказов ноль.

---

---

## Р-159 · Все заявки — раздел в кабинете, а не файл во внешнем хранилище (19.09.2026)

**Решение: `/cabinet/manage/leads` — перечень всех обращений с отбором,
поиском и выгрузкой в браузер; выгрузка на Яндекс Диск остаётся написанной и
выключенной.**

Заказчик просил видеть все заявки в таблице и добавил: где можно обойтись
бесплатно, лучше так. Разбор показал, что дело не в цене места. Личный Диск —
хоть бесплатный, хоть платный — работает по пользовательскому соглашению, а
поручение обработки персональных данных третьему лицу требует договора с
обязанностью соблюдать конфиденциальность (ч. 3 ст. 6 152-ФЗ). Такой договор
есть только у тарифа для организаций, и покупка места его не заменяет.

Раздел в кабинете закрывает ту же потребность и ничего не стоит: данные не
покидают сервер, третьих лиц нет, дополнять Политику нечем.

### Устройство

Отбор по странице сайта и состоянию, поиск по имени, контакту, организации и
теме. Порядок обратный очереди: перечень просматривают сверху вниз, и наверху
должно быть свежее, тогда как очередь разбирается с головы. Отзывы в перечень
не попадают — у них свой порядок работы (Р-111).

Отбор живёт в адресе строки браузера: страницу можно сохранить и вернуться к
тому же виду. Постраничность сохраняет условия отбора при листании.

### Выгрузка

Кнопка отдаёт файл в браузер и не оставляет копии на сервере: копия
персональных данных не должна заводиться там, где её никто не удалит.
Выгружается весь отбор, а не текущая страница — человек нажимает «выгрузить»,
чтобы получить всё найденное; страницы перебираются по одной, чтобы не
держать перечень в памяти целиком.

Каждая выгрузка пишется в журнал действий: число строк и условия отбора. Это
вынос персональных данных за пределы экрана, и он должен быть виден
проверяющему.

Метка BOM и перевод строк CRLF — как в остальных выгрузках проекта: без них
таблицы на Windows показывают кириллицу вопросительными знаками.

### Проверено

Стенд, 390 обращений: без отбора — 390 в восьми страницах по пятьдесят;
отбор по странице «Студентам» — 99; по состоянию «Новая» — 239; поиск
«proverka» — 39; поиск без совпадений — пустой экран с приглашением изменить
условия; вторая страница — записи с 51 по 100.

Выгрузка по отбору «Студентам» дала 99 строк — ровно то, что на экране, — с
меткой BOM и пятнадцатью столбцами. Эксперт на адресе выгрузки получает 404:
отказ и отсутствие неразличимы. Переливов нет при 360 и 1440.

## Р-160 · Доставка заявок с сайта видна на экране, а не в журнале контейнера (19.09.2026)

**Решение: раздел «Заявки с сайта» на экране «Очередь отправки» показывает,
дошло ли обращение в Telegram и на почту; названия страниц и состояний
заявки сведены в один перечень.**

Заказчик поставил условие: приём заявок идёт сам, и он должен видеть все
обращения. Первую половину закрывал Р-159 — раздел «Все заявки». Вторая
оставалась незакрытой в незаметном месте.

Обращение с сайта не проходит через очередь кабинета: маршрут приёма
(`app/src/app/api/lead/route.ts`) шлёт его сразу в оба канала, а исход пишет
в таблицу `Delivery` — с открытия сайта, то есть с 13.09.2026. Таблица не
показывалась нигде. Отказ канала — недоступный Telegram, отвергнутая почта,
незаданные настройки — был виден только тому, кто читает журнал контейнера
по ssh. Для заказчика это значило: заявка сохранена, письмо не пришло, и
узнать об этом неоткуда.

Экран очереди уже отвечал на такой вопрос для уведомлений кабинета. Второй
экран заводить не за чем: вопрос у руководителя один — дошло ли до меня то,
что пришло, — поэтому оба пути стоят на одном экране и разделены заголовками.

### Что показывается

Три плитки: заявок за сутки, доставок за сутки с временем последней, отказов
за тридцать дней. Число заявок стоит рядом с числом доставок намеренно: «за
сутки четыре заявки и ноль доставок» — это сразу видимая поломка, тогда как
одни только нули о ней не говорят.

Причина «канал не настроен» считается отдельно от отказов по существу. Она
ставится обоими отправителями до обращения к сети — это не поломка, а
незаполненная настройка, и смешивать их значит прятать настоящий отказ среди
пятисот строк ожидания.

Повтора отправки здесь нет, в отличие от очереди кабинета. Заявка уже в базе
и видна в разделе «Все заявки»; повторная доставка спустя дни ничего не
ускоряет, а код повторной отправки пришлось бы держать ради действия, которое
никому не нужно. Перечень отвечает на другой вопрос — работает ли канал.

Имя и контакт заявителя в перечень не выносятся: для разбора отказа довольно
страницы, темы и времени, а состав сведений держится минимальным (ч. 5 ст. 5
152-ФЗ). То же правило уже действует в перечне очереди кабинета.

### Названия сведены в один перечень

Перечни названий страницы-источника и состояния заявки стояли в четырёх
местах и разошлись: одна страница называлась «Бизнесу» на сводке и
«Компаниям» в перечне заявок, одно состояние — «Консультация» на экране и
«Консультация проведена» в выгрузке. Руководитель читает эти экраны подряд и
принимает расхождение за разные вещи.

Названия вынесены в `app/src/lib/cabinet/lead-labels.ts` и сверены с сайтом:
раздел обращается к компаниям, а не к бизнесу. Неизвестное значение
возвращается как есть — если в базе появится источник, о котором экран не
знает, руководитель увидит его код, а не пустое место.

### Проверено

Настоящая база стенда: 696 проверок, из них 67 на базе. Перечень показал один
отказ по существу (403 от api.telegram.org — след среды разработки, из
которой нет выхода наружу) и 515 строк ожидания настройки канала, разведённые
по разным счётчикам. Контакт заявителя в выдаче отсутствует — это отдельная
проверка. Менеджеру перечень недоступен: право то же, что у журналов.
Переливов нет при 390 и 1280.
